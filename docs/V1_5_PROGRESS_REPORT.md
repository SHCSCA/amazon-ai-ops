# Amazon AI Ops Agent v1.5 Progress Report

Date: 2026-06-15

## Goal

Execute the v1.5 plan while preserving the current codebase. Missing modules are documented in `docs/MISSING_MODULES_MATRIX.md` and filled in rather than hidden or silently skipped.

## Current Completion

Overall v1.5 report collection has packaged-executable evidence: ERP login, Ads entry, real Lingxing Ads download center, enabled page model, 8/8 single-report canaries, full live 8-report E2E, final acceptance audit, and the current fix that commits the Lingxing report date range picker after filling start/end dates. Real Lingxing Listing detail-page read-only extraction, DeepSeek live connection, live ad strategy diagnosis, ad recommendation AI explanation, Listing AI draft evidence, and the current-contract manual Ads UI execution/readback sample all have verifier evidence. The 2026-06-30 high-fidelity UI / AI output-contract and data-import row-fade refresh, real AI strategy JSON-contract fix, date-picker fix, encrypted credential save, login submit `aria-busy`/spinner and stable credential status feedback, AI/import feedback, OperatorTaskPanel loading micro-response plus non-blocking shimmer sweep, global typography contract, global button active `scale(0.98)` micro-response and disabled cursor lock, shared state-light hover +2px lift feedback, MicroStepper status-dot/pending-spinner feedback, operation-scope first-screen confirmation, page-level range FormTable field confirmation, ScopeBar field-level green confirmation feedback, non-layout-shifting ScopeBar editor popover, shared business-data pipeline 300ms scope debounce, product-management first-screen product-locking, product-card locked/idle tags and selection `aria-live` feedback, credential sandbox hover feedback, approval decision-strip hover/focus weak-fade feedback, recommendation direct action-button busy feedback, delivery readback work-package action-button busy feedback, readback evidence workflow action-button busy feedback, operation-event first-screen task feedback with optimistic form clear/rebound and failure draft restore, product-config bulk target ACOS apply, inline autosave, keyboard nudge, and live target health feedback, ad-quant metric focus dimming, settings threshold field-level validation feedback, settings rule-save action-button busy feedback, scheduler first-screen local task feedback plus scheduler controller/row action-button busy feedback, data-collection monitor drawer with Canvas browser preview, data-collection action-button busy feedback, delivery readback repair handoff, product maintenance, ad readback screenshot paste/drop capture with drag-over feedback plus fixed thumbnail/badge confirmation, readback time/value contract visualization, readback safety checkbox confirmation feedback, Listing local action-button busy feedback, keyword-opportunity sortable header, filter-axis feedback, and refresh action-button busy feedback, dashboard primary action jump feedback, data-import validation SHA-256 checksum column/sortable header/import-time read-only lock/direct action-button busy feedback/`aria-live`/200ms blur/sweep plus row fade-in feedback, and canonical daily metric explanation passed focused renderer/AI/data-collection-monitor/readback-capture/readback-contract/operator-task-panel/operation-scope/operation-event/product-config/product-management/scheduler/virtual-table/business-data/listing/recommendation/delivery/readback tests, desktop typecheck, current business UI smoke, Windows installer/portable packaging, packaged launch smoke, manifest-driven final-readiness, READY bundle export, and READY safety. Current APP_READY evidence is `output\codex-evidence\final-readiness-20260630142700.json` with bundle `output\delivery-bundles\v15-delivery-bundle-20260630142700-ready`; the previous `output\codex-evidence\final-readiness-2026-06-18-portable-fix.json` and `output\delivery-bundles\v15-delivery-bundle-2026-06-18-portable-fix-ready` are historical READY evidence only. The in-app ad execution button remains fail-closed, and future ad changes must each bind their own target, real report source files/row, approval, before/after screenshots, and readback evidence.

- 2026-06-29 Listing local-draft workbench pass: `Listing 优化` now merges keyword input, real-data gate, draft purpose, source counters, and generate/export actions into `关键词与本地草案工作台`. The primary UI no longer calls missing-data drafts placeholders or leaves operators guessing what a rule-source draft means; missing real ad data is shown as `待补齐真实广告数据` and `仅本地预览`, and these drafts cannot enter delivery evidence. Focused checks passed: `pnpm exec vitest run apps\desktop\src\renderer\pages\listing-optimization-page.test.ts apps\desktop\src\renderer\listing-workflow-summary.test.ts --reporter=basic`, desktop typecheck, `pnpm run smoke:business-ui-current` with `output\codex-evidence\current-business-ui-smoke-1782787351742.json`, `pnpm run verify:ad-execution`, `build:win`, package launch smoke `output\codex-evidence\package-launch-smoke-1782800794443.json`, manifest-driven final-readiness `output\codex-evidence\final-readiness-20260630142700.json`, READY bundle export, and READY safety.

- 2026-06-30 Listing local action-button busy feedback pass: `Listing 优化` now applies a shared local action busy view to manual save, Lingxing read-only fill, local draft generation, and draft export. The active action switches to `保存中...`, `读取中...`, `生成中...`, or `导出中...`, shows a spinner, exposes `aria-busy=true`, adds `button-loading`, and locks sibling Listing actions without making them look like the running task. This is only local Listing feedback; it does not submit Amazon or modify Lingxing Listing. TDD first failed because `listingLocalActionButtonView` did not exist, then passed with `pnpm exec vitest run apps\desktop\src\renderer\pages\listing-optimization-page.test.ts --reporter=basic`; production validation passed desktop typecheck, renderer build, `pnpm run smoke:business-ui-current` with `output\codex-evidence\current-business-ui-smoke-1782791738959.json`, `pnpm run verify:ad-execution`, Windows build, package launch smoke `output\codex-evidence\package-launch-smoke-1782800794443.json`, manifest-driven final-readiness `output\codex-evidence\final-readiness-20260630142700.json`, READY bundle export, and READY safety.

- 2026-06-30 scheduler controller action-button busy feedback pass: `定时任务` now extends the 16ms no-dead-click contract from the first-screen task panel into the lower controller and task table. The controller refresh button, inline confirm-run button, and row-level enable/disable buttons use `schedulerActionButtonView`; only the active action switches to `正在刷新...`, `执行中...`, `启用中...`, or `停用中...`, shows a spinner, exposes `aria-busy=true`, and carries `button-loading`, while peer scheduler controls lock without pretending to run. This is only local scheduler feedback and still cannot approve recommendations, modify bids, pause ads, add negatives, or write Amazon Ads. TDD first failed because `schedulerActionButtonView` did not exist, then passed with `pnpm exec vitest run apps\desktop\src\renderer\pages\scheduler-page.test.ts --reporter=basic`; production validation passed desktop typecheck, renderer build, `pnpm run smoke:business-ui-current` with `output\codex-evidence\current-business-ui-smoke-1782800714782.json`, `pnpm run verify:ad-execution`, Windows build, package launch smoke `output\codex-evidence\package-launch-smoke-1782800794443.json`, manifest-driven final-readiness `output\codex-evidence\final-readiness-20260630142700.json`, READY bundle export, and READY safety.

- 2026-06-30 keyword-opportunity refresh action-button busy feedback pass: `关键词机会` now extends the zero-dead-click contract from its first-screen task panel into the direct `刷新机会` action. The refresh button switches to `刷新中...`, shows a spinner, exposes `aria-busy=true`, and carries `button-loading` while rows reload; row-level `带入 Listing` is locked during refresh without pretending to be the running task. This is only local keyword/Listing handoff feedback and does not generate recommendations, modify Listing content, approve actions, or write Amazon Ads. TDD first failed because `keywordOpportunityActionButtonView` did not exist, then passed with `pnpm exec vitest run apps\desktop\src\renderer\pages\keyword-opportunities-page.test.ts --reporter=basic`; production validation passed desktop typecheck, renderer build, `pnpm run smoke:business-ui-current` with `output\codex-evidence\current-business-ui-smoke-1782800714782.json`, `pnpm run verify:ad-execution`, Windows build, package launch smoke `output\codex-evidence\package-launch-smoke-1782800794443.json`, manifest-driven final-readiness `output\codex-evidence\final-readiness-20260630142700.json`, READY bundle export, and READY safety.

- 2026-06-30 recommendation direct action-button busy feedback pass: `优化建议` now applies a shared busy view to folded `刷新建议`, workflow-step `生成解释`, and empty-state `生成优化建议`. The active action switches to `刷新中...` or `生成中...`, shows a spinner, exposes `aria-busy=true`, carries `button-loading`, and disables duplicate clicks; missing real scope or pipeline loading remains a plain unavailable state. This is only recommendation-page wait-state feedback and does not bypass real-report gates, approve rows, or write Amazon Ads. TDD first failed because `recommendationActionButtonView` did not exist, then passed with `pnpm exec vitest run apps\desktop\src\renderer\pages\recommendations-page.test.ts --reporter=basic`; production validation passed desktop typecheck, renderer build, `pnpm run smoke:business-ui-current` with `output\codex-evidence\current-business-ui-smoke-1782800714782.json`, `pnpm run verify:ad-execution`, Windows build, package launch smoke `output\codex-evidence\package-launch-smoke-1782800794443.json`, manifest-driven final-readiness `output\codex-evidence\final-readiness-20260630142700.json`, READY bundle export, and READY safety.

- 2026-06-30 delivery readback work-package action-button busy feedback pass: `交付验收` now applies a shared busy view to `导出交付包`, `导出数据口径核对`, `创建回读工作包`, `检查工作包`, `生成回读证据`, `校验回读证据`, and `用回读证据刷新最终验收`. The active action switches to `导出中...`, `创建中...`, `检查中...`, `生成中...`, `校验中...`, or `刷新中...`, shows a spinner, exposes `aria-busy=true`, carries `button-loading`, and disables peer delivery actions without making them look like the running task. This is only local delivery/readback evidence workflow feedback and does not satisfy readback proof, approve actions, or write Amazon Ads. TDD first failed because `deliveryActionButtonView` did not exist, then passed with `pnpm exec vitest run apps\desktop\src\renderer\pages\delivery-page.test.ts --reporter=basic`; production validation passed desktop typecheck, renderer build, `pnpm run smoke:business-ui-current` with `output\codex-evidence\current-business-ui-smoke-1782800714782.json`, `pnpm run verify:ad-execution`, Windows build, package launch smoke `output\codex-evidence\package-launch-smoke-1782800794443.json`, manifest-driven final-readiness `output\codex-evidence\final-readiness-20260630142700.json`, READY bundle export, and READY safety.

- 2026-06-30 readback evidence workflow action-button busy feedback pass: `执行回读` now applies a shared busy view to `导出回读证据`, `创建回读工作包`, `检查工作包`, `生成回读证据`, and `校验回读证据`. The active action switches to `导出中...`, `创建中...`, `检查中...`, `生成中...`, or `校验中...`, shows a spinner, exposes `aria-busy=true`, carries `button-loading`, and disables peer evidence-chain actions without making them look like the running task. This is only local readback evidence workflow feedback and does not satisfy final proof, approve actions, bypass verifier gates, or write Amazon Ads. TDD first failed because `readbackActionButtonView` did not exist, then passed with `pnpm exec vitest run apps\desktop\src\renderer\pages\readback-page.test.tsx --reporter=basic`; production validation passed desktop typecheck, renderer build, `pnpm run smoke:business-ui-current` with `output\codex-evidence\current-business-ui-smoke-1782800714782.json`, `pnpm run verify:ad-execution`, Windows build, package launch smoke `output\codex-evidence\package-launch-smoke-1782800794443.json`, manifest-driven final-readiness `output\codex-evidence\final-readiness-20260630142700.json`, READY bundle export, and READY safety.

- 2026-06-29 OperatorTaskPanel group-busy lock pass: the shared first-screen task panel now treats any busy action as a group lock. The active running action still shows `处理中...` or the page-specific busy copy, spinner, `button-loading`, and `aria-busy=true`; sibling actions become disabled without spinner/`aria-busy`, so operators see that the current click was accepted while other actions are intentionally blocked. This applies across pages that reuse `OperatorTaskPanel` and prevents duplicate saves/imports/generation clicks from racing each other. Focused checks passed first as a failing test, then green after implementation: `pnpm exec vitest run apps\desktop\src\renderer\components\operator-task-panel.test.tsx --reporter=basic`.

- 2026-06-29 global route-handoff feedback pass: the App Shell now gives every sidebar click and page-level `amazon-ai-ops:navigate` event the same 150ms `转跳中...` response. The pending nav target exposes `aria-busy=true`, displays a compact pending chip, and locks sibling nav buttons during the handoff; the main canvas shows a non-layout-shifting absolute status pill with `aria-live=polite` and a reduced-motion fallback. Focused checks passed first as failing app-shell tests, then green after implementation: `pnpm exec vitest run apps\desktop\src\renderer\components\app-shell.test.tsx --reporter=basic`; follow-up validation passed desktop typecheck, `pnpm run smoke:business-ui-current` with `output\codex-evidence\current-business-ui-smoke-1782787351742.json`, `pnpm run verify:ad-execution`, `build:win`, package launch smoke `output\codex-evidence\package-launch-smoke-1782800794443.json`, manifest-driven final-readiness `output\codex-evidence\final-readiness-20260630142700.json`, READY bundle export, and READY safety.

- 2026-06-30 product route consolidation pass: legacy `product-config` deep links now keep a visible navigation anchor by mapping active and pending App Shell state to `产品管理`, while product-management configuration actions and delivery-matrix product-context repair now route to `product-management` instead of the old hidden configuration page. This keeps ASIN identity, cost, minimum price, target ACOS/TACOS, and product-level next actions in the product workbench where operators expect them, while the legacy route remains compatible for direct links. Focused checks passed first as failing app-shell/product-management/delivery-matrix tests, then green after implementation: `pnpm exec vitest run apps\desktop\src\renderer\components\app-shell.test.tsx apps\desktop\src\renderer\pages\product-management-page.test.tsx apps\desktop\src\renderer\delivery-readiness-matrix.test.ts --reporter=basic`; follow-up validation passed desktop typecheck, `pnpm run smoke:business-ui-current` with `output\codex-evidence\current-business-ui-smoke-1782787351742.json`, `pnpm run verify:ad-execution`, `build:win`, package launch smoke `output\codex-evidence\package-launch-smoke-1782800794443.json`, and manifest-driven final-readiness `output\codex-evidence\final-readiness-20260630142700.json`.

- 2026-06-30 product-card lock feedback pass: `产品管理` product cards now make ASIN selection physically and semantically visible. `buildProductManagementOptionView` drives `aria-pressed`, `点击锁定` / `已锁定` tags, a short selected-card lock pulse with reduced-motion fallback, and a fixed `aria-live` status line that states the toolbar is unlocked and downstream pages will read the selected ASIN. This is selection feedback only; product configuration saves, recommendations, approvals, and Ads execution remain separate. TDD first failed on the missing view helper, then passed with `pnpm exec vitest run apps\desktop\src\renderer\pages\product-management-page.test.tsx --reporter=basic`; production validation passed desktop typecheck, renderer build, `pnpm run smoke:business-ui-current`, `pnpm run verify:ad-execution`, `build:win`, package launch smoke `output\codex-evidence\package-launch-smoke-1782800794443.json`, manifest-driven final-readiness `output\codex-evidence\final-readiness-20260630142700.json`, READY bundle export, and READY safety.

- 2026-06-30 Listing heatmap focus feedback pass: `Listing 优化` keyword rail clicks now produce a production-grade local focus response. Selected keyword buttons expose `aria-pressed`, repeated clicks retrigger a reduced-motion-safe flash on the rail item, matched heatmap sections, and active tokens, and the matrix writes the focused keyword plus matched sections into a fixed `aria-live` status line. This is local coverage review only; it does not submit Amazon or modify Lingxing Listing. TDD first failed on missing `buildListingHeatmapFocusAnnouncement`/flash CSS helpers, then passed with `pnpm exec vitest run apps\desktop\src\renderer\pages\listing-optimization-page.test.ts`; production validation passed desktop typecheck, renderer build, `pnpm run smoke:business-ui-current`, `pnpm run verify:ad-execution`, `build:win`, package launch smoke `output\codex-evidence\package-launch-smoke-1782800794443.json`, manifest-driven final-readiness `output\codex-evidence\final-readiness-20260630142700.json`, READY bundle export, and READY safety.

- 2026-06-30 login-entry micro-response pass: the login form now gives the same immediate production feedback as other async actions. `登录并进入 Ads` switches to `正在确认 ERP 和 Ads 会话...`, displays the shared spinner, exposes `aria-busy=true`, and disables repeat clicks while ERP/Ads session confirmation is running. Remembered-account/password and local-only login guidance now live in a stable `aria-live` status line instead of appearing as optional transient copy. Focused checks passed first as failing app-readiness tests, then green after implementation: `pnpm exec vitest run apps\desktop\src\renderer\app-readiness-status.test.ts --reporter=basic`; follow-up validation passed desktop typecheck, `pnpm run smoke:business-ui-current` with `output\codex-evidence\current-business-ui-smoke-1782787351742.json`, `pnpm run verify:ad-execution`, `build:win`, package launch smoke `output\codex-evidence\package-launch-smoke-1782800794443.json`, manifest-driven final-readiness `output\codex-evidence\final-readiness-20260630142700.json`, READY bundle export, and READY safety.

- 2026-06-25 Lingxing report date-picker commit fix: root cause for the user-visible pause on `创建报告` was that automation filled `dateStartInput` and `dateEndInput` but did not submit/close the Element date range picker before moving on to `每日明细` and `生成报告`. `fillAndCommitLingxingDateRange` now presses Enter, waits for the date picker to close, and falls back to Tab/outside click before failing with a clear operator-facing error. Regression coverage: `pnpm exec vitest run apps/desktop/src/main/download-center-date-range.test.ts`; follow-up validation included desktop typecheck, `build:main`, `build:win`, package launch smoke `output\codex-evidence\package-launch-smoke-1782800794443.json`, current business UI smoke `output\codex-evidence\current-business-ui-smoke-1782787351742.json`, manifest-driven final-readiness `output\codex-evidence\final-readiness-20260630142700.json`, READY bundle export, and READY safety.

- 2026-06-29 readback fixed-evidence preview pass: `执行回读` screenshot capture targets no longer collapse a saved screenshot into path-only text. After approval, before, after, or reload/readback capture succeeds, the target renders a fixed preview with thumbnail when a data URL is available, a file-extension fallback otherwise, the captured file name, and a green `证据已安全固定` badge. This gives the operator an immediate visible confirmation that the screenshot was retained without opening the evidence folder, while the verifier still requires independent approval/before/after/readback files and valid time/value checks. Focused checks passed: `pnpm exec vitest run apps\desktop\src\renderer\pages\readback-page.test.tsx --reporter=basic`, paired readback/delivery tests, desktop typecheck, `pnpm run smoke:business-ui-current` with `output\codex-evidence\current-business-ui-smoke-1782787351742.json`, `pnpm run verify:ad-execution`, `build:win`, package launch smoke `output\codex-evidence\package-launch-smoke-1782800794443.json`, manifest-driven final-readiness `output\codex-evidence\final-readiness-20260630142700.json`, READY bundle export, and READY safety.

- 2026-06-29 readback active-step slider pass: `执行回读` now adds a 2px blue slider under the four-step wizard tab rail. The slider is driven by `--readback-active-step` and `--readback-step-count`, moves horizontally with the active approval/before/after/readback step, and disables transition under `prefers-reduced-motion: reduce`. This is navigation feedback only and does not mark readback evidence complete or relax export verification. Focused checks passed: `pnpm exec vitest run apps\desktop\src\renderer\pages\readback-page.test.tsx --reporter=basic`, desktop typecheck, `pnpm run smoke:business-ui-current` with `output\codex-evidence\current-business-ui-smoke-1782707626681.json`, `pnpm run verify:ad-execution`, `build:win`, package launch smoke `output\codex-evidence\package-launch-smoke-1782800794443.json`, manifest-driven final-readiness `output\codex-evidence\final-readiness-20260630142700.json`, READY bundle export, and READY safety.

- 2026-06-30 readback safety checkbox feedback pass: `执行回读` approval and verification checkbox rows now provide visible local input confirmation. The row highlights on hover/focus, the label uses the shared `scale(0.98)` press response, checked boxes emit a compact green confirmation pulse, and reduced-motion disables animation. This only confirms the checkbox interaction; it does not satisfy approval proof, screenshot proof, readback values, export readiness, or `verify:ad-readback`. TDD first failed on missing `.checkbox-grid` focus/active/checked CSS contracts, then passed with `pnpm exec vitest run apps\desktop\src\renderer\pages\readback-page.test.tsx --reporter=basic`; production validation passed desktop typecheck, renderer build, `pnpm run smoke:business-ui-current`, `pnpm run verify:ad-execution`, `build:win`, package launch smoke `output\codex-evidence\package-launch-smoke-1782800794443.json`, manifest-driven final-readiness `output\codex-evidence\final-readiness-20260630142700.json`, READY bundle export, and READY safety.

- 2026-06-29 dashboard state-light refresh pass: `今日看板` now wires its primary task handoff into the `数据健康` state-light grid. When the first-screen action enters the 150ms `转跳中...` state, shared `StateLightGrid` receives `refreshing=true` and renders a 180ms blue pulse/sweep across the red/green status cards, with animation disabled under `prefers-reduced-motion: reduce`. This closes the spec requirement that the dashboard primary action visibly refresh the readiness lights without changing data or execution state. Focused checks passed: `pnpm exec vitest run apps\desktop\src\renderer\components\ui.test.tsx apps\desktop\src\renderer\pages\dashboard-page.test.ts --reporter=basic`, desktop typecheck, `pnpm run smoke:business-ui-current` with `output\codex-evidence\current-business-ui-smoke-1782708417458.json`, `pnpm run verify:ad-execution`, `build:win`, package launch smoke `output\codex-evidence\package-launch-smoke-1782800794443.json`, manifest-driven final-readiness `output\codex-evidence\final-readiness-20260630142700.json`, READY bundle export, and READY safety.

- 2026-06-26 ScopeBar field-level confirmation and editor popover pass: the global scope editor now gives immediate field-local feedback when an operator changes start date, end date, store, marketplace, ASIN, or batch. The changed input/select receives a green pulse and a reserved `aria-live` confirmation line such as `店铺已记录为待保存范围`, while the batch dropdown confirms `批次已记录为当前范围`; the reserved line prevents layout shift when the confirmation appears or fades. The editor itself now opens as an absolute popover under the sticky ScopeBar with its own border, shadow, and scroll boundary, so clicking `编辑范围` does not push the workspace down. Focused coverage passed: `pnpm exec vitest run apps\desktop\src\renderer\components\scope-bar.test.ts apps\desktop\src\renderer\components\scope-bar.test.tsx --reporter=basic`, `pnpm exec vitest run apps\desktop\src\renderer\components\scope-bar.test.ts apps\desktop\src\renderer\components\ui.test.tsx apps\desktop\src\renderer\components\operator-task-panel.test.tsx --reporter=basic`, desktop typecheck, current business UI smoke `output\codex-evidence\current-business-ui-smoke-1782787351742.json`, `build:win`, package launch smoke `output\codex-evidence\package-launch-smoke-1782800794443.json`, `verify:ad-execution`, and manifest-driven final-readiness `output\codex-evidence\final-readiness-20260630142700.json`.
- 2026-06-26 operation-scope page-level range form pass: `工作范围` now includes a `范围表单` below the first-screen task panel, so operators can adjust store, marketplace, date range, ASIN, and batch without discovering the top ScopeBar first. Each changed form control uses the same green pulse and reserved `aria-live` confirmation line as the ScopeBar editor, and the primary action saves the normalized draft through `saveOperationScope` before updating the shared scope. Focused coverage passed: `pnpm exec vitest run apps\desktop\src\renderer\pages\operation-scope-page.test.ts apps\desktop\src\renderer\components\scope-bar.test.ts --reporter=basic`, desktop typecheck, current business UI smoke `output\codex-evidence\current-business-ui-smoke-1782787351742.json`, `verify:ad-execution`, `build:win`, package launch smoke `output\codex-evidence\package-launch-smoke-1782800794443.json`, and manifest-driven final-readiness `output\codex-evidence\final-readiness-20260630142700.json`.

- 2026-06-25 approval decision-strip hover/focus feedback pass: shared `DecisionActionStrip` now renders as an accessible decision group and carries a `data-hover-fade` contract. CSS weakens non-focused available actions to 40% opacity on hover or keyboard focus, while the active approval/review/reject button keeps its highlight and active `scale(0.98)` response. Focused checks passed: `pnpm exec vitest run apps\desktop\src\renderer\components\ui.test.tsx apps\desktop\src\renderer\pages\approval-page.test.tsx --reporter=basic`, desktop typecheck, `build:renderer`, current business UI smoke `output\codex-evidence\current-business-ui-smoke-1782787351742.json`, `build:win`, package launch smoke `output\codex-evidence\package-launch-smoke-1782800794443.json`, `verify:ad-execution`, and manifest-driven final-readiness `output\codex-evidence\final-readiness-20260630142700.json`.

Implemented and verified:

- Restored and retained the existing v1.2 module set.
- Added v1.5 shared types, resources, field mappings, prompts, and page-model diagnostics.
- Added Lingxing report collector with 8 report definitions, batch manifest, file verification, report subset batches, manual single-report retry, automatic retry metadata, and failure evidence hooks.
- Added a local simulated download-center E2E test that drives the exact 8 report types through create, wait, download, file verification, and manifest generation, with manifest/result parity assertions. This proves the local orchestration path but does not replace live Lingxing E2E.
- Added Lingxing E2E acceptance audit export. After a batch, the app can export JSON/Markdown audit evidence plus DB-loaded batch result, DB-loaded same-model/same-date/same-store/same-marketplace diagnostic evidence, and a bounded manifest copy; the audit only passes when all 8 expected reports, files, filename date tokens, manifest identity/content, batch store/site scope, and matching diagnostic gate evidence are present.
- Added desktop main-process path-boundary tests for acceptance audit manifest handling, including same-prefix sibling directory rejection, manifest filename enforcement, unreadable JSON handling, safe export directory segment sanitization, and a capability-gated symlink escape regression.
- Added keyword report import, keyword opportunity scoring, Listing import/manual entry, coverage analysis, suggestions, drafts, status updates, and CSV/XLSX/Markdown export.
- Keyword report import now uses parse diagnostics: missing critical keyword columns fail clearly, invalid row ratios above 5% fail the import, and optional missing/unparseable numeric fields are surfaced as warnings instead of being silently hidden.
- Keyword parse diagnostics can now be exported as a local formula-safe CSV so operators can inspect warning/error rows outside the app.
- Duplicate keyword report imports now support merge, overwrite, or skip from the workspace UI. The backend treats duplicate scope as source type plus canonical file path, merge skips already-imported source rows, skip refreshes the UI without writing DB rows, and opportunity refreshes upsert by ASIN plus normalized keyword instead of appending duplicates.
- Added SQLite tables for v1.5 report batches/files, keyword metrics/opportunities, Listing content/coverage/suggestions/drafts, and download-center diagnostics.
- Added desktop v1.5 workbench UI and IPC/preload APIs.
- Added user guide and acceptance matrix.

## Latest Increment

- 2026-06-26 Data-import validation sortable feedback pass: `数据导入与校验` now exposes sortable headers on the virtualized 8-report validation table for report, real file, extension, size, imported rows, and status. Sorting is local and stable, never mutates the source report order, announces the active order plus real-report/imported-row counts through a fixed `aria-live` line, and applies a reduced-motion-safe 200ms blur/sweep plus row fade-in refresh to the table shell so operators get immediate feedback after clicking a header. TDD evidence: `apps\desktop\src\renderer\pages\data-import-validation-page.test.ts` first failed on missing sort helpers/ARIA/stylesheet contract, then passed after implementation. Verification passed: `pnpm exec vitest run apps\desktop\src\renderer\pages\data-import-validation-page.test.ts --reporter=basic`, desktop typecheck, `pnpm run smoke:business-ui-current` with `output\codex-evidence\current-business-ui-smoke-1782787351742.json`, `pnpm run verify:ad-execution`, `build:win`, package launch smoke `output\codex-evidence\package-launch-smoke-1782800794443.json`, manifest-driven final-readiness `output\codex-evidence\final-readiness-20260630142700.json`, READY bundle export, and READY safety.

- 2026-06-26 Ad-quant metric inactive-dimming pass: `TagMetricGroup` now supports opt-in inactive-chip dimming for controlled focus groups. `广告量化` enables it when the current focus is not `全部对象`, so non-active metric chips fade to 60% opacity and recover on hover/focus while staying clickable. This closes the spec's "非激活时透明度自动调至 60% 降噪" requirement without changing the existing focus filter semantics: diagnostic rows, object timelines, and review queues still use the same `adQuantDiagnosticMatchesFocus` and `adQuantTimelineMatchesFocus` contracts, and the `aria-live` line still states that focus does not mutate recommendations, approvals, or Ads state. TDD evidence: `apps\desktop\src\renderer\components\tag-metric-group.test.tsx` first failed on missing `tag-metric-dimmed`, then passed after implementation. Verification passed: `pnpm exec vitest run apps\desktop\src\renderer\components\tag-metric-group.test.tsx apps\desktop\src\renderer\pages\ad-quant-page.test.tsx --reporter=basic`, desktop typecheck, `pnpm run smoke:business-ui-current` with `output\codex-evidence\current-business-ui-smoke-1782787351742.json`, `build:win`, package launch smoke `output\codex-evidence\package-launch-smoke-1782800794443.json`, manifest-driven final-readiness `output\codex-evidence\final-readiness-20260630142700.json`, READY bundle export, and READY safety.

- 2026-06-26 Product-config bulk target ACOS pass: `产品配置` now adds a current-scope bulk toolbar above the product table. Operators can select individual product rows or all visible products, enter `目标 ACOS (%)`, and apply it to the selected products through the existing local `saveProductConfig` IPC. The helper normalizes percent values such as `35` or `12.5` into decimal thresholds, blocks invalid values and empty selections, preserves product identity/cost fields while replacing only `targetAcos`, clears selection on success, and keeps failed selections visible with an error message. This is local product-configuration maintenance only; it does not approve recommendations, generate Ads actions, or write Amazon Ads. TDD evidence: `apps\desktop\src\renderer\pages\product-config-page.test.ts` first failed on missing `normalizeProductConfigAcosPercent`, `buildProductConfigBulkApplyState`, and `buildProductConfigBulkSaveInput`, then passed after implementation. Verification passed: `pnpm exec vitest run apps\desktop\src\renderer\pages\product-config-page.test.ts --reporter=basic`, desktop typecheck, `pnpm run smoke:business-ui-current` with `output\codex-evidence\current-business-ui-smoke-1782787351742.json`, `build:win`, package launch smoke `output\codex-evidence\package-launch-smoke-1782800794443.json`, manifest-driven final-readiness `output\codex-evidence\final-readiness-20260630142700.json`, READY bundle export, and READY safety.

- 2026-06-26 Product-config keyboard nudge and live health feedback pass: `产品配置` now closes the v1.5 high-density product-target input gap. Cost, minimum price, target net margin, target ACOS, and target TACOS fields handle ArrowUp/ArrowDown as explicit draft nudges, clamp unsafe negative/rate overflow values, and show a fixed inline `已调整，失焦或回车保存` response without writing the DB until the existing blur-or-Enter autosave path runs. The fixed-cost, margin, target ACOS, and target TACOS summary chips now recolor live from the current draft value, so operators see product-threshold risk before saving. TDD evidence: `apps\desktop\src\renderer\pages\product-config-page.test.ts` first failed on missing `productConfigNudgeCostValue` and `productConfigMetricTone`, then passed after implementation. Verification passed: `pnpm exec vitest run apps\desktop\src\renderer\pages\product-config-page.test.ts --reporter=basic`, desktop typecheck, `pnpm run smoke:business-ui-current` with `output\codex-evidence\current-business-ui-smoke-1782787351742.json`, `pnpm run verify:ad-execution`, `build:win`, package launch smoke `output\codex-evidence\package-launch-smoke-1782800794443.json`, and manifest-driven final-readiness `output\codex-evidence\final-readiness-20260630142700.json`.

- 2026-06-26 VirtualDataTable row press/focus feedback pass: shared `VirtualDataTable` rows now close the v1.5 heavy-table physical feedback gap. Body rows keep the existing hover highlight, add a blue inset anchor and blue-tinted background on active press, and show the same anchor on `focus-within` for keyboard-driven row controls. `prefers-reduced-motion: reduce` now disables table-row and sort-arrow transitions and stops the skeleton shimmer animation. TDD evidence: `apps\desktop\src\renderer\components\virtual-data-table.test.tsx` covers active/focus row styling, reduced-motion sort-arrow and skeleton guards, sticky header, skeleton overlay, and cell containment. Verification passed: `pnpm exec vitest run apps\desktop\src\renderer\components\virtual-data-table.test.tsx --reporter=basic`, desktop typecheck, `pnpm run smoke:business-ui-current` with `output\codex-evidence\current-business-ui-smoke-1782787351742.json`, `pnpm run verify:ad-execution`, `build:win`, package launch smoke `output\codex-evidence\package-launch-smoke-1782800794443.json`, and manifest-driven final-readiness `output\codex-evidence\final-readiness-20260630142700.json`.

- 2026-06-26 FormTable focus-within glow feedback pass: shared `FormTableRow` now has row-level focus feedback for the v1.5 structured form contract. A focused row gets a stable blue inset anchor line, light blue row background, two-layer focus glow, and blue label text without changing row dimensions; blocked/warning input feedback and reserved `aria-live` repair text remain visible and non-jumpy. Because the atom is used by settings, product management, product config, and other structured editors, this improves field orientation globally instead of adding page-specific focus styling. TDD evidence: `apps\desktop\src\renderer\components\ui.test.tsx` covers the focus-within glow, label color, motion transition, reduced-motion guard, reserved feedback slot, and blocked styling. Verification passed: `pnpm exec vitest run apps\desktop\src\renderer\components\ui.test.tsx --reporter=basic`, desktop typecheck, `pnpm run smoke:business-ui-current` with `output\codex-evidence\current-business-ui-smoke-1782787351742.json`, `pnpm run verify:ad-execution`, `build:win`, package launch smoke `output\codex-evidence\package-launch-smoke-1782800794443.json`, and manifest-driven final-readiness `output\codex-evidence\final-readiness-20260630142700.json`.
- 2026-06-26 MicroStepper status indicator feedback pass: shared `MicroStepper` rows now render a leading status indicator so collection progress is no longer only textual. Ready rows show a solid green dot, warning and blocked rows show semantic color rings, and pending rows use a rotating front ring to match the v1.5 micro-stepper expectation for queued/running work. The animation is disabled under `prefers-reduced-motion: reduce`, and the component remains text-stable for strict smoke checks. TDD evidence: `apps\desktop\src\renderer\components\ui.test.tsx` covers the hidden indicator DOM, grid column contract, pending spinner animation, keyframes, and reduced-motion guard. Verification passed: `pnpm exec vitest run apps\desktop\src\renderer\components\ui.test.tsx --reporter=basic`, desktop typecheck, `pnpm run smoke:business-ui-current` with `output\codex-evidence\current-business-ui-smoke-1782787351742.json`, `pnpm run verify:ad-execution`, `build:win`, package launch smoke `output\codex-evidence\package-launch-smoke-1782800794443.json`, and manifest-driven final-readiness `output\codex-evidence\final-readiness-20260630142700.json`.

- 2026-06-26 StateLightGrid hover lift feedback pass: shared status-light cards now satisfy the v1.5 dashboard red/green-light micro-response contract. `.state-light-card` keeps fixed layout dimensions but adds hover `translateY(-2px)` plus a soft shadow, and `prefers-reduced-motion: reduce` disables the transform. Because `StateLightGrid` is reused by dashboard, operation scope, keyword opportunities, Listing optimization, recommendations, and scheduler pages, this closes the hover-feedback gap globally instead of patching individual pages. TDD evidence: `apps\desktop\src\renderer\components\ui.test.tsx` first caught the selector-collision edge case, then passed after the stylesheet contract assertion was tightened. Verification passed: `pnpm exec vitest run apps\desktop\src\renderer\components\ui.test.tsx --reporter=basic`, desktop typecheck, `pnpm run smoke:business-ui-current` with `output\codex-evidence\current-business-ui-smoke-1782787351742.json`, `pnpm run verify:ad-execution`, `build:win`, package launch smoke `output\codex-evidence\package-launch-smoke-1782800794443.json`, and manifest-driven final-readiness `output\codex-evidence\final-readiness-20260630142700.json`.

- 2026-06-25 readback time/value contract visualization pass: `执行回读` step `3. 补执行前后和回读` now renders a `时间和值安全合同` panel before the evidence form. The panel splits verifier-aligned prechecks into `时间顺序`, `前后值变化`, `回读值一致`, `动作方向`, and `截图不复用`, with `通过` / `待填写` / `阻断` status cards and an `aria-live` summary. It reuses the same value parsing, time ordering, lower-bid direction, and evidence-path reuse rules as export precheck, so blocked contracts remain gap drafts and cannot be mistaken for final proof. Verification passed: `pnpm exec vitest run apps\desktop\src\renderer\pages\readback-page.test.tsx --reporter=basic`, desktop typecheck, `build:renderer`, `node scripts\smoke-business-ui-ad-execution.js` with `output\codex-evidence\business-ui-ad-execution-smoke-1782377074871.json`, `pnpm run smoke:business-ui-current` with `output\codex-evidence\current-business-ui-smoke-1782787351742.json`, `pnpm run verify:ad-execution`, `build:win`, package launch smoke `output\codex-evidence\package-launch-smoke-1782800794443.json`, manifest-driven final-readiness `output\codex-evidence\final-readiness-20260630142700.json`, READY bundle export, and READY safety.

- 2026-06-25 scheduler first-screen local task feedback plus scheduler controller/row action-button busy feedback pass: `定时任务` now starts with an `OperatorTaskPanel` that makes local scheduling status and the next safe action visible before the lower task table. The default action refreshes scheduler state with the shared busy/spinner/disabled contract; manual run requests become a named confirmation state with `执行本地任务` / `返回任务列表`; refresh, pending, running, success, and failure states stay in a fixed `aria-live` feedback line. The panel repeats the safety boundary that local scheduling may download/import/generate local pending materials but cannot approve suggestions, adjust bids, add negatives, pause ads, or write Amazon Ads. Verification passed: `pnpm exec vitest run apps\desktop\src\renderer\pages\scheduler-page.test.ts apps\desktop\src\renderer\components\operator-task-panel.test.tsx --reporter=basic`, desktop typecheck, `build:renderer`, `node scripts\smoke-business-ui-shell.js` with `output\codex-evidence\business-ui-shell-smoke-1782372507446.json`, `pnpm run smoke:business-ui-current` with `output\codex-evidence\current-business-ui-smoke-1782787351742.json`, `pnpm run verify:ad-execution`, `build:win`, package launch smoke `output\codex-evidence\package-launch-smoke-1782800794443.json`, and manifest-driven final-readiness `output\codex-evidence\final-readiness-20260630142700.json`.

- 2026-06-25 product-management first-screen task panel pass: `产品管理` now starts with a `产品作战台` `OperatorTaskPanel` instead of leaving the next step buried in cards. The task panel reports product count, selected ASIN, imported metric rows, daily-row coverage, and Main credential sandbox hover/focus boundary; selection/save/loading/error states land in a fixed `aria-live` feedback line. The page no longer silently treats the first product as selected when `scope.asin` is empty, and selected products without imported metrics route the main task to `数据导入与校验` before AI quantification. Verification passed: `pnpm exec vitest run apps\desktop\src\renderer\pages\product-management-page.test.tsx apps\desktop\src\renderer\components\operator-task-panel.test.tsx --reporter=basic`, desktop typecheck, `build:renderer`, `pnpm run smoke:business-ui-current` with `output\codex-evidence\current-business-ui-smoke-1782787351742.json`, `pnpm run verify:ad-execution`, `build:win`, package launch smoke `output\codex-evidence\package-launch-smoke-1782800794443.json`, and manifest-driven final-readiness `output\codex-evidence\final-readiness-20260630142700.json`.

- 2026-06-25 product management workbench and maintenance implementation: `运营总览` now has `产品管理` as the ASIN-level operator entry. The new page builds product summaries from product context, ad diagnostics, product ledgers, and operation events; product selection writes `scope.asin`; product timelines include current-product events, ad-object events, and global operation events while excluding other products; `运营事件` now has product/global/all views; `ScopeBar` shows product title plus ASIN; and `广告量化` product selection now syncs to the shared scope. Focused implementation checks passed during this increment: `pnpm exec vitest run apps\desktop\src\renderer\product-management.test.ts --reporter=dot`, `pnpm exec vitest run apps\desktop\src\main\operation-event-scope.test.ts apps\desktop\src\main\ad-product-history-ledger.test.ts --reporter=dot`, `pnpm exec vitest run apps\desktop\src\renderer\pages\product-management-page.test.tsx --reporter=dot`, `pnpm exec vitest run apps\desktop\src\renderer\components\scope-bar.test.tsx apps\desktop\src\renderer\product-management.test.ts --reporter=dot`, `pnpm exec vitest run apps\desktop\src\renderer\pages\operation-events-page.test.tsx apps\desktop\src\renderer\product-management.test.ts --reporter=dot`, and `pnpm exec vitest run apps\desktop\src\renderer\ad-quant-product-groups.test.ts apps\desktop\src\renderer\pages\ad-quant-page.test.tsx --reporter=dot`. Final READY evidence for this code has been refreshed through `build:win`, package launch smoke `output\codex-evidence\package-launch-smoke-1782800794443.json`, manifest-driven final-readiness `output\codex-evidence\final-readiness-20260630142700.json`, READY bundle `output\delivery-bundles\v15-delivery-bundle-20260630142700-ready`, and READY safety.

- 2026-06-25 Lingxing full-8 collection auto-verification pass: root cause for the apparent "no response" full-8 refresh was the strict same-scope download-center diagnostic preflight. Older successful runs had matching diagnostics for their exact date range/store/site, while the current operator range did not, so the backend failed closed before clicking Lingxing report creation. The `重新获取完整 8 类报表` flow now detects that missing page-model/diagnostic gate, runs the read-only download-center diagnostic for the current range, stores the diagnostic result in the page, and retries the original create/download action only after the diagnostic is ready. Regression coverage was added around this retry contract. Verification passed: `pnpm exec vitest run apps\desktop\src\renderer\pages\data-collection-page.test.ts --reporter=dot`, focused renderer tests covering data collection/ad quant/evidence display, desktop typecheck, `build:renderer`, data-pipeline smoke `output\codex-evidence\business-ui-data-pipeline-smoke-1782369583516.json`, current business UI smoke `output\codex-evidence\current-business-ui-smoke-1782787351742.json`, `build:win`, package launch smoke `output\codex-evidence\package-launch-smoke-1782800794443.json`, and manifest-driven final-readiness `output\codex-evidence\final-readiness-20260630142700.json`.
- 2026-06-25 data-collection monitor drawer with Canvas browser preview pass: `数据采集` now opens a right-side `自动数据采集监控` drawer immediately for verify/download/recreate/import actions. The drawer shows running, blocked, and returned states, summarizes current steps and evidence paths, cannot be dismissed while a task is running, and avoids repeating page-level result/error text. The drawer uses non-intercepting pointer behavior after completion so it does not block the next main workflow click; this was validated by fixing the data-pipeline smoke failures where the drawer first intercepted `导入本地报表` and then duplicated strict text targets. Verification passed: `pnpm exec vitest run apps\desktop\src\renderer\pages\data-collection-page.test.ts --reporter=basic`, `pnpm --filter @amazon-ai-ops/desktop run build:renderer`, `node scripts\smoke-business-ui-data-pipeline.js` with `output\codex-evidence\business-ui-data-pipeline-smoke-1782369583516.json`, `pnpm run smoke:business-ui-current`, `pnpm --filter @amazon-ai-ops/desktop run build:win`, `pnpm run smoke:package-launch`, and manifest-driven final-readiness.

- 2026-06-25 OperatorTaskPanel loading micro-response pass: shared task-panel actions now accept `busy` and `busyLabel`, render a spinner, set `aria-busy`, disable the button, and switch copy to `处理中...` or the page-specific running label. Existing async task-panel actions for data collection, import validation, ad quant AI diagnosis, keyword opportunity recognition, Listing draft generation, recommendation generation, approval queue loading, and readback approved-action refresh now use this contract instead of only changing surrounding page text. TDD evidence: `apps\desktop\src\renderer\components\operator-task-panel.test.tsx` failed first on missing `busy` behavior/styles, then passed after implementation. Verification passed: `pnpm exec vitest run apps\desktop\src\renderer\components\operator-task-panel.test.tsx apps\desktop\src\renderer\pages\data-collection-page.test.ts apps\desktop\src\renderer\pages\data-import-validation-page.test.tsx apps\desktop\src\renderer\pages\ad-quant-page.test.tsx apps\desktop\src\renderer\pages\keyword-opportunities-page.test.tsx apps\desktop\src\renderer\pages\listing-optimization-page.test.ts apps\desktop\src\renderer\pages\recommendations-page.test.tsx apps\desktop\src\renderer\pages\approval-page.test.tsx apps\desktop\src\renderer\pages\readback-page.test.tsx --reporter=basic`, desktop typecheck, `build:renderer`, current business UI smoke `output\codex-evidence\current-business-ui-smoke-1782787351742.json`, `build:win`, package launch smoke `output\codex-evidence\package-launch-smoke-1782800794443.json`, `verify:ad-execution`, and manifest-driven final-readiness `output\codex-evidence\final-readiness-20260630142700.json`.

- 2026-06-25 settings AI connection first-screen feedback pass: `AI 设置` now starts with an `OperatorTaskPanel` for the current AI connection task. `保存 AI 设置` and `测试 AI 连接` use the shared busy/disabled/spinner contract, while saved/testing/success/failure copy is fixed in an `aria-live` feedback bubble and filtered out of the bottom status panel to avoid duplicate operator messages. TDD evidence: `apps\desktop\src\renderer\pages\settings-page.test.ts` first failed on the missing task-feedback helpers and duplicate-message filter, then passed after implementation. Verification passed: `pnpm exec vitest run apps\desktop\src\renderer\pages\settings-page.test.ts apps\desktop\src\renderer\components\operator-task-panel.test.tsx --reporter=basic`, desktop typecheck, `build:renderer`, `node scripts\smoke-business-ui-settings-delivery.js`, `pnpm run smoke:business-ui-current` with `output\codex-evidence\current-business-ui-smoke-1782787351742.json`, `build:win`, package launch smoke `output\codex-evidence\package-launch-smoke-1782800794443.json`, `verify:ad-execution`, and manifest-driven final-readiness `output\codex-evidence\final-readiness-20260630142700.json`.
- 2026-06-26 settings threshold field-level feedback pass: `AI 设置` now maps invalid advertising-threshold rules back onto the exact `FormTableRow` instead of only showing a global blocked sentence. `FormTableRow` supports a reserved `aria-live` feedback slot with blocked/warning/ready/pending tones, and invalid rows such as target ACOS, high ACOS threshold, no-order clicks, minimum spend, bid decrease ratio, max decrease, min CPC, and max CPC receive field-local repair copy plus red or amber input emphasis without replacing the original hint. TDD evidence: `apps\desktop\src\renderer\components\ui.test.tsx` and `apps\desktop\src\renderer\pages\settings-page.test.ts` cover the reserved feedback slot, CSS height contract, exact rule-to-field mapping, and valid-state silence. Verification passed: `pnpm exec vitest run apps\desktop\src\renderer\components\ui.test.tsx apps\desktop\src\renderer\pages\settings-page.test.ts --reporter=basic`, desktop typecheck, `pnpm run smoke:business-ui-current` with `output\codex-evidence\current-business-ui-smoke-1782787351742.json`, `pnpm run verify:ad-execution`, `build:win`, package launch smoke `output\codex-evidence\package-launch-smoke-1782800794443.json`, and manifest-driven final-readiness `output\codex-evidence\final-readiness-20260630142700.json`.

- 2026-06-26 global button active micro-response pass: the renderer stylesheet now uses `transform: scale(0.98)` for enabled button active press feedback and a global disabled `cursor: not-allowed` contract, matching the v1.5 16ms micro-response spec. Primary/secondary action buttons and data-collection action buttons no longer override active feedback with vertical `translateY(1px)`. TDD evidence: `apps\desktop\src\renderer\components\ui.test.tsx` checks the global CSS contract and asserts the old `translateY(1px)` fallback is absent. Verification passed: `pnpm exec vitest run apps\desktop\src\renderer\components\ui.test.tsx --reporter=basic`, desktop typecheck, `pnpm run smoke:business-ui-current` with `output\codex-evidence\current-business-ui-smoke-1782787351742.json`, `pnpm run verify:ad-execution`, `build:win`, package launch smoke `output\codex-evidence\package-launch-smoke-1782800794443.json`, and manifest-driven final-readiness `output\codex-evidence\final-readiness-20260630142700.json`.

- 2026-06-26 OperatorTaskPanel shimmer sweep pass: first-screen task panels now render the spec's subtle card shimmer through `.operator-task-panel::before` without adding DOM nodes or intercepting clicks. The sweep is layered below the task text/actions with `z-index`, uses `pointer-events: none`, and is disabled for `prefers-reduced-motion: reduce`, so it adds visible responsiveness without blocking controls or creating accessibility churn. TDD evidence: `apps\desktop\src\renderer\components\operator-task-panel.test.tsx` covers the pseudo-element, keyframes, content/action layer order, and reduced-motion guard. Verification passed: focused `operator-task-panel` and shared UI tests, desktop typecheck, `pnpm run smoke:business-ui-current` with `output\codex-evidence\current-business-ui-smoke-1782787351742.json`, `pnpm run verify:ad-execution`, `build:win`, package launch smoke `output\codex-evidence\package-launch-smoke-1782800794443.json`, and manifest-driven final-readiness `output\codex-evidence\final-readiness-20260630142700.json`.

- 2026-06-25 operation-scope first-screen confirmation, page-level range form, ScopeBar field-level confirmation feedback, and non-layout-shifting ScopeBar editor popover pass: `工作范围` now has a first-screen `OperatorTaskPanel` that summarizes real report coverage, imported metric rows, active batch, and USD scope. The primary action explicitly persists the current date/store/site/batch/ASIN/USD scope through `saveOperationScope`, uses the shared busy/disabled/spinner contract, and writes unsaved/saving/saved/error copy into a fixed `aria-live` feedback line. The page-level `范围表单` lets operators edit store, marketplace, date range, ASIN, and batch directly on the page; every changed control pulses green in reserved field space and clears stale batch binding when the date/store/site changes. The page-level `编辑范围` action still opens the sticky ScopeBar editor as an absolute popover through `amazon-ai-ops:open-scope-editor`, while the next-step action routes to data collection, import validation, or ad quantification from the current data state. TDD evidence: `apps\desktop\src\renderer\pages\operation-scope-page.test.ts` failed first on missing task/save-feedback helpers, then passed. Verification passed: `pnpm exec vitest run apps\desktop\src\renderer\pages\operation-scope-page.test.ts --reporter=basic`, `pnpm exec vitest run apps\desktop\src\renderer\components\scope-bar.test.tsx apps\desktop\src\renderer\components\operator-task-panel.test.tsx --reporter=basic`, desktop typecheck, `build:renderer`, current business UI smoke `output\codex-evidence\current-business-ui-smoke-1782787351742.json`, `build:win`, package launch smoke `output\codex-evidence\package-launch-smoke-1782800794443.json`, `verify:ad-execution`, and manifest-driven final-readiness `output\codex-evidence\final-readiness-20260630142700.json`.

- 2026-06-25 operation-event task feedback pass: `运营事件` now has a first-screen `OperatorTaskPanel` that summarizes current event count, product/global scope, bound ad-object context, and local-only boundary. The top action records the event with shared busy/disabled/spinner feedback, the lower form action is renamed `保存到上下文` so it is not confused with the page's primary task action, and the newest saved event card flashes briefly after persistence. TDD evidence: `apps\desktop\src\renderer\pages\operation-events-page.test.tsx` failed first on the missing task-state/card-class helpers and duplicate primary action naming, then passed after implementation. Verification passed: `pnpm exec vitest run apps\desktop\src\renderer\pages\operation-events-page.test.tsx apps\desktop\src\renderer\components\operator-task-panel.test.tsx --reporter=basic`, desktop typecheck, `build:renderer`, current business UI smoke `output\codex-evidence\current-business-ui-smoke-1782787351742.json`, `build:win`, package launch smoke `output\codex-evidence\package-launch-smoke-1782800794443.json`, `verify:ad-execution`, and manifest-driven final-readiness `output\codex-evidence\final-readiness-20260630142700.json`.
- 2026-06-26 operation-event optimistic clear pass: after `记录事件` or `保存到上下文`, the submitted event form now clears immediately, runs a short rebound animation, and shows `事件已提交，表单已清空，正在写入本地上下文...`; if IPC persistence fails, the page restores the submitted draft and tells the operator `保存失败，已恢复刚才填写的事件。`. Focused tests assert the submit-before-IPC ordering, failure restore path, and CSS rebound class. Verification passed: `pnpm exec vitest run apps\desktop\src\renderer\pages\operation-events-page.test.tsx --reporter=basic`, desktop typecheck, `pnpm run smoke:business-ui-current`, `pnpm run verify:ad-execution`, Windows build, package launch smoke `output\codex-evidence\package-launch-smoke-1782800794443.json`, manifest-driven final-readiness `output\codex-evidence\final-readiness-20260630142700.json`, READY bundle export, and READY safety.
- 2026-06-30 operation-events inline save action-button busy pass: the bottom `保存到上下文` action now uses `operationEventInlineSaveButtonView`. While the local operation-context save is pending, the active button switches to `保存中...`, shows the shared spinner, exposes `aria-busy=true`, carries `button-loading`, and disables duplicate submissions; incomplete drafts stay plain disabled without spinner/`aria-busy`, so unavailable is not confused with running. This remains local operation-context feedback only and does not generate recommendations, approve actions, mutate Listing, or write Amazon Ads. Incremental checks passed: red/green focused operation-events test, desktop typecheck, renderer build, `pnpm run verify:ad-execution`, `pnpm run smoke:business-ui-current`, Windows build, package launch smoke `output\codex-evidence\package-launch-smoke-1782800794443.json`, and manifest-driven final-readiness `output\codex-evidence\final-readiness-20260630142700.json`.

- 2026-06-25 product-config inline autosave feedback pass: `产品配置` now has a first-screen `OperatorTaskPanel` for product target maintenance, and cost/min-price/target net margin/target ACOS/target TACOS fields autosave on blur or Enter through the same `saveProductConfig` IPC. Each inline field has a fixed `aria-live` status for saving/saved/error, while `保存完整产品配置` remains for base product fields. TDD evidence: `apps\desktop\src\renderer\pages\product-config-page.test.ts` failed first on missing task/autosave helpers, then passed. Verification passed: focused product config/product management/operator-task-panel tests, desktop typecheck, `build:renderer`, current business UI smoke `output\codex-evidence\current-business-ui-smoke-1782787351742.json`, `build:win`, package launch smoke `output\codex-evidence\package-launch-smoke-1782800794443.json`, `verify:ad-execution`, and manifest-driven final-readiness `output\codex-evidence\final-readiness-20260630142700.json`.

- 2026-06-25 high-fidelity AI contract/tag/table UX refresh: Settings explains AI output as fixed system-owned contracts with tags `广告诊断 v1`, `广告解释 v1`, `Listing 草案 v1`, and `异常回退规则`; advanced settings no longer show raw contract IDs to the operator, and the persona field is expression-style only. Legacy low `aiMaxTokens` values are normalized up to 8192 before structured calls, and ad strategy diagnosis output filters missing/invented `evidenceRefs` and fills lifecycle, threshold, and candidate references from real current `evidencePack` IDs only. `AI+规则建议输入检查` defaults to compact metric tags and moves long explanations under `输入明细和判断依据`. Main workflow pages use compact status tags, state lights, decision strips, and table-like data entry to reduce first-screen text overload. `手工录入当前 Listing` is a table-like editor grouped by `基础信息`, `标题`, `五点`, and `详情与搜索词`. Verification passed: focused renderer/AI tests, desktop typecheck, `build:renderer`, `build:win`, `pnpm run smoke:business-ui-current` with `output\codex-evidence\current-business-ui-smoke-1782787351742.json`, `pnpm run smoke:package-launch` with `output\codex-evidence\package-launch-smoke-1782800794443.json`, `pnpm run verify:ad-execution`, manifest-driven final-readiness `output\codex-evidence\final-readiness-20260630142700.json`, READY bundle export to `output\delivery-bundles\v15-delivery-bundle-20260630142700-ready`, and READY safety.

- 2026-06-25 live AI strategy diagnosis JSON-contract fix: root cause for repeated operator-visible "AI 输出格式不满足固定合同 / 规则兜底" was not only prompt wording. The OpenAI-compatible provider ignored saved `AIConfig.temperature` and `AIConfig.maxTokens` when callers omitted per-call overrides, so structured strategy diagnosis fell back to `0.7 / 2000` even after settings normalized the UI value to 8192. The diagnosis prompt also showed placeholder JSON values such as `"number"` and `"0..1"`, which made malformed contract echoes more likely under long evidence packs. The fix makes provider defaults come from saved config, passes an 8192 token floor to both diagnosis and JSON repair, and emits concrete JSON examples from current rule config plus current `evidencePack` IDs. Regression coverage passed for `packages/ai-adapter/src/openai-compatible.test.ts`, `packages/ai-adapter/src/ad-strategy-diagnosis.test.ts`, and `apps/desktop/src/renderer/pages/ad-quant-page.test.tsx`; live DeepSeek verification passed with `output\codex-evidence\ad-strategy-live-1782358641101.json`, returning `source=ai`, `schemaVersion=ad_strategy_diagnosis_v1`, and no fallback for the current DB scope.

- 2026-06-25 ad readback screenshot paste/drop capture with drag-over feedback plus fixed thumbnail/badge confirmation: `执行回读` now has visible capture targets for approval proof, before screenshot, after screenshot, and reload/readback screenshot. Operators can drag a PNG/JPG/WebP file or paste a screenshot with Ctrl+V; drag-over switches the target copy to `松开即可存证`, animates a blue moving dashed border, and keeps the saving state separate so the operator sees recognition before releasing the file. The main process writes the image into the current session directory from `session-paths.json`, rejects non-image payloads and session path escapes, updates `session-input.json` path/time fields when present, and falls back to an app-owned export evidence folder before a session exists. The renderer auto-picks the next missing evidence slot for global paste on the evidence step while preserving the existing verifier boundary. Focused checks passed: `pnpm exec vitest run apps\desktop\src\renderer\pages\readback-page.test.tsx apps\desktop\src\main\ad-readback-capture.test.ts apps\desktop\src\preload\index.test.ts --reporter=basic` and `pnpm --filter @amazon-ai-ops/desktop run typecheck`; final package validation is covered by `build:win`, package launch smoke `output\codex-evidence\package-launch-smoke-1782800794443.json`, current business UI smoke `output\codex-evidence\current-business-ui-smoke-1782787351742.json`, manifest-driven final-readiness `output\codex-evidence\final-readiness-20260630142700.json`, READY bundle export, and READY safety.

- 2026-06-30 Windows desktop package refresh: reduced operator-facing technical wording, moved dense evidence and diagnostics behind progressive disclosures, added OperatorTaskPanel loading micro-response, added operation-scope first-screen confirmation, page-level range FormTable confirmation, ScopeBar field-level confirmation feedback, and non-layout-shifting ScopeBar editor popover, added operation-event first-screen task feedback plus optimistic clear/rebound/failure restore, added product-config bulk target ACOS apply, inline autosave, keyboard nudge, and live target health feedback, added scheduler first-screen local task feedback plus scheduler controller/row action-button busy feedback, added virtualized heavy tables for `关键词机会` and `数据导入与校验`, added keyword-opportunity sortable header, filter-axis feedback, and refresh action-button busy feedback, added StateLightGrid hover +2px lift feedback, added the Listing keyword heatmap matrix with strict-contained keyword rail cells, added the data-collection monitor drawer with Canvas browser preview, added data-collection lower action-button busy/spinner/striped-progress feedback, added approval decision-button busy/spinner/`aria-busy` feedback, added data-import direct action-button `处理中...`/spinner/`aria-busy` feedback, added delivery readback repair handoff, refreshed current business UI smoke at `output\codex-evidence\current-business-ui-smoke-1782800714782.json`, kept Windows packaging hardened with `npmRebuild=false`, rebuilt installer/portable, and ran `pnpm run smoke:package-launch`. Launch smoke evidence `output\codex-evidence\package-launch-smoke-1782800794443.json` proves `win-unpacked` reached `ipc-ready/window-created` and portable/no-install extracted to Temp and launched `AmazonAIOpsAgent.exe`. New package evidence: installer size `83126786`, SHA-256 `DA48A0F7AFE21F59A0264A31317F641C5C15F92FA8DCB47A369428E3B3994152`; portable/no-install size `82961036`, SHA-256 `07410BD3CDB034EBA9F14B7A12E2FAA68DC5EAD30457500109C6E20EC665A182`. Manifest-driven final-readiness was refreshed at `output\codex-evidence\final-readiness-20260630142700.json` from `output\codex-evidence\v15-final-readiness-evidence-manifest-20260630142700.json`, now including a hard `Package launch smoke` gate. READY bundle path is `output\delivery-bundles\v15-delivery-bundle-20260630142700-ready`.
- 2026-06-30 recommendation batch-selection micro-response pass: `优化建议` batch selection now exposes `countLabel`, `ariaStatus`, and active count-chip class from `recommendationBatchSelectionState`; the toolbar renders a stable `X/N` count chip with active pop feedback and an `aria-live` line, while business-table checkboxes get keyboard focus rings plus checked-state confirmation animation with reduced-motion fallback. The change is UI feedback only: batch handoff still passes selected formal-approval-ready recommendation IDs to `审批中心` and does not approve or execute Ads. TDD first failed on the missing state fields and CSS contract, then passed with `pnpm exec vitest run apps\desktop\src\renderer\pages\recommendations-page.test.ts --reporter=basic`; production validation passed desktop typecheck, `pnpm run smoke:business-ui-current`, `pnpm run verify:ad-execution`, `build:win`, package launch smoke `output\codex-evidence\package-launch-smoke-1782800794443.json`, manifest-driven final-readiness `output\codex-evidence\final-readiness-20260630142700.json`, READY bundle export, and READY safety.
- 2026-06-30 data-collection report-selection feedback pass: `数据采集` now exposes `collectionReportSelectionState` for the 8-report selector. The toolbar renders a stable `已选 X/8 类` count chip, progress rail, and `aria-live` status copy; report cards receive selected styling, focus-within rings, and checkbox checked animation with reduced-motion fallback. This is selection feedback only and does not change download/recreate/import semantics or real-report/DB import gates. TDD first failed because `collectionReportSelectionState` did not exist, then passed with `pnpm exec vitest run apps\desktop\src\renderer\pages\data-collection-page.test.ts --reporter=basic`; production validation passed desktop typecheck, `pnpm run smoke:business-ui-current`, `pnpm run verify:ad-execution`, `build:win`, package launch smoke `output\codex-evidence\package-launch-smoke-1782800794443.json`, manifest-driven final-readiness `output\codex-evidence\final-readiness-20260630142700.json`, READY bundle export, and READY safety.
- 2026-06-30 ad-quant AI-running radar feedback pass: `广告量化` now turns AI stage diagnosis pending state into a visible running surface instead of text-only feedback. `buildStrategyRunFeedback` exposes `ariaBusy` and `radarVisible`, the feedback panel sets `aria-busy=true`, and `.ad-quant-strategy-radar` renders a contained sweep/pulse with reduced-motion fallback. This is running-state feedback only and does not alter AI contracts, formal recommendation gates, approvals, or Ads execution. TDD first failed because the running feedback lacked busy/radar fields, then passed with `pnpm exec vitest run apps\desktop\src\renderer\pages\ad-quant-page.test.tsx --reporter=basic`; production validation passed desktop typecheck, renderer build, `pnpm run smoke:business-ui-current`, `pnpm run verify:ad-execution`, `build:win`, package launch smoke `output\codex-evidence\package-launch-smoke-1782800794443.json`, manifest-driven final-readiness `output\codex-evidence\final-readiness-20260630142700.json`, READY bundle export, and READY safety.
- 2026-06-29 ad-quant waste/high-risk spend tile pass: the `广告量化` product summary grid no longer exposes engineering placeholder wording. The new `buildWasteRiskSpendTile` helper renders `浪费/高风险花费`, shows the amount, explains its share of current product spend, includes the high-risk object count, and falls back to `待日级数据` with an instruction to import all 8 real reports when daily metrics are missing. This is display feedback only and does not change recommendation state, approvals, or Ads execution. Focused checks passed: `pnpm exec vitest run apps\desktop\src\renderer\pages\ad-quant-page.test.tsx --reporter=basic`, desktop typecheck, `pnpm run smoke:business-ui-current`, `pnpm run verify:ad-execution`, `build:win`, package launch smoke `output\codex-evidence\package-launch-smoke-1782800794443.json`, manifest-driven final-readiness `output\codex-evidence\final-readiness-20260630142700.json`, READY bundle export to `output\delivery-bundles\v15-delivery-bundle-20260630142700-ready`, and READY safety.
- 2026-06-29 data-collection lower action-button feedback pass: `数据采集` now gives `下载已创建`, `重建已选`, `重建全部 8 类`, and `导入本地` a shared action-button view model. TDD first failed on missing `collectionActionButtonView`; the helper now returns stable label/detail/disabled/class/`ariaBusy` state, the running action changes to `处理中...`, renders a spinner, exposes `aria-busy=true`, and keeps a blue striped progress surface while sibling actions lock. Focused checks passed: `pnpm exec vitest run apps\desktop\src\renderer\pages\data-collection-page.test.ts --reporter=basic`, `pnpm exec vitest run apps\desktop\src\renderer\pages\data-collection-page.test.ts apps\desktop\src\renderer\components\operator-task-panel.test.tsx apps\desktop\src\renderer\collection-action-summary.test.ts --reporter=basic`, desktop typecheck, `pnpm run smoke:business-ui-current`, `build:win`, package launch smoke, manifest-driven final-readiness, READY bundle export, and READY safety.
- 2026-06-29 approval decision-button feedback pass: `审批中心` approve/reject actions now use `approvalDecisionButtonView` so the active decision button switches to `处理中...`, gets `button-loading`, renders a spinner, exposes `aria-busy=true`, and locks the sibling decision action without making it look like the running action. TDD first failed on missing `approvalDecisionButtonView`, then passed after wiring the helper into the page. Focused checks passed: `pnpm exec vitest run apps\desktop\src\renderer\pages\approval-page.test.tsx --reporter=basic`, `pnpm exec vitest run apps\desktop\src\renderer\pages\approval-page.test.tsx apps\desktop\src\renderer\pages\recommendations-page.test.ts --reporter=basic`, desktop typecheck, `pnpm run smoke:business-ui-current`, `pnpm run verify:ad-execution`, `build:win`, package launch smoke, manifest-driven final-readiness, READY bundle export, and READY safety.
- 2026-06-29 data-import action-button feedback pass: `数据导入与校验` direct import/export buttons now use `dataImportBusyLabel`, `dataImportActionButtonView`, and `dataImportExportButtonView`. TDD first failed on the missing helpers; the first-screen import task button, folded `导入已下载表格` / `导入本地报表`, and `导出数据对账` now switch the active action to `处理中...`, render a spinner, expose `aria-busy=true`, carry `button-loading`, and lock sibling actions without making them look like the running task. Focused checks passed: `pnpm exec vitest run apps\desktop\src\renderer\pages\data-import-validation-page.test.ts --reporter=basic`, desktop typecheck, `pnpm run smoke:business-ui-current`, `pnpm run verify:ad-execution`, `build:win`, package launch smoke `output\codex-evidence\package-launch-smoke-1782800794443.json`, manifest-driven final-readiness `output\codex-evidence\final-readiness-20260630142700.json`, READY bundle export, and READY safety.
- 2026-06-30 data-import sort row-thaw feedback pass: `数据导入与校验` sorting now matches the v1.5 table-response contract more closely. Header clicks now run the 200ms blur/sweep refresh, row fade-in, and `aria-live` coverage update; the virtual table also receives a non-intercepting translucent `::after` overlay with `backdrop-filter: blur(2px)` so the sort action visibly thaws rows without blocking the next action. TDD first failed because `.data-import-table-refreshing .virtual-table-wrap::after` was missing; focused checks then passed for `apps\desktop\src\renderer\pages\data-import-validation-page.test.ts` and `apps\desktop\src\renderer\components\virtual-data-table.test.tsx`, followed by desktop typecheck, `pnpm run smoke:business-ui-current`, `pnpm run verify:ad-execution`, `build:win`, package launch smoke `output\codex-evidence\package-launch-smoke-1782800794443.json`, manifest-driven final-readiness `output\codex-evidence\final-readiness-20260630142700.json`, READY bundle export, and READY safety.
- 2026-06-29 delivery export blocked-state pass: `交付验收` now keeps the folded `文件与技术入口` export button physically disabled until both manifest-driven final readiness and installer/package evidence are recorded. The disabled export button uses a delivery-specific `delivery-export-blocked` state with `cursor: no-drop`, blocked-tone border/background, and a red forbidden glyph so an operator cannot mistake a blocked export for a slow or silently failing action. TDD first failed because `canExportDeliveryBundle` and the disabled CSS contract were missing; focused checks then passed for `apps\desktop\src\renderer\pages\delivery-page.test.ts`, followed by desktop typecheck, `pnpm run smoke:business-ui-current` with `output\codex-evidence\current-business-ui-smoke-1782787351742.json`, `pnpm run verify:ad-execution`, `build:win`, package launch smoke `output\codex-evidence\package-launch-smoke-1782800794443.json`, manifest-driven final-readiness `output\codex-evidence\final-readiness-20260630142700.json`, READY bundle export to `output\delivery-bundles\v15-delivery-bundle-20260630142700-ready`, and READY safety.
- 2026-06-29 Listing heatmap containment pass: the left keyword rail cells in `Listing 优化` now satisfy the v1.5 high-density CSS isolation contract. TDD first failed because `.listing-heatmap-keyword` lacked `contain: strict`; the keyword rail cells now use strict containment, while the right-side current/draft text cells keep their existing strict containment and outer auto-height sections remain content-driven. Focused checks passed: `pnpm exec vitest run apps\desktop\src\renderer\pages\listing-optimization-page.test.ts --reporter=basic`, `pnpm exec vitest run apps\desktop\src\renderer\pages\listing-optimization-page.test.ts apps\desktop\src\renderer\components\virtual-data-table.test.tsx --reporter=basic`, desktop typecheck, `pnpm run smoke:business-ui-current`, `pnpm run verify:ad-execution`, `build:win`, package launch smoke `output\codex-evidence\package-launch-smoke-1782800794443.json`, manifest-driven final-readiness `output\codex-evidence\final-readiness-20260630142700.json`, READY bundle export, and READY safety.
- 2026-06-29 VirtualDataTable actual-row zebra striping pass: shared heavy tables now compute `virtual-table-row-even` / `virtual-table-row-odd` from `virtualRow.index` instead of relying on DOM `nth-child`, so virtualized windows keep stable alternating row backgrounds while `@tanstack/react-virtual` recycles nodes. TDD first failed because `VirtualDataTable` did not expose row parity classes; the helper `virtualRowParityClass` and CSS row backgrounds were then added. Focused checks passed: `pnpm exec vitest run apps\desktop\src\renderer\components\virtual-data-table.test.tsx --reporter=basic`, `pnpm exec vitest run apps\desktop\src\renderer\components\virtual-data-table.test.tsx apps\desktop\src\renderer\pages\keyword-opportunities-page.test.ts apps\desktop\src\renderer\pages\data-import-validation-page.test.ts --reporter=basic`, desktop typecheck, `pnpm run smoke:business-ui-current`, `pnpm run verify:ad-execution`, `build:win`, package launch smoke `output\codex-evidence\package-launch-smoke-1782800794443.json`, and manifest-driven final-readiness `output\codex-evidence\final-readiness-20260630142700.json`.
- 2026-06-29 direct details disclosure feedback pass: direct native `<details>` summaries on dashboard, detail panels, and evidence disclosures now have a compact interaction surface with hover inset anchor, keyboard `focus-visible` outline, active `scale(0.98)`, and reduced-motion fallback. This extends the progressive-disclosure polish beyond shared `ProgressiveDetails` to pages that still render native details blocks, so folded evidence controls no longer look inert. TDD first failed on the missing grouped summary contract; focused checks then passed for `apps\desktop\src\renderer\components\ui.test.tsx`, dashboard, approval, ad-quant, recommendations, and settings pages, followed by desktop typecheck, `pnpm run smoke:business-ui-current`, `pnpm run verify:ad-execution`, `build:win`, package launch smoke `output\codex-evidence\package-launch-smoke-1782800794443.json`, manifest-driven final-readiness `output\codex-evidence\final-readiness-20260630142700.json`, READY bundle export, and READY safety.
- 2026-06-29 global typography contract pass: the renderer now follows the v1.5 desktop typography baseline exactly: `body` uses `system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Arial, sans-serif`, the global body line-height remains `1.25`, and business-table plus virtual-table fact cells explicitly keep `line-height: 1.25`. TDD first failed on the stale `"Segoe UI", "Microsoft YaHei UI", system-ui, sans-serif` stack and the missing shared business-table cell line-height; focused checks then passed for `apps\desktop\src\renderer\components\ui.test.tsx`, followed by desktop typecheck, `pnpm run smoke:business-ui-current`, `pnpm run verify:ad-execution`, `build:win`, package launch smoke `output\codex-evidence\package-launch-smoke-1782800794443.json`, manifest-driven final-readiness `output\codex-evidence\final-readiness-20260630142700.json`, READY bundle export, and READY safety.
- 2026-06-26 shared business-data pipeline debounce pass: `useBusinessDataPipeline` now keeps first load and explicit reload/data-updated events immediate, but merges scope-only changes through a 300ms debounce window and cancels stale timers before they call `getBusinessUiDataPipeline`. This implements the v1.5 high-throughput query guard for Dashboard, data collection, import validation, ad quantification, recommendations, approval, keyword opportunity, Listing, product management, scope, and readback surfaces without changing their page-specific data contracts. TDD evidence: `apps\desktop\src\renderer\components\business-data.test.ts` first failed because `businessPipelineLoadDelay` did not exist, then passed after the debounce contract was added. Verification passed: focused business-data test, desktop typecheck, `pnpm run smoke:business-ui-current` with `output\codex-evidence\current-business-ui-smoke-1782787351742.json`, `pnpm run verify:ad-execution`, Windows build, package launch smoke `output\codex-evidence\package-launch-smoke-1782800794443.json`, and manifest-driven final-readiness `output\codex-evidence\final-readiness-20260630142700.json`.

- 2026-06-18 historical portable startup fix closeout: fixed the no-install EXE startup `Error` window. Root cause was packaged runtime dependency incompleteness: the main process imports `@amazon-ai-ops/browser-worker`, which top-level loads `playwright`; the rebuilt package contained `playwright` but not its runtime dependency `playwright-core`, so startup failed before `[App] ready` and no BrowserWindow was created. `apps/desktop/package.json` now lists `playwright-core` as a direct desktop runtime dependency, and `electron-builder.yml` now packages only `dist/main`, `dist/preload`, and `dist/renderer` instead of broad `dist/**/*`, preventing stale `dist/win-unpacked`, `dist/linux-unpacked`, and old AppImage artifacts from being recursively bundled. Rebuilt package evidence: installer size `83042260`, SHA-256 `D92FA0509EAFB7392FE70DFA521714F2E08ABCED5CA2E2A84E33E2D962CC5934`; portable/no-install size `82876507`, SHA-256 `C3C2680431BCAC2DFA9D67B6ABDF052EBFE5F9006A841A17514102B7FABBB47D`. Startup verification passed for `release\win-unpacked\AmazonAIOpsAgent.exe` through first BrowserWindow plus `electronAPI.getState`, and the portable EXE produced an `Amazon AI Ops Agent` window from its temp extraction path. Manifest-driven final-readiness now passes at `output\codex-evidence\final-readiness-2026-06-18-portable-fix.json`, the READY bundle was exported to `output\delivery-bundles\v15-delivery-bundle-2026-06-18-portable-fix-ready`, and READY safety passed against the refreshed package hashes.
- 2026-06-18 historical product UI / AI fallback / Listing versioning closeout: implemented the operator-facing stabilization plan for the current worktree. UI density was reduced, long evidence sections moved behind disclosures, 广告量化 defaults to product/ASIN-scoped viewing instead of showing every ad object at once, AI strategy diagnosis now makes a structured JSON repair attempt and falls back with Chinese operator-facing messages instead of raw parser positions, and Listing 优化 now treats manual Listing entry/version history as the primary path with Lingxing read as auxiliary fill. Full validation passed: `pnpm test` (`100` test files, `594` passed, `2` skipped), `pnpm -r run typecheck`, `pnpm --filter @amazon-ai-ops/desktop run build:win`, and `pnpm run smoke:business-ui-current`. Current smoke evidence: `output\codex-evidence\current-business-ui-smoke-1781754371137.json`, with shell/data-pipeline/ad-execution/keyword-listing/settings-delivery child evidence. Package evidence: `apps\desktop\release\AmazonAIOpsAgent-1.5.0.exe`, size `83042260`, SHA-256 `D92FA0509EAFB7392FE70DFA521714F2E08ABCED5CA2E2A84E33E2D962CC5934`; portable/no-install `apps\desktop\release\AmazonAIOpsAgent-1.5.0-portable.exe`, size `82876507`, SHA-256 `C3C2680431BCAC2DFA9D67B6ABDF052EBFE5F9006A841A17514102B7FABBB47D`. Manifest-driven final-readiness now passes at `output\codex-evidence\final-readiness-2026-06-18-portable-fix.json`, the READY bundle was exported to `output\delivery-bundles\v15-delivery-bundle-2026-06-18-portable-fix-ready`, and `pnpm run verify:v15-ready-safety --final-readiness output\codex-evidence\final-readiness-2026-06-18-portable-fix.json --bundle-manifest output\delivery-bundles\v15-delivery-bundle-2026-06-18-portable-fix-ready\delivery-bundle-manifest.json` passed.
- 2026-06-18 real Ads UI readback closeout: the current rec-4 candidate generator was corrected so `search_term + lower_bid` no longer blindly becomes a writable `target`; it now resolves writable entity type from the source report grain and maps the packaged evidence to the FT-US paused keyword row `door lock`, campaign `D6-精准-首轮投测词 - 5/18/2026`, ad group `D6-手动精准-卧室核心长尾 - 5/18/2026`, data-id `85966379732611`. In Lingxing Ads UI the source recommendation was `1.63 -> 1.46`, but the live editable bid was already `1.30`, so writing `1.46` would have increased the bid and violated the `lower_bid` intent. The approved validation action therefore lowered the live bid `1.30 -> 1.17` and captured independent before, after, and reload/readback screenshots. PASS evidence: `output\codex-evidence\real-ad-execution-readback-candidate-rec-4-current-pass.json`; session folder: `output\codex-evidence\ad-readback-session-rec-4-current`; final manifest: `output\codex-evidence\v15-final-readiness-evidence-manifest-2026-06-18.json`; final readiness: `output\codex-evidence\final-readiness-2026-06-18.json`, status `APP_READY`. Incremental checks passed: `pnpm exec vitest run scripts/create-ad-readback-candidate.test.mjs scripts/prepare-ad-readback-session.test.mjs apps/desktop/src/main/ad-readback-session.test.ts --reporter=basic`, `node --check scripts\create-ad-readback-candidate-from-recommendation.js`, `node --check scripts\create-ad-readback-evidence-template.js`, `node --check scripts\prepare-ad-readback-session.js`, `pnpm run verify:ad-readback -- output\codex-evidence\real-ad-execution-readback-candidate-rec-4-current-pass.json`, and `pnpm run verify:v15-final-readiness --evidence-manifest output\codex-evidence\v15-final-readiness-evidence-manifest-2026-06-18.json --out output\codex-evidence\final-readiness-2026-06-18.json`.
- 2026-06-30 READY delivery bundle export pass: after switching README top-level delivery status to `APP_READY`, `pnpm run export:v15-delivery-bundle --final-readiness output\codex-evidence\final-readiness-20260630142700.json --data-reconciliation output\codex-evidence\real-lingxing-reconciliation-batch_20260612020905629_gkchz1.json --data-reconciliation-md output\codex-evidence\real-lingxing-reconciliation-batch_20260612020905629_gkchz1.md --out output\delivery-bundles\v15-delivery-bundle-20260630142700-ready` exported the current `APP_READY` bundle. The bundle keeps raw `.xlsx/.xls/.csv` reports out of the package, records them through `evidence/real-report-file-index.json`, and includes the package launch smoke evidence/logs.
- 2026-06-30 READY safety pass: `pnpm run verify:v15-ready-safety --final-readiness output\codex-evidence\final-readiness-20260630142700.json --bundle-manifest output\delivery-bundles\v15-delivery-bundle-20260630142700-ready\delivery-bundle-manifest.json` passed. It verified the manifest-selected readback evidence with `verify:ad-readback`, README and bundle README `APP_READY` alignment, current business UI smoke summary, real report index, current-scope data reconciliation, installer/portable package hash index, and package launch smoke gate.
- 2026-06-25 heavy table virtualization pass: added `@tanstack/react-virtual` plus a shared `VirtualDataTable` with sticky headers, bounded scroll, skeleton loading, actual-row zebra striping, row measurement, empty states, and detail expansion. `关键词机会` now virtualizes campaign/ad-group/search-term opportunity rows while preserving source-detail expansion and Listing handoff. `数据导入与校验` now virtualizes the 8-report import status grid with file status, row counts, and open-file actions. Incremental evidence: `pnpm exec vitest run apps\desktop\src\renderer\components\virtual-data-table.test.tsx apps\desktop\src\renderer\pages\data-collection-page.test.ts apps\desktop\src\renderer\pages\readback-page.test.tsx --reporter=basic`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, `pnpm --filter @amazon-ai-ops/desktop run build:renderer`, `pnpm run smoke:business-ui-current`, `pnpm --filter @amazon-ai-ops/desktop run build:win`, `pnpm run smoke:package-launch`, and refreshed manifest-driven final-readiness.
- 2026-06-25 Listing keyword heatmap matrix pass: `Listing 优化` now builds a normalized keyword/root model from opportunities, accepted suggestions, drafts, and the current Listing version. The UI presents a left keyword rail with current/draft coverage counts and recommended sections, and a right current-vs-draft matrix for `标题`, `五点`, `后台词`, and `详情/A+`; clicking a root highlights only that keyword across the matrix. Incremental evidence: `pnpm exec vitest run apps\desktop\src\renderer\pages\listing-optimization-page.test.ts --reporter=basic`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, `pnpm --filter @amazon-ai-ops/desktop run build:renderer`, `node scripts\smoke-business-ui-keyword-listing.js`, `pnpm --filter @amazon-ai-ops/desktop run build:win`, `pnpm run smoke:package-launch`, current business UI smoke, manifest-driven final-readiness, READY bundle export, and READY safety.
- 2026-06-17 current-readback candidate alignment pass: final-node validation was run on the current worktree. `pnpm -r run typecheck` passed, `pnpm test` passed after rebuilding `better-sqlite3` for the Node 24 ABI (`90` test files, `554` passed, `2` skipped), and `pnpm --filter @amazon-ai-ops/desktop run build:win` produced both installer and portable/no-install EXE artifacts. Package evidence: `apps\desktop\release\AmazonAIOpsAgent-1.5.0.exe`, size `89895931`, SHA-256 `F4956DAF89DA7C955DCC4059FF380492A085CF73CC843D2C684EA4230AB8B9D1`; `apps\desktop\release\AmazonAIOpsAgent-1.5.0-portable.exe`, size `89730178`, SHA-256 `CFE9C3DDFDB08E1C33BE788A935C1282892FCE2E821C1FC05B91A5071ED536AE`. Packaged-app read-only ad AI explanation evidence `output\codex-evidence\installed-ad-ai-explanation-packaged-final-20260617.json` passed `node scripts\verify-ad-ai-explanation-evidence.js`, proving packaged runtime, saved real AI settings, no ad writes, no report downloads, 2 AI-explained recommendations, and real source report file/source row traceability. Current readback candidates were regenerated from that packaged evidence; `output\codex-evidence\real-ad-execution-readback-candidate-rec-4-current.json` targets `FT-US-US / US / B0GTTJFQTM / D6-精准-首轮投测词 - 5/18/2026 / D6-手动精准-卧室核心长尾 - 5/18/2026 / target door lock / lower_bid`, carries source recommendation `1.63 -> 1.46`, real `.xlsx` source files, and source row `410`. `node scripts\verify-ad-readback-evidence.js output\codex-evidence\real-ad-execution-readback-candidate-rec-4-current.json` now passes source traceability preconditions and fails only because real Ads UI approval, before/after screenshots, live bid values, manual execution, and reload readback evidence are not filled. Manifest-driven final readiness was refreshed at `output\codex-evidence\final-readiness-2026-06-17.json` from `output\codex-evidence\v15-final-readiness-evidence-manifest-2026-06-17.json`; it is correctly `APP_NEEDS_WORK`, with only `Real ad execution readback` failing on the missing live execution/readback evidence. Do not declare APP_READY until the current candidate or another current-scope low-risk manual Ads UI action is executed and verified with the current readback contract.
- 2026-06-17 readback finalizer helper pass: added `pnpm run fill:ad-readback` backed by `scripts/fill-ad-readback-evidence.js`. The helper reads a current `NEEDS_WORK` candidate, writes a separate PASS-intended evidence JSON, fills approval, risk, before/after, execution, and readback fields from CLI arguments, refuses to overwrite the source candidate unless `--in-place` is explicit, verifies local evidence file existence, and immediately runs `verify:ad-readback`. This reduces manual JSON editing during the real Ads UI readback closeout while preserving the existing verifier as the authority. Incremental checks passed: `pnpm exec vitest run scripts/fill-ad-readback-evidence.test.mjs scripts/package-scripts.test.mjs scripts/export-v15-delivery-bundle.test.mjs`.
- 2026-06-17 readback operator packet command pass: `scripts/create-ad-readback-evidence-template.js` now writes a `Fill Command After Capture` section into every readback candidate Markdown. The command uses `pnpm run fill:ad-readback`, points `--source` at the immutable candidate JSON, writes `--out` to a separate `*-pass.json`, and keeps final verifier commands pointed at that PASS-intended file. The current `output\codex-evidence\real-ad-execution-readback-candidate-rec-4-current.md` was regenerated with this command template. Incremental checks passed: `pnpm exec vitest run scripts/create-ad-readback-template.test.mjs scripts/create-ad-readback-candidate.test.mjs scripts/fill-ad-readback-evidence.test.mjs`, `node --check scripts\create-ad-readback-evidence-template.js`, `node --check scripts\create-ad-readback-candidate-from-recommendation.js`, and `node --check scripts\fill-ad-readback-evidence.js`.
- 2026-06-17 readback session packet pass: added `pnpm run prepare:ad-readback-session`, backed by `scripts/prepare-ad-readback-session.js`, and `pnpm run verify:ad-readback-session`, backed by `scripts/verify-ad-readback-session.js`. The prepare command creates a per-action working folder with `operator-checklist.md`, `fill-ad-readback.ps1`, `session-paths.json`, and separate `approvals`, `screenshots\before`, `screenshots\after`, and `screenshots\readback` directories. It refuses candidates that already claim `PASS`, does not copy raw Lingxing report files, and keeps the output evidence path separate from the candidate JSON. The verifier checks the folder is safe for live capture but does not grant final readiness. A current rec-4 packet was generated at `output\codex-evidence\ad-readback-session-rec-4-current`; current verifier wording is `SESSION_STRUCTURE_READY` for structure only and does not imply captured evidence is complete. Incremental checks passed: `pnpm exec vitest run scripts/verify-ad-readback-session.test.mjs scripts/prepare-ad-readback-session.test.mjs scripts/package-scripts.test.mjs`, `node --check scripts\verify-ad-readback-session.js`, `node --check scripts\prepare-ad-readback-session.js`, and `node --check scripts\export-v15-delivery-bundle.js`.
- 2026-06-17 readback session-input fill pass: `prepare:ad-readback-session` now also writes `session-input.json`, and the generated `fill-ad-readback.ps1` calls `pnpm run fill:ad-readback-session -- --session <sessionDir>` instead of requiring the operator to edit a long command line. `scripts/fill-ad-readback-session.js` reads `session-paths.json` and `session-input.json`, refuses unresolved placeholder fields, writes the PASS-intended JSON to the session folder, and delegates to the existing `fill-ad-readback`/`verify:ad-readback` authority. The current rec-4 session was regenerated and structurally verified; unresolved `session-input.json` fields remain `CAPTURE_NEEDS_WORK` until the operator fills live approval, before/after, execution, and readback evidence. Incremental checks passed: `pnpm exec vitest run scripts/fill-ad-readback-session.test.mjs scripts/prepare-ad-readback-session.test.mjs scripts/verify-ad-readback-session.test.mjs scripts/package-scripts.test.mjs`, plus `node --check` for `fill-ad-readback-session.js`, `prepare-ad-readback-session.js`, `verify-ad-readback-session.js`, and `export-v15-delivery-bundle.js`.
- 2026-06-17 readback UI session-command pass: the `执行回读` technical panel now exposes session-based copy actions after exporting a readback JSON: `复制 prepare session 命令`, `复制 verify session 命令`, and `复制 fill session 命令`, while the old long-argument command remains as `复制长参数 fill 命令` for fallback. This aligns the desktop UI with the safer `session-input.json` workflow. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/renderer/pages/readback-page.test.tsx`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, `pnpm --filter @amazon-ai-ops/desktop run build:renderer`, and `node scripts\smoke-business-ui-ad-execution.js` with evidence `output\codex-evidence\business-ui-ad-execution-smoke-1781715122859.json`.
- 2026-06-18 readback visible session-workflow pass: the `执行回读` page now shows a main-flow `回读工作包` panel after exporting readback JSON, instead of hiding all session guidance in the technical foldout. The panel explains the single-action session directory, `session-input.json`, `approvals`, `screenshots\before`, `screenshots\after`, `screenshots\readback`, and the fact that session structure checks are not final readiness. It exposes operator-facing copy buttons for creating, checking, and filling the session, while the raw long command remains hidden as a fallback. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/renderer/pages/readback-page.test.tsx`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, `pnpm --filter @amazon-ai-ops/desktop run build:renderer`, and `node scripts\smoke-business-ui-ad-execution.js` with evidence `output\codex-evidence\business-ui-ad-execution-smoke-1781715881954.json`.
- 2026-06-18 in-app readback session creation pass: added a packaged-app-safe main-process helper `prepareAdReadbackSession()` and IPC `recommendations:prepare-ad-readback-session`, exposed as `electronAPI.prepareAdReadbackSession`. The `执行回读` page can now create the single-action session packet directly after exporting readback JSON and displays the session directory, `session-input.json`, checklist, and PASS output path. This removes the requirement that an operator manually run `pnpm` just to create the working folder, while keeping the CLI commands as fallback and keeping final readiness gated on real approval/before/after/readback evidence. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/main/ad-readback-session.test.ts apps/desktop/src/renderer/pages/readback-page.test.tsx`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, `pnpm --filter @amazon-ai-ops/desktop run build:main`, `pnpm --filter @amazon-ai-ops/desktop run build:preload`, `pnpm --filter @amazon-ai-ops/desktop run build:renderer`, and `node scripts\smoke-business-ui-ad-execution.js` with evidence `output\codex-evidence\business-ui-ad-execution-smoke-1781716619909.json`.
- 2026-06-18 in-app readback session verification pass: migrated the session structure safety checks into the packaged-app-safe `verifyAdReadbackSession()` helper and exposed IPC `recommendations:verify-ad-readback-session` / `electronAPI.verifyAdReadbackSession`. The `执行回读` page now has a visible `检查工作包` action after session creation and displays whether the session folder is structurally safe for live capture, including issues such as missing readback screenshot folder or raw `.xlsx/.xls/.csv` report files copied into the session. This check explicitly remains a pre-capture safety check, not final readiness. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/main/ad-readback-session.test.ts apps/desktop/src/renderer/pages/readback-page.test.tsx`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, `pnpm --filter @amazon-ai-ops/desktop run build:main`, `pnpm --filter @amazon-ai-ops/desktop run build:preload`, `pnpm --filter @amazon-ai-ops/desktop run build:renderer`, and `node scripts\smoke-business-ui-ad-execution.js` with evidence `output\codex-evidence\business-ui-ad-execution-smoke-1781716975010.json`.
- 2026-06-18 in-app readback session fill pass: migrated the session-input fill flow into the packaged-app-safe `fillAdReadbackSession()` helper and exposed IPC `recommendations:fill-ad-readback-session` / `electronAPI.fillAdReadbackSession`. After a session is created and checked, the `执行回读` page can now generate the PASS-intended JSON/Markdown directly from `session-input.json`. The helper uses the same desktop `buildAdReadbackEvidence()` completeness rules instead of hard-coding PASS, so missing screenshot files, unresolved placeholders, missing source batch/date/row/files, unchanged before/after values, invalid lower-bid direction, or timestamp-order issues produce `NEEDS_WORK`. The UI still states that final READY requires `verify:ad-readback` and manifest aggregation. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/main/ad-readback-session.test.ts apps/desktop/src/renderer/pages/readback-page.test.tsx`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, `pnpm --filter @amazon-ai-ops/desktop run build:main`, `pnpm --filter @amazon-ai-ops/desktop run build:preload`, `pnpm --filter @amazon-ai-ops/desktop run build:renderer`, and `node scripts\smoke-business-ui-ad-execution.js` with evidence `output\codex-evidence\business-ui-ad-execution-smoke-1781717424892.json`.
- 2026-06-18 in-app readback evidence verifier pass: migrated the authoritative readback evidence checks into packaged-app-safe `verifyAdReadbackEvidenceFile()` and exposed IPC `recommendations:verify-ad-readback-evidence` / `electronAPI.verifyAdReadbackEvidence`. The `执行回读` page now has a visible `校验回读证据` action after `生成回读证据`, and displays PASS/NEEDS_WORK plus concrete missing checks such as non-spreadsheet source files, reused before/after/readback proof, missing approval proof, invalid lower-bid direction, timestamp ordering, app-executor misuse, or secret leakage. This still does not create READY by itself; the verified JSON must be selected by the final evidence manifest and final readiness gate. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/main/ad-readback-evidence-verifier.test.ts apps/desktop/src/main/ad-readback-session.test.ts apps/desktop/src/renderer/pages/readback-page.test.tsx`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, `pnpm --filter @amazon-ai-ops/desktop run build:main`, `pnpm --filter @amazon-ai-ops/desktop run build:preload`, `pnpm --filter @amazon-ai-ops/desktop run build:renderer`, and `node scripts\smoke-business-ui-ad-execution.js` with evidence `output\codex-evidence\business-ui-ad-execution-smoke-1781718087861.json`.
- 2026-06-17 readback UI fill-command pass: the `执行回读` page now exposes a hidden technical copy action after exporting readback JSON: `复制 fill:ad-readback 命令`. The main operator flow remains form-first and fail-closed, while the folded technical section can copy a quoted PowerShell command using the current form fields and exported JSON path for final CLI verification. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/renderer/pages/readback-page.test.tsx`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, `pnpm --filter @amazon-ai-ops/desktop run build:renderer`, and `node scripts\smoke-business-ui-ad-execution.js` with evidence `output\codex-evidence\business-ui-ad-execution-smoke-1781713524722.json`.
- 2026-06-17 current-readback NEEDS_WORK bundle pass: after switching the manifest to `real-ad-execution-readback-candidate-rec-4-current.json`, exported `output\delivery-bundles\v15-delivery-bundle-2026-06-17-final-needs-work-current-readback` from the refreshed final readiness and current data reconciliation files. The bundle was refreshed again after adding `fill:ad-readback` and after the readback UI smoke, and `delivery-bundle-manifest.json` now includes `scripts\fill-ad-readback-evidence.js`. `pnpm run verify:v15-non-ready-safety --final-readiness output\codex-evidence\final-readiness-2026-06-17.json --delivery-bundle output\delivery-bundles\v15-delivery-bundle-2026-06-17-final-needs-work-current-readback` passed, confirming the bundle remains explicitly non-ready, preserves the single real-ad-readback blocker, and the README top-level delivery line does not claim READY.
- 2026-06-17 timeline-rule formal recommendation and AI action-explanation pass: formal recommendation generation now uses daily metric timelines instead of isolated single-day rows. The rules engine aggregates the current range into object-level timelines, loads enough current-batch rows for the full `2026-06-01` to `2026-06-12 / FT-US-US / US / batch_20260612020905629_gkchz1` scope, and creates executable `lower_bid` recommendations only when the row has current value, recommended value, USD metrics, real spreadsheet source file(s), and positive source row. `recommendations:generate` can now run from already-imported real reports without requiring a live Lingxing browser session, and the live runner accepts a previously saved hidden AI key without overwriting it or demanding plaintext key injection. Recommendation date filtering now treats evidence date ranges as overlapping the selected scope, stale `pending` / `needs_review` duplicates can be refreshed when regenerated evidence is more complete, and strategy-diagnosis fallback is separated from per-action AI explanation fallback so successful Chinese action explanations are not rejected because the batch-level strategy diagnosis needed fallback. Source-app live evidence `output\codex-evidence\installed-ad-ai-explanation-source-timeline-rules-runner-meta-20260612.json` is `PASS` for the business chain: saved-key DeepSeek settings accepted, `1596` real metric rows analyzed, `2` refreshed formal recommendations generated, both with source report path and source row, and both carrying non-fallback Chinese AI explanations. The older stale recommendation remains in DB history but is rejected by the verifier/UI because it lacks executable traceability. This is still not final delivery evidence because it was generated by `source-app`; packaged-app evidence, full tests, Windows build, package hashes, and final readiness aggregation remain pending. Incremental checks passed: `pnpm exec vitest run packages/rules-engine/src/recommendation.test.ts packages/rules-engine/src/quantification.test.ts apps/desktop/src/main/ad-recommendation-ai-context.test.ts`, `pnpm --filter @amazon-ai-ops/rules-engine run typecheck`, `pnpm exec vitest run packages/local-db/src/sqlite/repositories/recommendation-repo.test.ts`, `pnpm --filter @amazon-ai-ops/local-db run typecheck`, `pnpm exec vitest run apps/desktop/src/main/ad-action-explanation-merge.test.ts`, `pnpm exec vitest run scripts/verify-ad-ai-explanation-evidence.test.mjs`, `node scripts\verify-ai-settings-ux.js`, `node --check scripts\run-v15-installed-live.js`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, and `pnpm --filter @amazon-ai-ops/desktop run build:main`. Full tests and Windows packaging were intentionally not rerun at this incremental node.
- 2026-06-17 current-source AI evidence-chain live verification pass: source-app validation no longer fails on the Electron development runtime missing packaged Chromium. `BrowserController` now resolves the local `ms-playwright\chromium-*` executable only for unpackaged Electron source-app runs, while packaged apps still fail closed if bundled Chromium is missing; the generic browser error classifier no longer mislabels every `chromium.launchPersistentContext` failure as missing packaged runtime. The source live run now reaches Lingxing/Ads session reuse (`adsUrl=https://ads.lingxing.com/home`), saves sanitized AI settings without requiring plaintext key echo, passes DeepSeek connection, and runs current-scope ad strategy diagnosis against the real `2026-06-01` to `2026-06-12 / FT-US-US / US / batch_20260612020905629_gkchz1` data. The run proves AI diagnosis succeeds with evidence pack summary `28` (`23` metric, `3` timeline, `1` operation event, `1` product context), but correctly remains `NEEDS_WORK` because the current sample has only one metric day, AI returned only `insightOnlyCandidates`, and the existing historical recommendation is non-executable (`recommendedValue` empty, `sourceRow` missing). The ad-AI evidence verifier and runner now require executable current/recommended values plus real spreadsheet source file(s) and positive source row, so stale AI explanations no longer create false PASS evidence. AI default `maxTokens` is now `8192` for structured evidence-chain JSON; live runner no longer forces `700`, which previously truncated DeepSeek JSON. Incremental checks passed: `pnpm exec vitest run packages/browser-worker/src/controller.test.ts`, `pnpm exec vitest run apps/desktop/src/main/recommendation-generation-gate.test.ts apps/desktop/src/main/ad-recommendation-ai-context.test.ts packages/local-db/src/sqlite/repositories/recommendation-repo.test.ts`, `pnpm exec vitest run apps/desktop/src/main/ai-settings-normalization.test.ts packages/ai-adapter/src/ad-strategy-diagnosis.test.ts`, `pnpm exec vitest run scripts/verify-ad-ai-explanation-evidence.test.mjs`, `node scripts\verify-ai-settings-ux.js`, `node --check scripts\run-v15-installed-live.js`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, `pnpm --filter @amazon-ai-ops/desktop run build:main`, and `pnpm --filter @amazon-ai-ops/desktop run build:renderer`. Full tests and Windows packaging were intentionally not rerun at this incremental node.
- 2026-06-17 recommendation regeneration stale-duplicate refresh pass: `RecommendationRepository.insertIfNoDuplicate()` now refreshes an existing `pending` / `needs_review` duplicate when the old row lacks execution traceability and the regenerated recommendation has current value, executable recommended value, real spreadsheet source file(s), and a positive source row. Approved, rejected, or executed duplicates are still preserved and not overwritten. `recommendations:generate` now returns `refreshedDuplicates`, and the `优化建议` page explains refreshed historical incomplete suggestions instead of showing a misleading zero-new-output state. Incremental checks passed: `pnpm exec vitest run packages/local-db/src/sqlite/repositories/recommendation-repo.test.ts apps/desktop/src/main/ad-recommendation-ai-context.test.ts apps/desktop/src/main/recommendation-generation-gate.test.ts apps/desktop/src/renderer/pages/recommendations-page.test.ts`, `pnpm --filter @amazon-ai-ops/local-db run typecheck`, and `pnpm --filter @amazon-ai-ops/desktop run typecheck`.
- 2026-06-17 AI formal-action executable-value prompt pass: the ad strategy AI prompt now states that formal `aiCandidates` for `lower_bid`, `raise_bid`, and `adjust_campaign_budget` must return an absolute Ads-UI-executable numeric value, for example `1.26`, and must not use relative values such as `-10%`, `降低10%`, or `10%`. Relative-only AI judgments must go to `insightOnlyCandidates`. This aligns DeepSeek output expectations with the formal recommendation gate that now rejects non-executable AI-only values. Incremental checks passed: `pnpm exec vitest run packages/ai-adapter/src/ad-strategy-diagnosis.test.ts apps/desktop/src/main/ad-recommendation-ai-context.test.ts apps/desktop/src/renderer/pages/recommendations-page.test.ts packages/local-db/src/sqlite/repositories/recommendation-repo.test.ts`, `pnpm --filter @amazon-ai-ops/ai-adapter run typecheck`, and `pnpm --filter @amazon-ai-ops/desktop run typecheck`.
- 2026-06-17 AI-only executable-value gate pass: `createAiOnlyRecommendationsFromDecisions()` now refuses to create formal AI-only bid recommendations unless the AI decision carries an executable absolute bid value whose direction matches the action (`lower_bid` must be lower than the current metric value, `raise_bid` must be higher). Relative strings such as `-10%`, empty `recommendedValue`, or non-numeric values remain diagnosis/insight material and do not enter `action_recommendations`. This closes the path that produced review rows with real metric context but no executable recommended bid, which later blocked approval/readback. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/main/ad-recommendation-ai-context.test.ts apps/desktop/src/main/recommendation-approval-policy.test.ts` and `pnpm --filter @amazon-ai-ops/desktop run typecheck`. The full test suite and Windows packaging were intentionally not rerun at this incremental node.
- 2026-06-17 readback candidate source-traceability preflight pass: `scripts/create-ad-readback-candidate-from-recommendation.js` now refuses to generate a manual Ads UI readback candidate unless the selected recommendation or explicit CLI overrides include existing real `.xlsx/.xls/.csv` Lingxing source report file(s) and a positive original `sourceRow`. Older installed AI explanation summaries can still be used only when the operator supplies `--source-files` and `--source-row` from the real report row; source-less summary evidence fails before a candidate JSON/Markdown is written. Incremental checks passed: `pnpm exec vitest run scripts/create-ad-readback-candidate.test.mjs scripts/create-ad-readback-template.test.mjs scripts/verify-ad-readback-evidence.test.mjs`, `node --check scripts\create-ad-readback-candidate-from-recommendation.js`, and `pnpm run verify:v15-non-ready-safety`. The full test suite and Windows packaging were intentionally not rerun at this incremental node.
- 2026-06-17 final-readiness default discovery alignment pass: `verify-v15-ready-safety`, `verify-v15-non-ready-safety`, and `export-v15-delivery-bundle` now auto-discover both date-named `final-readiness-YYYY-MM-DD.json` files and default timestamp-named `final-readiness-<timestamp>.json` files, while still excluding smoke artifacts such as `final-readiness-smoke-*` and `final-readiness-export-bundle-smoke-*`. This prevents the safety gates from silently falling back to stale historical `APP_READY` evidence after `verify:v15-final-readiness` writes its default timestamp output. Current-code diagnostic evidence was refreshed with `pnpm run verify:v15-final-readiness --evidence-manifest output\codex-evidence\v15-final-readiness-evidence-manifest-2026-06-17.json --out output\codex-evidence\final-readiness-2026-06-17.json`; it correctly returns `APP_NEEDS_WORK`, with real ad execution approval, before/after screenshots, manual Ads UI change, and reload readback as the remaining READY blocker. Incremental checks passed: `pnpm exec vitest run scripts/verify-v15-non-ready-safety.test.mjs scripts/verify-v15-ready-safety.test.mjs scripts/export-v15-delivery-bundle.test.mjs`, `node --check scripts\verify-v15-non-ready-safety.js`, `node --check scripts\verify-v15-ready-safety.js`, `node --check scripts\export-v15-delivery-bundle.js`, `node --check scripts\verify-v15-non-ready-safety.test.mjs`, `pnpm run verify:v15-non-ready-safety`, and `git diff --check` with only Windows CRLF warnings. The full test suite and Windows packaging were intentionally not rerun at this incremental node.
- 2026-06-17 current APP_NEEDS_WORK delivery diagnostic bundle pass: exported `output\delivery-bundles\v15-delivery-bundle-2026-06-17-in-progress` from `output\codex-evidence\final-readiness-2026-06-17.json` and the current real-data reconciliation files. The bundle manifest is explicitly `APP_NEEDS_WORK`, preserves final-readiness blockers, includes canonical data reconciliation spend USD `617.87`, orders `19`, sales `1089.79`, clicks `363`, impressions `6374`, records installer/portable package hash index without copying EXE binaries, and now records real report index `16/16` existing with `missingCount=0`. `pnpm run verify:v15-non-ready-safety` now passes against the current diagnostic bundle instead of relying on the historical `APP_READY` bundle. `git diff --check` passed with only Windows CRLF warnings.
- 2026-06-17 real-report index and recommendation source-trace cleanup pass: `export-v15-delivery-bundle` no longer treats arbitrary `.xlsx/.xls/.csv` strings, `basename`, or `filename` fields as local source report paths, and skips synthetic UI/structural smoke JSON when building `real-report-file-index.json`. This keeps mock paths such as `C:/reports/source_user_search_term.xlsx` and filename-only text out of final report traceability. Recommendation generation also no longer backfills a single recommendation's `evidence.sourceFiles` with the whole current batch file list; each formal recommendation must carry its own metric-derived source file and positive source row, otherwise approval/readback gates remain blocked. Incremental checks passed: `pnpm exec vitest run scripts/export-v15-delivery-bundle.test.mjs`, `pnpm exec vitest run apps/desktop/src/main/ad-recommendation-ai-context.test.ts apps/desktop/src/main/recommendation-generation-gate.test.ts scripts/export-v15-delivery-bundle.test.mjs`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, `node --check scripts\export-v15-delivery-bundle.js`, `node --check scripts\export-v15-delivery-bundle.test.mjs`, `pnpm run export:v15-delivery-bundle --final-readiness output\codex-evidence\final-readiness-2026-06-17.json --data-reconciliation output\codex-evidence\real-lingxing-reconciliation-batch_20260612020905629_gkchz1.json --data-reconciliation-md output\codex-evidence\real-lingxing-reconciliation-batch_20260612020905629_gkchz1.md --out output\delivery-bundles\v15-delivery-bundle-2026-06-17-in-progress`, `pnpm run verify:v15-non-ready-safety`, and `git diff --check` with only Windows CRLF warnings.
- 2026-06-17 current business UI smoke alignment pass: data-pipeline and ad-execution smoke checks now match the current UI contract. Dashboard smoke expects recent AI diagnosis work output (`AI 已产出建议`) instead of only raw connection status, and readback smoke expects `字段已填写，待导出校验` before export so the UI does not overclaim final verifier readiness before backend file checks. Incremental checks passed: `node scripts\smoke-business-ui-data-pipeline.js` with evidence `output\codex-evidence\business-ui-data-pipeline-smoke-1781703438289.json`, `node scripts\smoke-business-ui-ad-execution.js` with evidence `output\codex-evidence\business-ui-ad-execution-smoke-1781703438923.json`, `node scripts\smoke-business-ui-shell.js` with evidence `output\codex-evidence\business-ui-shell-smoke-1781703486958.json`, `node scripts\smoke-business-ui-keyword-listing.js` with evidence `output\codex-evidence\business-ui-keyword-listing-smoke-1781703486985.json`, `pnpm exec vitest run apps/desktop/src/renderer/pages/dashboard-page.test.ts apps/desktop/src/renderer/pages/readback-page.test.tsx`, `node --check scripts\smoke-business-ui-data-pipeline.js scripts\smoke-business-ui-ad-execution.js`, and `git diff --check` with only Windows CRLF warnings. The full test suite and Windows packaging were intentionally not rerun at this incremental node.
- 2026-06-17 AI structured-output gate alignment pass: OpenAI-compatible provider calls now support an explicit `responseFormat: 'json_object'` option. Ad strategy diagnosis, per-action ad explanation, search-term relevance, daily report generation, and Listing AI draft generation request JSON object output, while plain AI connection tests remain text probes so `测试 AI 连接` is not forced to return JSON. `verify-ai-settings-ux` was updated from stale `App.tsx` string checks to the current routed UI contract: Settings provider controls, AI persona/output language, AI call audit, USD threshold copy, evidence-display sections in 广告量化/优化建议/审批中心, and opt-in JSON mode. The structural AI mock evidence now uses `/chat/completions`, includes `response_format: { type: 'json_object' }`, and remains `NO_FINAL_READINESS_CREDIT`. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/main/ad-ai-evidence-pack.test.ts apps/desktop/src/main/ad-ai-evidence-validator.test.ts apps/desktop/src/main/ad-recommendation-ai-context.test.ts packages/ai-adapter/src/ad-strategy-diagnosis.test.ts packages/ai-adapter/src/ad-action-reason.test.ts`, `pnpm exec vitest run packages/ai-adapter/src/openai-compatible.test.ts packages/ai-adapter/src/ad-strategy-diagnosis.test.ts packages/ai-adapter/src/ad-action-reason.test.ts apps/desktop/src/main/listing-ai-draft.test.ts apps/desktop/src/main/ai-settings-normalization.test.ts`, `pnpm --filter @amazon-ai-ops/ai-adapter run typecheck`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, `node scripts\verify-ai-settings-ux.js`, `node scripts\smoke-business-ui-settings-delivery.js` with evidence `output\codex-evidence\business-ui-settings-delivery-smoke-1781702884265.json`, `node scripts\run-ai-structural-mock.js`, and `node scripts\verify-ai-structural-mock-evidence.js output\codex-evidence\structural-ai-openai-compatible-mock-1781703077556.json`. The full test suite and Windows packaging were intentionally not rerun at this incremental node.
- 2026-06-17 final handoff command-order doc pass: README and `docs/USER_GUIDE_v1_5.md` now document the current final handoff order required by the stricter gates: build Windows installer and portable/no-install EXE first, run manifest-driven final readiness so `Release package hash` is recorded, export the delivery bundle, then update the README delivery line and run `verify:v15-ready-safety`. A package script test now locks this order so `verify:v15-ready-safety` is not documented before bundle export. Incremental check passed: `pnpm exec vitest run scripts/package-scripts.test.mjs`. The full test suite and Windows packaging were intentionally not rerun at this incremental node.
- 2026-06-17 APP_READY bundle stale-final-readiness guard pass: `export-v15-delivery-bundle` now refuses APP_READY export when the selected final readiness JSON lacks the current `Release package hash` gate and packageIndex, even if the release directory itself contains installer and portable EXE files. It also verifies final readiness packageIndex matches the current release package index before writing an APP_READY delivery bundle. This prevents stale pre-package-gate APP_READY JSON from being paired with fresh EXEs to create a misleading delivery bundle. Incremental checks passed: `pnpm exec vitest run scripts/export-v15-delivery-bundle.test.mjs`, `pnpm exec vitest run scripts/export-v15-delivery-bundle.test.mjs scripts/verify-v15-final-readiness.test.mjs scripts/verify-v15-ready-safety.test.mjs scripts/verify-v15-non-ready-safety.test.mjs`, `node --check scripts\export-v15-delivery-bundle.js`, `node --check scripts\export-v15-delivery-bundle.test.mjs`, `node --check scripts\verify-v15-final-readiness.js`, `node --check scripts\verify-v15-ready-safety.js`, and `node --check scripts\verify-v15-non-ready-safety.js`. The full test suite and Windows packaging were intentionally not rerun at this incremental node.
- 2026-06-17 non-ready historical-baseline hardening pass: `verify-v15-non-ready-safety` no longer lets stale historical `APP_READY` evidence pass when it lacks the current release contract. Historical baseline evidence now must include current package hash evidence with both installer and portable EXE paths, matching sizes, and recomputed SHA-256; old readback evidence that only lacks current real-report `sourceFiles/sourceRow` is now a `NEEDS_WORK` failure instead of a pass. Incremental checks passed: `pnpm exec vitest run scripts/verify-v15-non-ready-safety.test.mjs`, `pnpm exec vitest run scripts/verify-v15-non-ready-safety.test.mjs scripts/verify-v15-ready-safety.test.mjs scripts/verify-v15-final-readiness.test.mjs scripts/export-v15-delivery-bundle.test.mjs`, `node --check scripts\verify-v15-non-ready-safety.js`, `node --check scripts\verify-v15-non-ready-safety.test.mjs`, `node --check scripts\verify-v15-ready-safety.js`, `node --check scripts\verify-v15-final-readiness.js`, and `node --check scripts\export-v15-delivery-bundle.js`. The full test suite and Windows packaging were intentionally not rerun at this incremental node.
- 2026-06-17 no-install package gate pass: final readiness, READY safety, and APP_READY delivery bundle export now require both installer and portable no-install EXE package hash evidence. A release directory containing only the installer no longer satisfies `Release package hash`, and APP_READY bundle export fails closed with `portable no-install package hash evidence is missing`. Incremental checks passed: `pnpm exec vitest run scripts/verify-v15-final-readiness.test.mjs`, `pnpm exec vitest run scripts/export-v15-delivery-bundle.test.mjs`, `pnpm exec vitest run scripts/verify-v15-final-readiness.test.mjs scripts/verify-v15-ready-safety.test.mjs scripts/export-v15-delivery-bundle.test.mjs`, `node --check scripts\verify-v15-final-readiness.js`, `node --check scripts\verify-v15-ready-safety.js`, and `node --check scripts\export-v15-delivery-bundle.js`. The full test suite and Windows packaging were intentionally not rerun at this incremental node.
- 2026-06-17 delivery package-index file consistency pass: `verify-v15-ready-safety` now opens the delivery bundle's `evidence/release-package-index.json`, verifies the file exists, validates each indexed local package source path/size/SHA-256, and checks the package list matches final readiness package evidence. This closes the third layer between final readiness, delivery bundle manifest summary, and the actual bundle evidence file. Incremental checks passed: `pnpm exec vitest run scripts/verify-v15-ready-safety.test.mjs`, `pnpm exec vitest run scripts/verify-v15-final-readiness.test.mjs scripts/verify-v15-ready-safety.test.mjs scripts/export-v15-delivery-bundle.test.mjs`, `node --check scripts\verify-v15-final-readiness.js`, `node --check scripts\verify-v15-ready-safety.js`, and `node --check scripts\verify-v15-ready-safety.test.mjs`. The full test suite and Windows packaging were intentionally not rerun at this incremental node.
- 2026-06-17 READY safety package-index pass: `verify-v15-ready-safety` now rejects `APP_READY` evidence unless final readiness records complete installer/portable package hashes and the delivery bundle manifest includes a package hash index. The safety check also rereads the local package `sourcePath`, validates file size, and recomputes SHA-256 so stale or fabricated package summaries cannot pass. This aligns the READY safety gate with the new final-readiness `Release package hash` gate and prevents a bundle from being presented as READY when package evidence was omitted. Incremental checks passed: `pnpm exec vitest run scripts/verify-v15-ready-safety.test.mjs`, `pnpm exec vitest run scripts/verify-v15-final-readiness.test.mjs scripts/verify-v15-ready-safety.test.mjs`, `node --check scripts\verify-v15-final-readiness.js`, and `node --check scripts\verify-v15-ready-safety.js`. The full test suite and Windows packaging were intentionally not rerun at this incremental node.
- 2026-06-17 final-readiness package-gate pass: `verify-v15-final-readiness` now includes a `Release package hash` gate and writes `packageIndex` into the final readiness summary. `APP_READY` now requires manifest-driven business evidence plus local installer/portable EXE hash evidence, instead of allowing final readiness to pass before a deliverable package exists. Incremental checks passed: `pnpm exec vitest run scripts/verify-v15-final-readiness.test.mjs` and `node --check scripts\verify-v15-final-readiness.js`. The full test suite and Windows packaging were intentionally not rerun at this incremental node.
- 2026-06-17 delivery-bundle package-index pass: `export-v15-delivery-bundle` now writes a `release-package-index.json` evidence file and records `packageIndex` in `delivery-bundle-manifest.json`. Installer and portable EXE binaries remain excluded from the bundle by design, but their local source paths, file names, sizes, modified times, and SHA-256 hashes are indexed for final handoff. `APP_READY` bundle export now fails closed if installer/package hash evidence is missing. Incremental checks passed: `pnpm exec vitest run scripts/export-v15-delivery-bundle.test.mjs scripts/package-scripts.test.mjs`, `node scripts\smoke-export-v15-delivery-bundle.js` with evidence `output\codex-evidence\export-v15-delivery-bundle-smoke-1781699287301.json`, and `node --check scripts\export-v15-delivery-bundle.js scripts\smoke-export-v15-delivery-bundle.js`. The full test suite and Windows packaging were intentionally not rerun at this incremental node.
- 2026-06-17 delivery package evidence separation pass: the delivery matrix no longer infers installer/package readiness from `APP_READY` manifest status alone. `DeliveryEvidenceStatus` now scans `apps/desktop/release` for the real installer and portable exe, records the package path and SHA-256, and the delivery page shows `安装包未记录` until that independent package evidence exists. This prevents the UI from saying `最终 manifest 和安装包证据均已具备` when only final-readiness gates have passed. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/main/delivery-evidence-status.test.ts apps/desktop/src/renderer/delivery-readiness-matrix.test.ts apps/desktop/src/renderer/pages/delivery-page.test.ts`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, `pnpm --filter @amazon-ai-ops/desktop run build:renderer`, and `node scripts\smoke-business-ui-settings-delivery.js` with evidence `output\codex-evidence\business-ui-settings-delivery-smoke-1781699003421.json`. The full test suite and Windows packaging were intentionally not rerun at this incremental node.
- 2026-06-17 readback precheck wording alignment: the execution readback page no longer claims `字段完整` before backend export verifies that screenshot, readback evidence, and real report source files actually exist. When all form fields pass frontend checks, the precheck now says `字段已填写，待导出校验`, explains that export will perform local file-existence validation, and still relies on the backend `buildAdReadbackEvidence` / verifier contract for final PASS. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/renderer/pages/readback-page.test.tsx apps/desktop/src/main/ad-readback-evidence.test.ts apps/desktop/src/renderer/pages/approval-page.test.tsx apps/desktop/src/main/recommendation-approval-policy.test.ts`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, `pnpm --filter @amazon-ai-ops/desktop run build:renderer`, `node scripts\smoke-business-ui-ad-execution.js` with evidence `output\codex-evidence\business-ui-ad-execution-smoke-1781698238802.json`, and `git diff --check` with only Windows CRLF warnings.
- 2026-06-17 recommendation workflow action alignment: the recommendations page no longer enables `去审批中心` or `执行回读` just because any recommendation row exists. Workflow actions now use `formalApprovalCount`, review-only count, and evidence-blocker count: if all rows are AI-only/review-only or evidence-blocked, the page shows `先处理复核/证据` and keeps approval/readback navigation disabled; if formal approvable rows exist, only approval is enabled, while readback remains `审批后回读` until approval evidence exists. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/renderer/pages/recommendations-page.test.ts apps/desktop/src/renderer/pages/approval-page.test.tsx apps/desktop/src/main/recommendation-approval-policy.test.ts`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, `pnpm --filter @amazon-ai-ops/desktop run build:renderer`, `node scripts\smoke-business-ui-ad-execution.js` with evidence `output\codex-evidence\business-ui-ad-execution-smoke-1781698008839.json`, and `git diff --check` with only Windows CRLF warnings.
- 2026-06-17 dashboard formal-recommendation gate alignment: the dashboard now uses the same formal recommendation readiness boundary as the recommendation page instead of treating any real report plus imported metrics as enough. Partial coverage such as `3/8` report types with imported rows is shown as `已导入部分数据，待补齐报表`, workflow step 2 says the rows are imported but not at the formal recommendation gate, and the action queue sends operators back to data collection to complete all 8 report types instead of offering `生成优化建议`. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/renderer/pages/dashboard-page.test.ts apps/desktop/src/renderer/recommendation-readiness.test.ts apps/desktop/src/renderer/report-coverage.test.ts`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, `pnpm --filter @amazon-ai-ops/desktop run build:renderer`, `node scripts\smoke-business-ui-shell.js` with evidence `output\codex-evidence\business-ui-shell-smoke-1781697782868.json`, and `git diff --check` with only Windows CRLF warnings.
- 2026-06-17 delivery-page blocker visibility pass: the delivery page now surfaces the same final-readiness review/blocker reasons that are carried through the readiness JSON and delivery bundle. `DeliveryReadinessView` now preserves `recommendationReviewReasons`, `reviewBlockers`, `deliveryReviewReasons`, and `finalReadinessBlockers`; the `当前阻塞项` card de-duplicates those reasons with missing/action items and failed gate messages, so operators see concrete causes instead of only a generic `未就绪` state. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/renderer/pages/delivery-page.test.ts`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, and `git diff --check` with only Windows CRLF warnings.
- 2026-06-17 delivery-bundle blocker preservation pass: `export-v15-delivery-bundle` now writes `finalReadinessBlockers` into `delivery-bundle-manifest.json`, preserving final-readiness `missing`, `actionItems`, recommendation review reasons, delivery review blockers, and failed gate messages. This prevents `APP_NEEDS_WORK` bundles from hiding the exact reasons behind blockers such as missing product ASIN, missing real report `sourceFile/sourceRow`, or AI candidates that cannot bind to the current-scope ad object. Incremental checks passed: `pnpm exec vitest run scripts/export-v15-delivery-bundle.test.mjs scripts/package-scripts.test.mjs`, `node scripts\smoke-export-v15-delivery-bundle.js` with evidence `output\codex-evidence\export-v15-delivery-bundle-smoke-1781697115571.json`, and `git diff --check` with only Windows CRLF warnings.
- 2026-06-17 AI evidence validator product-binding pass: AI metric evidence can no longer validate a formal candidate in a non-ASIN scope unless the metric evidence itself carries a product ASIN. This keeps `evidenceRefs` from passing merely because they point to a real source file/row and entity name; formal AI candidates now also need product-level traceability before entering the recommendation pool. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/main/ad-ai-evidence-validator.test.ts`, `pnpm exec vitest run apps/desktop/src/main/ad-ai-evidence-validator.test.ts apps/desktop/src/main/ad-recommendation-ai-context.test.ts apps/desktop/src/main/recommendation-generation-gate.test.ts`, and `pnpm --filter @amazon-ai-ops/desktop run typecheck`.
- 2026-06-17 scope-ASIN recommendation backfill pass: rule-generated recommendations and diagnosis rule candidates now backfill the current scope ASIN before AI enrichment or persistence when the underlying metric row lacks ASIN but the operator explicitly selected an ASIN scope. This keeps the generation gate's scope-ASIN fallback from producing empty-ASIN recommendations later blocked by approval/readback. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/main/ad-recommendation-ai-context.test.ts`, `pnpm exec vitest run apps/desktop/src/main/ad-recommendation-ai-context.test.ts apps/desktop/src/main/recommendation-generation-gate.test.ts`, and `pnpm --filter @amazon-ai-ops/desktop run typecheck`.
- 2026-06-17 recommendation-generation product-binding gate pass: formal AI+rule recommendation generation now requires every loaded metric row to be bound to a product ASIN unless the current operation scope explicitly provides the ASIN. Both formal generation and diagnosis-only generation pass this product-binding signal into the gate, and AI-only recommendation creation now has a local fail-closed guard so it cannot emit a formal recommendation with an empty ASIN if reused outside the normal gate. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/main/recommendation-generation-gate.test.ts`, `pnpm exec vitest run apps/desktop/src/main/ad-recommendation-ai-context.test.ts`, `pnpm exec vitest run apps/desktop/src/main/ad-recommendation-ai-context.test.ts apps/desktop/src/main/recommendation-generation-gate.test.ts`, and `pnpm --filter @amazon-ai-ops/desktop run typecheck`.
- 2026-06-17 readback product-binding gate pass: the execution readback form and final readback evidence builder now require a concrete ASIN before a manual Ads UI readback artifact can be considered complete. This closes the remaining product-binding bypass after approval, so ad execution evidence cannot be marked PASS from store/site/campaign/ad group/object alone. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/renderer/pages/readback-page.test.tsx`, `pnpm exec vitest run apps/desktop/src/main/ad-readback-evidence.test.ts`, `pnpm exec vitest run apps/desktop/src/renderer/pages/readback-page.test.tsx apps/desktop/src/main/ad-readback-evidence.test.ts`, and `pnpm --filter @amazon-ai-ops/desktop run typecheck`.
- 2026-06-17 approval product-binding gate pass: backend approval policy and the approval-center UI now require every ordinary approvable recommendation to bind to a concrete product ASIN before approval. This prevents cross-product or ambiguous ad-object actions from moving into execution/readback, while still allowing the ASIN to come from either the recommendation row or its evidence payload on the backend. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/main/recommendation-approval-policy.test.ts`, `pnpm exec vitest run apps/desktop/src/renderer/pages/approval-page.test.tsx`, `pnpm exec vitest run apps/desktop/src/renderer/pages/approval-page.test.tsx apps/desktop/src/main/recommendation-approval-policy.test.ts`, and `pnpm --filter @amazon-ai-ops/desktop run typecheck`.
- 2026-06-17 recommendation ASIN filter consistency pass: `RecommendationRepository.findByFilter()` now filters ASIN case-insensitively, matching ad metrics and operation-event filtering. This prevents optimization, approval, delivery, and readback entry lists from missing current-scope recommendations when an operator-entered ASIN casing differs from the stored report value. Added repository coverage with a fake DB to avoid native SQLite ABI dependency during this targeted test. Incremental checks passed: `pnpm exec vitest run packages/local-db/src/sqlite/repositories/recommendation-repo.test.ts`, `pnpm --filter @amazon-ai-ops/local-db run typecheck`, and `pnpm --filter @amazon-ai-ops/desktop run typecheck`.
- 2026-06-17 delivery AI insight-count boundary pass: the delivery readiness matrix no longer counts insights attached to failed AI diagnosis runs as delivered AI insight output. `aiInsightOnlyCount` now only includes successful AI-source diagnosis runs, so the delivery page does not overstate AI participation when a failed/兜底 run carries diagnostic remnants. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/renderer/delivery-readiness-matrix.test.ts`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, and `pnpm --filter @amazon-ai-ops/desktop run build:renderer`.
- 2026-06-17 ad-quant AI diagnosis scope pass: the ad quantification page now sends the current ASIN when loading recent AI diagnosis runs and reloads those runs when `scope.asin` changes. This aligns the page with dashboard/main/repository ASIN filtering and prevents product-specific AI diagnosis history from mixing across ASINs under the same store/site/batch. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/renderer/pages/ad-quant-page.test.tsx`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, and `pnpm --filter @amazon-ai-ops/desktop run build:renderer`.
- 2026-06-17 AI diagnosis scope isolation pass: AI diagnosis run listing now honors ASIN in addition to date/store/site/batch filters, preventing product-specific dashboards or ad-quant pages from showing another ASIN's diagnosis records under the same batch. The ASIN filter is wired through preload, main IPC, and `AiDiagnosisRunRepository.findRecent()`. Incremental checks passed: `pnpm exec vitest run packages/local-db/src/sqlite/repositories/ai-diagnosis-run-repo.test.ts`, `pnpm --filter @amazon-ai-ops/local-db run typecheck`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, `pnpm --filter @amazon-ai-ops/desktop run build:main`, and `pnpm --filter @amazon-ai-ops/desktop run build:preload`.
- 2026-06-17 AI diagnosis visibility pass: the ad quantification page no longer renders insight-only AI diagnosis runs as `无诊断摘要`; it now states that AI completed diagnosis, produced insight-only output, and needs evidence refs/source row/ad-object binding before formal recommendations. The same historical AI run card now shows the top insight objects and invalid reasons under `洞察未入池原因`, so operators can see what must be fixed. The dashboard AI state now combines connection status with the latest AI work status in both the top data-health summary and the action card, surfacing `AI 有洞察待补证据` when the latest run produced insights but no approvable recommendation. That action card routes operators to `广告量化` for diagnosis review instead of always sending them to settings. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/renderer/pages/ad-quant-page.test.tsx apps/desktop/src/renderer/pages/dashboard-page.test.ts`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, and `pnpm --filter @amazon-ai-ops/desktop run build:renderer`.
- 2026-06-17 recommendation no-output diagnostics pass: the recommendations page now distinguishes `AI 仅生成洞察，未进入建议池` from generic empty-list states when AI returns insight-only or filtered candidates. The top empty-state explanation now includes AI candidate counts, filter/invalid reasons, and a concrete next step to补齐 source row、campaign/ad group、关键词/投放对象 binding and evidence before regenerating. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/renderer/pages/recommendations-page.test.ts`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, `pnpm --filter @amazon-ai-ops/desktop run build:renderer`, and `node scripts\smoke-business-ui-ad-execution.js` with evidence `output\codex-evidence\business-ui-ad-execution-smoke-1781693405647.json`.
- 2026-06-17 AI settings status preservation UI pass: the settings page now distinguishes connection-changing fields from non-connection prompt/output controls. Only API Key, Base URL, and Model edits reset the AI connection test state to `待测试`; Temperature, Max Tokens, output language, and AI persona edits preserve the last successful test status, matching the main-process save normalization that keeps `aiLastTestStatus` when the tested endpoint/model still match. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/renderer/pages/settings-page.test.ts apps/desktop/src/main/ai-settings-normalization.test.ts`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, `pnpm --filter @amazon-ai-ops/desktop run build:renderer`, and `node scripts\smoke-business-ui-settings-delivery.js` with evidence `output\codex-evidence\business-ui-settings-delivery-smoke-1781693194827.json`.
- 2026-06-17 data-collection action-choice clarity pass: the data collection page now gives each collection action a tested operator-facing guide with `when to use`, `whether it creates a Lingxing task`, and `where data goes next`. The four buttons now distinguish: download existing ready rows without creating tasks, recreate selected reports, recreate the full 8-report set, and import local Lingxing xlsx/xls/csv files directly into DB daily metrics. The action grid was adjusted to four desktop columns with taller buttons so the longer guidance remains readable. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/renderer/pages/data-collection-page.test.ts apps/desktop/src/renderer/collection-action-summary.test.ts`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, `pnpm --filter @amazon-ai-ops/desktop run build:renderer`, and `node scripts\smoke-business-ui-data-pipeline.js` with evidence `output\codex-evidence\business-ui-data-pipeline-smoke-1781692976392.json`.
- 2026-06-17 operator-facing fallback wording cleanup: renderer business pages and shared summaries now use Chinese operator copy such as `规则兜底`, `AI 独立洞察`, `规则独立`, and `标准 JSON 输出格式` instead of exposing raw mixed-language labels like `规则 fallback`, `AI-only`, `规则-only`, or `AI 已回落规则`. Covered pages/helpers include dashboard AI status, ad quantification strategy/threshold labels, optimization recommendation summaries, approval strategy labels, Listing draft workflow, delivery readiness matrix, and AI evidence summaries. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/renderer/evidence-display.test.ts apps/desktop/src/renderer/pages/listing-optimization-page.test.ts apps/desktop/src/renderer/pages/recommendations-page.test.ts apps/desktop/src/renderer/pages/ad-quant-page.test.tsx apps/desktop/src/renderer/pages/approval-page.test.tsx apps/desktop/src/renderer/pages/dashboard-page.test.ts apps/desktop/src/renderer/delivery-readiness-matrix.test.ts apps/desktop/src/renderer/listing-workflow-summary.test.ts` (8 files / 73 tests), `pnpm --filter @amazon-ai-ops/desktop run typecheck`, `pnpm --filter @amazon-ai-ops/desktop run build:renderer`, `node scripts\smoke-business-ui-data-pipeline.js` with evidence `output\codex-evidence\business-ui-data-pipeline-smoke-1781692598514.json`, and `node scripts\smoke-business-ui-ad-execution.js` with evidence `output\codex-evidence\business-ui-ad-execution-smoke-1781692614404.json`.
- 2026-06-17 renderer real-report coverage alignment: frontend readiness now uses audited unique Lingxing report-type coverage before falling back to raw file count. Dashboard, ad quantification, recommendations, keyword opportunities, Listing optimization, and the data-collection action summary no longer treat duplicate spreadsheet files as equivalent to 8/8 report coverage; user-facing copy now says `N/8 类真实报表` where the business meaning is coverage rather than raw files. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/renderer/report-coverage.test.ts apps/desktop/src/renderer/recommendation-readiness.test.ts apps/desktop/src/renderer/pages/dashboard-page.test.ts apps/desktop/src/renderer/pages/scheduler-page.test.ts`, `pnpm exec vitest run apps/desktop/src/renderer/pages/data-collection-page.test.ts apps/desktop/src/renderer/report-coverage.test.ts apps/desktop/src/renderer/pages/dashboard-page.test.ts`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, `pnpm --filter @amazon-ai-ops/desktop run build:renderer`, `node scripts\smoke-business-ui-data-pipeline.js` with latest evidence `output\codex-evidence\business-ui-data-pipeline-smoke-1781681924623.json`, `node scripts\smoke-business-ui-ad-execution.js` with evidence `output\codex-evidence\business-ui-ad-execution-smoke-1781681944112.json`, and `node scripts\smoke-business-ui-keyword-listing.js` with evidence `output\codex-evidence\business-ui-keyword-listing-smoke-1781681530993.json`.
- 2026-06-17 AI evidence display context pass: AI evidence summaries now include batch id, campaign, ad group, ASIN, entity type/name, source row, report type, and USD metrics where available. This makes `为什么这么判断` traceable to the actual ad structure instead of only showing opaque evidence ids or metric totals. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/renderer/evidence-display.test.ts`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, `pnpm --filter @amazon-ai-ops/desktop run build:renderer`, `node scripts\smoke-business-ui-ad-execution.js` with evidence `output\codex-evidence\business-ui-ad-execution-smoke-1781682237964.json`, and `node scripts\smoke-business-ui-data-pipeline.js` with evidence `output\codex-evidence\business-ui-data-pipeline-smoke-1781682268833.json`.
- 2026-06-17 rule-only approval alignment: rule-only recommendations can now carry batch-level AI strategy/lifecycle context without being misclassified as AI-sourced actions that require action-level `aiEvidenceRefs`. AI action evidence remains mandatory for `aligned` and `ai_only` decisions, while pure deterministic rule recommendations are not blocked just because the batch-level AI lifecycle judgment needs review. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/main/ad-recommendation-ai-context.test.ts apps/desktop/src/renderer/pages/recommendations-page.test.ts apps/desktop/src/renderer/pages/approval-page.test.tsx apps/desktop/src/main/recommendation-approval-policy.test.ts`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, `pnpm --filter @amazon-ai-ops/desktop run build:main`, `pnpm --filter @amazon-ai-ops/desktop run build:renderer`, and `node scripts\smoke-business-ui-ad-execution.js` with evidence `output\codex-evidence\business-ui-ad-execution-smoke-1781683128214.json`.
- 2026-06-17 delivery-matrix rule-only alignment: the delivery readiness matrix now uses the same rule-only vs AI-action split as recommendations and approval. A pending `rule_only` recommendation is still counted as formally approvable even when the batch-level AI lifecycle diagnosis requires review; only concrete AI-participated actions (`aligned` / `ai_only`, plus conflicts) trigger AI evidence or lifecycle review handling. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/renderer/delivery-readiness-matrix.test.ts apps/desktop/src/renderer/pages/recommendations-page.test.ts apps/desktop/src/renderer/pages/approval-page.test.tsx apps/desktop/src/main/recommendation-approval-policy.test.ts`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, `pnpm --filter @amazon-ai-ops/desktop run build:renderer`, and `node scripts\smoke-business-ui-settings-delivery.js` with evidence `output\codex-evidence\business-ui-settings-delivery-smoke-1781683517444.json`.
- 2026-06-17 dashboard coverage wording alignment: the dashboard quantification gate now states `N/8 类真实报表` instead of `N 个真实表格`, matching the audited report-type coverage used by data collection, import validation, ad quantification, recommendations, keyword opportunities, and Listing optimization. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/renderer/pages/dashboard-page.test.ts apps/desktop/src/renderer/report-coverage.test.ts`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, `pnpm --filter @amazon-ai-ops/desktop run build:renderer`, and `node scripts\smoke-business-ui-data-pipeline.js` with evidence `output\codex-evidence\business-ui-data-pipeline-smoke-1781684195681.json`.
- 2026-06-17 data-collection action summary wording alignment: the action summary now separates current report-type coverage from action-level downloaded files. It shows `当前范围覆盖 N/8 类` for business readiness and `本次新增真实文件 N 个` for the current action, so an existing complete batch is not confused with a fresh Lingxing download. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/renderer/collection-action-summary.test.ts apps/desktop/src/renderer/pages/data-collection-page.test.ts`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, `pnpm --filter @amazon-ai-ops/desktop run build:renderer`, and `node scripts\smoke-business-ui-data-pipeline.js` with evidence `output\codex-evidence\business-ui-data-pipeline-smoke-1781684413657.json`.
- 2026-06-17 recommendation operator-resolution count alignment: the recommendations page now reports `需处理` as the union of manual-review work and evidence blockers, instead of the older partial `需复核` count that missed AI-only, conflicts, insight-only, and missing-evidence rows. The page still separates `人工复核` and `证据不足阻断`, but the table/header summary now matches the approval gate reality. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/renderer/pages/recommendations-page.test.ts apps/desktop/src/renderer/pages/approval-page.test.tsx apps/desktop/src/main/recommendation-approval-policy.test.ts apps/desktop/src/renderer/delivery-readiness-matrix.test.ts`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, `pnpm --filter @amazon-ai-ops/desktop run build:renderer`, and `node scripts\smoke-business-ui-ad-execution.js` with evidence `output\codex-evidence\business-ui-ad-execution-smoke-1781684738503.json`.
- 2026-06-17 approval review-queue wording alignment: the approval center no longer labels `needs_review` as only `AI 复核`. It now uses `复核队列`, because the queue may contain AI-only, AI/rule conflicts, rule quantification review, evidence blockers, and other non-AI review causes. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/renderer/pages/approval-page.test.tsx apps/desktop/src/renderer/pages/recommendations-page.test.ts apps/desktop/src/main/recommendation-approval-policy.test.ts`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, `pnpm --filter @amazon-ai-ops/desktop run build:renderer`, and `node scripts\smoke-business-ui-ad-execution.js` with evidence `output\codex-evidence\business-ui-ad-execution-smoke-1781685053039.json`.
- 2026-06-17 readback decision-label wording alignment: the readback page no longer shows raw enum values such as `aligned / rule_ai` in the operator-facing evidence context. The UI now displays `规则+AI 一致 / 规则+AI 合并`, while the exported readback evidence still preserves the structured raw fields needed by verifiers. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/renderer/pages/readback-page.test.tsx apps/desktop/src/renderer/pages/approval-page.test.tsx apps/desktop/src/renderer/pages/recommendations-page.test.ts`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, `pnpm --filter @amazon-ai-ops/desktop run build:renderer`, and `node scripts\smoke-business-ui-ad-execution.js` with evidence `output\codex-evidence\business-ui-ad-execution-smoke-1781685384558.json`.
- 2026-06-17 global report-coverage wording closeout: the global scope bar, operation-scope page, dashboard task entry, data import flow, data collection action results, and shell smoke now consistently distinguish `N/8 类真实报表` coverage from DB metric rows and from action-level downloaded files. Operator-facing primary state no longer uses `真实表格` as the readiness label; file-level copy still names xlsx/xls/csv table files where it is specifically about opening or importing files. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/renderer/components/scope-bar.test.ts apps/desktop/src/renderer/data-readiness-ledger.test.ts apps/desktop/src/renderer/collection-action-summary.test.ts apps/desktop/src/renderer/pages/data-collection-page.test.ts apps/desktop/src/renderer/pages/dashboard-page.test.ts`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, `pnpm --filter @amazon-ai-ops/desktop run build:renderer`, `node scripts\smoke-business-ui-data-pipeline.js` with evidence `output\codex-evidence\business-ui-data-pipeline-smoke-1781686487569.json`, and `node scripts\smoke-business-ui-shell.js` with evidence `output\codex-evidence\business-ui-shell-smoke-1781686541443.json`.
- 2026-06-17 approval submit precheck alignment: the approval center now uses the same missing-evidence checks for button enablement that it uses at submit time. Rows missing current batch, source batch/date/row, loaded real current-batch source report files, campaign/ad group/object/action/current value/recommended value, or AI/rule blocker clearance show an explicit evidence-incomplete message and cannot click `批准并进入待执行`. This avoids the previous UX where the operator could click approve and only then receive a blocker. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/renderer/pages/approval-page.test.tsx`, `pnpm exec vitest run apps/desktop/src/renderer/pages/approval-page.test.tsx apps/desktop/src/main/recommendation-approval-policy.test.ts`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, `pnpm --filter @amazon-ai-ops/desktop run build:renderer`, and `node scripts\smoke-business-ui-ad-execution.js` with evidence `output\codex-evidence\business-ui-ad-execution-smoke-1781687677930.json`.
- 2026-06-17 recommendation-to-approval evidence gate alignment: the recommendations page now uses the same current-batch real report file list when counting formal approval-ready rows, manual review rows, evidence blockers, and selected-row evidence issues. If the current batch report file list has not loaded, a recommendation with source files is treated as blocked instead of being shown as approval-ready and then failing later in the approval center. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/renderer/pages/recommendations-page.test.ts apps/desktop/src/renderer/pages/approval-page.test.tsx apps/desktop/src/main/recommendation-approval-policy.test.ts apps/desktop/src/renderer/delivery-readiness-matrix.test.ts`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, `pnpm --filter @amazon-ai-ops/desktop run build:renderer`, and `node scripts\smoke-business-ui-ad-execution.js` with evidence `output\codex-evidence\business-ui-ad-execution-smoke-1781687921820.json`.
- 2026-06-17 AI fallback reason separation: ad strategy diagnosis fallback and per-action explanation fallback are now stored separately as `aiStrategyFallbackReason` and `aiActionFallbackReason`, while `aiFallbackReason` remains backward-compatible. A successful per-action AI explanation no longer clears an earlier strategy-diagnosis fallback reason, and the recommendations UI shows strategy fallback inside the strategy panel while showing single-action explanation fallback beside the explanation. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/main/ad-action-explanation-merge.test.ts apps/desktop/src/main/ad-recommendation-ai-context.test.ts apps/desktop/src/main/ad-action-ai-call-log.test.ts apps/desktop/src/renderer/pages/recommendations-page.test.ts`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, `pnpm --filter @amazon-ai-ops/desktop run build:main`, `pnpm --filter @amazon-ai-ops/desktop run build:renderer`, and `node scripts\smoke-business-ui-ad-execution.js` with evidence `output\codex-evidence\business-ui-ad-execution-smoke-1781688459217.json`.
- 2026-06-17 AI fallback readback traceability: separated AI fallback fields now flow into readback evidence exports. `buildAdReadbackEvidence` records `source.aiStrategyFallbackReason` and `source.aiActionFallbackReason`, the Markdown evidence names both fallback layers, and the readback page preserves both fields when loading an approved recommendation into the manual Ads UI evidence form. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/main/ad-readback-evidence.test.ts apps/desktop/src/renderer/pages/readback-page.test.tsx apps/desktop/src/main/ad-action-explanation-merge.test.ts apps/desktop/src/main/ad-recommendation-ai-context.test.ts apps/desktop/src/renderer/pages/recommendations-page.test.ts`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, `pnpm --filter @amazon-ai-ops/desktop run build:main`, `pnpm --filter @amazon-ai-ops/desktop run build:renderer`, and `node scripts\smoke-business-ui-ad-execution.js` with evidence `output\codex-evidence\business-ui-ad-execution-smoke-1781688805416.json`.
- 2026-06-17 AI fallback approval traceability: approval and rejection decision payloads now preserve `aiStrategyFallbackReason` and `aiActionFallbackReason` alongside the existing AI strategy and recommendation context. The approval center uses a tested `buildApprovalDecisionPayload` helper so the approval record, readback source, and final evidence package all carry the same fallback-layer explanation. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/renderer/pages/approval-page.test.tsx apps/desktop/src/main/recommendation-approval-policy.test.ts apps/desktop/src/renderer/pages/readback-page.test.tsx apps/desktop/src/main/ad-readback-evidence.test.ts`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, `pnpm --filter @amazon-ai-ops/desktop run build:renderer`, `pnpm --filter @amazon-ai-ops/desktop run build:main`, and `node scripts\smoke-business-ui-ad-execution.js` with evidence `output\codex-evidence\business-ui-ad-execution-smoke-1781689104975.json`.
- 2026-06-17 AI fallback script traceability: the recommendation-to-readback helper scripts now preserve the same separated fallback fields. `scripts/create-ad-readback-candidate-from-recommendation.js` reads `aiStrategyFallbackReason` and `aiActionFallbackReason` from recommendation evidence and passes them into `scripts/create-ad-readback-evidence-template.js`; the generated JSON stores them under `source`, and the Markdown checklist names both fallback layers. Incremental checks passed: `pnpm exec vitest run scripts/create-ad-readback-candidate.test.mjs scripts/create-ad-readback-template.test.mjs` and `node --check scripts\create-ad-readback-candidate-from-recommendation.js scripts\create-ad-readback-evidence-template.js`.
- 2026-06-17 readback verifier source/live separation: `verify-ad-readback-evidence` no longer requires recommendation source values to equal live Ads UI before/after values. Source `currentValue/recommendedValue` now prove the recommendation inputs from reports/rules/AI, while `before.value`, `after.value`, screenshots, live bid source note, execution timestamp, and independent reload/readback prove the real Ads UI action. This keeps future executions generic instead of forcing operators to overwrite report-derived values with live bid values. Incremental checks passed: `pnpm exec vitest run scripts/verify-ad-readback-evidence.test.mjs scripts/export-v15-delivery-bundle.test.mjs scripts/verify-v15-ready-safety.test.mjs scripts/verify-v15-final-readiness.test.mjs apps/desktop/src/main/ad-readback-evidence.test.ts apps/desktop/src/renderer/pages/readback-page.test.tsx apps/desktop/src/main/delivery-evidence-status.test.ts`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, `pnpm --filter @amazon-ai-ops/desktop run build:main`, `pnpm --filter @amazon-ai-ops/desktop run build:renderer`, `node scripts\smoke-export-v15-delivery-bundle.js` with evidence `output\codex-evidence\export-v15-delivery-bundle-smoke-1781690249094.json`, and `node scripts\smoke-business-ui-ad-execution.js` with evidence `output\codex-evidence\business-ui-ad-execution-smoke-1781690249714.json`.
- 2026-06-17 non-ready safety legacy-readback boundary: `verify:v15-non-ready-safety` now treats historical `APP_READY` readback evidence as baseline only when the README is explicitly non-ready. If the historical readback sample only fails the current stricter verifier because it lacks newly required `sourceFiles/sourceRow`, the guard records `historical real ad readback baseline lacks current source report traceability only` and still verifies the current `IN_PROGRESS` state; hollow or otherwise invalid readback evidence remains a failure. Incremental checks passed: `pnpm exec vitest run scripts/verify-v15-non-ready-safety.test.mjs`, `pnpm run verify:v15-non-ready-safety`, `node --check scripts\verify-v15-non-ready-safety.js`, and `git diff --check` with only Windows CRLF warnings.
- 2026-06-17 recommendation/approval copy cleanup: operator-facing recommendation and approval pages no longer show raw `AI-only` or `needs_review` enum names in merge summaries, review guidance, approval blockers, or panel descriptions. The UI now uses `AI 独立洞察` and `复核队列`, while internal filters and type names remain structured. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/renderer/pages/recommendations-page.test.ts apps/desktop/src/renderer/pages/approval-page.test.tsx`; active renderer scan now only finds raw enum strings in tests, type definitions, and internal filter logic.
- 2026-06-17 AI audit copy cleanup: Settings AI audit output now uses operator-facing labels for call type and output format. Raw `promptKey`, `schema`, `schemaVersion`, `promptVersion`, and prompt terminology are mapped to `广告策略诊断`, `广告建议解释`, `Listing 草案`, `输出格式 ...` and `完整提示词`, while still preserving structured fields internally for logs and tests. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/renderer/ai-call-diagnostics.test.ts apps/desktop/src/renderer/pages/settings-page.test.ts`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, `pnpm --filter @amazon-ai-ops/desktop run build:renderer`, and `node scripts\smoke-business-ui-settings-delivery.js` with evidence `output\codex-evidence\business-ui-settings-delivery-smoke-1781691622914.json`.
- 2026-06-16 navigation IA alignment pass: the sidebar now matches the current business-flow design. `数据与量化` contains 工作范围, 数据采集, 数据导入与校验, 运营事件, 产品配置, and 广告量化; `广告执行` contains 优化建议, 审批中心, and 执行回读; `系统与交付` contains 交付验收, 定时任务, and 设置. This removes the lingering split between old `数据资产` / `广告运营` / `运营上下文` labels and the user-facing plan/guide.
- Incremental checks for the navigation IA pass: `node --check scripts\smoke-business-ui-shell.js` passed, `node --check scripts\smoke-v15-product-readiness-ui.js` passed, `pnpm --filter @amazon-ai-ops/desktop run build:renderer` passed, `node scripts\smoke-business-ui-shell.js` passed with evidence `output\codex-evidence\business-ui-shell-smoke-1781541447674.json`, and `node scripts\smoke-business-ui-data-pipeline.js` passed with evidence `output\codex-evidence\business-ui-data-pipeline-smoke-1781541458285.json`.
- 2026-06-16 global operation scope evidence pass: A2 is documented against the current implementation. `ScopeBar` shows the current range as one business-readable sentence with date/store/site/USD, exposes only understandable batch modes, warns when a manually typed batch is not matched to the current range, and forces USD through `scope-store.ts` and `formatters.ts`. The latest shell/data-pipeline smoke evidence covers `当前操作范围`, `批次模式`, `当前批次`, `报表覆盖`, `已导入指标`, and absence of RMB markers.
- 2026-06-16 operation-event evidence pass: C1 is now documented against the current implementation. Operation event types cover Coupon, Deal, BD/LD, promotion, price change, inventory, Listing change, external/offsite traffic, review changes, and manual notes. The repository preserves date/store/site/ASIN plus campaign/ad group context, and the page gives quick templates for common operator inputs. After Windows packaging had rebuilt `better-sqlite3` for Electron ABI 119, the local Node test runtime was restored by deleting the package build directory and rebuilding with Python 3.11 for ABI 137. `pnpm exec vitest run packages/local-db/src/sqlite/repositories/operation-event-repo.test.ts` then passed, 1 file / 3 tests.
- 2026-06-16 approval policy evidence pass: E1 is now documented and verified. Approval requires concrete store/site/batch/date/campaign/ad group/object/action/current value/recommended value/source-file context. Main-process approval calls `assertRecommendationCurrentDataGate` and `assertRecommendationApprovalPolicy` before writing `approved`, so this is not just a frontend disabled-button guard. AI-only, AI/rule conflict, explicit review, quant-review, forbidden, and high-risk recommendations are blocked from normal approval. `pnpm exec vitest run apps/desktop/src/main/recommendation-approval-policy.test.ts` passed, 1 file / 4 tests; `node scripts\smoke-business-ui-ad-execution.js` passed with evidence `output\codex-evidence\business-ui-ad-execution-smoke-1781541981884.json`.
- 2026-06-17 readback source traceability hardening: `verify:ad-readback` now rejects evidence without real `.xlsx/.xls/.csv` source report file(s) and a positive original `source.sourceRow`. The readback template and recommendation-candidate generator now carry `source.sourceFiles` and `source.sourceRow`, so final readback evidence cannot pass using only audit JSON/PNG/HTML or summary metadata. Incremental checks passed: `pnpm exec vitest run scripts/create-ad-readback-template.test.mjs scripts/create-ad-readback-candidate.test.mjs scripts/verify-ad-readback-evidence.test.mjs`.
- 2026-06-17 readback action-direction hardening: `verify:ad-readback` now verifies that a `lower_bid` evidence packet actually lowered the live bid value. Evidence where `before.value` is `2.40` and `after/readback` is `2.60` now fails with `lower_bid action did not lower the bid value`, instead of passing merely because the value changed and readback matched. Incremental checks passed: `pnpm exec vitest run scripts/verify-ad-readback-evidence.test.mjs` and `node --check scripts\verify-ad-readback-evidence.js`.
- 2026-06-17 readback source-value traceability hardening: `verify:ad-readback` requires both `source.currentValue` and `source.recommendedValue` as recommendation-input traceability, while live Ads UI execution is proven separately by `before.value`, `after.value`, screenshots, live bid source note, and independent readback. This avoids forcing report-derived recommendation values to equal the current Ads UI bid. Incremental checks passed: `pnpm exec vitest run scripts/verify-ad-readback-evidence.test.mjs` and `node --check scripts\verify-ad-readback-evidence.js`.
- 2026-06-17 readback evidence-file independence hardening: `verify:ad-readback` now requires `before.screenshotPath`, `after.screenshotPath`, and `readback.evidencePath`/`readback.screenshotPath` to resolve to distinct files. Evidence that reuses the same screenshot for before/after or after/readback now fails instead of proving only file existence. Incremental checks passed: `pnpm exec vitest run scripts/verify-ad-readback-evidence.test.mjs` and `node --check scripts\verify-ad-readback-evidence.js`.
- 2026-06-17 readback export/verifier alignment: the desktop readback evidence builder now uses the same distinct before/after/readback evidence-file rule before marking an export `PASS`. Reused screenshot paths now keep the export at `NEEDS_WORK`, so the UI/export status no longer disagrees with `verify:ad-readback`. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/main/ad-readback-evidence.test.ts scripts/verify-ad-readback-evidence.test.mjs` and `pnpm --filter @amazon-ai-ops/desktop run typecheck`.
- 2026-06-17 readback export source-file alignment: the desktop readback evidence builder now requires `source.sourceFiles` to point to existing real spreadsheet reports (`.xlsx/.xls/.csv`) before marking an export `PASS`, matching the verifier contract. Missing files or audit JSON artifacts keep the export at `NEEDS_WORK`. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/main/ad-readback-evidence.test.ts scripts/verify-ad-readback-evidence.test.mjs` and `pnpm --filter @amazon-ai-ops/desktop run typecheck`.
- 2026-06-17 readback export value/direction alignment: the desktop readback evidence builder now requires source current/recommended values to be present, keeps them separate from live before/after values, and still requires `lower_bid` exports to prove `after.value < before.value` before marking an export `PASS`. This prevents the UI/export layer from claiming readiness for an invalid bid direction while preserving report-derived recommendation inputs. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/main/ad-readback-evidence.test.ts scripts/verify-ad-readback-evidence.test.mjs` and `pnpm --filter @amazon-ai-ops/desktop run typecheck`.
- 2026-06-17 readback page precheck alignment: the `执行回读` page precheck now mirrors the export/verifier contract for source value presence, `lower_bid` direction, and distinct before/after/readback evidence paths. The page no longer shows the success-state wording `字段完整` for evidence that the backend export or final verifier would reject. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/renderer/pages/readback-page.test.tsx apps/desktop/src/main/ad-readback-evidence.test.ts scripts/verify-ad-readback-evidence.test.mjs` and `pnpm --filter @amazon-ai-ops/desktop run typecheck`.
- 2026-06-17 readback page source-file type precheck: the `执行回读` page now checks that recommendation source files are spreadsheet report paths (`.xlsx/.xls/.csv`) before showing a complete precheck. Single audit JSON sources and mixed spreadsheet/audit lists are surfaced as source-file blockers instead of being left for backend export or `verify:ad-readback` to reject later. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/renderer/pages/readback-page.test.tsx apps/desktop/src/main/ad-readback-evidence.test.ts scripts/verify-ad-readback-evidence.test.mjs` and `pnpm --filter @amazon-ai-ops/desktop run typecheck`.
- 2026-06-17 readback value formatting alignment: the `执行回读` page, desktop readback evidence builder, and `verify:ad-readback` now compare `readback.actualValue` and `after.value` with the same executable-number normalization used for source current/recommended values. Equivalent USD formats such as `$1.08`, `1.08`, and `1.08 USD` no longer produce false blockers, while percentage values remain non-executable. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/renderer/pages/readback-page.test.tsx apps/desktop/src/main/ad-readback-evidence.test.ts scripts/verify-ad-readback-evidence.test.mjs` and `pnpm --filter @amazon-ai-ops/desktop run typecheck`.
- 2026-06-17 readback unchanged-value precheck alignment: the `执行回读` page now uses the same executable-number normalization for the `before/after 值不能相同` precheck. Equivalent USD formats such as `$1.08` and `1.08 USD` are now recognized as unchanged values instead of only relying on raw string equality. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/renderer/pages/readback-page.test.tsx apps/desktop/src/main/ad-readback-evidence.test.ts scripts/verify-ad-readback-evidence.test.mjs` and `pnpm --filter @amazon-ai-ops/desktop run typecheck`.
- 2026-06-17 readback unchanged-value backend/verifier alignment: the desktop readback evidence builder and `verify:ad-readback` now also use executable-number normalization when proving `before.value` and `after.value` changed. Evidence that only changes formatting, for example `$2.16` -> `2.16 USD`, no longer passes export/verifier as a live Ads UI change. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/renderer/pages/readback-page.test.tsx apps/desktop/src/main/ad-readback-evidence.test.ts scripts/verify-ad-readback-evidence.test.mjs` and `pnpm --filter @amazon-ai-ops/desktop run typecheck`.
- 2026-06-17 readback blocker display alignment: newly added verifier-aligned blockers are now grouped into visible operator recovery areas on the `执行回读` page. Missing source values appear under `建议来源`, while invalid lower-bid direction and reused before/after/readback evidence files appear under `证据文件和值`; the top `执行前后` step also counts these execution-evidence blockers. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/renderer/pages/readback-page.test.tsx apps/desktop/src/main/ad-readback-evidence.test.ts scripts/verify-ad-readback-evidence.test.mjs` and `pnpm --filter @amazon-ai-ops/desktop run typecheck`.
- 2026-06-17 delivery readback status hardening: `getDeliveryEvidenceStatus` no longer counts a readback JSON as verified merely because it says `status=PASS`. The delivery/dashboard summary now requires real spreadsheet source files, positive source row, source current/recommended values, actual value matching after, valid lower-bid direction, and independent before/after/readback evidence files before incrementing verified readback count. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/main/delivery-evidence-status.test.ts` and `pnpm --filter @amazon-ai-ops/desktop run typecheck`.
- 2026-06-17 READY safety readback verifier alignment: `verify:v15-ready-safety` now runs `scripts/verify-ad-readback-evidence.js` against the manifest-selected ad readback evidence instead of only checking that the file exists. A hollow `PASS` readback JSON can no longer satisfy the final READY safety layer. Incremental checks passed: `pnpm exec vitest run scripts/verify-v15-ready-safety.test.mjs`, `pnpm exec vitest run apps/desktop/src/main/delivery-evidence-status.test.ts scripts/verify-v15-ready-safety.test.mjs`, and `pnpm --filter @amazon-ai-ops/desktop run typecheck`.
- 2026-06-17 delivery bundle readback verifier alignment: `export:v15-delivery-bundle` now refuses `APP_READY` bundle export unless the manifest-selected ad readback evidence passes `verify:ad-readback`. The bundle smoke was updated to generate a complete local readback fixture instead of an empty manifest, so bundle export tests exercise the same proof boundary as final READY safety. Incremental checks passed: `pnpm exec vitest run scripts/export-v15-delivery-bundle.test.mjs` and `node scripts\smoke-export-v15-delivery-bundle.js`.
- 2026-06-17 non-ready safety historical-baseline hardening: `verify:v15-non-ready-safety` still allows a historical APP_READY final-readiness/bundle as baseline when the current README is explicitly non-ready, but it verifies the manifest-selected historical ad readback evidence enough to reject hollow or otherwise invalid readback files. A later boundary update allows the specific legacy case where old readback evidence only lacks the now-required `sourceFiles/sourceRow`, because current README remains `IN_PROGRESS` and final READY must be refreshed with current-contract evidence. Incremental checks passed: `pnpm exec vitest run scripts/verify-v15-non-ready-safety.test.mjs`.
- 2026-06-17 final-readiness AI live secret hardening: `verify:v15-final-readiness` now rejects AI live provider evidence containing obvious API-key or bearer-token shaped material before marking the AI live gate passed. This closes the gap where a structurally successful `deepseek-live` evidence file could still leak `sk-...` material and receive final readiness credit. Incremental checks passed: `pnpm exec vitest run scripts/verify-v15-final-readiness.test.mjs`.
- 2026-06-17 ad AI explanation secret-scan alignment: `verify:ad-ai-explanation` now rejects bearer-token shaped strings and `deepseek_api_key`-style fields in addition to `sk-...` patterns. This aligns ad recommendation AI evidence scanning with Listing AI draft and final-readiness gates, so a valid-looking packaged-app ad explanation file cannot receive final readiness credit while leaking provider credentials. Incremental checks passed: `pnpm exec vitest run scripts/verify-ad-ai-explanation-evidence.test.mjs scripts/verify-v15-final-readiness.test.mjs`, `node --check scripts\verify-ad-ai-explanation-evidence.js`, and `pnpm --filter @amazon-ai-ops/desktop run typecheck`.
- 2026-06-17 real-report reconciliation and bundle gate hardening: `reconcile-lingxing-full8-data` now emits top-level blockers when a real canonical report file is missing required spend/order/sales columns instead of silently totaling missing columns as zero. `export:v15-delivery-bundle` now refuses `APP_READY` export when data reconciliation has blockers, when canonical ad spend is not positive, when no real report source is indexed, or when any indexed `.xlsx/.xls/.csv` source report is missing. `verify:v15-ready-safety` now requires the delivery bundle real report index to exist, reference at least one source report, resolve every source report, and have `missingCount=0`. Incremental checks passed: `pnpm exec vitest run scripts/verify-v15-ready-safety.test.mjs scripts/export-v15-delivery-bundle.test.mjs scripts/reconcile-lingxing-full8-data.test.mjs`, `node scripts\smoke-export-v15-delivery-bundle.js`, and `node --check scripts\verify-v15-ready-safety.js; node --check scripts\export-v15-delivery-bundle.js; node --check scripts\reconcile-lingxing-full8-data.js`.
- 2026-06-17 AI recommendation approval evidence-detail hardening: normal approval now requires AI-sourced recommendations to have `aiEvidenceRefs` resolved into displayable `aiEvidenceDetails`. A recommendation that only stores ref strings, or whose details do not cover every referenced evidence id, is blocked before ordinary approval with `AI 建议缺少可展示的证据详情`. This closes the gap between AI evidence validation, recommendation persistence, and the UI "为什么这么判断" evidence cards. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/main/recommendation-approval-policy.test.ts` and `pnpm exec vitest run apps/desktop/src/main/ad-recommendation-ai-context.test.ts apps/desktop/src/main/recommendation-approval-policy.test.ts apps/desktop/src/main/ad-ai-evidence-validator.test.ts`.
- 2026-06-17 approval UI evidence-detail alignment: the `审批中心` page now mirrors the backend approval policy for AI evidence details. AI-sourced recommendations that have `aiEvidenceRefs` but no matching displayable `aiEvidenceDetails` show `AI 建议缺少可展示的证据详情` in the queue/detail blocker and cannot be approved from the UI. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/renderer/pages/approval-page.test.tsx apps/desktop/src/main/recommendation-approval-policy.test.ts apps/desktop/src/main/ad-ai-evidence-validator.test.ts` and `pnpm --filter @amazon-ai-ops/desktop run typecheck`.
- 2026-06-17 recommendation evidence-summary alignment: the shared recommendation evidence summary now treats any AI evidence ref without a matching displayable evidence detail as `证据缺失` / blocked, even when the rule/AI decision agreement is `aligned`. This prevents `优化建议` and `审批中心` from showing a missing-detail AI recommendation as a ready formal recommendation before the backend approval policy rejects it. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/renderer/evidence-display.test.ts apps/desktop/src/renderer/pages/recommendations-page.test.ts apps/desktop/src/renderer/pages/approval-page.test.tsx apps/desktop/src/main/recommendation-approval-policy.test.ts` and `pnpm --filter @amazon-ai-ops/desktop run typecheck`.
- 2026-06-17 recommendations formal-approval gate alignment: the `优化建议` page now includes unresolved AI evidence details in its own formal-approval gate, evidence-blocked count, and `送审前证据检查`. A recommendation with `aiEvidenceRefs` but no matching displayable `aiEvidenceDetails` no longer appears as formally approvable on the recommendation page before the `审批中心` or backend policy rejects it. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/renderer/pages/recommendations-page.test.ts`, `pnpm exec vitest run apps/desktop/src/renderer/pages/recommendations-page.test.ts apps/desktop/src/renderer/evidence-display.test.ts apps/desktop/src/renderer/pages/approval-page.test.tsx`, and `pnpm --filter @amazon-ai-ops/desktop run typecheck`.
- 2026-06-17 dashboard/delivery AI evidence readiness alignment: the dashboard delivery matrix no longer treats any successful `source=ai` diagnosis run as proof that the AI evidence chain is ready. A persisted diagnosis run must now have lifecycle-stage evidence refs and every ref must resolve to a displayable `evidencePackPreview` item, with no lifecycle review flag, before it increments the AI evidence success count. The renderer type for `AiDiagnosisRunView` now includes `lifecycleStageEvidenceRefs`, matching the main-process persisted diagnosis snapshot. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/renderer/delivery-readiness-matrix.test.ts`, `pnpm exec vitest run apps/desktop/src/renderer/delivery-readiness-matrix.test.ts apps/desktop/src/renderer/evidence-display.test.ts apps/desktop/src/renderer/pages/recommendations-page.test.ts`, and `pnpm --filter @amazon-ai-ops/desktop run typecheck`.
- 2026-06-17 ad-quant lifecycle evidence-detail alignment: the shared ad-quant diagnosis summary now treats AI lifecycle-stage evidence refs without matching displayable evidence details as `证据缺失` / blocked. The `广告量化` page no longer shows `AI 阶段诊断` as ready when the stage judgment cannot display the referenced evidence cards. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/renderer/evidence-display.test.ts apps/desktop/src/renderer/pages/ad-quant-page.test.tsx apps/desktop/src/renderer/pages/recommendations-page.test.ts` and `pnpm --filter @amazon-ai-ops/desktop run typecheck`.
- 2026-06-17 ad-quant dynamic-threshold evidence-detail alignment: the `广告量化` page now treats AI dynamic-threshold suggestions whose `evidenceRefs` cannot be resolved into displayable evidence details as review-required. Threshold cards show the missing detail reason in the same evidence line and state that human review is required before any rule-threshold override. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/renderer/pages/ad-quant-page.test.tsx apps/desktop/src/renderer/evidence-display.test.ts apps/desktop/src/renderer/pages/recommendations-page.test.ts` and `pnpm --filter @amazon-ai-ops/desktop run typecheck`.
- 2026-06-17 recommendation/approval threshold-review display alignment: the `优化建议` and `审批中心` AI dynamic-threshold summaries now append `需复核` plus validator review reasons when any threshold suggestion has `requiresReview`. Operators no longer see only the AI threshold value while missing the fact that the threshold cannot override rules without human review. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/renderer/pages/recommendations-page.test.ts apps/desktop/src/renderer/pages/approval-page.test.tsx`, `pnpm exec vitest run apps/desktop/src/renderer/pages/recommendations-page.test.ts apps/desktop/src/renderer/pages/approval-page.test.tsx apps/desktop/src/renderer/pages/ad-quant-page.test.tsx apps/desktop/src/renderer/evidence-display.test.ts`, and `pnpm --filter @amazon-ai-ops/desktop run typecheck`.
- 2026-06-17 AI lifecycle-review approval gate alignment: backend approval policy, the `审批中心` blocker list, and the `优化建议` formal-approval gate now block ordinary approval when `aiLifecycleStageRequiresReview=true`. This prevents aligned rule+AI recommendations from bypassing review when the AI product-stage judgment itself has missing or invalid evidence. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/main/recommendation-approval-policy.test.ts apps/desktop/src/renderer/pages/approval-page.test.tsx apps/desktop/src/renderer/pages/recommendations-page.test.ts`, `pnpm exec vitest run apps/desktop/src/main/ad-recommendation-ai-context.test.ts apps/desktop/src/main/recommendation-approval-policy.test.ts apps/desktop/src/renderer/evidence-display.test.ts apps/desktop/src/renderer/pages/recommendations-page.test.ts apps/desktop/src/renderer/pages/approval-page.test.tsx`, and `pnpm --filter @amazon-ai-ops/desktop run typecheck`.
- 2026-06-17 AI lifecycle-review recommendation status alignment: recommendation annotation now writes `status=needs_review` when the AI lifecycle-stage diagnosis requires review, even if the rule/AI action decision itself is aligned. This keeps DB status, recommendation-page counts, approval queue behavior, and backend approval blockers on the same review boundary. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/main/ad-recommendation-ai-context.test.ts apps/desktop/src/main/recommendation-approval-policy.test.ts apps/desktop/src/renderer/pages/recommendations-page.test.ts apps/desktop/src/renderer/pages/approval-page.test.tsx` and `pnpm --filter @amazon-ai-ops/desktop run typecheck`.
- 2026-06-17 readback AI-threshold metadata preservation: the `执行回读` recommendation loader now keeps the full AI dynamic-threshold metadata from approved recommendations, including `evidenceRefs`, `requiresReview`, and `reviewReasons`, instead of typing the local form as value/reason only. This prevents the manual readback/export stage from erasing upstream AI-threshold review context. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/renderer/pages/readback-page.test.tsx`, `pnpm exec vitest run apps/desktop/src/renderer/pages/recommendations-page.test.ts apps/desktop/src/renderer/pages/approval-page.test.tsx apps/desktop/src/renderer/pages/readback-page.test.tsx apps/desktop/src/main/ad-readback-evidence.test.ts`, and `pnpm --filter @amazon-ai-ops/desktop run typecheck`.
- 2026-06-17 delivery recommendation-status alignment: the `交付验收` page now reads the `needs_review` recommendation queue in addition to `pending` and `approved`. The delivery readiness matrix only marks `建议与审批` as `可审批` when there are formal pending or approved recommendations; review-only recommendations show `需复核` and route operators to the approval center without counting them as directly approvable. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/renderer/delivery-readiness-matrix.test.ts`, `pnpm exec vitest run apps/desktop/src/renderer/delivery-readiness-matrix.test.ts apps/desktop/src/main/ad-recommendation-ai-context.test.ts apps/desktop/src/main/recommendation-approval-policy.test.ts apps/desktop/src/renderer/pages/recommendations-page.test.ts apps/desktop/src/renderer/pages/approval-page.test.tsx`, and `pnpm --filter @amazon-ai-ops/desktop run typecheck`.
- 2026-06-17 recommendation-page review-queue visibility: the `优化建议` page now loads both `pending` and `needs_review` recommendations into the visible suggestion pool, while still counting formal approval only through the stricter approval gate. Review-only AI/规则冲突、AI-only, lifecycle-review, or evidence-blocked suggestions no longer disappear from the page after generation just because they are not ordinary `pending` rows. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/renderer/pages/recommendations-page.test.ts`, `pnpm exec vitest run apps/desktop/src/renderer/pages/recommendations-page.test.ts apps/desktop/src/renderer/pages/approval-page.test.tsx apps/desktop/src/renderer/delivery-readiness-matrix.test.ts apps/desktop/src/main/ad-recommendation-ai-context.test.ts apps/desktop/src/main/recommendation-approval-policy.test.ts`, and `pnpm --filter @amazon-ai-ops/desktop run typecheck`.
- 2026-06-17 current business UI smoke routing: the legacy package command `pnpm run smoke:v15-product-readiness-ui` now aliases `pnpm run smoke:business-ui-current` instead of directly running the historical `scripts/smoke-v15-product-readiness-ui.js` APP_READY-oriented smoke. The new `scripts/smoke-current-business-ui.js` runs the current business UI smoke suite (`shell`, `data-pipeline`, `ad-execution`, `keyword-listing`, and `settings-delivery`) and writes a summary JSON under `output/codex-evidence`. Incremental checks passed: `pnpm exec vitest run scripts/package-scripts.test.mjs` and `node --check scripts\smoke-current-business-ui.js`.
- 2026-06-17 data-collection action clarity: the data-collection page now separates the current scope's existing real report/DB status from the current button action's actual output. If a user clicks `下载并导入已创建报表` and no new `.xlsx/.xls/.csv` is produced, the action notice no longer says collection completed just because older scope data exists; it explicitly says this action produced no new real report file. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/renderer/pages/data-collection-page.test.ts scripts/package-scripts.test.mjs` and `node --check scripts\smoke-business-ui-data-pipeline.js`.
- 2026-06-17 READY safety UI evidence alignment: `verify:v15-ready-safety` now accepts current business UI smoke summaries without requiring raw `APP_READY` text in operator UI evidence, while still requiring manifest-driven final readiness, README APP_READY at the release layer, real ad readback selection, and delivery-bundle/data-reconciliation agreement. `export:v15-delivery-bundle` now collects `current-business-ui-smoke-*` and the five current business smoke JSON artifacts before falling back to historical `v15-product-readiness-ui-smoke-*`. Incremental checks passed: `pnpm exec vitest run scripts/verify-v15-ready-safety.test.mjs scripts/package-scripts.test.mjs`, `node --check scripts\verify-v15-ready-safety.js`, and `node --check scripts\export-v15-delivery-bundle.js`.
- 2026-06-17 current business UI smoke refresh: after rebuilding the renderer, `pnpm run smoke:business-ui-current` passed across the current business shell, data pipeline, ad execution, keyword/listing, and settings/delivery smoke scripts. Latest summary evidence: `output\codex-evidence\current-business-ui-smoke-1781670002570.json`; page-level evidence: `business-ui-shell-smoke-1781669973185.json`, `business-ui-data-pipeline-smoke-1781669976889.json`, `business-ui-ad-execution-smoke-1781669984273.json`, `business-ui-keyword-listing-smoke-1781669991104.json`, and `business-ui-settings-delivery-smoke-1781669995673.json`. During this run, ad-execution smoke was updated to expect explicit AI fallback status (`AI 已回落规则：AI 服务超时`) and keyword/listing smoke was updated to expect AI-vs-fallback draft result wording (`已生成 1 条 AI 草案`), matching the current operator-facing UI.
- 2026-06-17 Listing read-status UX hardening: the Listing optimization page now distinguishes `未读取`, `已探测未解析`, `已读取部分内容`, `待核对 ASIN`, `待核对范围`, and `完整读取` instead of showing `未读取` after a read attempt produced evidence. Field-level missing content is labeled as `缺失`, while `未读取` is reserved for the initial no-action state. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/renderer/listing-workflow-summary.test.ts`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, `pnpm --filter @amazon-ai-ops/desktop run build:renderer`, and `node scripts/smoke-business-ui-keyword-listing.js` with evidence `output\codex-evidence\business-ui-keyword-listing-smoke-1781666078846.json`.
- 2026-06-17 renderer update-event bridge hardening: preload now forwards main-process `business-ui:data-updated` IPC events to renderer DOM listeners, so settings save/test-AI, recommendation generation, and other main-process updates can refresh pages that already listen with `window.addEventListener('business-ui:data-updated', ...)`. This closes a stale-status class of bugs where the main process sent an update but renderer business pages did not receive it. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/preload/index.test.ts`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, and `pnpm --filter @amazon-ai-ops/desktop run build:preload`.
- 2026-06-17 AI recommendation no-action UX verification: the current `优化建议` UI already covers the case where AI runs diagnosis but produces no formal recommendation. The ad-execution smoke verifies visible reason distribution, `AI 诊断已完成，但未形成正式建议`, `洞察未采纳`, `最终可审批 0`, recovery steps to review ad quantification/product context/operation events, AI no-output fallback, formal approval blocking for missing AI evidence refs, and data-gate locking when real reports/DB metrics are missing. Incremental evidence: `node scripts/smoke-business-ui-ad-execution.js` passed with `output\codex-evidence\business-ui-ad-execution-smoke-1781666672642.json`.
- 2026-06-17 AI call-log audit coverage pass: ad action explanation calls now write `ai_call_logs` entries through `ad_action_reason_v1`, with a stable input hash, schema version, source-file/evidence-ref summary, redacted output JSON, and fallback error message when AI explanation falls back to rules. This closes the previous audit gap where strategy diagnosis and Listing draft calls were logged but per-recommendation AI explanations were not. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/main/ad-action-ai-call-log.test.ts packages/local-db/src/sqlite/repositories/ai-call-log-repo.test.ts`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, and `pnpm --filter @amazon-ai-ops/desktop run build:main`.
- 2026-06-17 AI call-log summary redaction hardening: `AiCallLogRepository` now applies the same `sk-...` redaction to `evidence_pack_summary_json` as it already applied to wrapped output JSON and error messages. This prevents abnormal diagnostic/evidence summary fields from leaking API-key-shaped secrets into local audit logs. Incremental checks passed: `pnpm exec vitest run packages/local-db/src/sqlite/repositories/ai-call-log-repo.test.ts` and `pnpm --filter @amazon-ai-ops/local-db run typecheck`.
- 2026-06-17 AI call-log bearer/API-key redaction hardening: `AiCallLogRepository` now also redacts bearer-token strings plus `authorization`, `deepseek_api_key`, `api_key`, and `apiKey`-style fields across wrapped output JSON, error messages, and `evidence_pack_summary_json`. This closes the remaining local audit-log leak path if provider errors or abnormal AI output echo request headers or API-key fields. Incremental checks passed: `pnpm exec vitest run packages/local-db/src/sqlite/repositories/ai-call-log-repo.test.ts`, `pnpm exec vitest run apps/desktop/src/main/ad-action-ai-call-log.test.ts apps/desktop/src/main/listing-ai-draft.test.ts packages/local-db/src/sqlite/repositories/ai-call-log-repo.test.ts`, and `pnpm --filter @amazon-ai-ops/local-db run typecheck`.
- 2026-06-17 AI diagnosis-run snapshot redaction hardening: `ai_diagnosis_runs` now uses the shared local DB secret-redaction helper before writing scope JSON, evidence-pack summary, evidence-pack preview, diagnosis JSON, insight JSON, and error messages. This closes the bypass where strategy diagnosis snapshots could persist provider echoes or abnormal AI output even though `ai_call_logs` was redacted. Incremental checks passed: `pnpm exec vitest run packages/local-db/src/sqlite/repositories/ai-diagnosis-run-repo.test.ts packages/local-db/src/sqlite/repositories/ai-call-log-repo.test.ts` and `pnpm --filter @amazon-ai-ops/local-db run typecheck`.
- 2026-06-17 AI audit UI wording pass: the settings `AI 调用审计` panel now labels evidence counts by call type instead of using one generic `证据 N` label. Strategy diagnosis shows `证据包 N 条`, ad action explanation shows `源文件/证据引用 N 条`, and Listing rewrite shows `草案证据 N 条`, making it clearer what the AI call actually used. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/renderer/ai-call-diagnostics.test.ts`, `pnpm --filter @amazon-ai-ops/desktop run build:renderer`, and `node scripts/smoke-business-ui-settings-delivery.js` with evidence `output\codex-evidence\business-ui-settings-delivery-smoke-1781667652528.json`.
- 2026-06-17 AI settings hidden-key normalization pass: AI settings sanitization, save normalization, and test normalization were extracted into `apps/desktop/src/main/ai-settings-normalization.ts` with direct regression coverage. The tests prove the renderer never receives the stored API key, saving persona/output settings from a hidden-key UI state preserves the saved key and previous available test status when provider/model still match, `settings:test-ai` can use the saved key even when the renderer passes an empty `aiApiKey` plus `aiKeyConfigured=true`, and the stored key is cleared only through an explicit `clearAiKey` request. The settings page now exposes `清除本地 AI Key`, so blank API Key input means "keep the hidden saved key" while the explicit clear action removes the key and resets AI test state. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/main/ai-settings-normalization.test.ts`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, `pnpm --filter @amazon-ai-ops/desktop run build:main`, `pnpm --filter @amazon-ai-ops/desktop run build:renderer`, `node --check scripts\smoke-business-ui-settings-delivery.js`, and `node scripts/smoke-business-ui-settings-delivery.js` with evidence `output\codex-evidence\business-ui-settings-delivery-smoke-1781669191490.json`.
- 2026-06-17 data collection action-error split pass: the data-collection page now exposes `collectionActionError` to regression tests and maps Chinese missing-report/missing-batch errors to action-specific operator guidance. `下载并导入已创建报表` now explicitly says it only looks for current-scope ready rows and will not create a new task, while `重新创建、下载并导入` keeps a separate "created but no real spreadsheet arrived" recovery path. This prevents different buttons from collapsing into the same generic "缺少可用文件或数据批次" message when Lingxing has no matching downloadable row. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/renderer/pages/data-collection-page.test.ts` and `pnpm --filter @amazon-ai-ops/desktop run typecheck`.
- 2026-06-17 recommendation AI status clarity pass: the recommendations page now distinguishes AI fallback and insight-only output instead of collapsing both into the vague `AI 无可用输出` label. When strategy diagnosis falls back because of schema/language/provider validation, the status shows `AI 已回落规则：<原因>`; when AI returned only invalid or unadopted candidates, it shows `AI 仅作洞察`. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/renderer/pages/recommendations-page.test.ts` and `pnpm --filter @amazon-ai-ops/desktop run typecheck`.
- 2026-06-17 Listing draft result clarity pass: Listing draft generation now reports how many local drafts came from AI versus rule fallback and includes the fallback reason when the provider returned malformed or incomplete output. This avoids a misleading generic "已生成 Listing 草案" message when AI did not actually produce usable drafts. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/renderer/pages/listing-optimization-page.test.ts` and `pnpm --filter @amazon-ai-ops/desktop run typecheck`.
- 2026-06-17 recommendation source-trace gate hardening: the formal recommendation gate now requires every loaded daily metric row to have both `source_file` and positive `source_row`, not merely "some source files exist for the current scope." This prevents AI+rule recommendations from being generated from partially untraceable DB rows. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/main/recommendation-generation-gate.test.ts` and `pnpm --filter @amazon-ai-ops/desktop run typecheck`.
- 2026-06-17 AI-only recommendation evidence-row binding pass: AI-only formal recommendations now bind to the metric row referenced by the AI candidate's metric evidence before falling back to entity-name matching. This prevents the same search term or target in multiple campaigns/ad groups from being attached to the wrong ASIN, campaign, ad group, CPC, source row, or source file. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/main/ad-recommendation-ai-context.test.ts` and `pnpm --filter @amazon-ai-ops/desktop run typecheck`.
- 2026-06-17 executable recommendation value gate: approval policy and the approval page now reject non-executable bid or budget values before normal approval. Bid actions must use a positive numeric amount, not a relative percentage such as `-10%`; `lower_bid` must be below the current bid and `raise_bid` must be above it. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/renderer/pages/approval-page.test.tsx apps/desktop/src/main/recommendation-approval-policy.test.ts` and `pnpm --filter @amazon-ai-ops/desktop run typecheck`.
- 2026-06-17 aligned AI/rule executable-value preservation: when AI and rules agree on an action but AI returns only a relative value such as `-12%`, the recommendation annotation now preserves the rule engine's executable amount (for example `1.08`) instead of overwriting it with a non-executable percentage. This keeps formal recommendations approval-ready while still retaining AI reasons/evidence. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/main/ad-recommendation-ai-context.test.ts apps/desktop/src/main/recommendation-approval-policy.test.ts` and `pnpm --filter @amazon-ai-ops/desktop run typecheck`.
- 2026-06-17 approval source-file all-real gate: backend approval policy now requires every `evidence.sourceFiles` entry to be a real `.xlsx/.xls/.csv` report, matching the frontend approval page and readback evidence contract. A mixed list such as one real report plus `acceptance-audit.json` is blocked instead of passing because at least one spreadsheet exists. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/main/recommendation-approval-policy.test.ts apps/desktop/src/renderer/pages/approval-page.test.tsx` and `pnpm --filter @amazon-ai-ops/desktop run typecheck`.
- 2026-06-17 ad action explanation schema pass: `AdActionReasonExplainer` now requires AI responses to include `schemaVersion: ad_action_reason_v1` and falls back to rule explanation when the schema is missing or mismatched. The prompt, AI result, and fallback result now use the same schema version, keeping per-recommendation explanations aligned with the broader structured-JSON AI contract. Incremental checks passed: `pnpm exec vitest run packages/ai-adapter/src/ad-action-reason.test.ts`, `pnpm --filter @amazon-ai-ops/ai-adapter run typecheck`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, and `pnpm --filter @amazon-ai-ops/desktop run build:main`.
- 2026-06-17 AI insight approval-blocking regression pass: frontend approval logic now has direct regression coverage for `AI-only`, `AI 洞察未进入正式建议池`, and AI-sourced recommendations with no `aiEvidenceRefs`. This aligns the visible approval page with the backend `assertRecommendationApprovalPolicy` gate and the validator/context split where invalid AI candidates remain insight-only rather than entering formal recommendations. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/renderer/pages/approval-page.test.tsx`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, and `pnpm exec vitest run apps/desktop/src/main/recommendation-approval-policy.test.ts apps/desktop/src/main/ad-ai-evidence-validator.test.ts apps/desktop/src/main/ad-recommendation-ai-context.test.ts`.
- 2026-06-17 ad quant AI evidence wording pass: the `广告量化` page now labels historical diagnosis evidence as `证据包 N 条` in the `最近 AI 诊断记录` panel, matching the evidence-pack terminology used by the AI audit panel and avoiding a vague `证据 N` label. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/renderer/pages/ad-quant-page.test.tsx`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, `pnpm --filter @amazon-ai-ops/desktop run build:renderer`, and `node scripts/smoke-business-ui-data-pipeline.js` with evidence `output\codex-evidence\business-ui-data-pipeline-smoke-1781668011249.json`.
- 2026-06-17 current business UI aggregate smoke refresh: `pnpm run smoke:business-ui-current` passed after the AI audit wording, structured ad-action schema, approval-blocking, and ad-quant evidence wording increments. Latest summary evidence: `output\codex-evidence\current-business-ui-smoke-1781668142846.json`; page-level evidence: `business-ui-shell-smoke-1781668106679.json`, `business-ui-data-pipeline-smoke-1781668113295.json`, `business-ui-ad-execution-smoke-1781668123388.json`, `business-ui-keyword-listing-smoke-1781668133183.json`, and `business-ui-settings-delivery-smoke-1781668137091.json`. This is current renderer/operator-flow evidence only; final delivery still requires full test/typecheck, Windows packaging, packaged smoke, hashes, and manifest refresh.
- 2026-06-17 non-ready safety recheck after docs: `pnpm run verify:v15-non-ready-safety` passed after the latest evidence-documentation update. The guard still treats historical manifest-driven `APP_READY` final-readiness and delivery-bundle evidence as baseline only, confirms historical real ad readback as baseline only, and verifies the README top-level delivery line remains non-ready.
- 2026-06-17 non-ready safety and bundle smoke refresh: `verify:v15-non-ready-safety` now treats historical manifest-driven `APP_READY` final-readiness and delivery-bundle evidence as baseline only when README explicitly says `DELIVERY: IN_PROGRESS`, instead of failing the non-ready guard just because old READY artifacts remain in `output`. Incremental checks passed: `pnpm exec vitest run scripts/verify-v15-non-ready-safety.test.mjs`, `pnpm run verify:v15-non-ready-safety`, and `node scripts\smoke-export-v15-delivery-bundle.js` with evidence `output\codex-evidence\export-v15-delivery-bundle-smoke-1781663815709.json`.
- 2026-06-16 real-data-chain evidence pass: B1/B2/B3 are now documented and verified. `数据采集` only counts `.xlsx/.xls/.csv` as real report files and keeps audit JSON/screenshots/DOM/manifest files out of the real-data count. `数据导入与校验` independently shows the current scope, real report coverage, imported metric rows, primary analysis grain, per-report file/import status, and import actions. Incremental verification passed: `node --check scripts\diagnose-real-ad-data-chain.js`, `node scripts\diagnose-real-ad-data-chain.js`, `node --check scripts\smoke-import-lingxing-batch-metrics.js`, and `node scripts\smoke-import-lingxing-batch-metrics.js`. Current real-data evidence is `output\codex-evidence\business-data-real-evidence-1781542255133.json`; import-smoke evidence is `output\codex-evidence\import-lingxing-batch-metrics-smoke-1781542262787.json`.
- 2026-06-16 integrated business-flow closeout and no-install build pass: keyword opportunities now split the same keyword by portfolio/campaign/ad group instead of merging opaque rows; Listing read from Lingxing now passes the expected ASIN into the main process before persistence; the dashboard first viewport now shows current data health, report coverage, imported row count, AI state, pending recommendation count, USD ad spend, USD sales/orders, and ACOS from the business data pipeline; shell smoke now blocks raw `APP_NEEDS_WORK`/technical status leakage in operator-facing navigation.
- Incremental checks for the 2026-06-16 closeout passed: `pnpm exec vitest run packages/keyword-opportunity/src/engine.test.ts`, `node scripts\smoke-business-ui-keyword-listing.js`, `pnpm exec vitest run apps/desktop/src/main/listing-lingxing-extractor.test.ts packages/listing-analyzer/src/draft.test.ts packages/listing-analyzer/src/export.test.ts`, `node scripts\smoke-business-ui-data-pipeline.js`, `node scripts\smoke-business-ui-shell.js`, `pnpm --filter @amazon-ai-ops/desktop run build:renderer`, and `pnpm --filter @amazon-ai-ops/desktop run typecheck` all passed during the increment.
- Current real-data diagnostic remains usable for the selected AppData scope: `node scripts\diagnose-real-ad-data-chain.js` passed against `C:\Users\wz\AppData\Roaming\@amazon-ai-ops\desktop\amazon-ai-ops.db`, batch `batch_20260612020905629_gkchz1`, with 8/8 real report files, 2416 imported metric rows, canonical `user_search_term` spend USD `617.87`, and 19 orders. Latest evidence: `output\codex-evidence\business-data-real-evidence-1781542255133.json`.
- 2026-06-16 final validation and no-install build pass: the obsolete `node scripts\run-tests.js` command is not present on current `master`; the current full-suite command is `pnpm test`. `pnpm test` passed with 50 test files / 240 passed / 2 skipped, `pnpm -r run typecheck` passed, and `pnpm --filter @amazon-ai-ops/desktop run build:win` passed. This is now historical baseline evidence because the current worktree has unbundled AI evidence-chain, UI, and readback-contract changes. The historical no-install portable executable was `apps\desktop\release\AmazonAIOpsAgent-1.5.0-portable.exe`, SHA-256 `71E82D4752EC2BE14C60CF34A405BB844929EFAB106BB68A38CEB412B6CBA913`, size `89643993` bytes, last write `2026-06-16 00:55:12`. The historical installer was `apps\desktop\release\AmazonAIOpsAgent-1.5.0.exe`, SHA-256 `49CB66AB2356475B69988571CC1D3707586E293BCC013B09D3D4F1320BAD207E`, size `89809746` bytes, last write `2026-06-16 00:55:11`. Final delivery must regenerate installer/no-install paths, sizes, and SHA-256 from the current code.
- 2026-06-16 READY safety closeout: `pnpm run verify:v15-ready-safety` initially exposed that default evidence discovery could select newer smoke/guard `final-readiness-*.json` files instead of the real manifest-driven APP_READY evidence, and that the latest delivery bundle lacked current-scope data reconciliation. The default final-readiness selector now only picks date-named final evidence by default, and the APP_READY bundle was refreshed with explicit `final-readiness-2026-06-10.json` plus current batch reconciliation JSON/Markdown. New bundle: `output\delivery-bundles\v15-delivery-bundle-2026-06-15T17-00-08-661Z`. Final checks passed: `pnpm run verify:v15-ready-safety`, `node --check scripts\verify-v15-ready-safety.js`, `node --check scripts\export-v15-delivery-bundle.js`, and `node scripts\smoke-export-v15-delivery-bundle.js` with evidence `output\codex-evidence\export-v15-delivery-bundle-smoke-1781542828364.json`.
- 2026-06-15 final release verification and no-install build pass: `pnpm test` passed with 37 test files / 193 passed / 2 skipped, `pnpm -r run typecheck` passed across workspace packages, and `pnpm --filter @amazon-ai-ops/desktop run build:win` passed. The no-install portable executable is `apps\desktop\release\AmazonAIOpsAgent-1.5.0-portable.exe`, SHA-256 `540AFF932C68A8A887B4F45CA6A645FC23C0A4CCD05D962DF5409013F65A9A2A`, size `89552301` bytes, last write `2026-06-15 12:53:59`. The installer is `apps\desktop\release\AmazonAIOpsAgent-1.5.0.exe`, SHA-256 `A615069F1575DAF8978ABD6B7C3C99B0A1123ABB25AB62BBAE7D51E081DB1136`, size `89718055` bytes, last write `2026-06-15 12:53:56`. `release\win-unpacked\AmazonAIOpsAgent.exe` remained alive after an 8-second packaged smoke check and no `AmazonAIOpsAgent` process remained after stop. The local Node 24 test environment first hit a `better-sqlite3` native ABI mismatch from the Electron rebuild; it was repaired by rebuilding the generated local native module with Python 3.11 and VS `v143` toolset before rerunning the full suite.
- 2026-06-15 audited-reject decision pass: rejecting an optimization recommendation now requires a handler name and rejection reason before the IPC call is made. The rejected decision payload includes rejectedBy, note, current scope, batch id, target context, values, and source files, matching the approval metadata path. The approval page also now preserves approve/reject success messages after refreshing the queue, so operators get visible feedback instead of the message being cleared immediately.
- Incremental checks for the audited-reject pass: `pnpm --filter @amazon-ai-ops/desktop run typecheck` passed, `pnpm --filter @amazon-ai-ops/desktop run build:renderer` passed, and `node scripts\smoke-business-ui-ad-execution.js` passed with evidence `output\codex-evidence\business-ui-ad-execution-smoke-1781495762492.json`. No full test suite was run in this increment.
- 2026-06-15 readback-evidence source markdown pass: ad readback evidence now persists `approval.note` and the generated Markdown exposes source recommendation id, source batch, source metric date, source report files, and source explanation provider/model. The readback form carries approval note from the approval decision into export, and the unit test now locks these fields so future evidence changes cannot silently drop the audit chain.
- Incremental checks for the readback-evidence source markdown pass: `pnpm exec vitest run apps/desktop/src/main/ad-readback-evidence.test.ts` passed, `pnpm --filter @amazon-ai-ops/desktop run typecheck` passed, `pnpm --filter @amazon-ai-ops/desktop run build:main` passed, `pnpm --filter @amazon-ai-ops/desktop run build:renderer` passed, and `node scripts\smoke-business-ui-ad-execution.js` passed with evidence `output\codex-evidence\business-ui-ad-execution-smoke-1781495509039.json`. No full test suite was run in this increment.
- 2026-06-15 approval-decision persistence pass: the `审批中心` no longer drops approver and note inputs. Approve/reject IPC calls now send a decision payload containing approver/rejector, note, decision time, current scope, batch id, action target, current/recommended values, and source files. The main process remains backward-compatible with numeric ids but now writes the decision payload into recommendation `evidence.approvalDecision` while updating status. `执行回读` now carries approved-by and decided-at values into the readback form when loading an approved action.
- Incremental checks for the approval-decision persistence pass: `pnpm --filter @amazon-ai-ops/desktop run typecheck` passed, `pnpm --filter @amazon-ai-ops/desktop run build:main` passed, `pnpm --filter @amazon-ai-ops/desktop run build:preload` passed, `pnpm --filter @amazon-ai-ops/desktop run build:renderer` passed, and `node scripts\smoke-business-ui-ad-execution.js` passed with evidence `output\codex-evidence\business-ui-ad-execution-smoke-1781495313076.json`. No full test suite was run in this increment.
- 2026-06-15 readback source-audit binding pass: the `执行回读` form now carries recommendation source audit fields into exported evidence: source batch id, metric date, source report files, explanation source, and AI model. The main-process ad readback evidence builder now persists these fields under `source`, so a readback JSON can be traced back to the exact recommendation/data batch instead of only showing recommendation id and current/recommended values.
- Incremental checks for the readback source-audit binding pass: `pnpm --filter @amazon-ai-ops/desktop run typecheck` passed, `pnpm --filter @amazon-ai-ops/desktop run build:main` passed, `pnpm --filter @amazon-ai-ops/desktop run build:renderer` passed, and `node scripts\smoke-business-ui-ad-execution.js` passed with evidence `output\codex-evidence\business-ui-ad-execution-smoke-1781494975315.json`. No full test suite was run in this increment.
- 2026-06-15 manual-batch precedence pass: downstream pages now respect a manually specified global data batch before falling back to the latest auto-matched batch. `优化建议`, `审批中心`, `执行回读`, and `关键词机会` now use `scope.batchId || latestBatch.id`, and keyword source-batch display also shows the manual batch when one is active. This prevents a user-selected historical/current batch from being silently overridden by the pipeline's latest matched batch.
- Incremental checks for the manual-batch precedence pass: `pnpm --filter @amazon-ai-ops/desktop run typecheck` passed, `pnpm --filter @amazon-ai-ops/desktop run build:renderer` passed, `node scripts\smoke-business-ui-ad-execution.js` passed with evidence `output\codex-evidence\business-ui-ad-execution-smoke-1781494746979.json`, and `node scripts\smoke-business-ui-keyword-listing.js` passed with evidence `output\codex-evidence\business-ui-keyword-listing-smoke-1781494747025.json`. No full test suite was run in this increment.
- 2026-06-15 responsive action-toolbar pass: the new report-selection toolbar and scheduler confirmation panel now collapse from `content + actions` columns into a single-column layout on narrower windows, and their action buttons become full-width on small screens. This prevents operator action controls from squeezing or overflowing in compact desktop windows.
- Incremental checks for the responsive toolbar pass: `pnpm --filter @amazon-ai-ops/desktop run build:renderer` passed and `node scripts\smoke-business-ui-data-pipeline.js` passed with evidence `output\codex-evidence\business-ui-data-pipeline-smoke-1781494319875.json`. No full test suite was run in this increment.
- 2026-06-15 global-scope normalization pass: the global operation range editor now trims copied/pasted whitespace from date, store, marketplace, ASIN, and manual batch id before saving. Validation and downstream data requests use the normalized values, preventing invisible spaces from causing batch/data mismatches.
- Incremental checks for the global-scope normalization pass: `pnpm --filter @amazon-ai-ops/desktop run typecheck` passed, `pnpm --filter @amazon-ai-ops/desktop run build:renderer` passed, and `node scripts\smoke-business-ui-settings-delivery.js` passed with evidence `output\codex-evidence\business-ui-settings-delivery-smoke-1781494255453.json`. No full test suite was run in this increment.
- 2026-06-15 report-selection control pass: the `数据采集` page now has an explicit report-selection toolbar with selected count, `全选 8 类`, `只选缺失报表`, `只选未导入`, and `清空`. The previous selection initialization used `selectedTypes.length === 0`, which meant a user-cleared selection could immediately be auto-restored to all reports. Selection is now initialized when the report set changes, while an intentional clear stays cleared and disables selected-report download/recreate actions.
- Incremental checks for the report-selection pass: `pnpm --filter @amazon-ai-ops/desktop run typecheck` passed, `pnpm --filter @amazon-ai-ops/desktop run build:renderer` passed, and `node scripts\smoke-business-ui-data-pipeline.js` passed with evidence `output\codex-evidence\business-ui-data-pipeline-smoke-1781494161866.json`. No full test suite was run in this increment.
- 2026-06-15 global-scope validation pass: the global operation range editor now blocks invalid ranges before they affect the rest of the app. It requires both dates, blocks a start date later than the end date, and requires store and marketplace before saving. This prevents the backend from showing confusing empty states caused by a malformed global range.
- Incremental checks for the global-scope validation pass: `pnpm --filter @amazon-ai-ops/desktop run typecheck` passed, `pnpm --filter @amazon-ai-ops/desktop run build:renderer` passed, and `node scripts\smoke-business-ui-settings-delivery.js` passed with evidence `output\codex-evidence\business-ui-settings-delivery-smoke-1781493916733.json`. No full test suite was run in this increment.
- 2026-06-15 global-scope stale-batch prevention pass: editing the global operation range now clears a previously selected manual batch when the operator changes start date, end date, store, or marketplace. This prevents a misleading state where the top bar appears to use a new range while downstream pages still read an old fixed batch. The range editor now states that changing those fields clears the old batch and that operators must re-enter a batch id if they intentionally want a fixed historical batch.
- Incremental checks for the stale-batch prevention pass: `pnpm --filter @amazon-ai-ops/desktop run typecheck` passed, `pnpm --filter @amazon-ai-ops/desktop run build:renderer` passed, and `node scripts\smoke-business-ui-settings-delivery.js` passed with evidence `output\codex-evidence\business-ui-settings-delivery-smoke-1781493798870.json`. No full test suite was run in this increment.
- 2026-06-15 scheduler run-now confirmation pass: the `定时任务` page no longer triggers `立即执行` on the first click. Operators now get an inline confirmation panel that names the task, explains its purpose, and states that it will not approve recommendations, change bids, or write to Amazon Ads. This pass also fixed a feedback bug where the success message was immediately cleared by the task-list refresh, leaving users with no visible confirmation after a manual run.
- Incremental checks for the scheduler confirmation pass: `pnpm --filter @amazon-ai-ops/desktop run typecheck` passed, `pnpm --filter @amazon-ai-ops/desktop run build:renderer` passed, and `node scripts\smoke-business-ui-shell.js` passed with evidence `output\codex-evidence\business-ui-shell-smoke-1781493606711.json`. No full test suite was run in this increment.
- 2026-06-15 keyword/Listing workflow clarity pass: the keyword opportunities page now exposes the business handoff boundary instead of only showing table rows. It shows real ad report count, imported metric rows, covered ASIN count, source batch ids, and states that this page is an ad-data-to-Listing handoff pool, not a Listing reader or Amazon modifier. The Listing optimization page now preserves and displays the handoff payload from keyword opportunities: keyword source, ASIN, keyword count, AI-vs-rule draft source, handoff scope, and the local-only/non-submit boundary.
- Incremental checks for the keyword/Listing pass: `pnpm --filter @amazon-ai-ops/desktop run typecheck` passed, `pnpm --filter @amazon-ai-ops/desktop run build:renderer` passed, and `node scripts\smoke-business-ui-keyword-listing.js` passed with evidence `output\codex-evidence\business-ui-keyword-listing-smoke-1781488187406.json`. Screenshot evidence: `output\codex-evidence\business-ui-keyword-listing-keywords-1781488187406.png` and `output\codex-evidence\business-ui-keyword-listing-listing-1781488187406.png`.
- 2026-06-15 data-collection file-location clarity pass: the data collection page now separates real ad spreadsheet location, collection Manifest, and audit/diagnostic evidence location in a dedicated `文件位置与用途` panel. The UI explicitly states that xlsx/xls/csv files are the only inputs for quantification and that audit JSON/screenshots/HTML prove the process but are not ad data. Global disabled button styling was added so blocked primary actions no longer look clickable.
- Incremental checks for the data-collection pass: `pnpm --filter @amazon-ai-ops/desktop run typecheck` passed before the CSS-only disabled-state fix, `pnpm --filter @amazon-ai-ops/desktop run build:renderer` passed, and `node scripts\smoke-business-ui-data-pipeline.js` passed after the final renderer build with evidence `output\codex-evidence\business-ui-data-pipeline-smoke-1781488466153.json`. Screenshot evidence confirms the disabled import button is visually grey when no real reports exist: `output\codex-evidence\business-ui-data-pipeline-data-collection-1781488466153.png`.
- 2026-06-15 dashboard operations-cockpit pass: the dashboard now has an `运营摘要` panel that shows data-gate status, real spreadsheet/imported-row availability, current USD spend/sales/orders, ACOS/CVR/CPC, and the top diagnostic object. A new `首要风险对象` panel exposes portfolio/campaign/ad group/ASIN/object/spend/sales/orders/diagnosis with direct routing to ad quantification and recommendation generation. The dashboard imported-row display now prefers `collection.fileAudit.importedRowCount` over the narrower quant summary row count, fixing a mismatch where a successful import of 96 rows could display 12 rows in the cockpit.
- Incremental checks for the dashboard pass: `pnpm --filter @amazon-ai-ops/desktop run typecheck` passed, `pnpm --filter @amazon-ai-ops/desktop run build:renderer` passed, and `node scripts\smoke-business-ui-data-pipeline.js` passed with evidence `output\codex-evidence\business-ui-data-pipeline-smoke-1781488736725.json`. Screenshot evidence: empty dashboard `output\codex-evidence\business-ui-data-pipeline-dashboard-1781488736725.png`; after-import dashboard `output\codex-evidence\business-ui-data-pipeline-dashboard-after-import-1781488736725.png`.
- 2026-06-15 ad-quant prioritization pass: the ad quantification page now adds a `复核队列` before the diagnostic table. It ranks the first objects to review using no-order spend, high ACOS, spend, and clicks, and displays the reason such as `ACOS 高于 50%` or `有花费无订单`. The page explicitly states that this queue only decides review order; real ad actions still require recommendations, approval, and readback.
- Incremental checks for the ad-quant pass: `pnpm --filter @amazon-ai-ops/desktop run typecheck` passed, `pnpm --filter @amazon-ai-ops/desktop run build:renderer` passed, and `node scripts\smoke-business-ui-data-pipeline.js` passed with evidence `output\codex-evidence\business-ui-data-pipeline-smoke-1781488908907.json`. Screenshot evidence: after-import ad quantification `output\codex-evidence\business-ui-data-pipeline-ad-quant-after-import-1781488908907.png`.
- 2026-06-15 recommendation-flow separation pass: the `优化建议` page now exposes a `建议处理路径` panel before the recommendation context and table. It separates `生成解释`, `审批决策`, and `执行回读`, with routing to the approval center and readback page, and states that this page does not approve, execute ads, or write to Amazon. The recommendation generation range also now displays imported rows using `collection.fileAudit.importedRowCount` when available, matching the rest of the business UI data gate.
- Incremental checks for the recommendation-flow pass: `pnpm --filter @amazon-ai-ops/desktop run typecheck` passed, `pnpm --filter @amazon-ai-ops/desktop run build:renderer` passed, and `node scripts\smoke-business-ui-ad-execution.js` passed with evidence `output\codex-evidence\business-ui-ad-execution-smoke-1781489159883.json`. Screenshot evidence: `output\codex-evidence\business-ui-ad-execution-recommendations-1781489159883.png`.
- 2026-06-15 approval/readback clarity pass: the `审批中心` page now has an `审批处理要求` panel that separates page responsibility, approval precheck, post-approval next step, and current queue count. It states that approval only moves a recommendation into待执行 and that the approval artifact path, before/after screenshots, readback value, and live-row proof must be completed in `执行回读`. The `执行回读` page now changes its export action label based on precheck state: missing fields show `导出缺口草稿`, while complete fields show `导出读回证据`, with explicit text that drafts cannot be used as final execution evidence.
- Incremental checks for the approval/readback pass: `pnpm --filter @amazon-ai-ops/desktop run typecheck` passed, `pnpm --filter @amazon-ai-ops/desktop run build:renderer` passed, and `node scripts\smoke-business-ui-ad-execution.js` passed with evidence `output\codex-evidence\business-ui-ad-execution-smoke-1781489370906.json`. Screenshot evidence: `output\codex-evidence\business-ui-ad-execution-approval-1781489370906.png`, `output\codex-evidence\business-ui-ad-execution-readback-1781489370906.png`, and `output\codex-evidence\business-ui-ad-execution-readback-after-export-1781489370906.png`.
- 2026-06-15 settings/delivery clarity pass: the `设置` page now distinguishes a configured but untested AI key from a missing key. A redacted saved key shows `已配置，待测试` rather than `未配置`, while the actual connection test still controls `可用` or `失败`. The `交付验收` page now uses `collection.fileAudit.importedRowCount` for imported-row display and copied summaries when available, matching data collection, dashboard, and recommendations rather than a narrower quant summary count.
- Incremental checks for the settings/delivery pass: `pnpm --filter @amazon-ai-ops/desktop run typecheck` passed, `pnpm --filter @amazon-ai-ops/desktop run build:renderer` passed, and `node scripts\smoke-business-ui-settings-delivery.js` passed with evidence `output\codex-evidence\business-ui-settings-delivery-smoke-1781489753472.json`. Screenshot evidence: `output\codex-evidence\business-ui-settings-delivery-settings-1781489753472.png` and `output\codex-evidence\business-ui-settings-delivery-delivery-1781489753472.png`.
- 2026-06-15 global-scope consistency pass: the `当前运营范围` bar no longer sums imported rows across all candidate batches in automatic mode. It now shows the latest matched batch as the active data source, displays the active batch id as its own field, and states that changing the batch only changes which local real reports and imported metrics are read; it does not trigger a new Lingxing download. The keyword opportunities page now uses `collection.fileAudit.importedRowCount` for its source summary, matching dashboard, data collection, recommendations, and delivery rather than adding per-file rows.
- Incremental checks for the global-scope consistency pass: `pnpm --filter @amazon-ai-ops/desktop run build:renderer` passed, `node scripts\smoke-business-ui-shell.js` passed with evidence `output\codex-evidence\business-ui-shell-smoke-1781490229753.json`, and `node scripts\smoke-business-ui-keyword-listing.js` passed with evidence `output\codex-evidence\business-ui-keyword-listing-smoke-1781490229750.json`. No full test suite was run in this increment.
- 2026-06-15 ad-quant threshold consistency pass: the `广告量化` page no longer hard-codes `50%` high ACOS or a fixed efficiency line. It now reads `getRuleConfig`, displays the active target ACOS, high-risk ACOS, no-order click threshold, and minimum spend, and uses those thresholds for high-ACOS counts, no-order waste, review queue ordering, and priority reasons. If the rule API is unavailable, the page falls back to the same default risk settings used by settings/recommendations.
- Incremental checks for the ad-quant threshold pass: `pnpm --filter @amazon-ai-ops/desktop run typecheck` passed, `pnpm --filter @amazon-ai-ops/desktop run build:renderer` passed, and `node scripts\smoke-business-ui-data-pipeline.js` passed with evidence `output\codex-evidence\business-ui-data-pipeline-smoke-1781490474400.json`. No full test suite was run in this increment.
- 2026-06-15 delivery-scope consistency pass: the `交付验收` page now reads the shared global scope from `useScopeStore` instead of a fixed default scope. Changing the top `当前运营范围` date/store/site now causes delivery to request `getBusinessUiDataPipeline` with that edited scope, so delivery status, copied summaries, and visible range no longer drift away from the rest of the app.
- Incremental checks for the delivery-scope pass: `pnpm --filter @amazon-ai-ops/desktop run typecheck` passed, `pnpm --filter @amazon-ai-ops/desktop run build:renderer` passed, and `node scripts\smoke-business-ui-settings-delivery.js` passed with evidence `output\codex-evidence\business-ui-settings-delivery-smoke-1781490607914.json`. The smoke edits the global scope to `2026-06-02 ~ 2026-06-13 / FT-US-TEST / CA` and asserts the delivery page requests that same scope. No full test suite was run in this increment.
- 2026-06-15 data-collection visible-file pass: the `数据采集` page now surfaces a compact real-report list inside the upper `文件位置与用途` panel. Operators can see the real xlsx/xls/csv filenames, report labels, extensions, and imported rows without scrolling to the bottom table. Empty state copy now explicitly says that audit packages alone cannot be used for quantification. The top progress line also uses `collection.fileAudit.importedRowCount` when available, keeping its imported-row count aligned with the rest of the business UI.
- Incremental checks for the visible-file pass: `pnpm --filter @amazon-ai-ops/desktop run typecheck` passed, `pnpm --filter @amazon-ai-ops/desktop run build:renderer` passed, and `node scripts\smoke-business-ui-data-pipeline.js` passed with evidence `output\codex-evidence\business-ui-data-pipeline-smoke-1781490845665.json`. No full test suite was run in this increment.
- 2026-06-15 renderer-shell cleanup pass: `apps/desktop/src/renderer/App.tsx` was reduced to the active app shell, login page, route switch, and session handling. Dead legacy renderer code for the old `v1.5 工作台`, old inline dashboard/recommendations/settings/scheduler components, old command-wall strings, RMB-era metric cards, and the large unused inline `styles` object were removed from the active source file. This lowers maintenance risk and makes future audit searches meaningful.
- Incremental checks for the renderer-shell cleanup pass: `pnpm --filter @amazon-ai-ops/desktop run typecheck` passed, `pnpm --filter @amazon-ai-ops/desktop run build:renderer` passed, and `node scripts\smoke-business-ui-shell.js` passed with evidence `output\codex-evidence\business-ui-shell-smoke-1781491028122.json`. A focused source search now finds no old workbench/RMB/command-wall strings in `App.tsx`, components, or active pages; the only remaining `APP_READY` occurrence is the manifest-gated status in `交付验收`.
- 2026-06-15 dashboard action-queue pass: the `仪表盘` now reads the same configurable ad thresholds as `设置`, `广告量化`, and `优化建议` instead of judging high ACOS with a hard-coded 50% line. It adds an `行动队列` panel that routes operators to data collection when real reports are missing, to data import when metrics are missing, or to ad quantification/recommendation generation when metrics are available. The panel shows the active target ACOS, high-risk ACOS, no-order click threshold, and minimum spend so the first screen explains why an object is prioritized.
- Incremental checks for the dashboard action-queue pass: `pnpm --filter @amazon-ai-ops/desktop run typecheck` passed, `pnpm --filter @amazon-ai-ops/desktop run build:renderer` passed, and `node scripts\smoke-business-ui-data-pipeline.js` passed with evidence `output\codex-evidence\business-ui-data-pipeline-smoke-1781491212589.json`. No full test suite was run in this increment.
- 2026-06-15 scheduler safety-boundary pass: the `定时任务` page now labels `daily_report_download` as `每日广告报表下载` instead of leaking the raw task id. It also adds a `任务职责` panel and turns the automation safety boundary into structured operator-facing blocks: allowed automated work is report download/import/local suggestion generation, while automatic ad approval, bid changes, negative keywords, pauses, and Amazon Ads writes remain prohibited. Each task row now includes its purpose so enabling or running a task does not look like permission to execute ad changes.
- Incremental checks for the scheduler pass: `pnpm --filter @amazon-ai-ops/desktop run typecheck` passed, `pnpm --filter @amazon-ai-ops/desktop run build:renderer` passed, and `node scripts\smoke-business-ui-shell.js` passed with evidence `output\codex-evidence\business-ui-shell-smoke-1781491392733.json`. No full test suite was run in this increment.
- 2026-06-15 listing-asin-match pass: the `Listing 优化` page now treats the target ASIN as `keyword-opportunities` handoff ASIN first, then global scope ASIN. Listing draft generation is blocked when the current Lingxing page ASIN does not match that target ASIN, even if the global scope ASIN is empty. The read evidence display now combines detail-probe ASIN evidence with target-ASIN matching, and the current Listing panel shows `目标 ASIN` plus a clear `页面匹配` status before draft generation.
- Incremental checks for the listing ASIN match pass: `pnpm --filter @amazon-ai-ops/desktop run typecheck` passed, `pnpm --filter @amazon-ai-ops/desktop run build:renderer` passed, and `node scripts\smoke-business-ui-keyword-listing.js` passed with evidence `output\codex-evidence\business-ui-keyword-listing-smoke-1781491628792.json`. No full test suite was run in this increment.
- 2026-06-15 approval-context precheck pass: the `审批中心` queue now exposes portfolio and ASIN in the recommendation list, and the selected approval detail shows current/recommended value, source files, and an approval precheck. Approving a recommendation is now blocked if the target lacks store, marketplace, campaign, ad group, entity name, action type, current value, or recommended value, preventing incomplete recommendation context from moving into execution/readback.
- Incremental checks for the approval precheck pass: `pnpm --filter @amazon-ai-ops/desktop run typecheck` passed, `pnpm --filter @amazon-ai-ops/desktop run build:renderer` passed, and `node scripts\smoke-business-ui-ad-execution.js` passed with evidence `output\codex-evidence\business-ui-ad-execution-smoke-1781491901731.json`. No full test suite was run in this increment.
- 2026-06-15 delivery-evidence traceability pass: the `交付验收` page now shows a `最终证据清单` with each final-readiness gate, pass/fail state, message, and evidence path. It also states that `APP_READY` only applies to the evidence selected by the final manifest; changing date, store, site, ASIN, or batch requires regenerating current-scope evidence before claiming that new scope is delivered. Non-ready gate messages are display-sanitized so historical or malformed evidence text cannot leak `APP_READY` into a non-ready page.
- Incremental checks for the delivery evidence pass: `pnpm --filter @amazon-ai-ops/desktop run typecheck` passed, `pnpm --filter @amazon-ai-ops/desktop run build:renderer` passed, and `node scripts\smoke-business-ui-settings-delivery.js` passed with evidence `output\codex-evidence\business-ui-settings-delivery-smoke-1781492170297.json`. No full test suite was run in this increment.
- 2026-06-15 data-collection real-file count pass: the `数据采集` page no longer uses generic downloaded file-record count for the `本地真实报表已下载` metric. It now displays only `realReportFileCount`, so diagnostic JSON/HTML/screenshots or stale manifest records cannot make the UI show `8/8` unless real `.xlsx/.xls/.csv` report files exist. The data-pipeline smoke now mocks `downloadedFileRecords=8` with `realReportFileCount=0` and asserts the UI still shows `0/8`.
- Incremental checks for the real-file count pass: `pnpm --filter @amazon-ai-ops/desktop run typecheck` passed, `pnpm --filter @amazon-ai-ops/desktop run build:renderer` passed, and `node scripts\smoke-business-ui-data-pipeline.js` passed with evidence `output\codex-evidence\business-ui-data-pipeline-smoke-1781492272053.json`. No full test suite was run in this increment.
- 2026-06-15 listing-draft trust-boundary pass: the `Listing 优化` page started labeling draft output as local review material instead of implying every result is an AI recommendation. This historical wording was later replaced by the 2026-06-29 `关键词与本地草案工作台`, which uses `仅本地预览`, `待补齐真实广告数据`, and `本地规则参考` instead of placeholder or mixed-language fallback copy. Draft rows continue to show business section labels such as `标题`, `五点`, and `后台词` instead of internal enum values like `backend_terms`.
- Incremental checks for the Listing draft boundary pass: `pnpm --filter @amazon-ai-ops/desktop run typecheck` passed, `pnpm --filter @amazon-ai-ops/desktop run build:renderer` passed, and `node scripts\smoke-business-ui-keyword-listing.js` passed with evidence `output\codex-evidence\business-ui-keyword-listing-smoke-1781492441537.json`. No full test suite was run in this increment.
- 2026-06-15 stale-selection cleanup pass: the `优化建议` and `审批中心` pages now clear their selected detail panels when a list reload no longer contains the selected recommendation, and also clear selection when no current batch exists. This prevents stale recommendation details from a previous scope/batch from remaining visible after the operator changes filters or refreshes data. The ad-execution smoke now opens a recommendation detail, forces the next recommendation refresh to return an empty list, and asserts the old source file path is removed from the page.
- Incremental checks for the stale-selection pass: `pnpm --filter @amazon-ai-ops/desktop run typecheck` passed, `pnpm --filter @amazon-ai-ops/desktop run build:renderer` passed, and `node scripts\smoke-business-ui-ad-execution.js` passed with evidence `output\codex-evidence\business-ui-ad-execution-smoke-1781492651023.json`. No full test suite was run in this increment.
- 2026-06-15 readback stale-export cleanup pass: the `执行回读` page now clears old export results whenever the operator edits the readback form or loads a different approved action. If the current scope/batch no longer has a selected approved action, the form and export result are reset with an operator-visible message. This prevents stale JSON/Markdown evidence paths from remaining visible after the evidence fields changed.
- Incremental checks for the readback cleanup pass: `pnpm --filter @amazon-ai-ops/desktop run typecheck` passed, `pnpm --filter @amazon-ai-ops/desktop run build:renderer` passed, and `node scripts\smoke-business-ui-ad-execution.js` passed with evidence `output\codex-evidence\business-ui-ad-execution-smoke-1781492796680.json`. The smoke exports readback evidence, edits the `After 值`, and asserts the old JSON/Markdown paths disappear. No full test suite was run in this increment.
- 2026-06-15 dashboard-delivery status pass: the `仪表盘` workflow strip no longer hard-codes `APP_NEEDS_WORK`. It reads final readiness when available and translates it into operator-facing status: `最终验收已通过`, `等待最终验收`, or `尚未生成最终证据`. Raw `APP_*` status remains confined to the dedicated `交付验收` page, where manifest evidence and scope caveats are visible.
- Incremental checks for the dashboard delivery-status pass: `pnpm --filter @amazon-ai-ops/desktop run typecheck` passed, `pnpm --filter @amazon-ai-ops/desktop run build:renderer` passed, and `node scripts\smoke-business-ui-data-pipeline.js` passed with evidence `output\codex-evidence\business-ui-data-pipeline-smoke-1781492927134.json`. No full test suite was run in this increment.
- 2026-06-15 app-header readiness pass: the global topbar no longer hard-codes `APP_NEEDS_WORK`. It reads delivery readiness and displays business-facing labels: `最终验收通过`, `等待最终验收`, or `待生成验收`. This keeps raw `APP_*` manifest status on the `交付验收` page, where evidence paths and scope caveats are visible, instead of showing a static status on every page.
- Incremental checks for the app-header readiness pass: `pnpm --filter @amazon-ai-ops/desktop run typecheck` passed, `pnpm --filter @amazon-ai-ops/desktop run build:renderer` passed, and `node scripts\smoke-business-ui-shell.js` passed with evidence `output\codex-evidence\business-ui-shell-smoke-1781493159281.json`. No full test suite was run in this increment.
- 2026-06-15 settings-threshold validation pass: the `设置` page now validates ad quantification thresholds before saving. It blocks incoherent configs such as high ACOS below target ACOS, negative minimum spend, invalid bid adjustment ranges, max decrement below per-action decrement, or minimum CPC above maximum CPC. The settings/delivery smoke now enters an invalid high-ACOS threshold, confirms the UI blocks saving, verifies `saveRuleConfig` is not called, then restores a valid threshold and saves.
- Incremental checks for the settings-threshold validation pass: `pnpm --filter @amazon-ai-ops/desktop run typecheck` passed, `pnpm --filter @amazon-ai-ops/desktop run build:renderer` passed, and `node scripts\smoke-business-ui-settings-delivery.js` passed with evidence `output\codex-evidence\business-ui-settings-delivery-smoke-1781493289386.json`. No full test suite was run in this increment.
- 2026-06-10 real ad readback closeout: after user approval for a paused target, a manual Ads UI action changed `紧密匹配` from live bid `1.20` to `1.08`, captured before/after/reload screenshots, and passed `pnpm run verify:ad-readback -- output\codex-evidence\real-ad-execution-readback-candidate-rec-1.json`. The refreshed manifest `output\codex-evidence\v15-final-readiness-evidence-manifest-2026-06-10.json` selects this ad readback evidence, and `output\codex-evidence\final-readiness-2026-06-10.json` is now `APP_READY`.
- The `优化建议` readback UI was generalized beyond the D6 sample. The page now separates “已验证 readback 样例” from a “通用执行合同”, exposes editable target fields for store/site/portfolio/campaign/ad group/ASIN/entity/action, records source recommendation fields separately from live before/after values, and blocks local precheck when dynamic target fields are missing or the action is outside the low-risk whitelist.
- Incremental checks after the readback/generalization update passed: `pnpm --filter @amazon-ai-ops/desktop run typecheck`, `pnpm --filter @amazon-ai-ops/desktop run build:renderer`, and `pnpm run smoke:v15-product-readiness-ui`. Latest UI evidence is `output\codex-evidence\v15-product-readiness-ui-smoke-1781072779324.json`, with delivery screenshot `output\codex-evidence\v15-delivery-ui-smoke-1781072776206.png` and recommendation/readback screenshot `output\codex-evidence\v15-recommendations-ad-readback-ui-smoke-1781072777685.png`.
- 2026-06-10 AI evidence closeout: real DeepSeek live evidence `output\codex-evidence\deepseek-live-1781066552798.json` passed `pnpm run verify:ai-live`. The OpenAI-compatible provider now disables DeepSeek thinking for app/provider calls, and the live verifier uses a larger connection-test token budget so short tests return visible content instead of reasoning-only output.
- Added Lingxing ad report metric backfill into the recommendation path. Full-8 collection now imports downloaded xlsx files into `ad_daily_metrics`; older completed batches can be backfilled automatically when scoped recommendation generation finds no metrics. Parser mappings now cover real Lingxing headers such as `曝光量`, `用户搜索词`, `投放`, `CPC-本币`, and no longer drops keyword/search-term/targeting rows merely because they lack an ASIN. Product context is backfilled from the advertised-product report when campaign/ad group/date uniquely identify an ASIN.
- Packaged-app ad AI explanation evidence `output\codex-evidence\installed-ad-ai-explanation-user-key-2026-06-10.json` passed `pnpm run verify:ad-ai-explanation -- <evidence>`. The run used `--limit 300`, processed 266 scoped executable metrics from the verified full-8 batch, generated 1 AI-explained recommendation, restored settings, performed no ad writes, and started no report downloads.
- Listing AI draft evidence `output\codex-evidence\installed-listing-ai-draft-user-key-2026-06-10.json` passed `pnpm run verify:listing-ai-draft -- <evidence>`, proving `source=ai`, no fallback, `AI reason`, settings restore, no full-8/ad-write side effects, and no secret leakage. The runner no longer requires a browser login for `listing-ai-draft` unless `--login` is explicitly requested.
- Refreshed final readiness with `output\codex-evidence\v15-final-readiness-evidence-manifest-2026-06-10.json`. With the real ad readback evidence selected, `pnpm run verify:v15-final-readiness --evidence-manifest ... --out output\codex-evidence\final-readiness-2026-06-10.json` now passes report collection, Listing read, AI live, ad AI explanation, Listing AI draft, and real ad readback, returning `APP_READY`.
- Superseded 2026-06-10 installer evidence after the readback/generalization closeout: `apps\desktop\release\AmazonAIOpsAgent-1.5.0.exe`, SHA-256 `96648ADD7F51C2CFAC8289CD61F5449FE2453C556127C049668F6FB3613FEBC1`, size `89678328` bytes, last write `2026-06-10 14:48:01`. This was later superseded by the 2026-06-16 no-install portable executable documented at the top of this increment section.
- 2026-06-10 incremental closeout kept the user's "no repeated full test runs" constraint. Only targeted checks were run: `node --check` for the updated final-readiness/evidence/bundle scripts, `pnpm run verify:ai-settings-ux`, `pnpm run verify:ad-execution`, `pnpm --filter @amazon-ai-ops/desktop run build:renderer`, `pnpm run smoke:v15-product-readiness-ui`, and `pnpm run verify:v15-non-ready-safety`.
- Added ad recommendation AI explanation as an explicit final-readiness gate. Recommendation generation now enriches rule-generated actions through `AdActionReasonExplainer` when a real DeepSeek/OpenAI-compatible key is configured, records `explanationSource`, AI explanation/model/risk fields, and falls back visibly to rule explanation when no key or provider failure exists. New runner mode `pnpm run run:v15-installed-live -- --mode ad-ai-explanation --out output\codex-evidence\installed-ad-ai-explanation-manual.json` applies the real key in the packaged app, tests the provider, generates scoped recommendations through app IPC, reads recommendations back, restores AI settings, and writes evidence without ad writes. New verifier `pnpm run verify:ad-ai-explanation -- <evidence>` requires packaged-app runtime, read-only safety flags, AI connection success, settings restore proof, real provider metadata, store/site/ASIN/entity/action/metricDate context, `explanationSource=ai`, no fallback, and no secret leakage.
- Refreshed final readiness with an explicit 2026-06-10 manifest. `pnpm run write:v15-evidence-manifest --ad-readback output\codex-evidence\real-ad-execution-readback-candidate-rec-1.json --out output\codex-evidence\v15-final-readiness-evidence-manifest-2026-06-10.json` selects full-8 delivery, Listing full-read evidence, real DeepSeek live evidence, packaged-app ad AI explanation evidence, Listing AI draft evidence, and the real ad readback evidence. `pnpm run verify:v15-final-readiness --evidence-manifest output\codex-evidence\v15-final-readiness-evidence-manifest-2026-06-10.json --out output\codex-evidence\final-readiness-2026-06-10.json` returned `APP_READY`.
- Historical note: the non-ready safety guard was used before final readback evidence existed. Final-node APP_READY validation uses `pnpm run verify:v15-ready-safety`.
- The handoff bundle at `output\delivery-bundles\v15-delivery-bundle-2026-06-10` was refreshed after the final readback closeout and README/UI updates at that time. Its manifest status was `APP_READY`; the exporter continues to exclude browser profiles, credentials, AppData DB files, raw XLSX reports, installer binaries, API keys, and passwords.
- Historical UI correction before the final readback closeout split AI proof into separate operator-visible items. This state was superseded by later delivery UI smoke and must be refreshed again for the current worktree.
- Follow-up subagent review used `.codex/agents` roles `agents-orchestrator` and `design-ux-architect`. The orchestrator accepted the ad AI explanation runner/final gate as a valid path but required a stricter verifier before final delivery. The UX architect confirmed the menu split is no longer a nested v1.5 workbench, but found that users still retype scope across pages and lack a shared current context.
- Strengthened `pnpm run verify:ad-ai-explanation -- <evidence>`. Passing evidence must now come from `runtimeMode=packaged-app`, prove `adAiExplanationOnly=true`, `adWriteActionsPerformed=false`, `full8Started=false`, no Lingxing report download requests, `ai.testSuccess=true`, settings restore proof, real provider metadata, AI explanation fields, and no obvious secret leakage. Historical no-key blocker evidence still fails as expected and is no longer the current top-level AI state.
- Added a shared verified full-8 scope preset to `广告报表` and `优化建议` so operators can apply `2026-05-01 ~ 2026-05-25 / FT-US-US / US` without retyping date/store/site on each page. This is a frontend-level UX improvement using existing verified evidence; it does not alter DB scope semantics or imply a different readiness status.
- Historical product smoke after the AI proof and pre-write approval-packet changes is retained in older evidence files. The smoke `output\codex-evidence\v15-product-readiness-ui-smoke-1781072779324.json` showed `APP_READY`, a verified readback sample, and the generalized ad execution contract for that historical build.
- Added a desktop readback evidence export IPC for the `优化建议` page. Operators can enter external approver/proof, approval flags, live before/after bid values, screenshot paths, independent readback evidence path, explicit approval/before/execution/after/readback timestamps, readback actual value, manual Ads UI executor, execution id, live bid row proof, and risk rationale after a separately approved Ads action; the app writes local JSON/Markdown evidence only and does not execute ads. The exported JSON records `execution.channel=manual_ads_ui` and `appExecutorUsed=false`. The UI treats a complete form as “待 verifier 确认” rather than final PASS, and `verify:ad-readback` remains the authoritative gate. The readback evidence builder is now isolated in `apps/desktop/src/main/ad-readback-evidence.ts` with targeted regression coverage for empty NEEDS_WORK exports, manual Ads UI boundaries, CPC-vs-live-bid separation, verifier-first Markdown, traceable approval artifacts, independent readback evidence file, and timestamp ordering.
- Added `docs\REAL_AD_READBACK_RUNBOOK.md` as the operator-facing readback runbook. It documents the verified low-risk sample and the generalized no-go/approval/before-after/readback fields required for future Ads actions.
- Completed a read-only real Ads location pass for the current readback candidate without any save/write action. Evidence `output\codex-evidence\ads-readonly-locate-2026-06-10T05-50-16-170Z.json` proves the session entered Ads through ERP, switched to `FT-US / US / USD`, located campaign `D6-自动-低价探索 - 5/18/2026`, ad group `D6-自动-卧室室内-挖词 - 5/18/2026`, editable target `紧密匹配`, entity id `430176146198591`, and live bid `1.20`. It also found service status `广告活动已暂停`, and confirms the earlier `2.40 -> 2.16` value was report-derived rather than a live Ads bid. The evidence is approval material only, not final before evidence, because it was captured before second write approval. A future write still requires explicit approval of the concrete live-bid action, then a new before screenshot, one manual Ads UI save, after screenshot, reload/readback screenshot, and `verify:ad-readback`.
- 2026-06-09 installed full-8 delivery evidence passed. `pnpm run run:v15-installed-live -- --mode full8 --login --invoke-timeout-ms 900000 --out output\codex-evidence\desktop-live-full-8-e2e-2026-06-09.json` produced batch `batch_20260609045655853_ft8uda`, `status: completed`, 8 downloaded `.xlsx`, 0 failed, manifest path under AppData, and acceptance audit export `C:\Users\wz\AppData\Roaming\@amazon-ai-ops\desktop\storage\exports\lingxing_acceptance_audit_batch_20260609045655853_ft8uda_1780981089445`.
- `pnpm run verify:v15-delivery -- output\codex-evidence\desktop-live-full-8-e2e-2026-06-09.json` passed. The verifier first passed `AD_EXECUTION_FAIL_CLOSED`, then cross-checked the full-8 evidence JSON, AppData SQLite batch/file rows, 8 unique report types, manifest parity, file existence/path containment/extension/size, report keyword/date tokens, acceptance audit status/checks, and xlsx metric reconciliation.
- Added full-8 metric reconciliation evidence at `output\codex-evidence\full8-data-reconciliation-2026-06-09.json`, and made it part of `verify:v15-delivery`. For `2026-05-01` to `2026-05-25` / `FT-US-US` / `US`, the canonical `user_search_term` total is spend `145.20`, orders `5`, sales `324.95`; `keyword` is spend `25.38`, orders `1`, sales `49.99`; `auto_targeting` is spend `119.82`, orders `4`, sales `274.96`; `product_targeting` is zero orders/sales. This evidence does not support the earlier expected `about 3 orders / 170+ USD spend` unless a separate ASIN/campaign/target filter is supplied and proven.
- Hardened the app-facing data grain boundary. Default ad metrics summaries now exclude duplicate account-summary grains (`campaign`, `ad_group`, `placement`, `advertised_product`) and keep executable/detail grains (`keyword`, `auto_targeting`, `product_targeting`, `user_search_term/search_term`) separate for recommendations and keyword opportunity analysis. At that historical point the renderer still exposed an `APP_READY` delivery summary; later business-UI passes moved raw `APP_*` status codes out of normal operator pages and keep current worktree delivery state as `IN_PROGRESS` until final manifest, packaged smoke, installer/no-install hashes, and READY safety are refreshed.
- Historical renderer smoke evidence captured before the real Listing detail read passed. `pnpm run smoke:v15-product-readiness-ui` wrote `output\codex-evidence\v15-product-readiness-ui-smoke-1780984867731.json` plus screenshot `output\codex-evidence\v15-product-readiness-ui-smoke-1780984866965.png`, showing the earlier state where Listing read was still pending. Current UI smoke is listed below and shows Listing detail read as verified.
- Added a read-only Lingxing Listing page extraction path. The desktop main process now exposes `v1_5:listing:extract-from-lingxing` and `v1_5:listing:open-and-extract-from-lingxing`, preload exposes `extractListingFromLingxing` and `openLingxingListingAndExtract`, and the v1.5 Listing panel has visible `从当前领星页面读取` and `打开 URL 并读取` actions. The extractor records URL, page title, screenshot path, field matches, and ASIN/title/bullets/backend-term completeness; it fails closed when the current page cannot prove ASIN and title. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/main/listing-lingxing-extractor.test.ts`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, `pnpm --filter @amazon-ai-ops/desktop run build:main`, `pnpm --filter @amazon-ai-ops/desktop run build:preload`, `pnpm --filter @amazon-ai-ops/desktop run build:renderer`, and `pnpm run smoke:v15-product-readiness-ui`. Latest UI evidence: `output\codex-evidence\v15-product-readiness-ui-smoke-1780986944131.json`, first-screen screenshot `output\codex-evidence\v15-product-readiness-ui-smoke-1780986941702.png`, Listing panel screenshot `output\codex-evidence\v15-listing-read-ui-smoke-1780986941817.png`.
- Added a source-app pre-installer Listing read evidence mode to `scripts/run-v15-installed-live.js`: `pnpm run run:v15-installed-live -- --mode listing-read --source-app --login --listing-url https://erp.lingxing.com/erp/listing --out output\codex-evidence\source-listing-read-2026-06-09-candidates.json`. The evidence proves ERP login reuse, Ads entry, read-only safety flags, screenshot capture, ASIN `B0GTTJFQTM`, and a nontrivial title from the real ERP Listing list page. It intentionally does not pass full Listing acceptance because the list page does not expose bullets or backend search terms.
- Added `pnpm run verify:listing-read` as a strict read-only Listing evidence verifier. It accepts only Listing-read evidence with no report/ad-write side effects, a sanitized Lingxing URL, screenshot existence, valid ASIN/title, and full Listing content. Historical list-page-only evidence `output\codex-evidence\source-listing-read-2026-06-09-candidates.json` passed safety/ASIN/title/screenshot checks but returned `NEEDS_WORK` because bullets were absent; the later detail-page evidence below closes that specific blocker.
- Fixed old AppData DB compatibility for Listing workflows. A real source-app Listing read initially hit an older `listing_content` schema missing `bullets_json`; startup migrations now add the missing v1.5 Listing columns for `listing_content`, `listing_suggestions`, and `listing_drafts`, and the source-app Listing read persisted successfully after the migration.
- Added read-only detail-entry candidate collection to Listing extraction evidence. The app now records visible links/buttons/dropdown items that look like detail/edit/view/product/listing entry points, stores their text/href/selector hint in `evidence.detailCandidates`, shows the first candidates in the Listing evidence panel, and writes `detailCandidateCount` into `run:v15-installed-live -- --mode listing-read` evidence. This does not click candidates and does not save Listing data; it exists to make the next detail-page proof selector-driven instead of guessed.
- Tightened Listing readiness semantics. The extractor now returns `partialReady` for ASIN/title and `fullContentReady` only when ASIN, title, bullets, and backend terms are all present. The renderer displays `列表页/当前页部分读取成功` versus `详情页完整读取成功`, and `verify:listing-read` now requires `fullContentReady`, bullets, backend terms, sanitized Lingxing URL, screenshot evidence, and either a detail/edit URL or recorded detail probe. Current list-page evidence is intentionally still `NEEDS_WORK`.
- Added an explicit fail-closed detail-page probe path. Renderer exposes `只读探测详情页`; preload exposes `probeLingxingListingDetailAndExtract`; main process only clicks a single visible safe detail/view/edit candidate within the current ASIN row, rejects ambiguous/no-candidate cases, validates the final URL against Lingxing Listing/Product allowlist, and rejects ASIN mismatch before persisting. `run:v15-installed-live -- --mode listing-read --probe-detail` records `detailProbeStarted`, `detailProbeClicked`, `detailProbeStatus`, final URL, and ASIN match state.
- Completed real Lingxing Listing detail-page read evidence. The detail probe now handles Lingxing's split fixed-column table by matching the current ASIN row to the same visible operation row, clicks the row `操作` trigger, selects `编辑在线商品`, verifies the final Lingxing edit URL and same ASIN, reads `Generic Keyword` from the basic tab, switches read-only to `描述`, merges title/backend terms with bullet fields, and persists only after `fullContentReady=true`. Evidence `output\codex-evidence\source-listing-read-detail-probe-2026-06-09-merged-detail.json` passed `pnpm run verify:listing-read`, with ASIN `B0GTTJFQTM`, `https://erp.lingxing.com/erp/editListing`, 10 bullet fields, backend terms, `detailProbeClicked=true`, and `detailProbeStatus: full_content_ready`.
- Incremental checks passed: `pnpm exec vitest run apps/desktop/src/main/listing-lingxing-extractor.test.ts`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, `node --check scripts\run-v15-installed-live.js`, `node --check scripts\verify-listing-read-evidence.js`, `node --check scripts\verify-listing-ai-draft-evidence.js`, `pnpm --filter @amazon-ai-ops/desktop run build:renderer`, and `pnpm run smoke:v15-product-readiness-ui`. Latest smoke: `output\codex-evidence\v15-product-readiness-ui-smoke-1780997584522.json`, first-screen screenshot `output\codex-evidence\v15-product-readiness-ui-smoke-1780997583789.png`, Listing screenshot `output\codex-evidence\v15-listing-read-ui-smoke-1780997584059.png`. The first viewport now separates historical delivery snapshot from current selected-scope collection status, shows the current safe mode, and keeps technical evidence paths/command copy controls under a collapsed technical acceptance section; remaining visible blockers are AI draft proof and ad write readback.
- Historical note: `pnpm run verify:ai-live` was added as a DeepSeek/OpenAI-compatible live evidence runner. It reads `DEEPSEEK_API_KEY` or `AI_API_KEY` only from the environment, uses base URL `https://api.deepseek.com` plus `/chat/completions` by default with `deepseek-v4-flash`, writes a redacted evidence JSON, and refuses to write if the key appears in evidence. Earlier no-key evidence `output\codex-evidence\deepseek-live-1780985745488.json` correctly produced `status: NEEDS_WORK`, `keyPresent: false`, and `REAL_DEEPSEEK_KEY_REQUIRED`; current AI readiness is now proven by the 2026-06-10 real-key evidence above.
- Added `pnpm run run:ai-structural-mock` and `pnpm run verify:ai-structural-mock -- <evidence>` as a no-network OpenAI-compatible structural evidence path. Latest evidence `output\codex-evidence\structural-ai-openai-compatible-mock-1781703077556.json` proves the request shape, redacted Authorization header, OpenAI-compatible `/chat/completions` payload, `max_tokens`, `response_format: json_object`, `choices[0].message.content` response shape, Listing draft JSON fields, source-code hooks for AI reason / `source: 'ai'` / fallback clearing, and no ad/full8/settings side effects. It is explicitly `STRUCTURAL_ONLY` with `NO_FINAL_READINESS_CREDIT`; it does not replace `deepseek-live-*` or real `installed-listing-ai-draft-*` evidence.
- Added an app-level Listing AI draft evidence gate. `run:v15-installed-live -- --mode listing-ai-draft` launches the app, temporarily applies redacted OpenAI-compatible AI settings from `DEEPSEEK_API_KEY` / `AI_API_KEY`, calls `settings:test-ai`, generates drafts from a safe `accepted` Listing suggestion through the real `generateListingDrafts` IPC, restores prior settings, and writes evidence. `pnpm run verify:listing-ai-draft -- <evidence>` requires no report/ad-write side effects, `listingAiDraftOnly=true`, real provider pass, accepted suggestion input, at least one `source=ai` draft, no fallback, `AI reason` evidence, non-empty drafted text, settings restore proof, and no obvious secret leakage. Historical no-key evidence `output\codex-evidence\listing-ai-draft-no-key-2026-06-09-ui-gate.json` correctly returns `NEEDS_WORK`; current Listing AI draft readiness is now proven by the 2026-06-10 real-key evidence above.
- Updated the Settings page to show the operator-facing AI draft delivery evidence commands. The UI now tells the user to run `pnpm run verify:ai-live`, then `pnpm run run:v15-installed-live -- --mode listing-ai-draft --source-app`, and finally `pnpm run verify:listing-ai-draft -- <evidence>`. `pnpm run verify:ai-settings-ux` now guards this visible guidance.
- Added `pnpm run verify:v15-final-readiness` as the final aggregation gate. Final aggregation is now manifest-first: write the evidence manifest, then run `verify:v15-final-readiness --evidence-manifest <manifest>`. Non-manifest final readiness output is not acceptable for handoff. Early evidence `output\codex-evidence\final-readiness-2026-06-09.json` was `APP_NEEDS_WORK`; the current 2026-06-10 manifest-driven evidence is `APP_READY`.
- Historical note: `pnpm run write:v15-evidence-manifest` and `--evidence-manifest` support were added in `verify:v15-final-readiness`. The earlier 2026-06-09 manifest fixed a no-key evidence set to avoid stale/latest ambiguity; the current 2026-06-10 manifest now selects real-key AI live, ad AI explanation, and Listing AI draft evidence, with real ad readback as the remaining blocker.
- Added `pnpm run verify:v15-non-ready-safety` as a read-only guard against false READY claims while the worktree is not final. Final APP_READY validation uses `pnpm run verify:v15-ready-safety`.
- Historical note: `pnpm run export:v15-delivery-bundle` was added and hardened for handoff packaging. The earlier `output\delivery-bundles\v15-delivery-bundle-2026-06-09` intentionally included no-key blocker evidence. The 2026-06-10 bundle selected real-key AI evidence and real ad readback evidence, stated `APP_READY`, refused non-manifest final-readiness JSON, and used allowed-source path checks, pre-copy secret scanning, unique destination names, and blocked extension checks so it intentionally excluded browser profiles, credentials, AppData DB files, raw downloaded XLSX reports, installer EXE files, API keys, and passwords.
- Historical navigation split: the first pass removed the nested `v1.5 工作台` entry and introduced business-domain left navigation. This has since been refined to the current `运营总览`, `数据与量化`, `广告执行`, `关键词与 Listing`, and `系统与交付` structure, keeping v1.5 as an upgrade of the backend rather than a workbench embedded inside another workbench.
- Added per-section page framing for `交付验收`, `广告报表采集`, `关键词机会`, and `Listing 优化`: each page now states its operator purpose, primary task, and acceptance proof. The Listing page also has a visible workflow strip: `读取 Listing -> 生成建议 -> 采纳建议 -> 生成草案 -> 导出交付`, so the page reads as a task flow rather than a pile of buttons.
- Updated renderer smoke coverage for the split navigation and fixed screenshot naming. Latest strict smoke: `output\codex-evidence\v15-product-readiness-ui-smoke-1781003733089.json`, with separately named page screenshots `output\codex-evidence\v15-delivery-ui-smoke-1781003731490.png`, `output\codex-evidence\v15-reports-ui-smoke-1781003731759.png`, `output\codex-evidence\v15-keywords-ui-smoke-1781003731949.png`, `output\codex-evidence\v15-listing-ui-smoke-1781003732183.png`, and `output\codex-evidence\v15-settings-ai-ui-smoke-1781003732657.png`. The strict smoke checks that old `v1.5 工作台` text is absent, business-domain nav groups render, each left-menu entry renders the correct h2 and section framing, report/keyword/listing controls do not bleed into the wrong menu section, Listing workflow steps are visible, `设置` exposes the AI evidence workflow plus safe `$env:DEEPSEEK_API_KEY` template, and `交付验收` exposes the manifest-driven final readiness command plus the `evidenceSelection.mode=manifest` requirement.
- Refined the `优化建议` page so it is a first-class advertising operations page rather than a hidden execution edge case. It now shows date/store/site/ASIN filters, `刷新列表`, `生成优化建议`, the ad execution/readback workflow, required readback evidence fields, and `复制广告 readback 验收命令`. Backend `recommendations:get` now uses `findByFilter` instead of the old date-only query, and the repository filters dates by `evidence.date` before falling back to `created_at`, so selecting a May advertising range still shows recommendations generated today from May metrics. Operator-triggered generation now requires explicit start date, end date, store, and marketplace instead of using the login username as store; generation uses scoped ad metrics, records campaign/ad group/ASIN/date context in evidence, and skips duplicate recommendations for the same store/site/ASIN/entity/action/metric date. Fail-closed execution also writes the failed action log even when browser screenshots are unavailable; screenshots are best-effort evidence, not a prerequisite for recording the blocked action. Incremental checks passed: `pnpm run verify:ad-execution`, `pnpm --filter @amazon-ai-ops/desktop run build:main`, `pnpm --filter @amazon-ai-ops/desktop run build:preload`, `pnpm --filter @amazon-ai-ops/desktop run build:renderer`, and `pnpm run smoke:v15-product-readiness-ui`. Latest strict smoke: `output\codex-evidence\v15-product-readiness-ui-smoke-1781054100447.json`, with `优化建议` screenshot `output\codex-evidence\v15-recommendations-ad-readback-ui-smoke-1781054099103.png`. SQLite repository test `pnpm exec vitest run packages/local-db/src/sqlite/repositories/ad-metrics-repo.test.ts` is still blocked in this workstation by `better-sqlite3` ABI mismatch: package built for Electron `NODE_MODULE_VERSION 119`, CLI Node requires `137`.
- Improved the Settings AI delivery flow. The AI configuration card now has an operator-facing evidence workflow: `保存真实 Key -> 测试 AI 连接 -> 生成 Listing AI 草案 -> 刷新最终验收`, plus a `复制 AI 验收命令` action that copies a safe PowerShell template and the required `verify:ai-live`, `run:v15-installed-live -- --mode listing-ai-draft --source-app --out output\codex-evidence\installed-listing-ai-draft-manual.json`, and `verify:listing-ai-draft -- output\codex-evidence\installed-listing-ai-draft-manual.json` commands without exposing a real secret.
- Hardened the AI evidence path. Desktop settings are now normalized across camelCase and snake_case (`aiApiKey`/`ai_api_key`, `aiBaseUrl`/`ai_base_url`, etc.) so UI saves and installed evidence runners cannot disagree. The installed Listing AI draft runner writes both key forms, rereads Settings to prove the env key/base/model are active before draft generation, and restores previous settings in `finally` with reread proof. `verify:ai-live` now uses the same OpenAI-compatible base URL plus `/chat/completions` rule as the app provider, and `scripts/verify-ai-live.js` exists as a compatibility wrapper.
- Enabled page-model diagnostic passed after live canary coverage. `output/codex-evidence/installed-live-diagnostic-enabled-model-2026-06-09.json` produced diagnostic id `26`, active model source `override`, `requiresManualVerification: false`, `diagnostic.ready: true`, and preflight checks `page_model_ready`, `diagnostic_evidence_ready`, and `browser_session_ready` all passed.
- 8/8 installed single-report canaries now pass. The final evidence set includes the prior `campaign`, `ad_group`, `placement`, plus `keyword`, `user_search_term`, `product_targeting`, `advertised_product`, and `auto_targeting` short-name canaries. `pnpm run verify:v15-enablement -- output\codex-evidence\installed-canary-auto_targeting-short-name-2026-06-09.json` passed and reported complete coverage for `campaign,ad_group,placement,advertised_product,auto_targeting,keyword,product_targeting,user_search_term`.
- Fixed long Lingxing report names being truncated by the live download center. Generated report names now use the shorter `AAO_<start>_<end>_<reportKeyword>_<time>` form so ready-row and download selectors match the actual DOM while filenames still contain report keyword and start/end date tokens.
- Hardened installed evidence runners/verifiers. `run-v15-installed-live.js` now writes checkpoint evidence during the run, supports controlled IPC/app-close timeouts, cleans the installed app process tree, supports `--mode full8`, and uses single-attempt canaries. `verify-v15-canary`, `verify:v15-enablement`, and `verify:v15-delivery` now use Python sqlite3 fallback when `better-sqlite3` is rebuilt for Electron and cannot load under CLI Node 24.
- Updated bundled `resources/page-models/lingxing-download-center.json` and current AppData override to `requiresManualVerification: false` only after the 8/8 canary and enablement evidence passed. A backup of the previous override was written under AppData `storage/page-models/backups`.
- Historical installer: `apps/desktop/release/AmazonAIOpsAgent-1.5.0.exe`, SHA-256 `F5B69BAFE2932EC3DA49E9ED6B25FE1EB635FB885E2415DEEC92D8D1B0868412`, size `89622081` bytes, last write `2026-06-09 12:52:50`.

Historical 2026-06-08 / earlier increments:

- Added a first-screen delivery-gate summary to the v1.5 report-collection panel. It shows the selected date/store/site scope, page-model readiness, same-scope diagnostic status, 8-report batch status, and final audit gate in one place so operators can see the next blocker before clicking live actions.
- Added an operator-facing `单报表验证` canary control with all 8 Lingxing report types. It uses a dedicated single-report gate so operators can prove live create/ready/download behavior on one report before attempting the full 8-report E2E run.
- Added a dedicated `run-lingxing-canary-report` IPC path for `单报表验证`. It still requires the browser session and current same-date/store/site diagnostic setup evidence, but it does not require the full collection preflight to pass `requiresManualVerification`; full 8-report collection and row-level retry remain fail-closed behind the original preflight.
- Current packaged app live-scope diagnostic evidence: `output/codex-evidence/desktop-live-scope-diagnostic-2026-06-08T02-47-44-379Z.json`, diagnostic id `5`, scope `2026-05-01` to `2026-05-25`, store `FT-US-US`, marketplace `US`. Diagnostic evidence passed; collection preflight remains blocked only by `download center page model still requires manual verification`.
- Renderer smoke evidence for the new delivery-gate UI: `output/codex-evidence/v15-delivery-gate-ui-smoke.png`. The smoke check verified the v1.5 page renders, the gate is visible, the canary dropdown exposes 8 report types, the canary button is in the first viewport, and no console warnings/errors are emitted.
- Fixed the packaged runtime dependency closure for `better-sqlite3` by making the desktop package explicitly include `bindings` and `file-uri-to-path`. Packaged smoke now reaches `sqlite-ready`, `ipc-ready`, and `window-created` instead of merely keeping an Electron process alive.
- A current packaged app launch migrated the live AppData DB to include `download_center_diagnostics.store_name/marketplace_code` and `lingxing_report_batches.store_name/marketplace_code`. Existing historical diagnostics still have null store/site, so they cannot unlock same-scope live collection.
- Historical 2026-06-08 audit state: the Electron DB then had only read-only diagnostic evidence and no current v1.5 completed report batch, so ready/download action selector checks did not yet prove a newly generated report row.
- Fixed the v1.5 workbench panel layout that let the keyword-opportunity panel overlap the report-collection panel and intercept the `验证页面` click target. The top-level panel grid and nested forms now use responsive minimum widths with `minWidth: 0`, and small buttons rely on layout gaps instead of extra margins.
- Renderer QA evidence now proves the `验证页面` button is the actual element under the click center and that the diagnostic success state can render through the UI path: `output/codex-evidence/renderer-v15-diagnose-layout-qa-1780561270634.json` and `output/codex-evidence/renderer-v15-diagnose-layout-qa-1780561270634.png`.
- Removed the deprecated `daily_report_download` scheduled task registration and scheduler task type. The current scheduler UI no longer exposes a legacy single-report path that is intentionally disabled under v1.5; operators should use `采集预检` / `验证页面` / `启动采集`.
- Added store/site inputs to the desktop report-collection panel and passes them through collect, preflight, retry, diagnostic, and enablement-audit IPC calls.
- Download-center automation now generates unique report names per report/date range and renders `{generatedReportName}`, `{storeName}`, and `{marketplaceCode}` placeholders for scoped selectors.
- Download-center navigation now handles the ERP-logged-in/Ads-not-entered case by entering the Ads system through the ERP advertising menu before navigating to the Ads download-center URL.
- Desktop login hardening: `browser:login` now closes any previous controlled browser before a new attempt, validates ERP first, enters Ads through the ERP advertising entry instead of first navigating directly to an Ads URL, and cleans up the browser controller on failure so profile locks do not persist.
- Browser launch errors are now converted to operator-readable messages before reaching the login UI, so Playwright launch arguments, profile paths, and raw `Call log` text are not rendered as red error text.
- Renderer login and v1.5 action errors now share user-facing error sanitization. The login page states the ERP -> Ads entry path, the app header shows ERP/Ads session status after login, and full 8-report collection is disabled until collection scope, page-model readiness, and same-scope diagnostic evidence are all satisfied.
- The v1.5 report collection workbench now uses an operator-facing layout: current scope, next-step guidance, compact gate cards, 0/8 single-report canary progress, report-type chips, grouped primary/secondary actions, and a collapsed advanced-diagnostics area for page model JSON, selectors, DOM, and trace details. Renderer smoke evidence: `output/codex-evidence/v15-workbench-operator-ui-smoke-2026-06-08.png`.
- Added `pnpm run verify:v15-enablement` as a read-only page-model enablement evidence gate. It requires recent same-scope diagnostic evidence plus successful same-scope canary coverage for all 8 Lingxing report types. It is intentionally separate from delivery readiness.
- Strengthened `pnpm run verify:v15-delivery` into a read-only delivery evidence gate that does not trust the E2E JSON alone. It now uses the full-8 evidence file as an entry point and then verifies the AppData SQLite batch/file rows, 8 unique report types, manifest parity, real downloaded files, filename date/report tokens, and a passing exported `acceptance-audit.json`. Historical 2026-06-08 evidence correctly failed this gate because full-8 evidence was incomplete; the 2026-06-09 full-8 evidence now passes.
- Added `pnpm run verify:v15-canary` as a read-only single-report evidence gate. It checks the canary JSON, AppData SQLite batch/file/diagnostic rows, manifest, downloaded report file, file size, report keyword, and start/end date tokens. It passes for `output/codex-evidence/installed-canary-campaign-2026-06-08T06-38Z.json` and intentionally fails the older polluted canary evidence that contains a Playwright `Too many arguments` error.
- Added `pnpm run verify:v15-diagnostic` as a read-only installed-app diagnostic evidence gate. It checks the diagnostic JSON, AppData SQLite diagnostic row, screenshot/DOM artifacts, exported diagnostic bundle, same-scope fields, and safety flags proving no full-8 collection or ad write action was started.
- Added `pnpm run run:v15-installed-live` as a repeatable installed-app evidence runner. It supports `--mode diagnostic` and `--mode canary --report-type <type>`, launches the packaged `win-unpacked` app through Electron/Playwright, uses the app's preload IPC, writes evidence under `output/codex-evidence`, and keeps full-8 collection and ad write actions off. Login is explicit via `--login` plus `LINGXING_USERNAME` / `LINGXING_PASSWORD`.
- Tightened the operator UI around live actions: `单报表验证` now requires current-scope diagnostic evidence before it can call IPC, asks for explicit confirmation that it will generate/download one real Lingxing report, and cancellation does not call the canary IPC. `启动 8 报表采集` also guards against direct function invocation when the scope/model/diagnostic gate is not passed and asks for explicit confirmation before generating all 8 reports.
- Fixed current-scope evidence accounting in the v1.5 workbench. Single-report coverage and full-8 progress now count only batches matching the current date/store/site scope; stale batch evidence is labeled as non-current and no longer contributes to the 0/8 gate or next-step guidance.
- Renderer error text is now more operator-facing for collection blockers: missing browser sessions, manual-verification gates, stale/missing diagnostic evidence, selector/locator failures, and Playwright launch output are summarized as next actions instead of raw technical text.
- Promoted the per-report Lingxing batch result table into the normal operator area instead of hiding it behind advanced diagnostics. Operators can now see each report type, status, retry count, file path, failure reason, evidence links, and retry/open actions without opening selector/page-model diagnostics.
- Added ad-recommendation execution confirmation and evidence columns. Approved recommendations now show target object, current/recommended value, reason/evidence text, risk, and confidence; the current UI labels the action as fail-closed audit generation rather than real execution while real executor/readback remains unavailable.
- Clarified Listing suggestion semantics in the UI: suggestions are marked locally and exported locally, not submitted to Amazon. Buttons now say `标记采纳` / `标记忽略`, and the table includes current text beside suggested text where available.
- Listing draft generation now respects operator decisions. Drafts are generated only from `accepted` suggestions; pending or ignored suggestions are filtered in both renderer flow and the rule-based draft builder. Draft exports are available as CSV/Excel/Markdown and include current text, draft text, source, AI fallback reason, evidence, and risk fields. The renderer now shows current text vs. draft, evidence, source/risk, AI fallback status, copy-draft action, latest export path, and an open-latest-export button in the same review surface.
- Improved keyword-opportunity operator UX. The panel now shows total/high/risky/listing-usable opportunity counts, filters by ASIN/opportunity level/risk state, parses evidence into click/order, cost/sales, and ACOS columns, shows recommended Listing sections, and displays a next-step CTA that tells the operator to fill/import Listing ASIN before generating Listing suggestions.
- Keyword opportunity evidence now includes cost, impressions, computed ACOS/CVR, source type, source file, source row, coverage penalty, and risk penalty. The UI keeps source trace as secondary text under cost/sales rather than reintroducing a primary source column.
- Closed the ad-execution false-success gap. Desktop `recommendations:execute` now fails closed when a real verified executor is unavailable, writes a failed action log with the blocking reason, keeps the recommendation approved, and surfaces the error to the operator instead of marking the recommendation executed. `AdActionExecutor` no longer treats missing success toast or failed bid/toggle readback as success.
- Added `pnpm run verify:ad-readback -- <evidence>` as the real ad execution evidence contract. It requires `kind=real-ad-execution-readback`, `status=PASS`, explicit operator approval scope/timestamp, low-risk policy-approved action type, store/site/campaign/ad group/entity/action context, before/after values that changed, before/after screenshot files, readback `actualValue` matching the after value, successful and verified execution result, no full8/listing-AI mixing, and no obvious secret leakage. `verify:v15-final-readiness` now uses this verifier when `--ad-readback` evidence is supplied instead of relying on loose inline fields. This does not unlock real writes; it defines the proof required before APP_READY can claim ad execution.
- Added `pnpm run verify:ad-execution`, and made `pnpm run verify:v15-delivery` run that gate before the full-8 report evidence checks. This prevents a future delivery claim from passing while ad execution can still produce fake success/verified/executed records.
- Added AI / DeepSeek settings UX. The settings page now exposes API Key, Base URL, model, temperature, max tokens, save, and `测试 AI 连接`; the desktop main process registers `settings:test-ai`, defaults to DeepSeek-compatible settings, returns operator-facing auth/network/quota/missing-key messages, and Listing AI generation falls back with `aiFallbackReason` if provider calls throw.
- Installed-app real canary evidence: account `SHC001` reached `ERP 已复用登录态；Ads 已进入：仪表盘`, selected `2026-05-01` to `2026-05-25` / `FT-US-US` / `US`, ran `广告活动报告`, and produced batch `batch_20260608063734381_rhuubk` with 1 downloaded `.xlsx`, 0 failed, manifest/DB/file size parity, SHA-256 `A472DFF204AFC13811DD2B08ED8A153F40168EDDF6453E6D81CCD2707C043B46`, and filename tokens `20260501` / `20260525`.
- Installed-app second canary evidence: `output/codex-evidence/installed-canary-ad_group-2026-06-09.json` used the same scope, reached `ERP 已复用登录态；Ads 已进入：仪表盘`, generated diagnostic id `10`, ran `广告组报告`, and produced batch `batch_20260609031748302_dzhx8o` with 1 downloaded `.xlsx`, 0 failed, manifest/DB/file size parity, report keyword `ad_group`, and filename tokens `20260501` / `20260525`.
- Installed-app third canary evidence: `output/codex-evidence/installed-canary-placement-2026-06-09.json` used the same scope, reached `ERP 已复用登录态；Ads 已进入：仪表盘`, generated diagnostic id `11`, ran `广告位报告`, and produced batch `batch_20260609032339827_tdk798` with 1 downloaded `.xlsx`, 0 failed, manifest/DB/file size parity, report keyword `placement`, and filename tokens `20260501` / `20260525`.
- Fixed the canary verifier freshness reference. `verify-v15-canary-evidence.js` now uses the canary batch creation time before falling back to evidence `createdAt`, so same-run diagnostics generated after the evidence JSON was opened are not falsely rejected by the 30-minute TTL check.
- Installed-app acceptance audit export for the canary batch: `C:\Users\wz\AppData\Roaming\@amazon-ai-ops\desktop\storage\exports\lingxing_acceptance_audit_batch_20260608063734381_rhuubk_1780900813665`. The audit status is `incomplete` because only 1/8 reports were downloaded; all single-report file/manifest/diagnostic checks pass.
- Create-report automation now opens the create-report page before filling controls, then fills store transfer search/selection, generated report name, report type dropdown, start/end dates, and daily-detail radio before the guarded generate action.
- The bundled download-center page model now contains an action selector draft for create-page controls, generated-report scoped ready rows, and generated-report scoped download buttons, while keeping `requiresManualVerification: true`.
- Download-center diagnostics now perform a two-phase read-only selector check: list-page selectors first, then create-report page setup selectors after opening the page and report type dropdown without clicking `生成报告`.
- The diagnostic evidence gate now requires the full create setup selector set: store search/option/move, report name, report type select/option, start/end dates, daily detail, create button, and confirm-create button.
- Real desktop IPC diagnostic evidence: `output/codex-evidence/desktop-ipc-two-phase-diagnostic-1780542152692.json`, diagnostic id `4`, `ready: true`, `missingRequiredSelectors: []`, URL `https://ads.lingxing.com/ak_download/download_center/download_report_log/index`, screenshot `C:\Users\wz\AppData\Roaming\@amazon-ai-ops\desktop\storage\screenshots\download_center_diagnostic_1780542191091.png`, DOM snapshot `C:\Users\wz\AppData\Roaming\@amazon-ai-ops\desktop\storage\dom-snapshots\download_center_diagnostic_1780542191254.html`.
- Desktop/native startup hardening now avoids pulling `duckdb` into desktop main/audit-log runtime paths, externalizes Playwright/DuckDB in the main-process build, and rebuilds packaged native dependencies with `npmRebuild: true`.
- Collector now retries each failed report up to 2 automatic retries before final failure.
- Final failed report rows record attempt errors, screenshot path, DOM snapshot path, Playwright Trace path when tracing starts successfully, and a Trace unavailable reason when tracing cannot be produced.
- Download-center automation now has a selector-driven Playwright path for create, wait-ready, and download. The path stays disabled until the page model is manually verified and action selectors are filled.
- Download-center diagnostics now persist screenshot, sanitized DOM snapshot, and selector candidates with uniqueness/match-count hints for manually solidifying the page model.
- Download-center diagnostics can now be exported as a local evidence bundle containing the diagnostic JSON, active page model, readiness result, selector candidates, action selector checks, copied screenshot/DOM evidence, and a manual verification checklist.
- Download-center diagnostic bundle export now takes only a diagnostic ID from the renderer and reloads the persisted diagnostic in the main process before writing evidence files.
- Download-center diagnostics can now generate a safe page-model draft from persisted evidence. The draft is exported with selector candidates and solidification notes, filled into the UI editor, and keeps `requiresManualVerification: true` until the operator deliberately completes and re-diagnoses the verified selector set. Unit tests cover the manual gate, trusted Lingxing URL promotion, selector deduplication, and operator notes.
- Added a page-model enablement audit for the final selector-solidification step. It exports JSON/Markdown evidence for the active saved page model and can only say manual verification may be disabled when scoped action selectors and recent same-model/same-date/same-store/same-marketplace diagnostic setup evidence both pass. Tests also pin the post-enable requirement: after `requiresManualVerification` is saved as `false`, an older manual-gated diagnostic no longer matches and collection remains blocked until a fresh enabled-snapshot diagnostic exists.
- Page-model enablement audit export now also applies diagnostic screenshot/DOM evidence file readiness, safely copies those files into the audit folder, and writes `diagnostic-evidence-files.json`; missing local evidence files block the audit from saying manual verification can be disabled.
- Acceptance audit diagnostic lookup now uses a tested same-model/same-date/same-store/same-marketplace query helper so the default UI path cannot accidentally fall back to a different page-model snapshot or another collection scope.
- Download-center collection now requires a recent matching diagnostic record for the active page-model snapshot, selected date range, store, and marketplace before any create, wait, or download action can run. The runtime gate verifies page/date/create selector evidence, while ready/download selectors are checked live immediately before use to avoid self-locking new date-range report creation.
- The diagnostic evidence gate is now a tested `lingxing-report-collector` helper covering missing/failed diagnostics, stale or future timestamps, malformed selector-check evidence, model/date/store/site mismatches, optional confirm dialogs, and new date-range creation without pre-existing ready/download rows.
- Local evidence opening is now constrained to app-owned download, screenshot, DOM snapshot, trace, report, and export directories with a safe extension allowlist.
- DOM evidence metadata now strips URL query/hash fragments and redacts token/session/cookie/auth-like values before writing local snapshots.
- Selector candidate collection now tolerates unusual DOM id/class values by using CSS escaping and skipping only the bad element instead of failing the whole diagnostic.
- Download-center page models now support a local override in app storage, with IPC/UI save, reload, reset, source display, validation, invalid-override recovery feedback, and readiness feedback. This lets verified live selectors be applied without rebuilding the app.
- Download-center page-model overrides now save metadata and automatically back up previous override files before save/reset, preserving selector-solidification history. Metadata also records whether manual verification is disabled and whether a fresh post-save diagnostic is required for the exact enabled snapshot.
- Download-center page-model validation is now isolated in a testable main-process helper. Tests cover manual-verification-on bundled models, complete selector-scoped verified models, missing required selectors, missing report/date placeholders, non-HTTPS/non-allowlisted URLs, and timeout bounds.
- Added a non-mutating Lingxing collection preflight. The desktop can now show whether collection is blocked by page-model readiness, missing/stale/mismatched same-model/date/store/site diagnostic evidence, or missing browser login/session before it clicks anything.
- `启动采集` and row-level `重试` now run the same preflight as a hard gate before creating a collection batch, so known-unready page models or missing diagnostics fail before noisy failed batches are produced.
- The hard preflight gate is now a package-level assertion helper reused by the desktop main process, with regression tests proving blocked starts throw a batch-preserving error and ready preflights pass through.
- Collection preflight now also verifies that the matching diagnostic screenshot and sanitized DOM snapshot files still exist inside the app-owned evidence directories before any collection batch is created.
- Lingxing E2E acceptance audit export now applies the same diagnostic screenshot/DOM evidence file readiness, copies those evidence files into the audit folder when safe, and writes `diagnostic-evidence-files.json` so final audit bundles remain self-contained.
- Lingxing E2E acceptance audit export now also copies and indexes failed-report screenshot, DOM snapshot, and Playwright Trace evidence into `report-failure-evidence` with `report-failure-evidence-files.json`, making failed live runs easier to review without chasing DB paths.
- Lingxing E2E acceptance audit now also checks batch download-directory layout, so a manifest or downloaded report file outside the batch `downloadDir` fails the final audit instead of being accepted as evidence.
- Lingxing E2E acceptance audit export now writes `downloaded-report-files.json`, a safety index for successfully downloaded report files that records path containment, existence, actual size, declared size, and filename date-range analysis without copying large raw report files.
- Lingxing E2E acceptance audit now re-checks expected report filename keywords, so a persisted campaign row pointing at a keyword report file fails final audit even when the manifest and date tokens otherwise match.
- `downloaded-report-files.json` now also records each report's expected filename keyword and whether the basename matches that report type, so manual live-review can inspect report/file mismatches from the export folder directly.
- `downloaded-report-files.json` now includes per-file `readyForAcceptance` and `acceptanceBlockers`, summarizing path safety, report keyword match, and selected date-token checks for manual audit review.
- Lingxing E2E acceptance audit now compares recorded downloaded file sizes with the current files on disk during export, so a report file that was replaced or truncated after collection fails final audit.
- `downloaded-report-files.json` now also treats recorded/current file size mismatches as per-file acceptance blockers, keeping the JSON index aligned with the final acceptance audit.
- Lingxing E2E acceptance audit now also checks manifest `appVersion` against the persisted batch `appVersion`, so version-trace evidence must stay consistent through final audit.
- Lingxing E2E acceptance audit now treats a missing batch/manifest `appVersion` as incomplete evidence, so final audit cannot pass without version trace.
- Lingxing E2E acceptance audit now also requires manifest `generatedAt`, so final audit cannot pass with a manifest missing its own generation timestamp.
- Lingxing E2E acceptance audit now validates manifest `generatedAt` as a parseable timestamp that does not predate the persisted batch creation/completion time and is not later than the audit execution time.
- Lingxing E2E acceptance audit now also verifies the manifest's embedded batch snapshot `appVersion`, `createdAt`, and `completedAt` against the persisted batch, so a copied or stale manifest cannot pass with only matching top-level metadata.
- Lingxing E2E acceptance audit now verifies the manifest's embedded batch `downloadDir` and `manifestPath` against the persisted batch, so a manifest copied from another batch folder cannot pass final evidence review.
- Lingxing E2E acceptance audit now also verifies manifest file-row `displayName`, `createdAt`, and `updatedAt` against persisted report file rows, so stale file metadata cannot pass with only matching path and size.
- Lingxing E2E acceptance audit now also verifies manifest file-row retry and failure evidence metadata, including auto retry counts, error messages, attempt errors, screenshot/DOM/Trace paths, and trace-unavailable reasons.
- Independent subagent review found that batch `appVersion` was not persisted through the desktop DB path; the local DB schema, migration, save query, and persisted batch loader now keep `app_version` so final manifest version-trace audit has real DB evidence.
- Independent subagent review found that package-level acceptance audit could pass when callers omitted file existence or size callbacks; the audit now returns incomplete unless both evidence callbacks are supplied.
- Independent subagent review found that package-level acceptance audit could accept a forged diagnostic readiness boolean; the audit now requires readiness provenance (`diagnosticId` and `checkedAt`) to match the persisted diagnostic before the diagnostic check can pass.
- Goodall subagent review found no high/medium risk in the hard collection preflight gate. The review confirmed the change moves known failures before batch creation without adding new success-path semantics.
- Added collection preflight export. Operators can write a local preflight evidence folder before attempting live report creation.
- Collection preflight export now also writes the active page model, diagnostic evidence-file readiness index, matching diagnostic JSON when available, and safe copies of diagnostic screenshot/DOM evidence. This makes before/after selector-solidification review possible from the export folder without querying the local database.
- Collection preflight export is now extracted into a pure main-process helper with tests covering both matching-diagnostic and missing-diagnostic evidence bundle outputs.
- Collection preflight export now includes `preflight-review-checklist.md`, so live selector-solidification sessions have a fixed local review checklist covering blocked checks, date range, store/site scope, active model, diagnostic evidence, and next required action.
- Download-center diagnostics, collection preflight, enablement audit, collection batches, batch manifest, persisted batch rows, and final acceptance audit now carry the selected store/site scope. A diagnostic for one store/site no longer unlocks collection or enablement for another scope, and the final manifest check compares persisted batch store/site with the manifest batch snapshot.
- A follow-up reality-check review found a package-level final-audit gap: callers could provide matching diagnostic readiness provenance while the diagnostic itself belonged to a different store/site. `auditLingxingAcceptanceEvidence` now directly compares diagnostic store/site scope with the persisted batch scope and stays incomplete on mismatch.
- Collection preflight export now also writes `preflight-bundle-index.json`, a machine-readable index of readiness, blocked checks, diagnostic evidence readiness, diagnostic ID, and expected bundle files for automated review of pre-live evidence folders.
- Download-center diagnostics now include read-only Playwright visible-locator counts for configured action selectors, including placeholder-expanded checks across the 8 report types, plus usable/ambiguous labels so broad selectors are not mistaken for safe automation targets.
- Real collection now repeats per-action visible-locator safety checks before filling fields or clicking create/download controls, and ready/download selectors must include both report and date placeholders so old report rows fail closed before download.
- Downloaded report verification now also requires valid collection dates and selected start/end date tokens in the filename, so wrong-date files are marked failed with preserved file-size evidence instead of being imported silently.
- Filename date-range verification now uses a shared analyzer that reports normalized filename digits and whether the start or end token is missing, so real Lingxing sample filename mismatches are easier to diagnose without loosening the fail-closed policy.
- Lingxing E2E acceptance audit JSON now includes per-report filename/date analyses and the desktop export writes `filename-date-range-analysis.json`, giving a machine-readable record for real Lingxing filename sample validation.
- Report status polling now has DOM-independent semantics for pending, created, generating, ready, failed, expired, skipped, and unknown states, with terminal failure fail-fast behavior covered by unit tests. Desktop automation can optionally use a verified `statusTextSelector` to read row status text before download; if omitted, it falls back to the ready-row selector.
- Keyword opportunity generation now keeps the same keyword separated by ASIN, preserving product-specific opportunity rows during repeated imports. Listing suggestions are generated from opportunities scoped to the current Listing ASIN.
- Local DB startup deduplicates historical keyword metric/opportunity rows before adding unique indexes for imported source rows and ASIN/keyword opportunities.
- SQLite persistence now stores automatic retry counts and failure evidence fields, including migrations for existing local databases.
- UI report table now shows automatic retry count and links to screenshot/DOM/Trace evidence when present.
- Manual row-level `重试` still creates a new single-report retry batch.
- Stabilized the status-poller success-path test timeout so full parallel test runs do not fail before the second status read under local load. Production polling defaults were not changed.
- Verified the real ERP login surface in a Playwright persistent browser session: `https://erp.lingxing.com/` uses `input[name="account"]`, `input[name="pwd"]`, and `button.loginBtn`; the older `www.lingxing.com/login` path is not the app login surface.
- Verified that the Ads system is a distinct surface at `https://ads.lingxing.com/home`; login handling now validates the Ads session after ERP login and throws a clear operator message if Ads authorization/session is not ready.
- Verified the real Ads download center at `https://ads.lingxing.com/ak_download/download_center/download_report_log/index`; the bundled page model now starts from that URL and allowlists `ads.lingxing.com`.
- Captured read-only live Ads download center evidence under `output/playwright/lingxing-ads-download-center-2026-06-03T02-16-06-375Z/`, including rows for the 8 SP report types and `.JS-download-report` download links.
- Captured read-only create-report evidence under `output/playwright/lingxing-ads-create-report-modal-2026-06-03T02-17-17-750Z/`, including store selection, report name, report type, start/end date fields, metric controls, and the `生成报告` button. No live report generation was clicked.
- Updated selector-candidate collection to include the real Ads download-center DOM families: DataTables rows, Element UI date/select/dialog controls, `.JS-download-report`, and role-based rows/dialogs.

## 2026-06-15 Business UI Batch/Scope Follow-up

- Added a read-only desktop IPC path for current-scope Lingxing report batch options. The renderer can now show real batch candidates with batch id, status, true report-file count, and imported metric-row count instead of forcing users to infer the data source from a manual batch text field.
- Updated the global operation scope bar to show USD, batch mode, real report availability, and imported metric rows. The UI now distinguishes automatic current-scope matching from manually specified batch ids.
- Fixed a stale global-scope display bug: after report import/download actions, data collection dispatches a `business-ui:data-updated` event so the top scope bar refreshes batch/import counts without requiring a route change.
- Added a real “download already-created reports” path. The collector now supports `downloadExistingLingxingReportBatch`, which navigates to the download center, waits for the current-scope ready row, and downloads it without clicking create. The desktop exposes this through `v1_5:reports:download-existing-lingxing-reports`, while “recreate and download” remains a separate action.
- Updated the data collection page so the three actions are no longer ambiguous: download already-created selected reports, recreate/download selected reports, and recreate/download all 8 reports.
- Renamed the collection status tile from “Lingxing task” to “data batch” so an automatically matched or composed batch is not shown as “not created” when true report files already exist.
- Added a persistent “latest action result” panel on the data collection page. After download/recreate/import, the operator can see the action type, batch ids, downloaded count, failed count, current imported metric rows, next step, and direct buttons for the action download directory and manifest.
- Corrected the post-action row-count wording to use the current imported metric row count from the collection audit instead of showing a misleading `0` rows after a download/recreate action that did not itself parse files.
- Expanded the ad quantification page with a visible “data source and quantification basis” section. It now shows true report count, imported metric rows, the summary basis, diagnostic count, source batch ids, and the reason for using search-term totals rather than duplicate-prone campaign/ad group/placement totals.
- Added a “post-quantification action” panel that routes operators back to data collection or forward to optimization suggestions while reminding them that ad changes still require approval, screenshots, and readback.
- Tightened the ad quantification empty state. When the current scope has no real report files/imported metrics, the page now shows only the data-source state and quantification blocker instead of rendering zero-value total spend/sales/ACOS KPI tiles that could be mistaken for a real quantified result.
- Tightened the dashboard empty state for the same reason. Before real report files and imported metrics exist, the operations summary now says `待导入指标` / `待量化`, and the duplicate total spend/sales/ACOS KPI tile group is hidden until the current scope is truly quantifiable.
- Expanded the optimization recommendation page with a visible priority and rule-standard section. It now surfaces the top recommendation by risk/spend, current rule thresholds, AI explanation count, pending count, and links to settings/approval so users can understand what to adjust first and where the threshold comes from.
- Recommendation detail now includes the active rule-threshold summary and explicitly states that the page only explains and sends recommendations to approval; it does not execute ad actions.
- Tightened recommendation detail readiness. The detail view now has a `送审前证据检查` section that shows whether source batch, source file, current value, recommended value, metric date, campaign/ad group, object, rule threshold, and AI/rule explanation are complete before the operator goes to approval. Source files are shown as full paths so users can trace the recommendation back to the actual Lingxing spreadsheet instead of an audit bundle.
- Tightened approval multi-object support. The approval precheck now requires current batch, source batch, source-batch match, metric date, source files, campaign, ad group, object type, object, action, current value, and recommended value before an action can move to readback. The approve/reject decision payload now carries portfolio, campaign, ad group, ASIN, entity type, entity name, source batch, metric date, source files, explanation source, and AI model so downstream readback can remain generic instead of tied to one sample ad.
- Tightened execution readback generic-target support. The readback approved-action list now shows portfolio, campaign, ad group, ASIN, entity type, object, action, and current/recommended values before loading a row. The readback precheck now requires entity type, and the exported evidence carries portfolio/ASIN/entityType in `target` plus entityType in `source`, preventing the page from silently behaving like a single paused-target sample.
- Fixed a stale keyword-to-Listing handoff gap. Listing optimization now validates incoming keyword opportunities against the current effective report batch (`manual batch` when selected, otherwise the latest current-scope completed batch). Old local handoff payloads from another batch are rejected with a visible reason and cleared from local storage before any Listing draft can use them.
- Tightened keyword-to-Listing traceability. Keyword opportunity detail now shows the full source spreadsheet path instead of a compact path, and the handoff payload carries portfolio, campaign, ad group, entity type, trigger keyword, clicks/orders, spend/sales, and source file. Listing optimization displays this ad-context block before draft generation so users can see exactly which advertising data produced the Listing keyword request.
- Tightened execution readback source validation. The readback precheck now treats recommendation source batch, metric date, source files, source current value, and source recommended value as required evidence fields. If the loaded recommendation source batch differs from the current effective batch, the page shows a source-batch mismatch and only allows a gap draft export, preventing stale approved actions from looking like final readback evidence.
- Tightened data-collection file visibility after download/import actions. The latest action result now falls back to the refreshed current-scope `fileAudit.downloadDir` and `fileAudit.manifestPath` when an IPC action returns files but omits batch path metadata, so users still get an `打开本次下载目录` path. The data-pipeline smoke now clicks a concrete `campaign.xlsx` file chip and the latest download directory button, proving the UI can open real report files instead of only audit bundles.
- Tightened data-collection path visibility. The `文件位置与用途` panel now shows full real report directory, collection manifest, and audit evidence paths instead of compact paths. The `真实原始报表文件` table now uses a full `文件路径` column, so users can see exactly where the Lingxing spreadsheet lives before importing or opening it.
- Tightened Settings and Delivery UX. Saving a newly typed AI API key now clears the password input after `saveSettings` succeeds and leaves only the masked configured state visible. The Delivery page now translates raw manifest status into business-facing `未就绪` / `可交付` labels and copies the same business-readable summary; raw `APP_*` fields remain test/manifest data, not primary UI text.
- Extended Delivery status sanitization beyond the main label. Gate messages, manifest action items, missing-action text, collection/quant blockers, export-bundle responses, and delivery exceptions now pass through a display sanitizer so raw `APP_READY`, `APP_NEEDS_WORK`, and standalone `READY` wording from verifier output or API responses does not leak back into the operator-facing page.
- Tightened Delivery report traceability. The delivery page now shows full original report spreadsheet paths in the `原始广告报表` evidence list, shows the real report directory and collection manifest in the top delivery metadata, and includes the report directory, collection manifest, and individual source file paths in the copied delivery summary. This makes it clear which files are real ad data versus final/readiness evidence.
- Kept the data gate conservative: only existing `.xlsx`, `.xls`, and `.csv` files count as real Lingxing ad reports; JSON, PNG, HTML, DOM snapshots, manifests, and audit bundles remain evidence only and do not unlock quantification.
- This iteration did not rerun full final readiness or rebuild the installer. It is an incremental UI/data-visibility fix, not a new final `APP_READY` claim.

## Verification

Latest local evidence:

- 2026-06-18 Ads UI locator guide pass: readback session packets now include `ads-ui-locator.md` alongside `operator-checklist.md` and `session-input.json`. The locator guide lists store, marketplace, ASIN, campaign, ad group, entity type/name, action, recommendation source values, source report row, and source evidence, and warns the operator to stop if the matching Ads UI row cannot be found or is not editable. The packaged-app-safe session verifier and CLI verifier now require this locator guide before reporting `SESSION_STRUCTURE_READY`. The current packet `output\codex-evidence\ad-readback-session-rec-4-current` was regenerated and structurally verified. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/main/ad-readback-session.test.ts scripts/prepare-ad-readback-session.test.mjs scripts/verify-ad-readback-session.test.mjs apps/desktop/src/renderer/pages/delivery-page.test.ts`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, `pnpm --filter @amazon-ai-ops/desktop run build:main`, `pnpm --filter @amazon-ai-ops/desktop run build:renderer`, `node --check scripts\prepare-ad-readback-session.js`, `node --check scripts\verify-ad-readback-session.js`, and `node scripts\smoke-business-ui-settings-delivery.js` with evidence `output\codex-evidence\business-ui-settings-delivery-smoke-1781720338101.json`.
- 2026-06-18 readback capture-readiness split: `verifyAdReadbackSession()` and `scripts/verify-ad-readback-session.js` now distinguish structural readiness from live evidence completion. `ready=true` / `SESSION_STRUCTURE_READY` means the folder, paths, checklist, locator guide, input JSON, fill script, and raw-report exclusion are safe; `captureReady=true` / `CAPTURE_READY` requires `session-input.json` to have no unresolved approval, before/after, execution, or readback fields. The `执行回读` and `交付验收` pages now display `结构通过，现场证据待填写` plus the unresolved field list instead of showing a generic pass state. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/main/ad-readback-session.test.ts apps/desktop/src/renderer/pages/readback-page.test.tsx apps/desktop/src/renderer/pages/delivery-page.test.ts scripts/verify-ad-readback-session.test.mjs`, `node --check scripts\verify-ad-readback-session.js`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, `pnpm --filter @amazon-ai-ops/desktop run build:main`, `pnpm --filter @amazon-ai-ops/desktop run build:renderer`, `node scripts\smoke-business-ui-settings-delivery.js` with evidence `output\codex-evidence\business-ui-settings-delivery-smoke-1781720941973.json`, and `node scripts\smoke-business-ui-ad-execution.js` with evidence `output\codex-evidence\business-ui-ad-execution-smoke-1781720941995.json`.
- 2026-06-18 readback missing-field localization pass: readback session verification now returns `captureMissingFields` with `{ field, label, group }`, so UI and CLI can show operator-facing missing evidence such as `审批/审批人`, `执行前/执行前 Ads UI live bid`, `执行后/执行后截图文件`, and `回读/刷新回读截图文件` instead of raw internal keys like `approverName` or `beforeValue`. The current rec-4 work package still reports `SESSION_STRUCTURE_READY` plus `CAPTURE_NEEDS_WORK`, now with Chinese grouped missing evidence. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/main/ad-readback-session.test.ts apps/desktop/src/renderer/pages/readback-page.test.tsx apps/desktop/src/renderer/pages/delivery-page.test.ts scripts/verify-ad-readback-session.test.mjs`, `node --check scripts\verify-ad-readback-session.js`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, `pnpm --filter @amazon-ai-ops/desktop run build:main`, `pnpm --filter @amazon-ai-ops/desktop run build:renderer`, `pnpm run verify:ad-readback-session -- output\codex-evidence\ad-readback-session-rec-4-current`, `node scripts\smoke-business-ui-settings-delivery.js` with evidence `output\codex-evidence\business-ui-settings-delivery-smoke-1781721320860.json`, and `node scripts\smoke-business-ui-ad-execution.js` with evidence `output\codex-evidence\business-ui-ad-execution-smoke-1781721320869.json`.
- 2026-06-18 readback session-input open action pass: after creating a readback work package, both `执行回读` and `交付验收` now expose `打开填写文件` for the generated `session-input.json`, in addition to opening the session folder, checklist, and Ads UI locator guide. This keeps the operator in the UI workflow instead of requiring manual folder navigation before filling live approval/before/after/readback fields. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/renderer/pages/readback-page.test.tsx apps/desktop/src/renderer/pages/delivery-page.test.ts`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, `pnpm --filter @amazon-ai-ops/desktop run build:renderer`, `node scripts\smoke-business-ui-settings-delivery.js` with evidence `output\codex-evidence\business-ui-settings-delivery-smoke-1781721554757.json`, and `node scripts\smoke-business-ui-ad-execution.js` with evidence `output\codex-evidence\business-ui-ad-execution-smoke-1781721556094.json`.
- 2026-06-18 readback session-input guide pass: readback session packets now include `session-input-guide.md`, a Chinese field-by-field guide explaining what to fill in `session-input.json`, where to place approval/before/after/readback files, and when to stop instead of executing the Ads UI action. `session-paths.json`, the packaged-app helper, CLI prepare script, CLI verifier, `执行回读`, and `交付验收` all expose or require this guide. The current rec-4 work package `output\codex-evidence\ad-readback-session-rec-4-current` was regenerated and `pnpm run verify:ad-readback-session -- output\codex-evidence\ad-readback-session-rec-4-current` reports `SESSION_STRUCTURE_READY` plus `CAPTURE_NEEDS_WORK` for missing live evidence. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/main/ad-readback-session.test.ts`, `pnpm exec vitest run scripts/prepare-ad-readback-session.test.mjs`, `pnpm exec vitest run scripts/verify-ad-readback-session.test.mjs`, `pnpm exec vitest run apps/desktop/src/renderer/pages/readback-page.test.tsx`, `pnpm exec vitest run apps/desktop/src/renderer/pages/delivery-page.test.ts`, `node --check scripts\prepare-ad-readback-session.js`, `node --check scripts\verify-ad-readback-session.js`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, `pnpm --filter @amazon-ai-ops/desktop run build:main`, `pnpm --filter @amazon-ai-ops/desktop run build:renderer`, `node scripts\smoke-business-ui-settings-delivery.js` with evidence `output\codex-evidence\business-ui-settings-delivery-smoke-1781722241760.json`, and `node scripts\smoke-business-ui-ad-execution.js` with evidence `output\codex-evidence\business-ui-ad-execution-smoke-1781722257389.json`.
- 2026-06-18 readback work-package localization pass: the current real ad readback candidate `output\codex-evidence\real-ad-execution-readback-candidate-rec-4-current.json` now has a refreshed working packet at `output\codex-evidence\ad-readback-session-rec-4-current`. The operator checklist and default `session-input.json` guidance are now Chinese, explicitly state that recommendation values are not live Ads UI values, and preserve the exact campaign/ad group/ASIN/entity/action/source report rows needed for manual capture. The actual packet passed `pnpm run verify:ad-readback-session -- output\codex-evidence\ad-readback-session-rec-4-current`. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/main/ad-readback-session.test.ts scripts/prepare-ad-readback-session.test.mjs scripts/verify-ad-readback-session.test.mjs scripts/fill-ad-readback-session.test.mjs apps/desktop/src/renderer/pages/delivery-page.test.ts`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, `pnpm --filter @amazon-ai-ops/desktop run build:main`, and `node --check scripts\prepare-ad-readback-session.js`.
- 2026-06-18 in-app final-readiness/readback-bridge pass: `交付验收` now has a `刷新最终验收` action that asks the desktop main process to write a current evidence-selection manifest and a final readiness diagnostic file, then displays the manifest path, final readiness path, status, and failed gate count in the UI. When the failed final-readiness gate is real ad readback, the same page now shows `广告回读补证` with direct actions to create a readback work package from the failed candidate evidence path, open the candidate, open the work package directory, open the operator checklist, check the session structure, generate the PASS-intended readback JSON/Markdown, run the readback evidence verifier, and refresh final readiness using that generated readback JSON. The refresh is conservative and still reports `APP_NEEDS_WORK` until a real operator fills approval, before/after Ads UI screenshots, live value change, and reload readback evidence. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/main/final-readiness-refresh.test.ts apps/desktop/src/renderer/pages/delivery-page.test.ts`, `node --check scripts\smoke-business-ui-settings-delivery.js`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, `pnpm --filter @amazon-ai-ops/desktop run build:main`, `pnpm --filter @amazon-ai-ops/desktop run build:preload`, `pnpm --filter @amazon-ai-ops/desktop run build:renderer`, and `node scripts\smoke-business-ui-settings-delivery.js` with evidence `output\codex-evidence\business-ui-settings-delivery-smoke-1781719610304.json`.
- 2026-06-17 delivery review-blocker visibility pass: the delivery readiness matrix now extracts concrete review-only recommendation blockers from AI lifecycle invalid reasons, AI evidence sufficiency blockers, AI insight invalid reasons, decision reasons, risk warnings, and quant-review reasons. The `建议与审批` delivery item no longer only says that recommendations need review; it also lists representative causes such as missing product ASIN, missing real report `sourceFile/sourceRow`, or unbound ad objects. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/renderer/delivery-readiness-matrix.test.ts`, `pnpm exec vitest run apps/desktop/src/renderer/delivery-readiness-matrix.test.ts apps/desktop/src/renderer/pages/approval-page.test.tsx apps/desktop/src/renderer/pages/recommendations-page.test.ts`, and `pnpm --filter @amazon-ai-ops/desktop run typecheck`.
- 2026-06-17 approval blocker detail pass: the approval-center blocker list now carries concrete AI lifecycle invalid reasons, such as missing product ASIN or unavailable evidence refs, instead of only the generic `AI 阶段判断需要人工复核` message. This makes the final pre-execution gate explain exactly why an AI-participated recommendation cannot be approved. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/renderer/pages/approval-page.test.tsx`, `pnpm exec vitest run apps/desktop/src/renderer/pages/approval-page.test.tsx apps/desktop/src/main/recommendation-approval-policy.test.ts apps/desktop/src/renderer/pages/recommendations-page.test.ts`, and `pnpm --filter @amazon-ai-ops/desktop run typecheck`.
- 2026-06-17 ad-quant blocker visibility pass: the ad quantification evidence summary now prioritizes evidence-sufficiency blockers when formal AI actions are blocked, so operators see concrete causes such as missing real report `sourceFile/sourceRow` or missing product ASIN instead of only generic lifecycle text. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/renderer/evidence-display.test.ts`, `pnpm exec vitest run apps/desktop/src/renderer/evidence-display.test.ts apps/desktop/src/renderer/pages/ad-quant-page.test.tsx apps/desktop/src/renderer/pages/recommendations-page.test.ts`, and `pnpm --filter @amazon-ai-ops/desktop run typecheck`.
- 2026-06-17 AI evidence sufficiency traceability pass: `assessAdEvidenceSufficiency()` now treats high-click/high-spend metric samples as formal-action usable only when the cited metric evidence has real report traceability (`.xlsx/.xls/.csv` source file plus positive source row) and product ASIN binding, or an explicit ASIN scope. This aligns the AI prompt/UI sample-strength signal with the later validator gate, so untraceable or product-unbound metrics are shown as low evidence and insight-only rather than "high" formal evidence. Incremental checks passed: `pnpm exec vitest run packages/ai-adapter/src/ad-strategy-diagnosis.test.ts`, `pnpm exec vitest run packages/ai-adapter/src/ad-strategy-diagnosis.test.ts apps/desktop/src/main/ad-ai-evidence-validator.test.ts apps/desktop/src/main/ad-recommendation-ai-context.test.ts`, `pnpm --filter @amazon-ai-ops/ai-adapter run typecheck`, and `pnpm --filter @amazon-ai-ops/desktop run typecheck`.
- 2026-06-17 AI stage/threshold product-binding gate pass: `ad-ai-evidence-validator` now requires metric/timeline/product-context evidence used for AI lifecycle-stage judgment and dynamic threshold suggestions to bind to a product ASIN, or to a current scope ASIN. In non-ASIN scopes, product-unbound metric evidence can no longer silently support formal stage/threshold conclusions; the UI-facing diagnosis is marked `requiresReview` with explicit missing-ASIN reasons. Incremental check passed: `pnpm exec vitest run apps/desktop/src/main/ad-ai-evidence-validator.test.ts`.
- 2026-06-17 incremental hardening: formal recommendation generation now requires the current scope to have the complete Lingxing 8-report real-file set before AI+rule recommendations are generated. The backend gate no longer allows "some xlsx plus DB rows" to bypass the data-collection page's readiness contract, and the optimization/quantification pages now show the same 8/8 blocker before the operator enters formal recommendation generation. Diagnosis-only AI stage analysis remains allowed on partial real-report coverage when the loaded metrics have traceable source files/source rows, so operators can inspect stage/threshold insights without creating formal recommendations. The renderer batch fallback was also aligned so pages accept `sourceBatchIds[0]` when `latestBatch` is not populated. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/main/recommendation-generation-gate.test.ts apps/desktop/src/renderer/recommendation-readiness.test.ts`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, `pnpm --filter @amazon-ai-ops/desktop run build:renderer`, and `pnpm run smoke:business-ui-current` with summary `output\codex-evidence\current-business-ui-smoke-1781665541305.json`.
- 2026-06-17 recommendation review-queue alignment: `优化建议` now loads both `pending` and `needs_review` recommendations into the visible suggestion pool, while formal approval counts remain separated from review-only rows. `交付验收` reads the same review queue and shows review-only recommendations as `需复核` instead of treating them as directly approvable. Incremental checks passed: `node --check scripts\smoke-business-ui-ad-execution.js`, `node --check scripts\smoke-business-ui-settings-delivery.js`, `node scripts\smoke-business-ui-ad-execution.js` with evidence `output\codex-evidence\business-ui-ad-execution-smoke-1781678899260.json`, and `node scripts\smoke-business-ui-settings-delivery.js` with evidence `output\codex-evidence\business-ui-settings-delivery-smoke-1781678920066.json`.
- 2026-06-17 dashboard recommendation queue alignment: `仪表盘` now reads both pending and `needs_review` recommendation queues, shows `待处理建议` with separate pending/review counts, routes formal pending rows to `审批中心`, and routes review-only rows back to `优化建议`. This removes the misleading homepage state where review-only AI/rule candidates were invisible. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/renderer/pages/dashboard-page.test.ts apps/desktop/src/renderer/delivery-readiness-matrix.test.ts`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, `pnpm --filter @amazon-ai-ops/desktop run build:renderer`, and `node scripts\smoke-business-ui-data-pipeline.js` with evidence `output\codex-evidence\business-ui-data-pipeline-smoke-1781679654078.json`.
- 2026-06-17 scheduler safety-copy alignment: `定时任务` no longer describes recommendation generation as only "pending approval" output. It now states that scheduled recommendation generation creates a local `待处理建议池`, and that review-required items are not auto-approved or written to Amazon Ads. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/renderer/pages/scheduler-page.test.ts apps/desktop/src/renderer/pages/dashboard-page.test.ts`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, `pnpm --filter @amazon-ai-ops/desktop run build:renderer`, and `node scripts\smoke-business-ui-shell.js` with evidence `output\codex-evidence\business-ui-shell-smoke-1781680227184.json`.
- 2026-06-17 report-coverage counting hardening: business pipeline readiness, batch dropdown counts, data-consistency export, and formal recommendation gates now count current-scope Lingxing coverage by unique expected `reportType`, not by raw spreadsheet file count. Duplicate files for the same report type can no longer make the UI or generation gate look like `8/8` coverage. Incremental checks passed: `pnpm exec vitest run apps/desktop/src/main/business-report-coverage.test.ts apps/desktop/src/main/recommendation-generation-gate.test.ts`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, and `pnpm --filter @amazon-ai-ops/desktop run build:main`.
- `pnpm --filter @amazon-ai-ops/desktop run typecheck`: passed after the current-scope batch option IPC and scope-bar refresh changes.
- `pnpm --filter @amazon-ai-ops/desktop run build:renderer`: passed after the same changes.
- `pnpm --filter @amazon-ai-ops/lingxing-report-collector test -- batch-runner.test.ts`: passed, 1 file / 11 tests. It now covers downloading already-created report rows without calling `createReport`.
- `node scripts\smoke-business-ui-data-pipeline.js`: passed; latest evidence `output\codex-evidence\business-ui-data-pipeline-smoke-1781498680271.json`. Visual check confirmed the top scope bar updates from `0` to `96` imported rows after import, the data collection page shows separate download/recreate actions, full real report directory/manifest/source file paths, the latest action result panel shows batch ids/current metric rows/download count/failure count/directory/manifest/next step, the empty dashboard/quantification states hide zero-value total spend/sales KPI tiles, the imported dashboard/quantification pages show data source, search-term total basis, batch id, diagnostic count, and post-quantification action guidance, and concrete xlsx file/open-directory actions reach `openReportPath`.
- `node scripts\smoke-business-ui-ad-execution.js`: passed; latest evidence `output\codex-evidence\business-ui-ad-execution-smoke-1781498116094.json`. It additionally covers recommendation detail `送审前证据检查`, approval multi-object precheck fields, approve/reject payload target metadata, readback generic target/source metadata, source-batch match status, full source file path visibility, current/recommended value display, stale source-batch mismatch messaging, required recommendation source fields, no visible readback command wall, and restoration to the normal export flow.
- `node scripts\smoke-business-ui-keyword-listing.js`: passed; latest evidence `output\codex-evidence\business-ui-keyword-listing-smoke-1781498338354.json`. It now includes a stale-batch negative case where an old `amazon-ai-ops-listing-handoff` payload is rejected in automatic batch mode, plus full source spreadsheet path visibility and keyword-to-Listing ad-context handoff before the normal draft flow continues.
- `node scripts\smoke-business-ui-shell.js`: passed; latest evidence `output\codex-evidence\business-ui-shell-smoke-1781498808393.json`. It rechecked the global shell after the latest path/traceability changes and found no visible old nested workbench, raw false ready state, RMB markers, or command-wall text.
- `node scripts\smoke-business-ui-settings-delivery.js`: passed; latest evidence `output\codex-evidence\business-ui-settings-delivery-smoke-1781485885713.json`.
- `node scripts\smoke-business-ui-settings-delivery.js`: passed; latest evidence `output\codex-evidence\business-ui-settings-delivery-smoke-1781498507495.json`. It covers AI key input clearing after save, masked configured status, business-readable Delivery readiness labels, no visible raw `APP_*` status in the active page, delivery display and copied summaries that include real report directory/manifest/source file paths, copied summaries that use `未就绪` rather than raw manifest status, and export-bundle messages that do not leak raw `APP_NEEDS_WORK` or `READY 交付包`.
- `git diff --check`: passed with only expected Windows LF-to-CRLF warnings.

- `pnpm vitest run packages/lingxing-report-collector/src/page-model-diagnostic.test.ts packages/lingxing-report-collector/src/diagnostic-evidence-gate.test.ts packages/lingxing-report-collector/src/page-model-enablement-audit.test.ts packages/lingxing-report-collector/src/collection-preflight.test.ts packages/lingxing-report-collector/src/batch-runner.test.ts apps/desktop/src/main/download-center-page-model-validation.test.ts`: passed, 6 test files / 52 passed.
- `pnpm -r run typecheck`: passed across workspace packages and desktop app.
- `pnpm test`: passed, 29 test files / 167 passed / 2 skipped.
- `pnpm run verify:ad-execution`: passed. It checks the desktop execution handler, recommendation execution policy, and action executor source for fail-closed behavior around success/verified/executed.
- `pnpm run verify:listing-draft-ux`: passed. It guards accepted-only draft generation, draft export fields, copy/open-export actions, current-text/draft/evidence display, and AI fallback visibility.
- `pnpm run verify:ai-settings-ux`: passed. It guards the AI test IPC, preload exposure, DeepSeek defaults, missing-key message, settings-page controls, and Listing AI exception fallback.
- `pnpm run smoke:listing-draft-renderer`: passed. It opens the built renderer through a local static server with mocked local IPC and verifies import -> opportunity -> suggestion -> accepted-only draft generation -> CSV draft export -> open latest export -> copy draft. Evidence: `output/codex-evidence/listing-draft-renderer-smoke-1780974664172.json`; screenshot: `output/codex-evidence/listing-draft-renderer-smoke-1780974663851.png`.
- `pnpm run verify:v15-enablement`: returns `NEEDS_WORK` as expected because the prior diagnostic evidence is stale and only `campaign` canary coverage exists; the remaining 7 report types are still missing.
- `pnpm run verify:v15-delivery`: first passed `AD_EXECUTION_FAIL_CLOSED`, then returned `NEEDS_WORK` as expected because `desktop-live-full-8-e2e-2026-06-08T02-58-34-833Z.json` contains a page/context/browser-closed error and lacks a full-8 batch id, so DB/manifest/file/audit cross-checking cannot prove delivery readiness.
- `pnpm --filter @amazon-ai-ops/desktop run build:renderer`: passed after the ad-execution fail-closed and Listing accepted-only draft changes.
- `pnpm run run:v15-installed-live -- --mode diagnostic --out output\codex-evidence\installed-live-diagnostic-probe-2026-06-08.json`: failed closed as expected on a fresh app launch because `browser:is-ready` was false and no `--login` credentials were provided. The evidence proves the runner does not proceed into live Lingxing actions without an explicit browser session.
- Renderer live-action protection smoke: `output/codex-evidence/v15-workbench-live-action-confirm-smoke-2026-06-08.png`; Playwright verified that canary is blocked before current-scope diagnostic evidence, diagnostic success then shows a real-report confirmation dialog, cancelling that dialog does not call `runLingxingCanaryReport`, and console errors are empty.
- Renderer operator UX guard smoke: `output/codex-evidence/v15-operator-ux-guard-smoke-2026-06-08.png`; Playwright verified that cancelling ad execution confirmation does not call `executeRecommendation`, a successful mocked canary exposes `本次采集结果` in the normal operator area, Listing local-only notice is visible, and console errors are empty.
- Renderer keyword-opportunity UX smoke: `output/codex-evidence/v15-keyword-opportunity-ux-smoke-2026-06-08.png`; Playwright verified summary counts, structured evidence columns, next-step CTA, risk filtering, and no console errors.
- `pnpm run verify:v15-canary -- output\codex-evidence\installed-canary-campaign-2026-06-08T06-38Z.json`: passed, including DB diagnostic id `8`, completed batch `batch_20260608063734381_rhuubk`, one downloaded `campaign` file, manifest parity, path containment, size parity, report keyword, and date-token checks.
- `pnpm run verify:v15-canary -- output\codex-evidence\installed-canary-ad_group-2026-06-09.json`: passed, including DB diagnostic id `10`, completed batch `batch_20260609031748302_dzhx8o`, one downloaded `ad_group` file, manifest parity, path containment, size parity, report keyword, and date-token checks.
- `pnpm run verify:v15-canary -- output\codex-evidence\installed-canary-placement-2026-06-09.json`: passed, including DB diagnostic id `11`, completed batch `batch_20260609032339827_tdk798`, one downloaded `placement` file, manifest parity, path containment, size parity, report keyword, and date-token checks.
- `pnpm run verify:v15-canary -- output\codex-evidence\desktop-live-canary-campaign-2026-06-08T02-56-32-125Z.json`: failed as expected because the old evidence contains a top-level Playwright `Too many arguments` error and lacks the current safety/request fields.
- Targeted scope-regression run: `pnpm test -- acceptance-audit.test.ts diagnostic-evidence-gate.test.ts batch-runner.test.ts collection-preflight-export.test.ts page-model-enablement-audit-export.test.ts` passed, 5 test files / 53 passed.
- After packaging rebuilt `better-sqlite3` for Electron ABI `119`, the local Node-side binary was restored with `D:\PY\python.exe`, VS BuildTools/MSBuild, and `PlatformToolset=v143`; `pnpm --filter @amazon-ai-ops/local-db exec node -e "require('better-sqlite3')"` passed, followed by `pnpm test -- db.test.ts acceptance-audit.test.ts` passing, 2 files / 32 passed.
- Deprecated scheduler cleanup targeted checks: `pnpm --filter @amazon-ai-ops/scheduler run typecheck` and `pnpm --filter @amazon-ai-ops/desktop run typecheck` both passed after removing `daily_report_download`.
- Renderer layout QA: `output/codex-evidence/renderer-v15-diagnose-layout-qa-1780561270634.json` shows `elementFromPoint` at the `验证页面` button center resolves to the button and that the mocked UI diagnostic success message appears.
- `pnpm test -- download-center-page-model-validation.test.ts page-model-diagnostic.test.ts`: passed after accepting `ads.lingxing.com` and updating the bundled page model.
- `pnpm --filter @amazon-ai-ops/desktop run build:win`: passed and generated `apps/desktop/release/AmazonAIOpsAgent-1.5.0.exe`.
- Historical installer evidence: size `89607384` bytes, SHA-256 `80EB3E1A7AE8809C852FA99427512E32384732451BF74CF8DEFD879A0A77623E`, last write time `2026-06-08 14:24:29`.
- Installed-app smoke evidence: the previously installed 2026-06-03 app under `C:\Users\wz\AppData\Local\Programs\@amazon-ai-opsdesktop` still contained the old workspace dependency layout and opened a main-process JavaScript error. The old install directory was removed without touching `Roaming\@amazon-ai-ops` user data, then the current installer was rerun. The installed app now has last write time `2026-06-08 14:24:26`, packaged dependencies include `playwright`, and launching through the system app id opens the v1.5.0 login screen with the ERP -> Ads entry guidance.
- Installed live login/diagnostic/preflight evidence: from the installed app, account `SHC001` reached `ERP 已复用登录态；Ads 已进入：仪表盘`; read-only diagnostic id `8` matched `2026-05-01` to `2026-05-25` / `FT-US-US` / `US`, exported `download_center_diagnostic_8_1780899435257`, and exported `lingxing_collection_preflight_2026-05-01_2026-05-25_1780899413099`. The preflight has `browser_session_ready: passed`, `diagnostic_evidence_ready: passed`, and `page_model_ready: blocked` only because `requiresManualVerification: true`.
- Packaged executable smoke test: `apps/desktop/release/win-unpacked/AmazonAIOpsAgent.exe` remained alive after 8 seconds, then was stopped.
- Real desktop IPC diagnostic evidence: diagnostic id `4`, `ready: true`, `missingRequiredSelectors: []`; create-page setup selectors were all found and usable with match count `1`. `readyReportSelector` and `downloadButton` remain unproven because no generated report row for the unique generated name exists yet.
- `pnpm --filter @amazon-ai-ops/desktop test -- collection-preflight-export.test.ts`: passed after extracting the preflight evidence bundle writer, adding the review checklist, and adding `preflight-bundle-index.json`.
- `pnpm --filter @amazon-ai-ops/desktop test -- download-center-diagnostic-evidence-files.test.ts`: passed after adding the preflight diagnostic evidence bundle helper.
- `git diff --check`: passed with only expected Windows LF-to-CRLF warnings.
- `pnpm build`: previously passed after the preflight evidence-bundle export change and generated `apps/desktop/release/AmazonAIOpsAgent-1.5.0.exe`; the latest installer evidence above is from `build:win`.
- A re-run after a transient NSIS-stage nonzero exit also passed; the retry reached `building block map` and exited successfully.
- Earlier packaged executable smoke test: process started and remained alive after 8 seconds.
- Latest post-smoke process check found no remaining `AmazonAIOpsAgent`, `7za`, or `electron` process.

Known build warnings:

- Vite CJS Node API deprecation warning.
- electron-builder warns `asar: false`.
- electron-builder warns `cannot find prebuild-install` during native dependency rebuild, but the build continued and completed NSIS packaging.

## Final Release Gates

Report collection is no longer the active blocker for the current scope. Real ERP -> Ads navigation, enabled download-center page model, 8/8 canaries, full 8-report generation/download, DB/manifest/file/audit checks, and xlsx metric reconciliation are verified for `2026-05-01` to `2026-05-25` / `FT-US-US` / `US`.

Manifest-driven `APP_READY` evidence exists for the 2026-06-30 high-fidelity Windows UI / AI output-contract refresh, encrypted credential save, login submit `aria-busy`/spinner and stable credential status feedback, AI/import feedback refresh, OperatorTaskPanel loading feedback, approval decision stamp and queue exit feedback, ad-quant metric focus filtering with inactive-chip dimming, ad-quant AI-running radar feedback, recommendation status-bucket filtering, recommendation batch approval handoff, recommendation direct action-button busy feedback, delivery readback work-package action-button busy feedback, readback evidence workflow action-button busy feedback, keyword-opportunity sortable header, filter-axis feedback, and refresh action-button busy feedback, dashboard primary action jump feedback, data-import validation SHA-256 checksum column/sortable header/import-time read-only lock/direct action-button busy feedback/`aria-live`/200ms blur/sweep plus row fade-in feedback, StateLightGrid hover +2px lift feedback, ProgressiveDetails and direct details disclosure summary feedback, readback time/value contract visualization, readback safety checkbox confirmation feedback, Lingxing report date-picker fix, OperatorTaskPanel loading micro-response, operation-scope first-screen confirmation, page-level range FormTable confirmation, ScopeBar field-level confirmation feedback, and non-layout-shifting ScopeBar editor popover, product-config direct save/bulk action-button busy feedback, product-config bulk target ACOS apply, inline autosave, keyboard nudge, and live target health feedback, settings threshold field-level validation feedback, settings rule-save action-button busy feedback, scheduler first-screen local task feedback plus scheduler controller/row action-button busy feedback, data-collection monitor drawer with Canvas browser preview, data-collection action-button busy feedback, delivery readback repair handoff with field-level red repair rings, Listing local action-button busy feedback, Listing keyword heatmap matrix plus strict-contained keyword rail cells and draft diff/skeleton/limit feedback, canonical daily metric explanation, product-level workbench refresh, and product-management save action-button busy feedback at `output\codex-evidence\final-readiness-20260630142700.json`, with evidence manifest `output\codex-evidence\v15-final-readiness-evidence-manifest-20260630142700.json`, package launch smoke `output\codex-evidence\package-launch-smoke-1782800794443.json`, and READY bundle `output\delivery-bundles\v15-delivery-bundle-20260630142700-ready`. The historical `output\codex-evidence\final-readiness-2026-06-18-portable-fix.json` remains a baseline only. The 2026-06-16 business-flow closeout still verifies the AppData data chain for batch `batch_20260612020905629_gkchz1`: 8/8 real report files, 2416 imported metric rows, canonical `user_search_term` spend USD `617.87`, and 19 orders.

- Real DeepSeek provider success: closed by `output\codex-evidence\deepseek-live-1781066552798.json`.
- Real ad strategy diagnosis: closed by `output\codex-evidence\ad-strategy-live-1782358641101.json`, which validates the OpenAI-compatible saved settings path, 8192 token floor, concrete JSON output contract, schema validation, and no-fallback `source=ai` result on the current DB scope.
- Real Lingxing Listing read: read-only extraction IPC/UI, URL open/read flow, detail-entry candidate evidence, unit tests, UI smoke, DB migration, a source-app real list-page probe, and a source-app real detail/edit-page probe are implemented. Current evidence `output\codex-evidence\source-listing-read-detail-probe-2026-06-09-merged-detail.json` passes full Listing readiness with ASIN/title/bullets/backend search terms.
- Listing AI rewrite: closed by `output\codex-evidence\installed-listing-ai-draft-user-key-2026-06-10.json`.
- Ad recommendation AI explanation: closed by `output\codex-evidence\installed-ad-ai-explanation-packaged-final-20260617.json`.
- Real ad write execution/readback: closed under the current stricter contract by `output\codex-evidence\real-ad-execution-readback-candidate-rec-4-current-pass.json`, which passed `verify:ad-readback`. It proves one approved paused FT-US keyword `door lock` live bid decrease `1.30 -> 1.17`; the source recommendation `1.63 -> 1.46` was preserved as report-derived context and intentionally not written because it would have increased the current live bid.
- 2026-06-25 product-level workbench and product maintenance refresh: `产品管理` now supports inline product base/cost/threshold editing, saving updates `scope.asin`, and the page displays per-product daily ad metrics from the DB before AI work. The dashboard now gates ASIN-specific work behind explicit product selection instead of silently showing the first product ledger. AI recommendation/data-gate errors now map remote IPC blockers to product/import actions without exposing raw `source_file` or `remote method` text.
- 2026-06-30 product-management save action-button busy pass: the direct `保存产品信息` button now uses `productManagementActionButtonView`, so the active save action switches to `保存中...`, shows the shared spinner, exposes `aria-busy=true`, carries `button-loading`, and disables duplicate saves. `打开完整配置` is locked while saving but remains a plain disabled peer without spinner or `aria-busy`, so operators can tell exactly which action is running. This remains local product configuration feedback only and does not generate recommendations, approve actions, or write Amazon Ads. Incremental checks passed: red/green focused product-management test, desktop typecheck, renderer build, `pnpm run verify:ad-execution`, `pnpm run smoke:business-ui-current`, Windows build, package launch smoke, manifest-driven final-readiness, READY bundle export, and READY safety.
- 2026-06-30 product-config direct action-button busy pass: the bottom `保存完整产品配置` and bulk `应用到 X 个产品` actions now use `productConfigActionButtonView`. Active actions switch to `保存中...` or `批量应用中...`, show the shared spinner, expose `aria-busy=true`, carry `button-loading`, and disable duplicate submissions; navigation peers (`补充运营事件`, `进入广告量化`) lock as plain disabled buttons while the full-form save is pending. This remains local product-target maintenance feedback only and does not generate recommendations, approve actions, or write Amazon Ads. Incremental checks passed: red/green focused product-config test, desktop typecheck, renderer build, `pnpm run verify:ad-execution`, `pnpm run smoke:business-ui-current`, Windows build, package launch smoke, manifest-driven final-readiness, READY bundle export, and READY safety.
- 2026-06-30 settings rule-save action-button busy pass: the direct `保存广告阈值` action now uses `settingsRuleActionButtonView`. While the local threshold save is pending, the active button switches to `保存中...`, shows the shared spinner, exposes `aria-busy=true`, carries `button-loading`, and disables duplicate submissions; environments without `saveRuleConfig` remain plain disabled without spinner/`aria-busy`, so unavailable is not confused with running. This remains local rule-configuration feedback only and does not generate recommendations, approve actions, or write Amazon Ads. Incremental checks passed: red/green focused settings-page test, desktop typecheck, renderer build, `pnpm run verify:ad-execution`, `pnpm run smoke:business-ui-current`, Windows build, package launch smoke `output\codex-evidence\package-launch-smoke-1782800794443.json`, manifest-driven final-readiness `output\codex-evidence\final-readiness-20260630142700.json`, READY bundle export, and READY safety.
- 2026-06-30 operation-events inline save action-button busy pass: the bottom `保存到上下文` action now uses `operationEventInlineSaveButtonView`. The running local save switches to `保存中...`, shows spinner, exposes `aria-busy=true`, carries `button-loading`, and locks duplicate submissions; incomplete drafts remain plain disabled without running copy. This remains local operation-context feedback only and does not generate recommendations, approve actions, mutate Listing, or write Amazon Ads. Incremental checks passed: red/green focused operation-events test, desktop typecheck, renderer build, `pnpm run verify:ad-execution`, `pnpm run smoke:business-ui-current`, Windows build, package launch smoke `output\codex-evidence\package-launch-smoke-1782800794443.json`, manifest-driven final-readiness `output\codex-evidence\final-readiness-20260630142700.json`, READY bundle export, and READY safety.
- 2026-06-25 approval decision stamp feedback pass: `审批中心` now shows first-screen `SEALING` / `PASSED` / `REJECTED` / `BLOCKED` stamp feedback for approve, approved, reject, rejected, and blocked decision paths. Decision buttons disable while the IPC is pending, exact operator-facing errors stay in the page message area to avoid duplicate strict text, and the stamp remains a local approval/rejection signal rather than Ads execution proof. Incremental checks passed: `pnpm exec vitest run apps\desktop\src\renderer\pages\approval-page.test.tsx apps\desktop\src\renderer\components\operator-task-panel.test.tsx --reporter=basic`, desktop typecheck, renderer build, `node scripts\smoke-business-ui-ad-execution.js`, `pnpm run smoke:business-ui-current`, `pnpm run verify:ad-execution`, Windows build, package launch smoke, manifest-driven final-readiness, READY bundle export, and READY safety.
- 2026-06-26 approval queue exit feedback pass: `审批中心` keeps the stamp feedback and now gives visible queue closure after a successful approve or reject. The decided row gets a tone-specific exit class, slides out for 180ms, is removed from the local visible table, and then the page reloads the real approval queue. This is only approval-decision feedback; Ads execution still requires separate readback proof. Incremental checks passed: `pnpm exec vitest run apps\desktop\src\renderer\pages\approval-page.test.tsx --reporter=basic`, desktop typecheck, `pnpm run smoke:business-ui-current` with `output\codex-evidence\current-business-ui-smoke-1782787351742.json`, Windows build, package launch smoke `output\codex-evidence\package-launch-smoke-1782800794443.json`, manifest-driven final-readiness `output\codex-evidence\final-readiness-20260630142700.json`, READY bundle export, and READY safety.
- 2026-06-25 ad-quant metric focus filter pass plus 2026-06-26 inactive dimming refresh: `广告量化` first-screen metric chips behave as controlled focus filters for all objects, no-order waste, high ACOS, ordered objects, scale-ready objects, and review-needed objects. The selected focus updates the visible/total counts for the entity diagnostic table and object timeline, uses the same rule thresholds for high-ACOS/no-order waste classification, and states in an `aria-live` line that focus only changes the page view and does not mutate recommendations, approvals, or Ads state. The latest refresh dims non-active metric chips to 60% opacity when focus is not `全部对象`, while hover/focus restores clarity and keeps the chip clickable. Incremental checks passed: `pnpm exec vitest run apps\desktop\src\renderer\components\tag-metric-group.test.tsx apps\desktop\src\renderer\pages\ad-quant-page.test.tsx --reporter=basic`, desktop typecheck, renderer build, ad-execution smoke, `pnpm run smoke:business-ui-current`, `pnpm run verify:ad-execution`, Windows build, package launch smoke, manifest-driven final-readiness, READY bundle export, and READY safety.
- 2026-06-25 recommendation batch approval handoff pass: `优化建议` now lets operators select only formal-approval-ready recommendations, shows a compact `X/N` batch toolbar, and hands those IDs to `审批中心` as a local session hint. `审批中心` displays `批量送审`, selects the first matching queue row, and still reloads the real approval queue with the same blockers, so batch handoff cannot approve review-only rows or execute Ads. Incremental checks passed: `pnpm exec vitest run apps\desktop\src\renderer\pages\recommendations-page.test.ts apps\desktop\src\renderer\pages\approval-page.test.tsx --reporter=basic`, desktop typecheck, renderer build, ad-execution smoke, `pnpm run smoke:business-ui-current`, `pnpm run verify:ad-execution`, Windows build, package launch smoke, manifest-driven final-readiness, READY bundle export, and READY safety.
- 2026-06-25 recommendation status-bucket filtering pass: `优化建议` first-screen buckets now filter the main table across all suggestions, evidence blockers, manual-review rows, and formal-approval-ready rows. The filter uses the same approval policy helpers as checkbox/send-to-approval, updates the visible/total table count, and clears selection when switching buckets to prevent hidden-row batch submission. Incremental checks passed: `pnpm exec vitest run apps\desktop\src\renderer\pages\recommendations-page.test.ts --reporter=basic`, desktop typecheck, renderer build, ad-execution smoke, `pnpm run smoke:business-ui-current`, `pnpm run verify:ad-execution`, Windows build, package launch smoke, manifest-driven final-readiness, READY bundle export, and READY safety.
- 2026-06-30 recommendation direct action-button busy pass: folded `刷新建议`, workflow-step `生成解释`, and empty-state `生成优化建议` now use `recommendationActionButtonView`. Active recommendation-page actions switch to `刷新中...` or `生成中...`, show the shared spinner, expose `aria-busy=true`, carry `button-loading`, and disable duplicate clicks; missing real scope or pipeline loading stays a plain unavailable state. This remains local recommendation generation/refresh feedback only and does not bypass source-report gates, approve rows, or write Amazon Ads. Incremental checks passed: red/green focused recommendations test, desktop typecheck, renderer build, `pnpm run verify:ad-execution`, `pnpm run smoke:business-ui-current`, Windows build, package launch smoke `output\codex-evidence\package-launch-smoke-1782800794443.json`, manifest-driven final-readiness `output\codex-evidence\final-readiness-20260630142700.json`, READY bundle export, and READY safety.
- 2026-06-26 keyword-opportunity sortable header and filter-axis pass: `VirtualDataTable` now supports optional sortable headers with `aria-sort`, a header button, and a 150ms rotating arrow on the active column. `关键词机会` sorts filtered rows locally without mutating source rows, defaults to high opportunity levels first, uses ascending order for text columns and descending order for numeric columns when first selected, updates a fixed `aria-live` result line for filter/sort changes, and applies a 100ms vertical crossfade to the table shell while keeping the virtualized long-table path intact. Incremental checks passed: `pnpm exec vitest run apps\desktop\src\renderer\components\virtual-data-table.test.tsx apps\desktop\src\renderer\pages\keyword-opportunities-page.test.ts --reporter=basic`, desktop typecheck, `pnpm run smoke:business-ui-current`, `pnpm run verify:ad-execution`, Windows build, package launch smoke, manifest-driven final-readiness, READY bundle export, and READY safety.
- 2026-06-26 Listing draft diff/skeleton/limit feedback pass: `Listing 优化` now turns current-vs-draft changes into per-section diff chips inside the heatmap matrix: removed original words are red strikethrough chips, added draft words are green chips, draft generation overlays a skeleton wave on the draft pane without shifting layout, and title/bullet counters flash red when over limit. Incremental checks passed: `pnpm exec vitest run apps\desktop\src\renderer\pages\listing-optimization-page.test.ts --reporter=basic`, focused keyword/listing/virtual-table tests, desktop typecheck, `pnpm run smoke:business-ui-current`, `pnpm run verify:ad-execution`, Windows build, package launch smoke, manifest-driven final-readiness, READY bundle export, and READY safety.
- 2026-06-30 high-fidelity UI / AI contract source validation, encrypted credential save, login submit `aria-busy`/spinner and stable credential status feedback, AI/import feedback, Lingxing date-picker fix, canonical daily metric explanation, approval queue exit feedback, OperatorTaskPanel loading micro-response, StateLightGrid hover lift feedback, MicroStepper status indicator feedback, operation-scope first-screen confirmation, page-level range FormTable confirmation, ScopeBar field-level confirmation feedback, non-layout-shifting ScopeBar editor popover, shared business-data pipeline 300ms scope debounce, product-config direct save/bulk action-button busy feedback, product-config bulk target ACOS apply, inline autosave, keyboard nudge, and live target health feedback, settings threshold field-level validation feedback, settings rule-save action-button busy feedback, scheduler first-screen local task feedback plus scheduler controller/row action-button busy feedback, operation-event optimistic clear/rebound/failure draft restore, data-collection monitor drawer with Canvas browser preview, data-collection action-button busy feedback, delivery readback repair handoff with field-level red repair rings, delivery readback work-package action-button busy feedback, readback evidence workflow action-button busy feedback, heavy table virtualization, actual-row zebra striping, ProgressiveDetails and direct details disclosure summary feedback, keyword-opportunity sortable header, filter-axis feedback, and refresh action-button busy feedback, recommendation direct action-button busy feedback, dashboard primary action jump feedback, data-import validation SHA-256 checksum column/sortable header/import-time read-only lock/direct action-button busy feedback/`aria-live`/200ms blur/sweep plus row fade-in feedback, Listing local action-button busy feedback, Listing keyword heatmap matrix plus strict-contained keyword rail cells and draft diff/skeleton/limit feedback, ad-quant metric focus filters with inactive dimming, readback time/value contract visualization, readback safety checkbox confirmation feedback, product-management save action-button busy feedback, and product-level package refresh: focused renderer/package/AI/product/data-collection-monitor/readback-capture/readback-contract/virtual-table/listing-heatmap/listing-action/scheduler/business-data/recommendation/delivery/readback tests, the new date-range commit regression test, desktop typecheck, Windows build, current business UI smoke, package hash recording, package launch smoke, manifest-driven final-readiness, READY bundle export, and `verify:v15-ready-safety` are complete for the refreshed package. The installer SHA-256 is `DA48A0F7AFE21F59A0264A31317F641C5C15F92FA8DCB47A369428E3B3994152`; the portable/no-install SHA-256 is `07410BD3CDB034EBA9F14B7A12E2FAA68DC5EAD30457500109C6E20EC665A182`.

## Next Work

1. Prepare a clean release commit excluding `.codex/config.toml`, `output/`, `storage/`, browser profiles, raw downloaded reports, AppData DB files, installer binaries, and secrets. Keep app-side batch ad execution fail-closed; each future ad action must still be manually scoped, approved, screenshotted, executed, and independently read back until a generalized executor can prove the same per-action evidence automatically.
2. For any future scope, product, package rebuild, or ad action, rerun manifest-driven final-readiness, export a fresh READY bundle, and rerun `verify:v15-ready-safety` before claiming that new state is delivered.
