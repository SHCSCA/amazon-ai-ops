# v1.5 Acceptance Matrix

| Requirement | Current Evidence | Status |
|---|---|---|
| Search Term Report import | `parseKeywordMetrics`, renderer import flow, `keyword_metrics` persistence, tests | Implemented |
| SQP report import | `parseKeywordMetrics` source mode and resource mapping support | Implemented |
| Keyword performance report import | `keyword_report` source mode and Lingxing mapping support | Implemented |
| Duplicate keyword report import policy | Desktop duplicate policy selector for merge/overwrite/skip; backend duplicate key uses source type plus canonical file path; merge skips already-imported source rows; skip refreshes the UI without writing DB rows; old duplicates are deduplicated before unique indexes are added; opportunity refresh upserts by ASIN plus normalized keyword without resetting accepted/ignored status | Implemented structurally |
| Keyword report parse failure handling | `parseKeywordMetricsWithDiagnostics` fails when critical keyword column is missing, tracks invalid rows, fails when invalid rows exceed 5%, preserves optional-field warnings, and desktop import surfaces diagnostics | Implemented structurally |
| Keyword parse error-row export | `keywordMetricDiagnosticsToCsv`, desktop `exportKeywordDiagnostics` IPC/UI button, formula-safe CSV tests | Implemented |
| Field mappings | Packaged `resources/field-mappings`, parser `fieldMappingsDir`, tests | Implemented |
| Keyword normalization | `keyword-opportunity` aggregation and scoring tests | Implemented |
| Keyword opportunity levels | `scoreKeywordOpportunity`, UI opportunity table | Implemented |
| Listing manual entry | `Listing 优化` uses manual entry as the primary path and now presents it as a table-like editor grouped by `基础信息`, `标题`, `五点`, and `详情与搜索词`, with field status tags and `保存为新版本` writing local version history. Lingxing read-only fill remains auxiliary and does not submit Amazon/Lingxing changes. Focused coverage: `apps/desktop/src/renderer/pages/listing-optimization-page.test.ts` and `scripts/smoke-business-ui-keyword-listing.js`; refreshed package coverage is included in `output\codex-evidence\final-readiness-20260624090830.json`. | Verified in refreshed APP_READY package |
| Listing Excel import | `parseListingContent`, `importListingContent` IPC, UI button, tests | Implemented |
| Listing coverage analysis | `analyzeKeywordCoverage`, persisted `keyword_coverage` | Implemented |
| Listing suggestions | `buildListingSuggestions`, risk blocking, persisted suggestions, tests | Implemented |
| Accept / ignore suggestions | `updateListingSuggestionStatus` IPC and UI controls | Implemented |
| Excel / CSV / Markdown export | `suggestionsToXlsxBuffer`, `suggestionsToCsv`, `suggestionsToMarkdown`, tests | Implemented |
| AI Listing draft | `generateListingDrafts` IPC uses only `accepted` suggestions, records `aiFallbackReason` when falling back to rule drafts, and exposes CSV/XLSX/Markdown draft exports with current text, draft text, source, fallback reason, evidence, and risk fields. The renderer shows current text, drafted text, evidence, source/risk, AI fallback status, copy-draft action, and latest-export opening; generation completion now states how many drafts came from AI versus rule fallback and includes fallback reasons when AI output was unusable. Tests cover accepted-only draft generation, formula-safe draft exports, and fallback-vs-AI result messaging; `pnpm run verify:listing-draft-ux` guards the renderer/export/regression surface; `pnpm run smoke:listing-draft-renderer` clicks through import -> suggestion -> accepted-only draft -> export -> open latest export -> copy draft with mocked local IPC. Real AI evidence `output/codex-evidence/installed-listing-ai-draft-user-key-2026-06-10.json` passed `pnpm run verify:listing-ai-draft`, proving `source=ai`, no fallback, AI reason, settings restore, and no secret leakage. | Verified real AI draft evidence; fallback messaging covered structurally |
| AI output contract UX | Settings renders fixed contract tags `广告诊断 v1`, `广告解释 v1`, `Listing 草案 v1`, and `异常回退规则` from renderer metadata. Primary copy says the system owns the AI output contract and pages only read validated fields; the persona field is labeled as expression style and does not control fixed fields. Raw contract IDs are not visible in the operator settings surface; advanced settings show `系统固定` labels instead. Focused coverage: `apps/desktop/src/renderer/ai-output-contracts.test.ts`, `apps/desktop/src/renderer/components/tag-metric-group.test.tsx`, `apps/desktop/src/renderer/pages/settings-page.test.ts`, and refreshed package/final-readiness evidence `output\codex-evidence\final-readiness-20260624090830.json`. | Verified in refreshed APP_READY package |
| AI strategy diagnosis contract | Structured AI settings normalize legacy low `aiMaxTokens` values to the 8192 token floor before calls are made, preventing old settings such as `700` from truncating diagnosis output. `AdStrategyDiagnoser` filters missing or invented `evidenceRefs` and deterministically fills lifecycle, threshold, and formal candidate references from real current `evidencePack` items only; formal ad actions still require traceable metric source files/rows and pass the existing validator before entering recommendations. Focused coverage: `apps/desktop/src/main/ai-settings-normalization.test.ts`, `packages/ai-adapter/src/ad-strategy-diagnosis.test.ts`, `apps/desktop/src/main/ad-ai-evidence-validator.test.ts`, `apps/desktop/src/main/ad-recommendation-ai-context.test.ts`, `apps/desktop/src/renderer/evidence-display.test.ts`, `apps/desktop/src/renderer/delivery-readiness-matrix.test.ts`, and `apps/desktop/src/renderer/pages/ad-quant-page.test.tsx`. | Verified structurally and in refreshed UI smoke |
| Version trace | `appVersion` in report manifest, persisted Lingxing report batches, suggestions, drafts, export tests, and Lingxing acceptance audit manifest-version presence/consistency checks | Implemented |
| Packaged runtime startup | Desktop package explicitly includes `better-sqlite3` runtime loader dependencies `bindings` and `file-uri-to-path`; packaged smoke log reaches `sqlite-ready`, `ipc-ready`, and `window-created`, and current AppData DB migration adds store/site columns for diagnostics and batches | Implemented |
| Installed runtime startup | Current 2026-06-24 UX/AI-contract build produced `apps/desktop/release/AmazonAIOpsAgent-1.5.0.exe`, SHA-256 `7F91C2A3C2812DDDFDE76FB09E5E508D2B6F6FAFDF94F719C30D293779EBF788`, size `83054292` bytes, last write `2026-06-24 09:07:37`; the no-install portable executable is `apps/desktop/release/AmazonAIOpsAgent-1.5.0-portable.exe`, SHA-256 `C8531FB7744551E5A57615AE8FE58E90904E9D0F0D918E2CEB15F2A15EC3218C`, size `82888546` bytes, last write `2026-06-24 09:07:38`. `pnpm run smoke:package-launch` passed with `output\codex-evidence\package-launch-smoke-1782263264718.json`, proving `win-unpacked` reached `ipc-ready/window-created` and portable extracted to Temp and launched its app child process. The refreshed final-readiness `output\codex-evidence\final-readiness-20260624090830.json` includes both `Release package hash` and `Package launch smoke` gates. | Verified current package build, launch smoke, and final-readiness package gates |
| 8 Lingxing report definitions | `LINGXING_AD_REPORTS`, collector batch test covers 8 reports | Implemented |
| Batch manifest | `writeManifest`, batch runner test | Implemented |
| Automatic failed report retry | Collector retries each failed report up to 2 times, records attempt errors and final status in manifest/tests | Implemented structurally |
| Report status poller semantics | `status-poller.ts` classifies pending/created/generating/ready/failed/expired/skipped/unknown text, exposes structured phases, fails fast on terminal failure, and tests Chinese/English/traditional/ambiguous tokens | Implemented structurally |
| Downloaded file date-range verification | `verifyDownloadedFile` and `analyzeFilenameDateRange` check file type, size, report keyword, valid date inputs, selected start/end date tokens in filename, and report missing-token details in batch errors/audit output; tests cover wrong-date, compact-date, missing-token, and invalid-date failure with preserved file-size evidence | Implemented structurally; requires real Lingxing filename samples before unattended enablement |
| Failure evidence after retries | Failure screenshot/DOM snapshot hooks, Playwright Trace start/stop on final attempt, SQLite persistence, UI evidence links, and acceptance-audit export copies/indexes failed-report screenshot/DOM/Trace evidence | Implemented structurally: live trace content still needs real Lingxing failure-path verification |
| Single failed report retry | `RunBatchOptions.reportTypes`, retry IPC/preload/UI row action, retry batch tests | Implemented structurally: manual single-report retry exists; real success still depends on verified live download automation |
| Single-report canary collection | v1.5 report-collection UI exposes `单报表验证` with all 8 Lingxing report types and uses a dedicated `run-lingxing-canary-report` IPC path. Installed-app live proof now covers all 8 report types for `2026-05-01` to `2026-05-25` / `FT-US-US` / `US`: `campaign`, `ad_group`, `placement`, `advertised_product`, `auto_targeting`, `keyword`, `product_targeting`, and `user_search_term` each downloaded 1 `.xlsx`, 0 failed, with DB/manifest/file size/date-token parity and `pnpm run verify:v15-canary -- <evidence>` passing. | Verified live 8/8 canaries |
| Local simulated report-collection E2E | `simulated-download-center-e2e.test.ts` runs a stateful local download-center double through create → wait → download for the exact 8 report types, verifies filenames/file sizes, and asserts manifest/result parity; `batch-runner.test.ts` covers partial multi-report failure as `completed_with_errors` | Implemented structurally; does not replace real Lingxing E2E |
| Lingxing E2E acceptance audit export | `auditLingxingAcceptanceEvidence`, desktop `导出验收审计` IPC/UI export by persisted batch id, JSON/Markdown audit, structured `filename-date-range-analysis.json`, `downloaded-report-files.json`, DB/manifest/file-system parity, diagnostic evidence, and per-file readiness checks. Live full-8 batch `batch_20260609045655853_ft8uda` exported acceptance audit under AppData and `pnpm run verify:v15-delivery -- output\codex-evidence\desktop-live-full-8-e2e-2026-06-09.json` verified status passed, downloadedCount 8, failedCount 0, 8 expected report types, and all audit checks passed. | Verified live full-8 audit |
| Acceptance audit diagnostic lookup regression | `download-center-diagnostic-store.test.ts` verifies the default diagnostic lookup is constrained by page model name, exact page-model snapshot JSON, date range, store, and marketplace | Implemented |
| Acceptance audit path-boundary regression | `apps/desktop/src/main/acceptance-audit-export.test.ts` covers safe manifest path handling, same-prefix sibling directory rejection, `manifest.json` basename enforcement, unreadable JSON handling, export directory segment sanitization, and capability-gated symlink escape rejection | Implemented; symlink case runs only when the OS account can create file symlinks |
| File path and status display | v1.5 report batch table, open path IPC constrained to app-owned evidence/download/export directories and safe file extensions | Implemented |
| Download center page-model diagnostic | Read-only diagnostic IPC/UI, `download_center_diagnostics` table, screenshot path, sanitized DOM snapshot, selector candidates, action selector locator checks, active page-model source/readiness display, and diagnostic screenshot/DOM evidence file readiness tests. Enabled model diagnostic evidence `output/codex-evidence/installed-live-diagnostic-enabled-model-2026-06-09.json` produced diagnostic id `26`, `requiresManualVerification: false`, `ready: true`, and collection preflight passed all checks. Full-8 run produced diagnostic id `27`. | Verified live enabled model |
| Download center diagnostic evidence bundle | UI/IPC export by persisted diagnostic id creates a local folder with DB-loaded diagnostic JSON, active page model, readiness JSON, selector candidates, action selector checks, screenshot/DOM evidence copies, and a manual verification checklist | Implemented structurally |
| Download center page-model draft from diagnostic | `buildDownloadCenterPageModelDraft`, desktop `生成模型草稿` IPC/UI export by persisted diagnostic id, exported draft JSON and solidification notes; draft keeps `requiresManualVerification: true` so it cannot enable unattended collection by itself | Implemented structurally |
| Download center page-model enablement audit | `auditDownloadCenterPageModelEnablement`, desktop `导出启用审计` IPC/UI export, JSON/Markdown audit, active saved model, optional DB-loaded diagnostic evidence, diagnostic screenshot/DOM file readiness, safe diagnostic evidence copies, and machine-readable `enablement-bundle-index.json`; audit says yes only when scoped automation selectors, same-model/same-date/same-store/same-marketplace setup diagnostic evidence, and local diagnostic evidence files are present | Implemented structurally |
| Download center page-model override validation | `download-center-page-model-validation.test.ts` covers manual-verification-on bundled models, fully scoped verified selectors, missing required action selectors, missing report/date placeholders, non-HTTPS/non-allowlisted URLs, trusted `ads.lingxing.com` download-center URLs, and timeout bounds | Implemented |
| Download center page-model override history | Saving an override writes metadata and backs up the previous override; resetting backs up the current override before removal; `download-center-page-model-override-store.test.ts` covers first save, replacement backup, and reset backup | Implemented |
| Collection preflight | `buildDownloadCenterCollectionPreflight`, desktop `采集预检`, and hard start gates for `启动采集` / row-level `重试` combine page-model automation readiness, latest same-model/same-date/same-store/same-marketplace diagnostic evidence readiness, local screenshot/DOM evidence file presence, and browser login/session readiness before full collection or retry creates a batch. Enabled model diagnostic id `26` produced `page_model_ready`, `diagnostic_evidence_ready`, and `browser_session_ready` all passed; full-8 collection then completed. | Verified live preflight |
| Collection preflight export | Desktop `导出预检` writes `collection-preflight.json`, `collection-preflight.md`, `active-page-model.json`, `diagnostic-evidence-files.json`, `preflight-review-checklist.md`, and machine-readable `preflight-bundle-index.json`; when a matching diagnostic exists, it also writes `diagnostic.json` plus safe copied diagnostic screenshot/DOM evidence into a local export folder for before/after verification evidence | Implemented structurally |
| v1.5 workbench clickability | Report-collection and keyword-opportunity panels no longer overlap; renderer QA evidence `output/codex-evidence/renderer-v15-diagnose-layout-qa-1780561270634.json` proves the `验证页面` center click target resolves to the button and the diagnostic success state renders | Implemented |
| v1.5 delivery-gate UI | Report-collection panel shows current scope, operator next-step guidance, page-model readiness, same-scope diagnostic readiness, single-report canary coverage, 8-report batch state, and acceptance-audit gate in the first viewport. Advanced page-model JSON, selectors, DOM, and trace tables are collapsed behind `展开高级诊断`. Renderer smoke evidence `output/codex-evidence/v15-workbench-operator-ui-smoke-2026-06-08.png` proves the updated gate, 0/8 canary progress, diagnostics toggle, hidden selector table by default, and no console errors. | Implemented |
| v1.5 product readiness UI | v1.5 backend navigation is split into business-domain left-menu groups: `运营总览`, `数据与量化`, `广告执行`, `关键词与 Listing`, and `系统与交付`, instead of embedding every v1.5 task under one nested workbench. `数据与量化` contains scope, collection, import validation, operation events, product config, and ad quantification; `广告执行` contains recommendation generation, approval, and readback. `交付验收` is manifest-driven and must not claim deliverability unless refreshed final readiness passes for the current code. `数据采集`, `数据导入与校验`, `关键词机会`, and `Listing 优化` each expose their own page purpose, primary task, and proof boundary. `优化建议` exposes date/store/site/ASIN filters, refresh/generate actions, ad structure context, explanation source/fallback, generalized execution contract, dynamic target fields for store/site/portfolio/campaign/ad group/ASIN/entity/action, source recommendation fields kept separate from live before/after values, local precheck, manual Ads UI evidence fields, and independent readback evidence path. `定时任务` states that automation may create a local pending/review recommendation pool, but cannot auto-approve review-required items or write Amazon Ads. The in-app execution button remains fail-closed and does not batch-write ads. Current business UI smoke evidence `output\codex-evidence\current-business-ui-smoke-1782263174619.json` passed across shell, data pipeline, ad execution, keyword/listing, and settings/delivery pages; packaged launch smoke `output\codex-evidence\package-launch-smoke-1782263264718.json` passed for unpacked and portable startup; refreshed final-readiness `output\codex-evidence\final-readiness-20260624090830.json` is `APP_READY` and includes the AI contract tags, compact metric tags, and Listing table editor. | Verified in refreshed APP_READY package |
| Dashboard / delivery AI evidence summary | The dashboard and delivery matrix derive AI evidence readiness from persisted diagnosis runs, but a run only counts as successful AI evidence when it is `source=ai`, not failed, not marked lifecycle-review-required, has lifecycle evidence refs, and every lifecycle ref resolves to a displayable `evidencePackPreview` item. This prevents a structurally successful but non-explainable AI response from turning the delivery AI evidence card green. The dashboard and delivery matrix also read the `needs_review` recommendation queue and do not count review-only recommendations as directly approvable; dashboard `待处理建议` separates pending/review counts and `建议与审批` shows `需复核` until at least one formal pending or approved recommendation exists. Incremental evidence: `pnpm exec vitest run apps/desktop/src/renderer/delivery-readiness-matrix.test.ts apps/desktop/src/renderer/evidence-display.test.ts apps/desktop/src/renderer/pages/recommendations-page.test.ts`, `pnpm exec vitest run apps/desktop/src/renderer/delivery-readiness-matrix.test.ts apps/desktop/src/main/ad-recommendation-ai-context.test.ts apps/desktop/src/main/recommendation-approval-policy.test.ts apps/desktop/src/renderer/pages/recommendations-page.test.ts apps/desktop/src/renderer/pages/approval-page.test.tsx`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, `node scripts\smoke-business-ui-settings-delivery.js` with evidence `output\codex-evidence\business-ui-settings-delivery-smoke-1781678920066.json`, and `node scripts\smoke-business-ui-data-pipeline.js` with evidence `output\codex-evidence\business-ui-data-pipeline-smoke-1781679654078.json` passed. | Verified current renderer smoke; current package refresh complete |
| Global operation scope UX | The global scope bar shows one current operation range for date/store/site/USD and explains that data collection, import validation, quantification, recommendations, approval/readback, keyword opportunities, and Listing drafts all read from that range. Batch selection is limited to automatic latest current-scope batch, selected verified batch, or manual batch ID; unmatched manual batches show a warning. USD is forced in the scope store and formatter, and smoke guards reject RMB markers. Evidence: `output\codex-evidence\business-ui-shell-smoke-1781541447674.json` and `output\codex-evidence\business-ui-data-pipeline-smoke-1781541458285.json`. | Verified |
| Real ad data chain | `数据采集` counts only real `.xlsx/.xls/.csv` report files as downloaded reports and excludes audit JSON, screenshots, DOM HTML, manifests, and diagnostics from the real-data count. Current readiness and generation gates count Lingxing coverage by unique expected report type, not raw file count, so duplicate spreadsheets cannot falsely satisfy `8/8`. Renderer pages now share the same audited coverage helper, so Dashboard, 数据采集, 广告量化, 优化建议, 关键词机会, and Listing 优化 use `fileAudit.realReportFileCount` before raw file fallback; user-facing coverage copy says `N/8 类真实报表` instead of raw `N 个文件` when that number controls business readiness. `下载并导入已创建报表` and `重新创建、下载并导入` keep separate operator guidance: the former only searches current-scope ready rows and explicitly does not create a new task, while the latter creates new Lingxing tasks and handles "created but no real spreadsheet arrived" as a separate recovery path. `数据导入与校验` shows current-scope real report coverage, imported metric rows, primary analysis grain, per-report file/import status, and import actions. Current diagnostic evidence `output\codex-evidence\business-data-real-evidence-1781542255133.json` proves `READY_FOR_ANALYSIS` against `C:\Users\wz\AppData\Roaming\@amazon-ai-ops\desktop\amazon-ai-ops.db`, batch `batch_20260612020905629_gkchz1`, 8/8 real report types, 2416 imported metric rows, canonical `user_search_term` spend USD `617.87`, and 19 orders. Import smoke evidence: `output\codex-evidence\import-lingxing-batch-metrics-smoke-1781542262787.json`; incremental action-error/counting evidence: `pnpm exec vitest run apps/desktop/src/renderer/pages/data-collection-page.test.ts`, `pnpm exec vitest run apps/desktop/src/main/business-report-coverage.test.ts apps/desktop/src/main/recommendation-generation-gate.test.ts`, and `pnpm exec vitest run apps/desktop/src/renderer/report-coverage.test.ts apps/desktop/src/renderer/recommendation-readiness.test.ts apps/desktop/src/renderer/pages/dashboard-page.test.ts`. Latest renderer smoke evidence: `output\codex-evidence\business-ui-data-pipeline-smoke-1781681924623.json`, `output\codex-evidence\business-ui-ad-execution-smoke-1781681944112.json`, and `output\codex-evidence\business-ui-keyword-listing-smoke-1781681530993.json`. | Verified |
| Report coverage wording closeout | Global scope, work-scope, dashboard, data collection, and data import pages now use `N/8 类真实报表` for business readiness, `已导入 N 行` for DB metric readiness, and `本次新增真实文件 N 个` only for the current action result. This prevents operators from confusing audit packages or existing files with newly downloaded Lingxing data or imported daily metrics. Incremental evidence: `pnpm exec vitest run apps/desktop/src/renderer/components/scope-bar.test.ts apps/desktop/src/renderer/data-readiness-ledger.test.ts apps/desktop/src/renderer/collection-action-summary.test.ts apps/desktop/src/renderer/pages/data-collection-page.test.ts apps/desktop/src/renderer/pages/dashboard-page.test.ts`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, `pnpm --filter @amazon-ai-ops/desktop run build:renderer`, `node scripts\smoke-business-ui-data-pipeline.js` with `output\codex-evidence\business-ui-data-pipeline-smoke-1781686487569.json`, and `node scripts\smoke-business-ui-shell.js` with `output\codex-evidence\business-ui-shell-smoke-1781686541443.json`. | Verified current renderer smoke; current package refresh complete |
| Operation event context | `operation_events` stores date, store, marketplace, ASIN, campaign, ad group, event type, expected impact, notes, and evidence path. The operation-event page gives quick templates for Coupon, BD, promotion, Deal, price change, inventory issue, and Listing changes, and states that these events feed ad quantification and AI diagnosis without executing ads. `OperationEventRepository` tests verify scope filtering, ASIN case-insensitive matching, campaign/ad-group context, update, and delete behavior. Incremental evidence: `pnpm exec vitest run packages/local-db/src/sqlite/repositories/operation-event-repo.test.ts` passed, 1 file / 3 tests. | Verified |
| v1.5 live-action protection UI | `单报表验证` requires current-scope diagnostic evidence before IPC, asks for explicit confirmation that it will generate/download one real Lingxing report, and cancelled confirmation does not call the canary IPC. Full 8-report collection is guarded by the same current scope/model/diagnostic gate and requires confirmation before execution. Renderer smoke evidence `output/codex-evidence/v15-workbench-live-action-confirm-smoke-2026-06-08.png` passed with no console errors. | Implemented |
| v1.5 current-scope evidence accounting | The workbench counts canary/full-8 progress only when the batch matches the selected date/store/site scope. Stale batch evidence is displayed as non-current and does not advance 0/8 progress or next-step guidance. | Implemented |
| v1.5 operator result visibility | Per-report Lingxing batch results are visible in the normal operator area, including report type, status, retry count, file path, failure reason, evidence links, and retry/open actions. Selector/page-model tables remain behind advanced diagnostics. Smoke evidence `output/codex-evidence/v15-operator-ux-guard-smoke-2026-06-08.png` verifies the normal result table. | Implemented |
| Ad recommendation execution UX | Approved recommendations show target object, current/recommended value, reason/evidence text, risk, confidence, and ad structure context from evidence date/campaign/ad group/ASIN/search term. The page can refresh existing recommendations by date/store/site/ASIN/status and trigger recommendation generation only after explicit start date, end date, store, and marketplace are entered. Backend generation uses scoped ad metrics, requires the current scope to have all 8 Lingxing real `.xlsx/.xls/.csv` report files before formal AI+rule recommendations are generated, does not fall back from empty store to login username, and skips duplicate recommendations for the same store/site/ASIN/entity/action/metric date. Recommendation list date filtering uses metric `evidence.date` before `created_at`, so recommendations generated today from an older advertising range remain visible under that older range. The visible action says `生成阻断审计`; app-side execution stays fail-closed. Real execution readiness is proven by the separate manual Ads UI readback evidence path, and the UI now supports generalized target/source evidence entry for any future store/site/campaign/ad group/object/action. | Verified readback sample; app-side batch execution remains fail-closed |
| Approval policy fail-closed | Approval requires concrete store, marketplace, source batch, metric date, campaign, ad group, entity type/name, action, current value, recommended value, and source file evidence. The main process calls `assertRecommendationCurrentDataGate` and `assertRecommendationApprovalPolicy` before updating a recommendation to `approved`; AI-only, AI/rule conflicts, explicit review flags, quant-review flags, forbidden, and high-risk recommendations cannot pass normal approval. Incremental evidence: `pnpm exec vitest run apps/desktop/src/main/recommendation-approval-policy.test.ts` passed, 1 file / 4 tests, and `node scripts\smoke-business-ui-ad-execution.js` passed with evidence `output\codex-evidence\business-ui-ad-execution-smoke-1781541981884.json`. | Verified |
| AI insight approval separation | AI candidates must pass evidence validation before they can participate in rule/AI merge or AI-only recommendation creation. Candidates missing evidence refs, source file/source row traceability, real spreadsheet sources, executable action type, confidence, current-scope binding, or resolved evidence details become insight-only or approval-blocked. Normal approval now requires AI-sourced recommendations to have `aiEvidenceRefs` resolved into displayable `aiEvidenceDetails`; ref-only AI recommendations cannot be approved because the operator cannot inspect the evidence cards. The shared evidence summary used by `广告量化`, `优化建议`, and `审批中心` marks any AI evidence ref without a matching detail as `证据缺失` / blocked, so lifecycle-stage diagnostics and aligned rule+AI suggestions are not presented as ready when their evidence cards cannot be shown. The `优化建议` page loads both `pending` and `needs_review` rows into the visible suggestion pool, while formal-approval count, evidence-blocked count, and `送审前证据检查` still use the stricter approval gate before the operator goes to `审批中心`. Recommendations whose AI lifecycle-stage diagnosis requires review are marked `needs_review` during recommendation annotation and are blocked by backend approval policy, approval UI blockers, and the recommendation-page formal-approval gate, even if rule/AI action agreement is aligned. AI dynamic-threshold cards mark unresolved threshold evidence refs as review-required and show the missing-detail reason before any rule-threshold override is considered; the `优化建议` and `审批中心` AI threshold summaries also append `需复核` plus validator reasons when any threshold suggestion has `requiresReview`. The approval page mirrors the backend blocker and shows `AI 建议缺少可展示的证据详情` or `AI 阶段判断需要人工复核` in the queue/detail precheck when details are missing or lifecycle review is required. Incremental evidence: `pnpm exec vitest run apps/desktop/src/renderer/pages/ad-quant-page.test.tsx apps/desktop/src/renderer/evidence-display.test.ts apps/desktop/src/renderer/pages/recommendations-page.test.ts`, `pnpm exec vitest run apps/desktop/src/renderer/evidence-display.test.ts apps/desktop/src/renderer/pages/ad-quant-page.test.tsx apps/desktop/src/renderer/pages/recommendations-page.test.ts`, `pnpm exec vitest run apps/desktop/src/renderer/evidence-display.test.ts apps/desktop/src/renderer/pages/recommendations-page.test.ts apps/desktop/src/renderer/pages/approval-page.test.tsx apps/desktop/src/main/recommendation-approval-policy.test.ts`, `pnpm exec vitest run apps/desktop/src/main/recommendation-approval-policy.test.ts apps/desktop/src/main/ad-ai-evidence-validator.test.ts apps/desktop/src/main/ad-recommendation-ai-context.test.ts`, `pnpm exec vitest run apps/desktop/src/renderer/pages/recommendations-page.test.ts apps/desktop/src/renderer/evidence-display.test.ts apps/desktop/src/renderer/pages/approval-page.test.tsx`, `pnpm exec vitest run apps/desktop/src/renderer/pages/recommendations-page.test.ts apps/desktop/src/renderer/pages/approval-page.test.tsx apps/desktop/src/renderer/pages/ad-quant-page.test.tsx apps/desktop/src/renderer/evidence-display.test.ts`, `pnpm exec vitest run apps/desktop/src/main/recommendation-approval-policy.test.ts apps/desktop/src/renderer/pages/approval-page.test.tsx apps/desktop/src/renderer/pages/recommendations-page.test.ts`, `pnpm exec vitest run apps/desktop/src/main/ad-recommendation-ai-context.test.ts apps/desktop/src/main/recommendation-approval-policy.test.ts apps/desktop/src/renderer/evidence-display.test.ts apps/desktop/src/renderer/pages/recommendations-page.test.ts apps/desktop/src/renderer/pages/approval-page.test.tsx`, `pnpm exec vitest run apps/desktop/src/main/ad-recommendation-ai-context.test.ts apps/desktop/src/main/recommendation-approval-policy.test.ts apps/desktop/src/renderer/pages/recommendations-page.test.ts apps/desktop/src/renderer/pages/approval-page.test.tsx`, `pnpm exec vitest run apps/desktop/src/renderer/pages/recommendations-page.test.ts apps/desktop/src/renderer/pages/approval-page.test.tsx apps/desktop/src/renderer/delivery-readiness-matrix.test.ts apps/desktop/src/main/ad-recommendation-ai-context.test.ts apps/desktop/src/main/recommendation-approval-policy.test.ts`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, and `node scripts\smoke-business-ui-ad-execution.js` with evidence `output\codex-evidence\business-ui-ad-execution-smoke-1781678899260.json` passed. | Verified current renderer smoke; current package refresh complete |
| Ad execution fail-closed backend | Desktop `recommendations:execute` no longer constructs a hard-coded `success: true` execution result. Until a real executor and verified readback are wired, it writes a failed action log with failure reason, keeps the recommendation approved, and surfaces a blocking error to the operator. `AdActionExecutor` now ties bid/toggle success to readback verification and negative-keyword success to a visible success toast. Regression tests `recommendation-execution-policy.test.ts` and `ad-actions.test.ts` plus `pnpm run verify:ad-execution` cover this gate; `verify:v15-delivery` runs it before full-8 delivery evidence checks. | Implemented fail-closed; real ad write/readback still blocked |
| Real ad readback evidence contract | `output\codex-evidence\real-ad-execution-readback-candidate-rec-4-current-pass.json` contains a current-contract approved manual Ads UI action on a paused FT-US keyword row: live bid `1.30 -> 1.17`, before/after/reload screenshots, ordered timestamps, traceable approval artifact, real `.xlsx` source report file(s), positive `sourceRow=410`, `execution.channel=manual_ads_ui`, and `appExecutorUsed=false`. The source recommendation remains traceable as `1.63 -> 1.46`, but the live Ads UI bid was already below that recommended value, so the operator did not write the stale recommendation back into Ads. `pnpm run verify:ad-readback -- output\codex-evidence\real-ad-execution-readback-candidate-rec-4-current-pass.json` passes. The desktop readback evidence builder remains generic and supports future actions through dynamic target/source fields; future executions must not reuse this D6 `door lock` scope and must each provide their own approval, target context, before/after values, screenshots, and readback evidence. The `执行回读` recommendation loader preserves upstream AI dynamic-threshold metadata, including evidence refs, review-required flags, and review reasons, so manual readback/export does not erase AI-threshold review context after approval. Incremental evidence: `pnpm exec vitest run scripts/create-ad-readback-candidate.test.mjs scripts/prepare-ad-readback-session.test.mjs apps/desktop/src/main/ad-readback-session.test.ts --reporter=basic`, `node --check scripts\create-ad-readback-candidate-from-recommendation.js`, `node --check scripts\create-ad-readback-evidence-template.js`, and `node --check scripts\prepare-ad-readback-session.js`. | Verified current-contract real readback sample and generic contract |
| Readback source/live value separation | The readback verifier treats source recommendation values and live Ads UI values as separate evidence layers. `source.currentValue` and `source.recommendedValue` must be present as recommendation-input traceability, but they no longer have to equal `before.value` and `after.value`; the real write proof is the live before/after value change, screenshots, live bid source note, execution proof, and independent readback. This supports recommendations generated from report/rule/AI values while the operator executes the actual current Ads UI bid. Incremental evidence: `pnpm exec vitest run scripts/verify-ad-readback-evidence.test.mjs scripts/export-v15-delivery-bundle.test.mjs scripts/verify-v15-ready-safety.test.mjs scripts/verify-v15-final-readiness.test.mjs apps/desktop/src/main/ad-readback-evidence.test.ts apps/desktop/src/renderer/pages/readback-page.test.tsx apps/desktop/src/main/delivery-evidence-status.test.ts`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, `pnpm --filter @amazon-ai-ops/desktop run build:main`, `pnpm --filter @amazon-ai-ops/desktop run build:renderer`, `node scripts\smoke-export-v15-delivery-bundle.js` with `output\codex-evidence\export-v15-delivery-bundle-smoke-1781690249094.json`, and `node scripts\smoke-business-ui-ad-execution.js` with `output\codex-evidence\business-ui-ad-execution-smoke-1781690249714.json`. | Verified structurally; current package refresh complete |
| AI / DeepSeek settings UX | Settings page exposes API Key, Base URL, model, temperature, max tokens, output language, AI persona, save action, `测试 AI 连接`, and `清除本地 AI Key`. It also shows the evidence flow and command-copy action with a safe `$env:DEEPSEEK_API_KEY="<your-deepseek-key>"` template. Desktop main registers `settings:test-ai`, defaults to `https://api.deepseek.com` / `deepseek-v4-flash`, normalizes camelCase and snake_case AI settings, returns operator-facing missing-key/network/auth/quota messages, and falls back with `aiFallbackReason` if provider calls throw. AI settings normalization now has pure-function regression coverage proving renderer sanitization hides the stored API key, hidden-key save preserves the saved key and available test state when provider/model still match, hidden-key test requests use the saved key in the main process, and the stored key is removed only by an explicit `clearAiKey` request. DeepSeek thinking is explicitly disabled for OpenAI-compatible calls so short connection tests and structured generation return visible content. Real provider evidence `output/codex-evidence/deepseek-live-1781066552798.json` passed `pnpm run verify:ai-live`; final readiness now rejects AI live evidence that contains obvious API-key or bearer-token shaped material before granting AI-live credit. Incremental hidden-key/evidence evidence: `pnpm exec vitest run apps/desktop/src/main/ai-settings-normalization.test.ts`, `pnpm exec vitest run scripts/verify-v15-final-readiness.test.mjs`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, `pnpm --filter @amazon-ai-ops/desktop run build:main`, `pnpm --filter @amazon-ai-ops/desktop run build:renderer`, `node --check scripts\smoke-business-ui-settings-delivery.js`, and `node scripts/smoke-business-ui-settings-delivery.js` with `output\codex-evidence\business-ui-settings-delivery-smoke-1781669191490.json`. | Verified real provider evidence; hidden-key preserve/clear flow covered structurally |
| Ad recommendation AI explanation evidence | `pnpm run run:v15-installed-live -- --mode ad-ai-explanation --limit 300 --out output\codex-evidence\installed-ad-ai-explanation-manual.json` generates packaged-app evidence by applying the real OpenAI-compatible key, testing the provider, generating scoped recommendations through the real IPC, reading the recommendations back, and restoring prior AI settings without executing ad writes. `pnpm run verify:ad-ai-explanation -- <evidence>` validates packaged-app runtime, read-only safety flags, `ai.testSuccess=true`, settings restore proof, real AI provider metadata, recommendation context, `explanationSource=ai`, nontrivial `aiExplanation`, recorded model, no fallback reason, and no obvious secret leakage including `sk-...`, bearer-token shaped strings, and `deepseek_api_key`-style fields. The recommendations UI now distinguishes `AI 已回落规则：<原因>` and `AI 仅作洞察` from true no-output status, so schema/language fallback and evidence-invalid AI output remain explainable to operators. Real evidence `output/codex-evidence/installed-ad-ai-explanation-user-key-2026-06-10.json` passed; it also proved old full-8 xlsx can be backfilled into `ad_daily_metrics`, using 266 scoped executable metrics and producing 1 AI-explained recommendation. Incremental status/security evidence: `pnpm exec vitest run apps/desktop/src/renderer/pages/recommendations-page.test.ts` and `pnpm exec vitest run scripts/verify-ad-ai-explanation-evidence.test.mjs`. | Verified real packaged-app AI evidence; UI status split covered structurally |
| AI evidence display details | Renderer evidence summaries resolve `evidenceRefs` into operator-readable facts: evidence type, batch id, report type, date range, campaign, ad group, ASIN, entity type/name, source row, cost/sales/orders/clicks in USD, operation events, and product context where available. Missing evidence details are surfaced as blockers instead of silently showing raw ids. Incremental evidence: `pnpm exec vitest run apps/desktop/src/renderer/evidence-display.test.ts`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, `pnpm --filter @amazon-ai-ops/desktop run build:renderer`, `node scripts\smoke-business-ui-ad-execution.js` with `output\codex-evidence\business-ui-ad-execution-smoke-1781682237964.json`, and `node scripts\smoke-business-ui-data-pipeline.js` with `output\codex-evidence\business-ui-data-pipeline-smoke-1781682268833.json`. | Verified current renderer smoke; current package refresh complete |
| Rule-only vs AI-action approval split | Batch-level AI strategy/lifecycle context may be attached to rule recommendations for operator visibility, but approval gates require action-level `aiEvidenceRefs` only when the concrete action is `aligned` with AI or `ai_only`. Rule-only deterministic recommendations are not moved to `needs_review` merely because the AI lifecycle diagnosis needs review; AI action candidates still require resolved evidence details and remain blocked or review-only when evidence is missing. Incremental evidence: `pnpm exec vitest run apps/desktop/src/main/ad-recommendation-ai-context.test.ts apps/desktop/src/renderer/pages/recommendations-page.test.ts apps/desktop/src/renderer/pages/approval-page.test.tsx apps/desktop/src/main/recommendation-approval-policy.test.ts`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, `pnpm --filter @amazon-ai-ops/desktop run build:main`, `pnpm --filter @amazon-ai-ops/desktop run build:renderer`, and `node scripts\smoke-business-ui-ad-execution.js` with `output\codex-evidence\business-ui-ad-execution-smoke-1781683128214.json`. | Verified current renderer smoke; current package refresh complete |
| Delivery matrix rule-only approval split | The delivery readiness matrix now uses the same split as recommendations and approval: batch-level AI lifecycle review does not demote pure `rule_only` pending recommendations, while concrete AI-participated actions (`aligned` / `ai_only`, plus conflicts) still trigger evidence and lifecycle review handling. This keeps the delivery page from saying `需复核` when a deterministic rule recommendation is otherwise ready for normal approval. Incremental evidence: `pnpm exec vitest run apps/desktop/src/renderer/delivery-readiness-matrix.test.ts apps/desktop/src/renderer/pages/recommendations-page.test.ts apps/desktop/src/renderer/pages/approval-page.test.tsx apps/desktop/src/main/recommendation-approval-policy.test.ts`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, `pnpm --filter @amazon-ai-ops/desktop run build:renderer`, and `node scripts\smoke-business-ui-settings-delivery.js` with `output\codex-evidence\business-ui-settings-delivery-smoke-1781683517444.json`. | Verified current renderer smoke; current package refresh complete |
| Recommendation operator-resolution summary | The recommendations page now aligns its summary count with the actual approval gate. `需处理` includes both manual-review rows and evidence-blocked rows, so AI-only, conflicts, insight-only, missing AI evidence details, and missing source data are not omitted from the visible workload. The UI still shows the separate `人工复核` and `证据不足阻断` counts so operators know whether to judge the action or repair the evidence. Incremental evidence: `pnpm exec vitest run apps/desktop/src/renderer/pages/recommendations-page.test.ts apps/desktop/src/renderer/pages/approval-page.test.tsx apps/desktop/src/main/recommendation-approval-policy.test.ts apps/desktop/src/renderer/delivery-readiness-matrix.test.ts`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, `pnpm --filter @amazon-ai-ops/desktop run build:renderer`, and `node scripts\smoke-business-ui-ad-execution.js` with `output\codex-evidence\business-ui-ad-execution-smoke-1781684738503.json`. | Verified current renderer smoke; current package refresh complete |
| Recommendation-to-approval evidence gate alignment | The recommendations page uses the current batch's loaded real report source-file list when deciding whether a row can enter formal approval, whether it is evidence-blocked, and which selected-row evidence issues to show. If the current batch source files are unavailable or a recommendation points at a stale report file, the row is treated as evidence-blocked before the operator reaches the approval center. Incremental evidence: `pnpm exec vitest run apps/desktop/src/renderer/pages/recommendations-page.test.ts apps/desktop/src/renderer/pages/approval-page.test.tsx apps/desktop/src/main/recommendation-approval-policy.test.ts apps/desktop/src/renderer/delivery-readiness-matrix.test.ts`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, `pnpm --filter @amazon-ai-ops/desktop run build:renderer`, and `node scripts\smoke-business-ui-ad-execution.js` with `output\codex-evidence\business-ui-ad-execution-smoke-1781687921820.json`. | Verified current renderer smoke; current package refresh complete |
| Approval review queue wording | The approval center labels `needs_review` as `复核队列`, not `AI 复核`, because the queue can contain AI-only actions, AI/rule conflicts, rule quantification review, evidence blockers, and other non-AI review causes. Blocker copy also says `建议已进入复核队列`, keeping the operator's next step clear without over-attributing every blocker to AI. Incremental evidence: `pnpm exec vitest run apps/desktop/src/renderer/pages/approval-page.test.tsx apps/desktop/src/renderer/pages/recommendations-page.test.ts apps/desktop/src/main/recommendation-approval-policy.test.ts`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, `pnpm --filter @amazon-ai-ops/desktop run build:renderer`, and `node scripts\smoke-business-ui-ad-execution.js` with `output\codex-evidence\business-ui-ad-execution-smoke-1781685053039.json`. | Verified current renderer smoke; current package refresh complete |
| Approval submit precheck alignment | The approval center disables `批准并进入待执行` using the same combined blocker set used by submit-time validation: missing current scope/batch/source batch/date/source row/loaded real current-batch report file/campaign/ad group/object/action/current value/recommended value, plus AI-only/conflict/insight-only/review/high-risk/value-direction blockers. The page also shows an explicit `审批证据不完整` message before the operator submits, so missing evidence is visible rather than discovered after clicking. Incremental evidence: `pnpm exec vitest run apps/desktop/src/renderer/pages/approval-page.test.tsx`, `pnpm exec vitest run apps/desktop/src/renderer/pages/approval-page.test.tsx apps/desktop/src/main/recommendation-approval-policy.test.ts`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, `pnpm --filter @amazon-ai-ops/desktop run build:renderer`, and `node scripts\smoke-business-ui-ad-execution.js` with `output\codex-evidence\business-ui-ad-execution-smoke-1781687677930.json`. | Verified current renderer smoke; current package refresh complete |
| Readback operator-facing decision labels | The readback page displays decision context in business language instead of raw enum values. `aligned / rule_ai` is rendered as `规则+AI 一致 / 规则+AI 合并` for operators, while the export payload still carries the raw structured fields for verifier compatibility. Incremental evidence: `pnpm exec vitest run apps/desktop/src/renderer/pages/readback-page.test.tsx apps/desktop/src/renderer/pages/approval-page.test.tsx apps/desktop/src/renderer/pages/recommendations-page.test.ts`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, `pnpm --filter @amazon-ai-ops/desktop run build:renderer`, and `node scripts\smoke-business-ui-ad-execution.js` with `output\codex-evidence\business-ui-ad-execution-smoke-1781685384558.json`. | Verified current renderer smoke; current package refresh complete |
| Ad action explanation structured JSON | `AdActionReasonExplainer` requires `schemaVersion: ad_action_reason_v1`, simplified Chinese natural-language fields, USD-only monetary wording, and JSON-only output. Missing or mismatched schema falls back to rule explanation and records `aiFallbackReason`, so malformed AI text cannot become a trusted recommendation explanation. Incremental evidence: `pnpm exec vitest run packages/ai-adapter/src/ad-action-reason.test.ts`, `pnpm --filter @amazon-ai-ops/ai-adapter run typecheck`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, and `pnpm --filter @amazon-ai-ops/desktop run build:main` passed. | Verified structurally; current package refresh complete |
| AI fallback reason separation | Strategy-level fallback and per-action explanation fallback are separated as `aiStrategyFallbackReason` and `aiActionFallbackReason`, with legacy `aiFallbackReason` retained for compatibility. A later successful action explanation no longer erases a prior strategy-diagnosis fallback, and the recommendations UI displays each fallback in its own context. Incremental evidence: `pnpm exec vitest run apps/desktop/src/main/ad-action-explanation-merge.test.ts apps/desktop/src/main/ad-recommendation-ai-context.test.ts apps/desktop/src/main/ad-action-ai-call-log.test.ts apps/desktop/src/renderer/pages/recommendations-page.test.ts`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, `pnpm --filter @amazon-ai-ops/desktop run build:main`, `pnpm --filter @amazon-ai-ops/desktop run build:renderer`, and `node scripts\smoke-business-ui-ad-execution.js` with `output\codex-evidence\business-ui-ad-execution-smoke-1781688459217.json`. | Verified current renderer smoke; current package refresh complete |
| AI fallback readback traceability | Manual Ads UI readback evidence exports preserve both fallback layers in `source.aiStrategyFallbackReason` and `source.aiActionFallbackReason`, and the Markdown evidence names both strategy and action-explanation fallback states. The readback page also carries those fields from an approved recommendation into the export form, so final evidence can explain whether AI failed at strategy diagnosis, single-action explanation, or neither. Incremental evidence: `pnpm exec vitest run apps/desktop/src/main/ad-readback-evidence.test.ts apps/desktop/src/renderer/pages/readback-page.test.tsx apps/desktop/src/main/ad-action-explanation-merge.test.ts apps/desktop/src/main/ad-recommendation-ai-context.test.ts apps/desktop/src/renderer/pages/recommendations-page.test.ts`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, `pnpm --filter @amazon-ai-ops/desktop run build:main`, `pnpm --filter @amazon-ai-ops/desktop run build:renderer`, and `node scripts\smoke-business-ui-ad-execution.js` with `output\codex-evidence\business-ui-ad-execution-smoke-1781688805416.json`. | Verified current renderer smoke; current package refresh complete |
| AI fallback approval traceability | Approval and rejection decision payloads preserve `aiStrategyFallbackReason` and `aiActionFallbackReason`, keeping the approval record aligned with recommendation evidence and later readback exports. The approval center now uses a tested payload builder so the fallback-layer explanation is not lost during approval. Incremental evidence: `pnpm exec vitest run apps/desktop/src/renderer/pages/approval-page.test.tsx apps/desktop/src/main/recommendation-approval-policy.test.ts apps/desktop/src/renderer/pages/readback-page.test.tsx apps/desktop/src/main/ad-readback-evidence.test.ts`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, `pnpm --filter @amazon-ai-ops/desktop run build:renderer`, `pnpm --filter @amazon-ai-ops/desktop run build:main`, and `node scripts\smoke-business-ui-ad-execution.js` with `output\codex-evidence\business-ui-ad-execution-smoke-1781689104975.json`. | Verified current renderer smoke; current package refresh complete |
| AI fallback script traceability | The standalone readback candidate/template scripts preserve the same separated fallback fields outside the renderer flow. `create-ad-readback-candidate-from-recommendation.js` reads `aiStrategyFallbackReason` and `aiActionFallbackReason` from recommendation evidence, passes them into `create-ad-readback-evidence-template.js`, and the generated JSON/Markdown records both fallback layers under `source`. Incremental evidence: `pnpm exec vitest run scripts/create-ad-readback-candidate.test.mjs scripts/create-ad-readback-template.test.mjs` and `node --check scripts\create-ad-readback-candidate-from-recommendation.js scripts\create-ad-readback-evidence-template.js`. | Verified structurally; current package refresh complete |
| AI call-log audit coverage | `ai_call_logs` now covers ad strategy diagnosis, per-recommendation ad action explanation, and Listing draft generation. Ad action explanation logs use `ad_action_reason_v1`, store a stable input hash, schema version, source-file/evidence-ref summary, redacted output JSON, and fallback error messages without storing API keys or raw prompt text. The same local DB redaction helper is used by `ai_call_logs` and `ai_diagnosis_runs`, covering `sk-...`, bearer-token strings, `authorization`, `deepseek_api_key`, `api_key`, and `apiKey`-style fields in wrapped output JSON, diagnosis snapshots, insights, evidence-pack summaries/previews, scope JSON, and error messages. Incremental evidence: `pnpm exec vitest run apps/desktop/src/main/ad-action-ai-call-log.test.ts packages/local-db/src/sqlite/repositories/ai-call-log-repo.test.ts`, `pnpm exec vitest run packages/local-db/src/sqlite/repositories/ai-call-log-repo.test.ts`, `pnpm exec vitest run apps/desktop/src/main/ad-action-ai-call-log.test.ts apps/desktop/src/main/listing-ai-draft.test.ts packages/local-db/src/sqlite/repositories/ai-call-log-repo.test.ts`, `pnpm exec vitest run packages/local-db/src/sqlite/repositories/ai-diagnosis-run-repo.test.ts packages/local-db/src/sqlite/repositories/ai-call-log-repo.test.ts`, `pnpm --filter @amazon-ai-ops/local-db run typecheck`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, and `pnpm --filter @amazon-ai-ops/desktop run build:main` passed. | Verified structurally; current package refresh complete |
| AI structural mock evidence | `pnpm run run:ai-structural-mock` writes `structural-ai-openai-compatible-mock-*.json`, using `/chat/completions`, `max_tokens`, and `response_format: { type: 'json_object' }` to match current structured AI calls while keeping Authorization redacted. The structural evidence verifies OpenAI-compatible request shape, `choices[0].message.content`, Listing draft JSON shape, source hooks for AI reason / `source: 'ai'` / fallback clearing, and no ad/full8/settings side effects. Latest evidence `output\codex-evidence\structural-ai-openai-compatible-mock-1781703077556.json` passed. This is intentionally `STRUCTURAL_ONLY` and `NO_FINAL_READINESS_CREDIT`; final readiness still requires real `deepseek-live-*` and `installed-listing-ai-draft-*` evidence. | Verified structural only; no final READY credit |
| Lingxing Listing read-only extraction | Desktop main exposes `v1_5:listing:extract-from-lingxing`, `v1_5:listing:open-and-extract-from-lingxing`, and `v1_5:listing:probe-detail-and-extract`; preload exposes `extractListingFromLingxing`, `openLingxingListingAndExtract`, and `probeLingxingListingDetailAndExtract`; the Listing panel exposes `从当前领星页面读取`, URL-based `打开 URL 并读取`, and `只读探测详情页`. The extractor records page URL/title, screenshot path, field matches, ASIN/title/bullets/backend-term completeness, read-only detail/edit/view candidate entries, and detail probe status. Readiness is split: `partialReady` means ASIN/title only; `fullContentReady` requires ASIN, title, bullets, and backend terms. Detail probe is fail-closed: it matches the current ASIN row to the same visible operation row in Lingxing's split table, clicks only the safe `操作` -> `编辑在线商品` path, validates final Lingxing URL and same ASIN, reads `Generic Keyword`, switches read-only to `描述`, and merges bullet fields. Incremental checks passed: Listing extractor unit test, desktop typecheck, and main build. Source-app live evidence `output\codex-evidence\source-listing-read-detail-probe-2026-06-09-merged-detail.json` passed `pnpm run verify:listing-read`, proving ASIN `B0GTTJFQTM`, title, 10 bullets, backend terms, screenshot, `https://erp.lingxing.com/erp/editListing`, `detailProbeClicked=true`, and `fullContentReady=true`. | Verified real detail-page read |
| Listing local-only UX | Listing suggestions are clearly marked as local-only and not submitted to Amazon. Buttons use `标记采纳` / `标记忽略`, the table shows current text beside suggested text when available, and `用已采纳建议生成草案` is disabled until at least one suggestion is accepted so ignored/pending suggestions cannot enter drafts. Draft review now keeps copy/open-export actions in the same local workflow. | Implemented structurally; real AI rewrite still needs live evidence |
| Keyword opportunity operator UX | Keyword opportunity panel shows total/high/risky/listing-usable counts, ASIN/level/risk filters, structured click/order/impressions/CVR/cost/sales/ACOS evidence, source file/row trace as secondary text instead of a primary source column, recommended Listing sections, and a next-step CTA for generating Listing suggestions after ASIN is filled/imported. Smoke evidence `output/codex-evidence/v15-keyword-opportunity-ux-smoke-2026-06-08.png` passed with no console errors; `engine.test.ts` covers cost/source trace preservation. | Implemented |
| Keyword opportunity ad-context split | Same ASIN plus keyword is now kept separate by portfolio/campaign/ad group/source instead of being merged into one opaque row. The operator page exposes campaign and ad-group context, campaign/ad-group counts, and an ad-group filter so users can distinguish where the same search term behaves differently. Incremental evidence: `pnpm exec vitest run packages/keyword-opportunity/src/engine.test.ts` and `node scripts\smoke-business-ui-keyword-listing.js` passed. | Verified |
| Dashboard data-health first viewport | The dashboard first viewport now shows current range health instead of placeholder KPI cards: report coverage, imported metric rows, AI state, pending recommendation count, USD ad spend, USD sales/orders, and ACOS from the current data pipeline. It shows blocked placeholders when real reports are missing instead of implying fake conclusions. Incremental evidence: `node scripts\smoke-business-ui-data-pipeline.js` passed. | Verified |
| Listing read ASIN guard | Lingxing Listing read now passes the expected ASIN and current store/site scope to the main process before persistence, so mismatched page reads are rejected before saving and draft generation requires ASIN, title, bullets, backend terms, and matched scope. The UI distinguishes `未读取`, `已探测未解析`, `已读取部分内容`, `待核对 ASIN`, `待核对范围`, and `完整读取`, with field-level gaps shown as `缺失` instead of implying the whole read did not run. Incremental evidence: `pnpm exec vitest run apps/desktop/src/renderer/listing-workflow-summary.test.ts`, `pnpm --filter @amazon-ai-ops/desktop run typecheck`, `pnpm --filter @amazon-ai-ops/desktop run build:renderer`, and `node scripts\smoke-business-ui-keyword-listing.js` passed with evidence `output\codex-evidence\business-ui-keyword-listing-smoke-1781666078846.json`. | Verified |
| Full-8 metric reconciliation | `scripts/reconcile-lingxing-full8-data.js` reads the downloaded xlsx files from the live full-8 evidence and exports `output\codex-evidence\full8-data-reconciliation-2026-06-09.json`. Current canonical `user_search_term` total is spend `145.20`, orders `5`, sales `324.95`; `keyword + auto_targeting + product_targeting` reconciles to the same spend/orders/sales total but must remain separate from campaign/ad_group/placement/advertised_product account-summary grains. Reconciliation now emits top-level blockers when real canonical report files are missing required spend/order/sales columns, so malformed spreadsheets cannot silently total missing columns as zero. `verify:v15-delivery` now fails if required metric columns are missing, duplicate summary grains do not reconcile to canonical total, or targeting breakdown cannot reconcile to total. Incremental evidence: `pnpm exec vitest run scripts/reconcile-lingxing-full8-data.test.mjs`. | Verified live in delivery gate; column-blocker regression covered |
| Enablement evidence script | `pnpm run verify:v15-enablement` checks the current AppData DB for recent same-scope diagnostic evidence plus successful same-scope canary coverage across all 8 Lingxing report types. It now passes for `output\codex-evidence\installed-canary-auto_targeting-short-name-2026-06-09.json`, covering all 8 report types. | Verified live enablement |
| Diagnostic evidence script | `pnpm run verify:v15-diagnostic` checks installed-app diagnostic evidence against AppData SQLite, screenshot/DOM artifacts, diagnostic export bundle contents, scope fields, and safety flags proving no full-8 collection or ad write action was started. | Implemented; expected to fail for the no-credential probe and pass only after a real login diagnostic |
| Delivery evidence script | `pnpm run verify:v15-delivery` first runs the ad-execution fail-closed gate, then treats the full-8 E2E JSON as an entry point and cross-checks AppData SQLite batch/file rows, exactly 8 unique Lingxing report types, downloaded file existence/size/date/report-keyword tokens, manifest containment/scope parity, final exported `acceptance-audit.json` status/checks, and full-8 xlsx metric reconciliation. It passes for `output\codex-evidence\desktop-live-full-8-e2e-2026-06-09.json`. | Verified live delivery evidence |
| Final readiness aggregation | `output\codex-evidence\final-readiness-20260624090830.json` returned `APP_READY` from manifest `output\codex-evidence\v15-final-readiness-evidence-manifest-20260624090830.json`. Gates passed for the 2026-06-24 high-fidelity UI / AI contract package: report collection delivery, Lingxing Listing full read, AI live provider, ad recommendation AI explanation, Listing AI draft, real ad execution readback, release package hash, and package launch smoke. Historical `output\codex-evidence\final-readiness-2026-06-18-portable-fix.json` remains baseline evidence only. | Current APP_READY verified |
| READY safety gate | `pnpm run verify:v15-ready-safety` parses final-readiness evidence, evidence-selection manifest, README, delivery bundle, data reconciliation, real report index, current business UI smoke summary, package hash index, and package launch smoke summary. It also executes `scripts/verify-ad-readback-evidence.js` against the manifest-selected ad readback JSON, so an empty or hollow `status=PASS` readback file cannot satisfy the release safety layer. Current command: `pnpm run verify:v15-ready-safety -- --final-readiness output\codex-evidence\final-readiness-20260624090830.json --bundle-manifest output\delivery-bundles\v15-delivery-bundle-20260624090830-ready\delivery-bundle-manifest.json`. | Current READY safety verified |
| Delivery evidence bundle | `pnpm run export:v15-delivery-bundle` exports a bounded handoff bundle from final-readiness evidence. It includes docs, verifier sources, git status, evidence-selection manifest, final-readiness JSON, full-8/Listing/UI/real-AI evidence, screenshots, full-8 manifest, acceptance-audit summaries, real-ad readback evidence, current-scope data reconciliation JSON/Markdown, package hash index, package launch smoke logs, and `evidence/real-report-file-index.json` for raw report path/hash traceability, while allowlisting source roots and blocking DB/profile/raw XLSX/installer/secret leakage. Current bundle: `output\delivery-bundles\v15-delivery-bundle-20260624090830-ready`; historical bundle `output\delivery-bundles\v15-delivery-bundle-2026-06-18-portable-fix-ready` remains a baseline artifact only. | Current READY bundle verified |
| Canary evidence script | `pnpm run verify:v15-canary` checks latest or explicit installed canary evidence for no top-level errors, no full-8 start, no ad write actions, exactly one valid Lingxing report type, AppData diagnostic freshness/scope/evidence files/setup selectors, completed DB batch, manifest parity, one downloaded DB/manifest file row, file existence/path containment/extension/size, report keyword, and start/end date tokens. It passes for all 8 installed canaries and uses Python sqlite fallback when Electron rebuilds `better-sqlite3` for packaged runtime. | Verified live 8/8 canary evidence |
| Deprecated scheduler entry cleanup | `daily_report_download` is no longer registered or present in the active scheduler task type, so the scheduler UI does not expose the legacy single-report path that v1.5 intentionally replaced | Implemented |
| Selector-driven download-center automation path | Page model `actionSelectors`, optional `statusTextSelector`, local page-model override save/reset, readiness gate, recent same-model/same-date/same-store/same-marketplace diagnostic setup-selector gate with unit tests, per-action visible-locator uniqueness and report/date scope preflight, guarded Playwright create/wait/status/download implementation, generated report-name scoping, store/site request plumbing, batch/manifest store/site traceability, ERP-to-Ads entry through the ERP advertising menu, and sanitized browser startup errors. The bundled model and current AppData override now use `requiresManualVerification: false` after 8/8 canary and full-8 evidence. | Verified live automation path |
| Real Lingxing download center automation | Real Ads download-center URL, enabled model diagnostic id `26`, full-8 diagnostic id `27`, 8/8 canaries, and full-8 batch `batch_20260609045655853_ft8uda` prove create/ready/download selectors for all report types. | Verified live full-8 automation |
| E2E with real Lingxing session | Real ERP login, Ads navigation, Ads download-center page, create-report page, enabled page-model preflight, and full 8-report generation/download were verified with installed app evidence. | Verified live full-8 E2E |
| User can open downloaded/evidence files | UI open path IPC implemented with path/extension allowlist; full-8 downloaded files and acceptance audit paths are under AppData owned download/export directories and verified by delivery gate. | Implemented |

## Final Release Gates

Report collection and the non-report readiness gates have evidence for the documented Lingxing scope. The current source tree has passed focused renderer/package tests, desktop typecheck, Windows installer/portable packaging, current business UI smoke, package launch smoke, manifest-driven final-readiness, READY bundle export, and READY safety for the latest package hashes.

- Real ad write execution/readback: verified for one low-risk paused FT-US keyword sample by `output\codex-evidence\real-ad-execution-readback-candidate-rec-4-current-pass.json`; future ad actions must each provide their own target, approval, before/after screenshots, source report files/row, and readback evidence.
- Real DeepSeek provider success: verified by `output/codex-evidence/deepseek-live-1781066552798.json`.
- Real Listing AI rewrite: verified by `output/codex-evidence/installed-listing-ai-draft-user-key-2026-06-10.json`.
- 2026-06-24 high-fidelity UI / AI contract validation: focused renderer/AI tests passed, `pnpm --filter @amazon-ai-ops/desktop run typecheck` passed, `pnpm --filter @amazon-ai-ops/desktop run build:win` produced installer SHA-256 `7F91C2A3C2812DDDFDE76FB09E5E508D2B6F6FAFDF94F719C30D293779EBF788` and portable SHA-256 `C8531FB7744551E5A57615AE8FE58E90904E9D0F0D918E2CEB15F2A15EC3218C`, `pnpm run smoke:package-launch` passed, and current `APP_READY` aggregation is `output\codex-evidence\final-readiness-20260624090830.json`.
- 2026-06-24 high-fidelity UI validation: focused renderer tests, `build:renderer`, desktop typecheck, `build:win`, `smoke:business-ui-current`, `smoke:package-launch`, `verify:ad-execution`, manifest-driven final-readiness, READY bundle export, and READY safety passed.
- Future package rebuilds, scope changes, or ad actions must rerun manifest-driven final-readiness, export a fresh bundle, and run READY safety before declaring that new state APP_READY.
