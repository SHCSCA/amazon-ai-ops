# Amazon AI Ops Agent - Agent Instructions

<!-- CCG-FAST-CONTEXT-START -->
# fast-context MCP 工具使用指南（辅助模式）

## 核心原则

**主检索工具为 ace-tool（`mcp__ace-tool__search_context`）。当 ace-tool 无法满足语义搜索需求时，使用 `mcp__fast-context__fast_context_search` 作为补充。**

适合使用 fast-context 的场景：
- 用自然语言描述要找的逻辑（如"部署流程"、"事件处理"）
- 跨模块、跨层级的调用链路追踪
- 中文语义搜索（工具支持中英文双语查询）
<!-- CCG-FAST-CONTEXT-END -->

## Project Scope

- This repo is a local-first Electron desktop app for Amazon operators. Target Windows desktop only; do not spend effort on mobile layouts unless explicitly requested.
- Preserve the current worktree. Do not roll back user changes, generated evidence, or local runtime state unless the user explicitly asks.
- `output/`, `storage/`, AppData DB/profile files, raw Lingxing spreadsheets, release EXE binaries, and secrets are local artifacts. Do not commit them.

## Current Delivery State

- Current packaged state is `APP_READY` for the high-fidelity Windows desktop UI, AI output-contract refresh, real ad strategy diagnosis JSON-contract fix, Lingxing report date-picker commit fix, product-level workbench/product maintenance refresh, product-management first-screen task panel with explicit product locking, product-card locked/idle tags, selection `aria-live` readback, product-management save action-button busy feedback, product-management inline mini table field cells, product ACOS visible navigation restoration, operation-events navigation label aligned to `运营事件标记`, and credential sandbox hover/focus summary, operation-scope first-screen confirmation, page-level range FormTable with field-level green confirmation feedback, ScopeBar field-level green confirmation feedback, non-layout-shifting ScopeBar editor popover, and editor save action-button busy feedback, shared business data pipeline 300ms scope-change debounce with immediate explicit reload, product-config first-screen task, loaded-row confirmation feedback, direct save/bulk action-button busy feedback, bulk target ACOS row-selection apply, bulk selection count/progress/`aria-live` feedback, keyboard nudge, live target health chips, and inline autosave feedback, operation-event first-screen task feedback with optimistic form clear/rebound, failure draft restore, timeline AI-context readback, card hover/focus isolation feedback, and refresh/delete action-button busy feedback, settings-page first-screen AI connection feedback plus threshold field-level validation feedback, rule-save action-button busy feedback, and local utility action-button busy feedback, shared FormTable focus-within glow feedback, scheduler first-screen local task feedback plus scheduler controller/row action-button busy feedback, encrypted login credential saving, login submit `aria-busy`/spinner and stable credential status feedback, visible AI/import feedback, OperatorTaskPanel loading micro-response and non-blocking shimmer sweep, ProgressiveDetails disclosure summary hover/focus/active feedback with visible open/close chips, global typography contract, global button active `scale(0.98)` micro-response and disabled cursor lock, StateLightGrid hover +2px lift feedback, MicroStepper status-dot and pending-spinner feedback, data-collection monitor drawer with Canvas browser preview, data-collection action buttons with explicit `aria-busy`/spinner/striped progress feedback plus data-collection feedback repair/refresh buttons with focused spinner/`aria-busy` feedback, recommendation direct refresh/generate action-button busy feedback, approval decision buttons with their own `处理中...`/spinner/`aria-busy` busy contract, delivery readback repair handoff, delivery readback work-package action-button busy feedback, delivery export blocked-state red no-drop feedback, ad readback screenshot paste/drop capture with drag-over feedback plus fixed thumbnail/badge confirmation, readback time/value contract visualization, readback safety checkbox hover/focus/press/checked confirmation feedback, readback evidence workflow action-button busy feedback, readback local path-open busy feedback, heavy table virtualization with explicit sticky anchor columns, actual-row zebra striping and row press/focus feedback, keyword-opportunity sortable headers with arrow/ARIA feedback plus filter-axis 100ms crossfade/live feedback and structured keyword filter field cells, data-import validation sortable headers with `aria-live` count feedback, import-time read-only lock, 200ms blur/sweep plus row fade-in refresh, and direct import/export action buttons with `处理中...`/spinner/`aria-busy` busy feedback, Listing keyword heatmap matrix with strict-contained keyword rail cells plus draft diff/skeleton/over-limit feedback, Listing keyword/draft production workbench with `仅本地预览` and `待补齐真实广告数据` states instead of placeholder/fallback primary copy, ad-quant metric focus filters with 60% inactive-chip dimming, ad-quant AI-running radar feedback, ad-quant direct AI-run/recommendation-entry action-button busy and peer-lock feedback, and canonical daily metric explanations verified in the 2026-07-01 package.
- The current package also includes the 2026-06-29 `执行回读` wizard step-rail refresh: the four readback tabs expose a 2px blue active-step slider driven by renderer state/CSS variables, with smooth horizontal movement and a reduced-motion fallback. This is navigation feedback only; it does not weaken approval, screenshot, readback-value, or `verify:ad-readback` gates.
- The current package also includes the 2026-06-30 `执行回读` safety-checkbox feedback refresh: approval and verification checkbox rows now respond to hover/focus/press, checked boxes animate a compact green confirmation pulse, and reduced-motion disables the animation. This is input confirmation only; it does not mark approval, screenshots, readback values, export, or `verify:ad-readback` complete.
- The current package also includes the 2026-06-30 `执行回读` evidence-workflow action-button busy refresh: `导出回读证据`, `创建回读工作包`, `检查工作包`, `生成回读证据`, and `校验回读证据` use `readbackActionButtonView`. Only the active evidence-chain action switches to `导出中...`, `创建中...`, `检查中...`, `生成中...`, or `校验中...`, renders the shared spinner, exposes `aria-busy=true`, and carries `button-loading`; peer evidence actions lock as plain disabled controls. This is local readback evidence workflow feedback only and does not make evidence final, bypass verifier gates, approve actions, or write Amazon Ads.
- The current package also includes the 2026-06-30 `执行回读` local path-open busy refresh: `打开导出文件`, `打开工作包`, `打开填写文件`, and `打开填写说明` use `readbackOpenPathButtonView`. Only the active path action switches to `打开中...`, renders the shared spinner, exposes `aria-busy=true`, and carries `button-loading`; peer path actions lock as plain disabled controls. This is OS file/folder navigation feedback only and does not generate evidence, verify readback proof, approve actions, or write Amazon Ads.
- The current package also includes the 2026-07-01 `执行回读` structured field-cell refresh: the target/source, approval, and manual execution evidence inputs now render through `ReadbackFieldCell` with `.readback-field-cell` borders, white pill labels, focus-within blue glow, and stable textarea/input containment instead of bare label/input pairs. Existing repair red rings, screenshot capture targets, time/value contract checks, evidence export, and verifier gates remain unchanged.
- The current package also includes the 2026-06-30 `Listing 优化` local action-button busy refresh: manual save, Lingxing read, version-history refresh, local draft generation, and draft export use a shared `listingLocalActionButtonView` contract. The active action shows `处理中...`-style copy, spinner, `button-loading`, and `aria-busy=true`; sibling local actions lock without pretending to run. This is local Listing workflow feedback only and does not submit Amazon or overwrite Lingxing.
- The current package also includes the 2026-06-30 `定时任务` controller action-button busy refresh: the lower controller refresh button, inline confirm-run button, and row-level enable/disable buttons use `schedulerActionButtonView`. Only the active scheduler action shows running copy, spinner, `button-loading`, and `aria-busy=true`; peer scheduler controls lock without pretending to run. This is local scheduler feedback only and does not approve recommendations or write Amazon Ads.
- The current package also includes the 2026-06-29 dashboard state-light refresh pass: when `今日看板` primary task navigation enters the short `转跳中...` handoff, the `数据健康` state-light grid receives a 180ms blue pulse/sweep through the shared `StateLightGrid` `refreshing` prop. This is first-screen response feedback only and does not mutate data, recommendations, approvals, or Ads execution state.
- The current package also includes the 2026-06-29 OperatorTaskPanel group-busy lock refresh: when any first-screen task action is busy, the whole task action group is disabled immediately to prevent sibling-button races; only the active running action gets `处理中...`/business busy copy, spinner, `button-loading`, and `aria-busy=true`, while locked peers stay plain disabled and do not impersonate the active task. This is UI concurrency feedback only and does not change page routing, data writes, approvals, or Ads execution state.
- The current package also includes the 2026-06-29 global route-handoff refresh: sidebar clicks and in-page `amazon-ai-ops:navigate` route events now enter a 150ms `转跳中...` handoff. The target nav item exposes `aria-busy=true`, shows a compact pending chip, and temporarily locks sibling nav buttons; the main canvas shows an absolute-positioned `route-handoff-feedback` status pill with reduced-motion fallback, so route changes have visible feedback without pushing ScopeBar or page content.
- The current package also includes the 2026-07-01 shared PageHeader title contract: all primary renderer pages source their first-screen title from `PAGE_HEADER_TITLES`, aligning page headers with the v1.5 IA blueprint (`今日运营看板与自动化链路就绪健康度总览`, `广告全口径量化诊断中心`, `本地运营资产交付验证门与最终就绪审计中心`, and related route titles). This is information-architecture consistency only; it does not change page routing, data queries, recommendations, approvals, or Amazon Ads writes.
- The current package also includes the 2026-07-01 product ACOS visible-navigation restoration: `产品管理` remains the product-scope locking workbench, while `产品 ACOS 配置` is again a distinct App Shell entry under `数据与量化`. `product-config` active/pending route feedback now highlights that visible entry, and product-management repair/configuration actions route to `product-config` instead of looping back to the product workbench. Product configuration remains local-only and does not approve recommendations or write Ads.
- The current package also includes the 2026-06-30 product-card lock feedback refresh: `产品管理` product cards expose `aria-pressed`, show stable `点击锁定` / `已锁定` tags, animate a short selected-card lock pulse with reduced-motion fallback, and write selection status into a fixed `aria-live` line. This is product-scope feedback only; it does not save product configuration, generate recommendations, approve actions, or write Ads.
- The current package also includes the 2026-06-30 product-management save action-button busy refresh: `保存产品信息` uses `productManagementActionButtonView`; the active save action switches to `保存中...`, renders the shared spinner, exposes `aria-busy=true`, and carries `button-loading`, while `打开完整配置` locks during the save without pretending to run. This is local product configuration feedback only; it does not generate recommendations, approve actions, or write Ads.
- The current package also includes the 2026-07-01 product-management inline field-cell refresh: the grouped `成本与售价` and `广告目标` maintenance inputs now render as labeled mini table cells through the shared `.inline-field` contract, with a contained border, light background, white pill label, focus-within blue glow, and right-aligned tabular numeric input. This is product form readability feedback only; it does not change product persistence, generate recommendations, approve actions, or write Ads.
- The current package also includes the 2026-06-30 product-config direct action-button busy refresh: `产品配置` now routes `保存完整产品配置` and bulk `应用到 X 个产品` through `productConfigActionButtonView`. Active actions show `保存中...` or `批量应用中...`, spinner, `aria-busy=true`, and `button-loading`; peer navigation actions lock as plain disabled buttons without impersonating the running action. This is local product-target feedback only; it does not generate recommendations, approve actions, or write Ads.
- The current package also includes the 2026-06-30 product-config loaded-row confirmation refresh: clicking `载入编辑` in the current-scope product table now marks the edited row with `product-config-row-loaded`, turns the row button into `已载入` with `aria-pressed=true`, flashes the row, and announces the loaded ASIN through the existing fixed `aria-live` message. This is local edit-context feedback only; it does not save product data, generate recommendations, approve actions, or write Ads.
- The current package also includes the 2026-07-01 product-config bulk-selection feedback refresh: the bulk target ACOS toolbar now shows a stable `已选 X/N 个产品` count chip, a progress rail, and an `aria-live` readback that states whether the local target ACOS write will affect selected products or none. This is selection feedback only; it does not save until the explicit bulk apply action runs, and it never approves recommendations or writes Amazon Ads.
- The current package also includes the 2026-07-01 product-config row target ACOS edit refresh: the current-scope product table's target ACOS cell is now an inline percent input with ArrowUp/ArrowDown 0.5-point nudges, blur/Enter save, row-local `保存中...` / `已保存` / `保存失败` chips, and fixed cell dimensions so the table does not jump during feedback. This is local product-target maintenance only; it does not generate recommendations, approve actions, or write Amazon Ads.
- The current package also includes the 2026-07-01 product-config row health refresh: the current-scope product table now adds a fixed `健康度` column that translates target ACOS into `待配置`, `目标正常`, `需复核`, or `高风险` chips with a stable detail line. This is table readability feedback only; it does not generate recommendations, approve actions, or write Amazon Ads.
- The current package also includes the 2026-06-30 ScopeBar editor save action-button busy refresh: the sticky `编辑范围` popover now treats `保存范围` as an async `saveOperationScope` persistence action. The button switches to `正在保存...`, renders a spinner, exposes `aria-busy=true`, blocks duplicate saves, and keeps the popover open with the returned error when persistence fails. This is local scope-context feedback only; it does not collect reports, import DB rows, generate recommendations, approve actions, or write Ads.
- The current package also includes the 2026-06-30 settings rule-save action-button busy refresh: `AI 设置` routes `保存广告阈值` through `settingsRuleActionButtonView`. The active local threshold save switches to `保存中...`, renders the shared spinner, exposes `aria-busy=true`, and carries `button-loading`; unavailable environments stay plain disabled without pretending to run. This is local rule-configuration feedback only; it does not generate recommendations, approve actions, or write Amazon Ads.
- The current package also includes the 2026-06-30 settings local utility action-button busy refresh: `AI 设置` routes `清除本地 AI Key` and `复制诊断检查清单` through `settingsLocalActionButtonView`. Only the active utility action switches to `清除中...` or `复制中...`, renders the shared spinner, exposes `aria-busy=true`, and carries `button-loading`; the peer utility action locks as a plain disabled button. This is local settings/diagnostic feedback only; it does not change AI output contracts, generate recommendations, approve actions, or write Amazon Ads.
- The current package also includes the 2026-06-30 operation-events inline/refresh/delete action-button busy refresh: `运营事件` bottom `保存到上下文` routes through `operationEventInlineSaveButtonView`, manual `刷新` routes through `operationEventRefreshButtonView`, and row-level `删除` routes through `operationEventRowDeleteButtonView`. The active local event save, refresh, or delete action switches to `保存中...`, `刷新中...`, or `删除中...`, renders the shared spinner, exposes `aria-busy=true`, and carries `button-loading`; incomplete drafts and locked peer delete buttons stay plain disabled without pretending to run. This is local operation-context feedback only; it does not generate recommendations, approve actions, mutate Listing, or write Amazon Ads.
- The current package also includes the 2026-07-01 operation-events timeline context refresh: each `运营事件` timeline card now exposes a focused `AI 上下文` readback strip, an `aria-label` combining title/scope/impact/context usage, and hover/focus/active card isolation with reduced-motion fallback. This is timeline readability and keyboard feedback only; it does not change event persistence, generate recommendations, approve actions, mutate Listing, or write Amazon Ads.
- The current package also includes the 2026-07-01 operation-events page-copy alignment: the page header now uses `核心运营事件时间轴标记`, the first-screen task says `把关键运营动作标记进 LLM 上下文`, and the add/timeline panels read as `新增运营事件标记` and `运营事件时间轴`. This is information-architecture clarity only; it does not change event persistence, AI eligibility, recommendation approval, or Amazon Ads writes.
- The current package also includes the 2026-06-30 login-entry micro-response refresh: the login submit button immediately switches to `正在确认 ERP 和 Ads 会话...`, renders the shared spinner, exposes `aria-busy=true`, and locks duplicate clicks while ERP/Ads session confirmation runs. Login credential notices now stay in a fixed `aria-live` status line, so remembered-account/password and local-only login states do not rely on transient text or silent waits.
- The current package also includes the 2026-06-30 recommendation batch-selection micro-response refresh: the `优化建议` batch toolbar now uses a stable selected-count chip with active pop feedback and `aria-live` status copy, and table checkboxes expose keyboard focus rings plus checked-state confirmation animation. This is selection feedback only; batch handoff still only passes formal-approval-ready recommendation IDs to `审批中心` and never approves or executes Ads.
- The current package also includes the 2026-06-30 recommendation direct action-button busy refresh: `优化建议` routes folded `刷新建议`, workflow-step `生成解释`, and empty-state `生成优化建议` through `recommendationActionButtonView`. The active action switches to `刷新中...` or `生成中...`, renders the shared spinner, exposes `aria-busy=true`, and carries `button-loading`; missing real scope or pipeline locks remain plain disabled. This is recommendation-page feedback only and does not bypass real-report gates, approve actions, or write Amazon Ads.
- The current package also includes the 2026-06-30 delivery readback work-package action-button busy refresh: `交付验收` routes `导出交付包`, `导出数据口径核对`, `创建回读工作包`, `检查工作包`, `生成回读证据`, `校验回读证据`, and `用回读证据刷新最终验收` through `deliveryActionButtonView`. Only the active action switches to `导出中...`, `创建中...`, `检查中...`, `生成中...`, `校验中...`, or `刷新中...`, renders the shared spinner, exposes `aria-busy=true`, and carries `button-loading`; peer delivery actions lock as plain disabled controls. This is local delivery/readback evidence workflow feedback only and does not satisfy readback proof or write Amazon Ads.
- The current package also includes the 2026-06-30 delivery summary-copy busy refresh: the `交付验收` first-screen `复制摘要` action now has its own busy state. While the clipboard write is pending, the action switches to `复制中...`, shows the shared spinner through `OperatorTaskPanel`, exposes `aria-busy=true`, and locks the peer first-screen action as a plain disabled control. This is local clipboard feedback only and does not change final-readiness evidence, export eligibility, or Ads execution state.
- The current package also includes the 2026-06-30 data-collection report-selection feedback refresh: the 8-report selector now exposes a stable selected-count chip, progress rail, `aria-live` status copy, selected-card styling, checkbox checked animation, and focus-within ring. This is chooser feedback only; the download/recreate/import actions and real-report/import gates remain unchanged.
- The current package also includes the 2026-06-30 ad-quant AI-running radar feedback refresh: while `广告量化` runs AI stage diagnosis, the feedback panel exposes `aria-busy=true`, keeps the existing operator copy, and renders a contained radar sweep/pulse with reduced-motion fallback. This is running-state feedback only; it does not change diagnosis contracts, recommendation gates, approvals, or Ads execution state.
- The current package also includes the 2026-06-30 ad-quant direct action-button busy refresh: the in-page `运行 AI 阶段分析` button and AI feedback-card retry action use `adQuantActionButtonView`. The active AI action switches to `AI 分析中...`, renders the shared spinner, exposes `aria-busy=true`, and carries `button-loading`; recommendation-entry peers lock as plain disabled buttons while AI diagnosis is running. This is local diagnosis feedback only; it does not change AI output contracts, generate recommendations, approve actions, or write Amazon Ads.
- The current package also includes the 2026-06-30 Listing heatmap focus feedback refresh: keyword rail buttons expose `aria-pressed`, selected roots trigger a short reduced-motion-safe flash on the matching rail item, section, and highlighted tokens, and the matrix writes the selected root plus matched sections into an `aria-live` status line. This is local Listing coverage feedback only; it does not submit Amazon or modify Lingxing Listing.
- The current package also includes the 2026-06-30 readback backup copy-command busy refresh: the folded `执行回读` technical command buttons for creating/checking/filling readback work packages and long-argument fill command now use `readbackCopyCommandButtonView`. Only the active command-copy button switches to `复制中...`, renders the shared spinner, exposes `aria-busy=true`, and locks peer copy buttons as plain disabled controls. This is backup-command clipboard feedback only and does not make evidence final, bypass verifier gates, approve actions, or write Amazon Ads.
- Approval center decisions now show immediate stamp-style first-screen feedback for approving, approved, rejecting, rejected, and blocked states. The active approve/reject button also switches to `处理中...`, renders a spinner, exposes `aria-busy=true`, and locks sibling decision actions while IPC is pending. Approved/rejected queue rows take a 180ms tone-coded slide-out and are locally removed from the visible queue before the authoritative queue reloads, so the operator sees that the decision was accepted without mistaking it for Ads execution. The three-state decision strip also fades non-focused available actions to 40% opacity on hover/focus so the active approval/review/reject choice is visually isolated. The stamp and row exit are status feedback only; ad execution still requires separate manual Ads UI proof, screenshots, reload/readback, and verifier approval.
- `优化建议` now supports batch-selecting only formal-approval-ready recommendations and handing their IDs to `审批中心`; the selection count and checkbox micro-response are UI feedback only, and approval still reloads the real queue, revalidates evidence, and requires per-row human decision feedback before any manual Ads UI readback workflow.
- `优化建议` status buckets are interactive table filters. `全部`, `高风险强阻断`, `需人工复核`, and `已就绪可批准` only change the visible recommendation table and clear current checkbox selection; they must not change recommendation state, bypass blockers, or submit hidden rows.
- `广告量化` metric chips are interactive focus filters. `全部对象`, `浪费超支`, `高 ACOS`, `出单对象`, `可扩量`, and `待复核` only change the current quantification view, object timeline, and review queue; when a non-all focus is active, inactive chips dim to 60% opacity for visual triage only. The product summary tile now uses `浪费/高风险花费` with amount, share of current product spend, and high-risk object count instead of engineering placeholder wording; missing daily data must instruct the operator to import all 8 real reports. These views must not change recommendation state, approvals, or Ads execution state.
- Authoritative final readiness: `output\codex-evidence\final-readiness-1782879062227.json`.
- Evidence manifest: `output\codex-evidence\v15-final-readiness-evidence-manifest-1782879016283.json`.
- Package launch smoke: `output\codex-evidence\package-launch-smoke-1782878975868.json`.
- READY bundle: `output\delivery-bundles\v15-delivery-bundle-2026-07-01T04-11-14-525Z`.
- Installer SHA-256: `DB422F4F401DA984BDA89971D14B75AEFD1A675E7103B712DB13520B62A53F8A`.
- Portable/no-install SHA-256: `57724F52FC94FCD3A01B70ABE0F465A65354C34363BFA50BFBD9D35DCA90A0C0`.
- The 2026-06-25 refresh keeps the high-fidelity business-domain navigation with active glow bar, compact status/tag surfaces, AI output contract tags, table-like Listing editor, Listing keyword heatmap matrix, structured AI token floor, strategy-diagnosis evidence-ref normalization, Lingxing date-range picker commit behavior, product-first dashboard gate, and data-collection monitor drawer with Canvas browser preview. It additionally fixes the real AI strategy diagnosis format-fallback root cause by making the OpenAI-compatible provider honor saved temperature/maxTokens, forcing diagnosis and JSON repair calls to the 8192 token floor, and replacing placeholder schema text with concrete evidence-driven JSON examples. Live DeepSeek evidence `output\codex-evidence\ad-strategy-live-1782358641101.json` returned `source=ai` with no fallback for the current scope. It also stores remembered Lingxing login credentials locally through Electron `safeStorage`, makes AI strategy diagnosis and import actions show first-viewport running/success/failure feedback, gives OperatorTaskPanel actions a shared busy contract with immediate disabled state, spinner, `aria-busy`, and `处理中...`/business-specific running copy, gives OperatorTaskPanel cards a non-blocking reduced-motion-safe shimmer sweep behind their content, aligns global button active feedback to `scale(0.98)` with disabled `cursor: not-allowed`, gives shared state-light cards a reduced-motion-safe hover `translateY(-2px)` lift and shadow, adds settings, operation-scope, product-management, product-config, operation-events, and scheduler task panels so AI connection testing, scope confirmation, explicit ASIN locking, product targets, BD/Coupon/price/stock/Listing context, and local automation status are visible as first-screen actions, keeps product management from silently using the first product when no ASIN is selected, routes selected products without imported metrics to `数据导入与校验` before AI quantification, exposes `凭证映射通过` as a hover/focus chip that shows only Main Sandboxed ID, site/period, and UI no-plaintext-retention copy, lets ad-quant metric chips focus the page on all objects, no-order waste, high ACOS, ordered, scale-ready, or review-needed objects with visible/total counts, keeps AI save/test results in a fixed `aria-live` feedback bubble without duplicating them in the bottom status panel, lets `工作范围` explicitly save the current scope with fixed `aria-live` saved/error feedback, edit the same scope through a page-level FormTable, pulse changed date/store/site/ASIN/batch controls green in reserved field space, and open the ScopeBar editor from the page as a non-layout-shifting popover, lets current-scope product rows be selected and bulk-applied to a target ACOS percent through `saveProductConfig`, autosaves product cost/min-price/target fields on blur or Enter with stable `aria-live` saved/error feedback, supports ArrowUp/ArrowDown keyboard nudges with immediate inline adjustment copy, shows live target health chips, clears and rebounds the operation-event form immediately on submit, restores the draft if persistence fails, and flashes the newest saved operation-event card, keeps scheduler refresh/run-now confirmation/failure feedback plus controller/row action-button busy feedback visible in fixed status lines, opens a non-layout-shifting right-side monitor drawer with a Canvas browser-preview frame for data-collection verify/download/recreate/import actions, labels downloaded reports as `已下载待入库` until SQLite rows exist, virtualizes the `关键词机会` and `数据导入与校验` heavy tables with `@tanstack/react-virtual`, sticky headers, skeleton loading, and scroll containment, and gives `数据导入与校验` sortable report/file/type/size/SHA-256/row/status headers with `aria-live` count feedback and a 200ms blur/sweep plus row fade-in refresh, shows Listing keyword/root coverage as a left keyword rail plus right current-vs-draft title/bullets/backend/details heatmap with click-to-highlight, colors Listing draft deleted/added tokens as red/green diff chips, overlays a draft-generation skeleton wave, flashes over-limit character counts, explains that daily totals use the selected batch/source files plus canonical report priority rather than cross-batch or cross-grain summing, lets the `执行回读` page save approval/before/after/readback screenshots by drag/drop or Ctrl+V into the current readback session folder while backfilling evidence paths and timestamps, and visualizes the readback time/value safety contract before export so operators see time-order, value-change, readback-match, bid-direction, and evidence-reuse blockers before a draft can be mistaken for final proof. Focused renderer/product/import/data-collection-monitor/AI/readback-capture/readback-contract/virtual-table/listing-heatmap/operator-task-panel/operation-event/product-config/operation-scope/settings-page/scheduler/ad-quant focus tests, desktop typecheck, `smoke:business-ui-current`, `build:win`, `smoke:package-launch`, manifest-driven final-readiness, READY bundle export, and READY safety have been rerun for this source state.
- The approval feedback refresh adds `PASSED` / `REJECTED` / `BLOCKED` stamp copy in an `aria-live` status block, disables duplicate decision submissions while IPC is pending, and keeps exact operator error text in the page message area to avoid strict text duplication. Focused approval tests, desktop typecheck, renderer build, ad-execution smoke, current business UI smoke, `verify:ad-execution`, `build:win`, package launch smoke, manifest-driven final-readiness, READY bundle export, and READY safety were rerun for this package state.
- The 2026-06-26 keyword-opportunity refresh makes `VirtualDataTable` headers optionally sortable with `aria-sort`, a 150ms rotating arrow, and stable button feedback. `关键词机会` now sorts filtered rows locally without mutating source order, defaults to high opportunity levels first, and gives filter/sort changes a fixed `aria-live` result line plus a 100ms vertical crossfade while preserving virtual scrolling for long tables. Focused virtual-table and keyword-opportunity tests, desktop typecheck, `smoke:business-ui-current`, `verify:ad-execution`, `build:win`, package launch smoke, manifest-driven final-readiness, READY bundle export, and READY safety were rerun for this package state.
- The 2026-06-26 dashboard/data-import validation refresh gives `今日看板` primary task actions a 150ms `转跳中...` busy/disabled handoff before navigation, and wires sortable headers into `数据导入与校验` for report name, real file, extension, size, SHA-256 checksum, imported rows, and status. Sorting updates the virtualized table without mutating source order, announces the active order and current real-report/imported-row counts through `aria-live`, and applies a reduced-motion-safe 200ms blur/sweep plus row fade-in refresh so operators can see the action took effect. While import writes metrics into SQLite, the validation table enters an explicit read-only lock: sortable header buttons lose their callback, row open-file buttons disable, and a fixed `aria-live` lock notice explains that row counts will refresh after import completes. Focused dashboard/data-import validation tests, desktop typecheck, `smoke:business-ui-current`, `verify:ad-execution`, `build:win`, package launch smoke, manifest-driven final-readiness, READY bundle export, and READY safety were rerun for this package state.
- The 2026-06-26 Listing draft feedback refresh adds per-section diff chips to the Listing heatmap matrix: removed original tokens are red and struck through, added draft tokens are green chips, AI/rule draft generation overlays a non-layout-shifting skeleton wave on the draft pane, and character counts flash red when a title/bullet exceeds its limit. Focused Listing tests, desktop typecheck, `smoke:business-ui-current`, `verify:ad-execution`, `build:win`, package launch smoke, manifest-driven final-readiness, READY bundle export, and READY safety were rerun for this package state.
- The 2026-06-26 operation-scope form refresh adds a page-level `范围表单` under the first-screen task panel. Operators can adjust store, marketplace, date range, ASIN, and batch on the `工作范围` page itself; each changed control reuses the green field-confirmation pulse and reserved `aria-live` line, and saving persists the normalized draft through `saveOperationScope` before updating the shared scope. Focused operation-scope and ScopeBar tests, desktop typecheck, `smoke:business-ui-current`, `verify:ad-execution`, `build:win`, package launch smoke, manifest-driven final-readiness, READY bundle export, and READY safety were rerun for this package state.
- The 2026-06-26 business-data pipeline refresh adds the v1.5 high-throughput query guard to `useBusinessDataPipeline`: first load and explicit reload/data-updated refreshes stay immediate, while scope-only changes are merged through a 300ms debounce window and stale timers are cancelled. This protects Dashboard, data collection/import validation, ad quantification, recommendations, approval, keywords, Listing, product, scope, and readback pages from firing duplicate current-scope IPC reads during rapid ScopeBar edits. Focused business-data tests, desktop typecheck, `smoke:business-ui-current`, `verify:ad-execution`, `build:win`, package launch smoke, manifest-driven final-readiness, READY bundle export, and READY safety were rerun for this package state.
- The 2026-06-26 approval queue exit refresh keeps the existing decision stamp contract but adds visible queue closure: after approve/reject success, the decided row receives an approved/rejected exit class, animates out for 180ms, is removed from the local table, and then the page reloads the real approval queue. Focused approval tests, desktop typecheck, `smoke:business-ui-current`, `build:win`, package launch smoke, manifest-driven final-readiness, READY bundle export, and READY safety were rerun for this package state.
- The 2026-06-26 product-config bulk ACOS refresh adds row selection and a current-scope toolbar to `产品配置`: operators can select one or all visible products, enter a target ACOS percent, and apply it through the existing local `saveProductConfig` IPC. The helper normalizes percent input to decimal thresholds, preserves product identity/cost fields, shows disabled/busy/ready/error feedback, and does not approve recommendations or write Ads. Focused product-config tests, desktop typecheck, `smoke:business-ui-current`, `build:win`, package launch smoke, manifest-driven final-readiness, READY bundle export, and READY safety were rerun for this package state.
- The 2026-06-26 ad-quant metric dimming refresh makes `TagMetricGroup` support opt-in inactive-chip dimming. `广告量化` enables it when the focus is not `全部对象`, so selected metric chips stay prominent while other metric chips fade to 60% opacity and recover on hover/focus. This is visual triage only and does not change filtering rules, recommendation state, approval, or Ads execution. Focused tag-metric/ad-quant tests, desktop typecheck, `smoke:business-ui-current`, `build:win`, package launch smoke, manifest-driven final-readiness, READY bundle export, and READY safety were rerun for this package state.
- The 2026-06-29 data-collection action-button refresh gives the four lower `数据采集` actions (`下载已创建`, `重建已选`, `重建全部 8 类`, `导入本地`) the same explicit busy contract as primary task buttons. The active action switches to `处理中...`, exposes `aria-busy=true`, shows a spinner, keeps a blue striped progress surface even while disabled, and locks sibling actions without making the running state look inert. Focused data-collection tests, desktop typecheck, `smoke:business-ui-current`, `build:win`, package launch smoke, manifest-driven final-readiness, READY bundle export, and READY safety were rerun for this package state.
- The 2026-06-29 approval decision-button refresh applies the same micro-response contract to `审批中心` direct approve/reject actions. `approvalDecisionButtonView` now gives the active decision button `处理中...`, `button-loading`, spinner, `aria-busy=true`, and a disabled lock while sibling decision actions remain disabled without impersonating the running action. Focused approval/recommendation tests, desktop typecheck, `smoke:business-ui-current`, `verify:ad-execution`, `build:win`, package launch smoke, manifest-driven final-readiness, READY bundle export, and READY safety were rerun for this package state.
- The 2026-06-29 data-import action-button refresh applies the same loading contract to `数据导入与校验` direct actions. `dataImportActionButtonView`, `dataImportExportButtonView`, and `dataImportBusyLabel` make the first-screen import task button, folded import buttons, and reconciliation export button switch to `处理中...`, render a spinner, expose `aria-busy=true`, and carry `button-loading` only on the running action while sibling buttons are locked without impersonating the active task. Focused data-import tests, desktop typecheck, `smoke:business-ui-current`, `verify:ad-execution`, `build:win`, package launch smoke, manifest-driven final-readiness, READY bundle export, and READY safety were rerun for this package state.
- The 2026-06-29 Listing heatmap containment refresh extends the v1.5 high-density CSS isolation contract to the left keyword rail cells in `Listing 优化`. `.listing-heatmap-keyword` now declares `contain: strict`, matching the existing strict containment on right-side current/draft text cells while avoiding strict containment on auto-height outer sections. Focused Listing and virtual-table tests, desktop typecheck, `smoke:business-ui-current`, `verify:ad-execution`, `build:win`, package launch smoke, manifest-driven final-readiness, READY bundle export, and READY safety were rerun for this package state.
- The 2026-06-29 Listing local-draft workbench refresh merges the old keyword coverage and draft export panels into `关键词与本地草案工作台`. It shows keyword input, real-data gate, draft purpose, source counts, and generate/export actions in one stable grid; missing real ad data is now shown as `仅本地预览` / `待补齐真实广告数据`, and production UI no longer calls these drafts placeholders. Focused Listing workflow tests, desktop typecheck, `smoke:business-ui-current`, `verify:ad-execution`, `build:win`, package launch smoke, manifest-driven final-readiness, READY bundle export, and READY safety were rerun for this package state.
- The 2026-06-30 Listing local-action busy refresh closes the remaining static-button gap in `Listing 优化`: saving a manual Listing version, reading from Lingxing, refreshing version history, generating local drafts, and exporting drafts now share busy labels, spinner, disabled group lock, and `aria-busy` on the active action. Focused Listing tests, desktop typecheck, renderer build, `smoke:business-ui-current`, `verify:ad-execution`, `build:win`, package launch smoke, manifest-driven final-readiness, READY bundle export, and READY safety were rerun for this package state.
- The 2026-06-29 VirtualDataTable zebra refresh completes the v1.5 heavy-table scanability contract for virtualized tables. `VirtualDataTable` now emits `virtual-table-row-even` / `virtual-table-row-odd` from the actual `virtualRow.index`, not DOM `nth-child`, so `关键词机会` and `数据导入与校验` keep stable alternating row backgrounds while rows are recycled by `@tanstack/react-virtual`. Focused virtual-table, keyword-opportunity, and data-import validation tests, desktop typecheck, `smoke:business-ui-current`, `verify:ad-execution`, `build:win`, package launch smoke, manifest-driven final-readiness, READY bundle export, and READY safety were rerun for this package state.
- The 2026-07-01 VirtualDataTable sticky-anchor refresh makes heavy-table frozen columns explicit in the column contract instead of relying on `:first-child`. `关键词机会` declares ASIN as the left sticky anchor and `数据导入与校验` declares report name as the left sticky anchor, so future column reordering cannot silently freeze the wrong field. This is scanability feedback only; it does not import reports, change recommendations, approve actions, or write Amazon Ads. Focused virtual-table, keyword-opportunity, and data-import validation tests, desktop typecheck, `smoke:business-ui-current`, `verify:ad-execution`, `build:win`, package launch smoke, manifest-driven final-readiness, READY bundle export, and READY safety were rerun for this package state.
- The 2026-06-29 ProgressiveDetails/direct details disclosure refresh completes the shared progressive-disclosure micro-response contract. `ProgressiveDetails` summaries now have a dedicated `.progressive-details-summary` surface with hover inset anchor, keyboard `focus-visible` ring, active `scale(0.98)`, visible `展开`/`收起` state chips, hidden native marker, and reduced-motion fallback. Direct native details summaries used by dashboard, detail panels, and evidence disclosures now share the same hover inset anchor, focus ring, active press, and reduced-motion behavior so folded evidence controls are no longer dead-looking click targets. This improves every page that hides technical audit/evidence details behind progressive disclosure without adding primary-screen text. Focused progressive-details, direct-details, ScopeBar, dashboard, approval, ad-quant, recommendations, and settings tests, desktop typecheck, `smoke:business-ui-current`, `verify:ad-execution`, `build:win`, package launch smoke, manifest-driven final-readiness, READY bundle export, and READY safety were rerun for this package state.

Any future code, package, scope, or ad-action change invalidates applying this `APP_READY` claim to that modified state until the final gates are rerun.

## Required Safety Boundaries

- App-side batch ad execution remains fail-closed. The UI must not claim that blocked audit output equals a real Amazon Ads write.
- Recommendation batch handoff is not batch execution. It may preselect or hint approval rows, but it must not update Ads, bypass approval blockers, or convert review-only rows into formal approvals.
- Recommendation bucket filtering is visual triage only. It should reuse `recommendationHasEvidenceBlocker`, manual-review, and formal-approval gates so the visible buckets match approval policy.
- Ad-quant metric focus filtering is visual triage only. It should use shared quant thresholds for high ACOS/no-order waste buckets and must not update recommendations, approvals, or Ads state.
- Readback time/value contract cards are operator-facing prechecks, not proof of execution by themselves. Final readiness still requires distinct screenshots, valid time order, before/after value change, readback value equality, source traceability, and `verify:ad-readback`.
- Every future Ads UI action needs its own target, source report file(s), source row, approval, before screenshot, after screenshot, reload/readback screenshot, and `verify:ad-readback` pass.
- Structural/mock AI evidence never gives final readiness credit. Real readiness requires live provider evidence and the real ad/Listings verifier chain.
- Operator-facing UI should stay concise: avoid raw `APP_*` status codes, long command walls, or dense evidence text in primary views. Put technical details behind secondary panels.
- Main window text overload was a known UX problem; prefer task-first copy, compact summaries, and clear next action labels.
- AI output schemas are system-owned contracts. Settings may show contract tags and allow persona/expression tuning, but user-edited persona text must not control `schemaVersion`, fixed fields, or formal action eligibility.
- Dense readiness summaries should prefer compact tags/chips in primary views, with detailed evidence and explanations behind progressive disclosure.

## Verification Commands

Use focused checks while iterating, then rerun the delivery chain before claiming readiness.

```powershell
pnpm --filter @amazon-ai-ops/desktop run typecheck
pnpm --filter @amazon-ai-ops/desktop run build:win
pnpm run smoke:business-ui-current
pnpm run smoke:package-launch
pnpm run verify:ad-execution
pnpm run verify:ad-strategy-live -- --input output\codex-evidence\ad-strategy-live-input-current-scope.json
pnpm exec vitest run scripts\verify-v15-final-readiness.test.mjs scripts\verify-v15-ready-safety.test.mjs scripts\verify-v15-non-ready-safety.test.mjs scripts\package-scripts.test.mjs
```

Final delivery refresh:

```powershell
pnpm run write:v15-evidence-manifest --ad-readback output\codex-evidence\real-ad-execution-readback-candidate-rec-4-current-pass.json --out output\codex-evidence\v15-final-readiness-evidence-manifest-1782879016283.json
pnpm run verify:v15-final-readiness --evidence-manifest output\codex-evidence\v15-final-readiness-evidence-manifest-1782879016283.json --package-launch-smoke output\codex-evidence\package-launch-smoke-1782878975868.json --out output\codex-evidence\final-readiness-1782879062227.json
pnpm run export:v15-delivery-bundle --final-readiness output\codex-evidence\final-readiness-1782879062227.json --data-reconciliation output\codex-evidence\real-lingxing-reconciliation-batch_20260612020905629_gkchz1.json --data-reconciliation-md output\codex-evidence\real-lingxing-reconciliation-batch_20260612020905629_gkchz1.md --out output\delivery-bundles\v15-delivery-bundle-2026-07-01T04-11-14-525Z
pnpm run verify:v15-ready-safety --final-readiness output\codex-evidence\final-readiness-1782879062227.json --bundle-manifest output\delivery-bundles\v15-delivery-bundle-2026-07-01T04-11-14-525Z\delivery-bundle-manifest.json
```

## Docs To Keep In Sync

When delivery status, package hashes, readiness gates, UX boundaries, or evidence paths change, update these together:

- `README.md`
- `docs\USER_GUIDE_v1_5.md`
- `docs\V1_5_ACCEPTANCE_MATRIX.md`
- `docs\V1_5_PROGRESS_REPORT.md`
- `docs\V1_5_ORCHESTRATOR_CLOSEOUT.md`

After doc changes that affect delivery state, re-export the READY bundle and rerun `verify:v15-ready-safety` so bundled docs match the workspace docs.
